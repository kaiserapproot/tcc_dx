**1.** **`new`** **/** **`delete`** **演算子の扱い** 動的メモリ確保とコンストラクタ／デストラクタの呼び出しをセットで行う `new` / `delete` （および `new[]` / `delete[]`）について、実装ステージにも除外リストにも記載がありません。

* **提案**: 第4ステージ（コンストラクタ/デストラクタの暗黙呼び出し）の付近に、`new` / `delete` 演算子のオーバーロードとパースについて追記しておくと完璧です。

**2. 初期化子リスト (Member Initializer List)** コンストラクタにおける `Class() : member1(0), member2(1) {}` という構文です。

* **提案**: こちらもコンストラクタのパースに関連するため、第4ステージに「初期化子リストのパースとメンバ初期化」の一言があると、パーサ実装時の見落としを防げます。

**3. 多重継承 (Multiple Inheritance) の制限明記** 第4ステージで「単一継承のサポート」とされているため方針は明確ですが、DirectX (COM) を扱う上で「COMインターフェースを複数継承して自作クラスを作る」といった用途を期待するユーザーがいるかもしれません。

* **提案**: 「除外（初期方針）」のセクションに、「**多重継承** — COM等のインターフェース呼び出しはサポートするが、TCC側での多重継承クラスの実装はコスト高のため初期段階では除外（単一継承のみサポート）」と一筆書き加えておくと、仕様の境界線がさらに強固になります。

**4. 静的メンバ (`static`** **メンバ変数 / 関数)** 第3ステージに「非静的メンバ関数の〜」とあるため設計には含まれていると推測しますが、静的メンバ変数の実体確保（グローバル変数と同様にデータセクションへ配置する処理）が必要です。

* **提案**: 第2ステージか第3ステージのどこかに「静的メンバのパースとデータセクションへの配置」が含まれているとより正確です。

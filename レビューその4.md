## 1. 毎回壊れる根本原因（プロセス面）

「ビルド不可」「ソース破損」「テスト動かない」が繰り返される最大の理由は **プラン側ではなく作業手順の規律が欠落していること** です。

| 症状           | 真因                                                         | 対処                                                            |
| :----------- | :--------------------------------------------------------- | :------------------------------------------------------------ |
| ソースが壊れる      | **1 コミット = 1 ガイドの規約がない**。中間状態で次のガイドに手をつけ、戻せなくなる            | 各 A-N 完了 = 必ずコミット。**壊れていたらコミットしない**を厳格化                       |
| ビルドできない      | **コミット前 msbuild が hook 化されていない**。「checklist に書く」だけでは絶対に飛ばす | `pre-commit` で `msbuild /t:Build /v:quiet` を強制。失敗 = コミット拒否    |
| テストが動かなくなる   | **回帰テストが手動**。新機能で `.c` 既存テストが壊れていることに数時間後に気付く              | `build.bat` 末尾に `con_c_vs_test.exe` 実行を含める                    |
| 戻し方が乱暴       | `git checkout tccgen.c` をいきなり実行（履歴.md 425, 429 行）          | 作業ブランチを切り、**壊した時は branch を捨てて master から再 branch**。直接戻さない      |
| 検証ベースラインが揺れる | 「upstream CLEAN」と言うが、その**コミット ID/タグが固定されていない**             | `git tag clean-baseline-2026-05-24` を打ち、各ガイドはそこからの diff として管理 |

> **問題と原因.md §3** で「最小単位化・段階検証が不足」と自己分析していますが、IMPLEMENTATION\_GUIDE A-10 のチェックリストはこの分析を反映できていません（手動チェックボックスのまま）。

***

## 2. プラン内容の致命的な抜け

### A-2（キーワードゲート）— **このまま実装すると確実に壊れる**

```
if (!tcc_state->cpp && is_cpp_only_keyword(tok))
    tok = TOK_IDENT;  /* これだけでは動かない */

```

`TOK_IDENT` に書き換えても、後段の `unary()` / `expr_eq()` 等は `tokc` から識別子文字列を引きにいきます。`ts->tok` / `tokc` が `TOK_CLASS` の状態のまま `tok` だけ `TOK_IDENT` にしても **シンボルテーブル参照が壊れます**。

正しいアプローチは「キーワード登録自体を `cpp` 時のみ有効にする」か、`tok_alloc` で得た **`ts->tok`** **ごと差し替える** こと。プランの「推奨実装（シンプル）」は嘘です。ここを直さないと A-2 で確実にハマります。

### A-4（extern "C"）— **方針が 2 案併記のまま「レビューしてください」で終わっている**

A-4-1 と A-4-2 のどちらで実装するか **未決定**。さらに：

* `unget_tok(TOK_EXTERN)` が TCC に存在するか未確認（一般的に TCC は trivial unget 不可）
* `tokc.str.data` のフィールドアクセスが現リポジトリの定義と一致するか未確認
* 実装前に「衝突しないか確認」がレビュー宿題のまま

→ **実装中に方針変更が起こり、ソース破損の温床**。実装着手前に **どちらか 1 案で確定** し、上記 3 点を実コードで先に検証する必要があります。

### A-5（class 最小パース）— **最小の定義が曖昧で、過去の破綻と同じ轍**

履歴.md・問題と原因.md で起きた破綻は **すべて A-5 のメンバ関数本体の扱い** が原因です。にもかかわらずプランには：

> 「メンバ関数の本体 `{ }` はセミコロン省略可（インライン定義）— 今回は `skip_or_save_block` せず、既存の `gen_function` フローに任せる（単純な `{ return 0; }` 程度なら動作）」

これは **誤り** です。`struct_decl` の途中で `gen_function` を呼ぶと、シンボルテーブル状態 (`local_stack`, `cur_scope`)、`func_vt`、`vtop` が壊れます。Stage 1 では：

* 必ず `skip_or_save_block(NULL)`（保存もしない）で **読み飛ばすだけ**
* もしくは `;` 必須にして本体定義を文法的に拒否

のどちらかに **明示的に限定** すべき。「動作する」と書くと実装者が深追いして壊します。

### A-6（CPPUnit テスト）— **TCC のテストになっていない**

```
TEST_METHOD(ClassKeywordParse) {
    class Point { ... };  // MSVC がコンパイルする
    Assert::IsTrue(sizeof(Point) >= sizeof(int) * 2);
}

```

これは **MSVC が C++ を理解できるか** のテストであり、**TCC のテストではありません**。MSVC が `class` を通すのは当然です。

本来必要なのは：

1. `dev/tcc.exe test_class.cpp -o test_class.exe` が成功すること
2. 実行結果が期待値であること
3. これを CUnit/CPPUnit から `system()` で叩き、終了コードと stdout を assert

A-9 で「将来：TCC コンパイルテスト（基盤完了後）」と書かれていますが、**これは基盤完了後ではなく A-1 完了時点から必須**。テスト階層が逆転しています。

***

## 3. 考慮漏れリスト（追加すべき項目）

| #  | 項目                                            | 理由                                                                                                                         |
| :- | :-------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------- |
| 1  | **ファイルエンコーディング規約**                            | 問題と原因.md §6 で「UTF-8(BOM) に統一」とあるが IMPLEMENTATION\_GUIDE に未記載。MSVC `C4819` と TCC の BOM 扱いで壊れる                               |
| 2  | **ONE\_SOURCE フラグの維持**                        | 履歴.md 2026-05-16 のセットアップ問題。`tccgen.c` 関数追加時に `ONE_SOURCE=0/1` 両方でビルド確認必須なのに明記なし                                            |
| 3  | **x86\_64 double 6 引数バグの回帰テスト**               | 履歴.md 2026-05-23 の修正が新規実装中に消える危険。`repro_double6.c` を CI 化必須                                                                |
| 4  | **MSVC C90 スコープ厳格性**                          | `error C2065: method_body` が再発（履歴.md §3）。**変数宣言はブロック先頭** を実装規約に明文化                                                         |
| 5  | **`tcc_compile()`** **の再入**                   | inline 関数遅延展開で同じ `tcc_compile` 経路が走る。`s1->cpp` をスタック push/pop しないとリークする                                                    |
| 6  | **`__cplusplus = 199711L`** **の妥当性**          | Windows SDK の一部ヘッダは C++11 以上を要求。MinGW/MSVCRT 系インクルードでビルドエラーになる可能性。実検証必要                                                    |
| 7  | **`.h`** **ファイルの C/C++ 判定**                   | プランは `.h` を常に C 扱い。`extern "C"` で囲まれた SDK ヘッダの挙動が未定義                                                                       |
| 8  | **既存** **`.c`** **での** **`class`** **等識別子調査** | TCC 自身のソース・テストに `class`/`this`/`true` を変数名として使っている箇所がないかの grep が事前手順にない                                                    |
| 9  | **依存関係グラフ**                                   | A-1→A-2→...→A-5 の **必須前提** が暗黙。A-5 を A-1 なしで触ると無関係に壊れる。プランに DAG を明記すべき                                                     |
| 10 | **失敗時の切り分けフロー**                               | 「リンクエラー出たら何を見るか」（ONE\_SOURCE / vcxproj / シンボル消失）の手順が無く、毎回手探りで「壊れた→checkout」になる                                             |
| 11 | **dev/tcc.exe 配置後の自動 smoke test**             | A-8 でコピーするだけ。`dev/tcc.exe -v` と `dev/tcc.exe hello.c -o hello.exe && hello.exe` を `build.bat` に組み込むべき                      |
| 12 | **作業ブランチ運用**                                  | 全部 master で作業している（status: M IMPLEMENTATION\_GUIDE.md, M dev/tcc.exe）。**ガイド単位の feature branch** と **PR 形式 self-review** がない |

***

## 4. 推奨する修正方針

優先度順に：

1. **規律のインフラ化を先にやる**（コードを 1 行も触る前）
   * `git tag clean-baseline-2026-05-24`
   * `pre-commit` フックで `msbuild` + 既存 CUnit + `repro_double6.c` 実行
   * `build.bat` で **ビルド→dev コピー→smoke test→既存テスト** を一気通貫
   * **これだけで「気付かないうちに壊れていた」がほぼ消える**
2. **A-2 と A-4 と A-5 の不確定箇所を実装着手前に解決**
   * A-2: `ts->tok` 書き換え方式で 1 ファイル試作
   * A-4: `unget_tok` / `tokc.str` の実在確認、1 案に確定
   * A-5: 「本体は `skip_or_save_block(NULL)` で読み捨て、`;` 省略不可」に確定
3. **テスト階層を是正**
   * MSVC CPPUnit は **TCC が出力したバイナリを実行するハーネス** に作り替える
   * `dev/tcc.exe foo.cpp -o foo.exe && vstest` で foo.exe を呼ぶ形
4. **ガイドの実装は 1 つずつ、必ず緑のままコミット**

***

要点だけ言うと、**プランの「内容」より「進め方の規律」が弱い** ことが毎回の破綻の主因です。次に内容面では **A-2 / A-4 / A-5 の未確定事項** が地雷として埋まったままになっています。

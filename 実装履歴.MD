# 実装履歴 — Phase 1 / Stage 1（C++ 基盤）

**文書版**: 1.2  
**最終更新**: 2026-05-25  
**参照**: [IMPLEMENTATION_GUIDE.md](IMPLEMENTATION_GUIDE.md) v2.5（A-P 〜 A-9）、[レビューその9.md](レビューその9.md)、[レビューその10.md](レビューその10.md)  
**ベースラインタグ**: `clean-baseline-2026-05-24`（作成済み）  
**TCC バイナリ**: `dev\tcc.exe` — tcc version 0.9.28rc (x86_64 Windows)  
**Phase 1 状態**: **A-P 〜 A-9 完了**（Stage 1 基盤）  
**Stage 2 状態**: **完了**（マングリング・オーバーロード・参照型・修飾名・レビュー10 Critical 対応済み）  
**Stage 3 状態**: **一部完了**（`this`・メンバ関数呼び出し・メンバ変数アクセス・クラス型名解決。デフォルト引数・インライン本体は未接続）

---

## 0. 現在の到達状態（2026-05-25）

### 0.1 検証結果（実施済み）

| ゲート | 結果 |
|--------|------|
| `msbuild tcc.vcxproj` Release\|x64 + Win32 Debug | 成功 |
| `build.bat` 端到端 | 成功 |
| `dev/test/run_all.bat`（L1 `-c` + `mixed_link.bat`） | 成功 |
| CUnit `con_c_vs_test.exe` | 5 tests, 0 failures（Stage2 含む） |
| CPPUnit `vstest.console.exe` | **15/15** passed |
| `repro_double6.c` | 成功（double 回帰） |
| `dev/test/a7/member_call.cpp` リンク・実行 | **exit 0**（Stage 3 代表） |

### 0.2 `dev\tcc.exe` で通る C++ 相当の仕様（Stage 1 + Stage 2 + Stage 3 一部）

詳細は [実装済み.md](実装済み.md)。概要:

| 層 | 状態 |
|----|------|
| Stage 1（TU / 字句 / `extern "C"` ブロック / `class` 最小パース） | ○ 完了 |
| Stage 2（TCC 独自マングル `__tcc_<name>_<args>`、オーバーロード解決、参照型 `&`、修飾名 `::`） | ○ 完了 |
| Stage 3（`this`、メンバ関数呼び出し `obj.method()`、メンバ変数 `x = a`、`Point p` 型ローカル） | △ **§19 参照**。デフォルト引数・インライン本体は未接続 |
| `extern "C" void foo();` 単一宣言 | ○ `decl_once_flag` で 1 宣言のみ消費 |
| `extern "C++"` | × エラー（Stage 1 方針維持） |
| 継承 / テンプレート / STL / rvalue ref `&&` | × Stage 4 以降 |

### 0.3 未コミット変更（git status 時点）

**変更済み（代表）**:

- `tcc.h`, `libtcc.c`, `tccpp.c`, `tccgen.c`（Stage 1 + Stage 2）
- `build.bat`, `tcc.vcxproj`, `dev/tcc.exe`
- `test/cppuniut/cppuniut.cpp`, `test/vs_test/`（CUnit Stage2、CPPUnit a6）
- `.gitignore`（P1 作業用 `_*.c` 等）

**新規（未追跡・代表）**:

- `dev/test/a5/`, `dev/test/a6/`, `dev/test/a7/member_call.cpp`
- `test/vs_test/test_stage2_mangling.c`, `test/vs_test/test_common.h`
- `レビューその9.md`, `レビューその10.md`, `実装済み.md`

---

## 1. 概要

### 1.1 何を実装したか

TCC に **C++ Stage 1 基盤**を追加した。ISO C++98 コンパイラではなく、次の層のみ。

| 層 | 内容 |
|----|------|
| TU 管理 | `.cpp` 等を C++ モード（`cpp=1`）でコンパイル |
| 字句 | C++ キーワードの条件付き認識／降格（混合コンパイル対応） |
| プリプロ | C++ TU のみ `#define __cplusplus 199711L` |
| 構文 | `extern "C" { }` ブロック、`class` の最小パース（中身はほぼ struct） |
| テスト | `dev/test/` L1、`build.bat`、CPPUnit/CUnit ハーネス |

### 1.2 できないこと（Stage 3 以降）

メンバ関数のコード生成、`this`、継承、テンプレート、STL、`extern "C++"`、`extern "C" { #include ... }` 等は **未実装**。  
Stage 2 で対応済みの項目（マングル、オーバーロード、参照型 `&`、修飾名 `::`、`extern "C" void foo();` 単一宣言）は [実装済み.md](実装済み.md) を参照。

---

## 2. 作業規律（必読）

### 2.1 ソースエンコーディング

| ファイル種別 | エンコーディング |
|--------------|------------------|
| `tcc.h`, `libtcc.c`, `tccpp.c`, `tccgen.c` | **CP932（Shift_JIS）** — 既存日本語メッセージ維持 |
| 新規テスト・`cppuniut.cpp`・`vs_test_main.c` | **ASCII 推奨**（エラー文言は英語） |

**禁止**: Cursor の Write/StrReplace で CP932 ファイルを編集（`Encoding not recognized` または UTF-8 化で文字化け）。

**推奨パッチ方法**（PowerShell）:

```powershell
$enc = [System.Text.Encoding]::GetEncoding(932)
$p = "e:\work\work_github\tpp\tcc_dx\tccgen.c"
$c = [System.IO.File]::ReadAllText($p, $enc)
# $c = $c.Replace("old", "new")
[System.IO.File]::WriteAllText($p, $c, $enc)
```

新規 ASCII ファイルは `Set-Content -Encoding Ascii` で作成。

### 2.2 ビルド

```batch
call "C:\Program Files\Microsoft Visual Studio\2022\Professional\Common7\Tools\VsDevCmd.bat" -arch=x64
msbuild tcc.vcxproj /p:Configuration=Release /p:Platform=x64
copy /Y x64\Release\tcc.exe dev\tcc.exe
```

### 2.3 完了ゲート

```batch
build.bat
cd test\vs_test && call build_vs_vs_test.bat
vstest.console.exe ..\cppuniut\x64\Debug\cppuniut.dll
```

---

## 3. 変更ファイル一覧

### 3.1 TCC 本体（手でパッチ）

| ファイル | 変更種別 |
|----------|----------|
| `tcc.h` | 構造体・マクロ・TCCState フィールド追加 |
| `libtcc.c` | `is_cpp_source`, `tcc_compile` フラグ, `guess_filetype`, `-x c++` |
| `tccpp.c` | キーワード降格, `__cplusplus`, `tok_alloc_demote` |
| `tccgen.c` | `class`, `extern "C"`, `struct_decl` 拡張 |

**変更しない**: `tcctok.h`（C++ キーワードは既存 DEF で定義済み）

### 3.2 インフラ・テスト（新規／更新）

| パス | 内容 |
|------|------|
| `build.bat` | Release ビルド + smoke + repro + CUnit + run_all |
| `dev/tcc_set.bat` | `TCC` 環境変数 |
| `dev/test/**` | L1 テストソース・`run_all.bat` |
| `dev/test/repro_double6.c` | double 回帰 |
| `.githooks/pre-commit.bat` | msbuild + repro |
| `.githooks/pre-commit` | bat 呼び出しラッパー |
| `test/cppuniut/cppuniut.cpp` | TCC 駆動 CPPUnit |
| `test/vs_test/test_common.h` | 新規 |
| `test/vs_test/vs_test_main.c` | Foundation CUnit |
| `test/vs_test/con_c_vs_test.vcxproj` | include パス・CompileAsC |

---

## 4. A-1: C++ モード（TU フラグ）

### 4.1 `tcc.h`

**TokenSym**（`char str[1]` の直前に 1 行）:

```c
    int alt_ident_tok;   /* A-2: demoted identifier tok cache */
    char str[1];
```

**マクロ**（TokenSym 定義の後）:

```c
#define TCC_CXX_VERSION 199711L
```

**TCCState**（`cversion` 付近）:

```c
    /* C++ language options */
    unsigned char cpp;
    unsigned char cpp_forced;
    unsigned char extern_c;
    unsigned char lex_c;
```

### 4.2 `libtcc.c`

**forward 宣言**（`tcc_compile` の直前）:

```c
static int is_cpp_source(const char *filename);
```

**`tcc_compile()` 先頭付近** — 保存・設定:

```c
    unsigned char saved_cpp, saved_extern_c, saved_lex_c;
    ...
    saved_cpp = s1->cpp;
    saved_extern_c = s1->extern_c;
    saved_lex_c = s1->lex_c;

    if (setjmp(s1->error_jmp_buf) == 0) {
        ...
        s1->cpp = s1->cpp_forced;
        if (!s1->cpp && str && fd != -1)
            s1->cpp = is_cpp_source(str);
        s1->extern_c = 0;
        s1->lex_c = 0;
```

**`tcc_compile()` 終了前** — 復元:

```c
    s1->cpp = saved_cpp;
    s1->extern_c = saved_extern_c;
    s1->lex_c = saved_lex_c;
```

**新関数**:

```c
static int is_cpp_source(const char *filename)
{
    const char *ext = tcc_fileextension(filename);
    if (!ext[0])
        return 0;
    ext++;
    return !PATHCMP(ext, "cpp") || !PATHCMP(ext, "cxx")
        || !PATHCMP(ext, "cc")  || !PATHCMP(ext, "hpp");
}
```

**`guess_filetype()`** — `.cpp/.cxx/.cc/.hpp` を `AFF_TYPE_C` に:

```c
            else if (!PATHCMP(ext, "cpp") || !PATHCMP(ext, "cxx")
                  || !PATHCMP(ext, "cc")  || !PATHCMP(ext, "hpp"))
                filetype = AFF_TYPE_C;
```

**`tcc_parse_args()` — `TCC_OPTION_x`**:

```c
            if (*optarg == 'c' && !optarg[1])   /* -x c のみ。 -x c++ と区別 */
                x = AFF_TYPE_C;
            else if (!strcmp(optarg, "c++") || !strcmp(optarg, "c++-header"))
                x = AFF_TYPE_C, s->cpp_forced = 1;
```

> **重要**: `*optarg == 'c'` だけだと `-x c++` の先頭 `c` にマッチして壊れる。`!optarg[1]` 必須。

---

## 5. A-2: C++ キーワード条件付き有効化

### 5.1 `tccpp.c` — 新規関数（`add_char` の後あたり）

```c
static int effective_cpp_lex(TCCState *s1)
{
    if (s1->lex_c)
        return 0;
    if (!s1->cpp)
        return 0;
    return 1;
}

static int is_cpp_only_keyword(int t)
{
    switch (t) {
    case TOK_CLASS:
    case TOK_PUBLIC:
    case TOK_PRIVATE:
    case TOK_PROTECTED:
    case TOK_VIRTUAL:
    case TOK_THIS:
    case TOK_OPERATOR:
    case TOK_NAMESPACE:
    case TOK_TRUE:
    case TOK_FALSE:
        return 1;
    default:
        return 0;
    }
}

static TokenSym *tok_alloc_demote(const char *str, int len)
{
    TokenSym *ts, **ptable;
    int i;

    if (tok_ident >= SYM_FIRST_ANOM)
        tcc_error("memory full (symbol)");

    i = tok_ident - TOK_IDENT;
    if ((i % TOK_ALLOC_INCR) == 0) {
        ptable = tcc_realloc(table_ident, (i + TOK_ALLOC_INCR) * sizeof(TokenSym *));
        table_ident = ptable;
    }

    ts = tal_realloc(toksym_alloc, 0, sizeof(TokenSym) + len);
    table_ident[i] = ts;
    ts->tok = tok_ident++;
    ts->sym_define = NULL;
    ts->sym_label = NULL;
    ts->sym_struct = NULL;
    ts->sym_identifier = NULL;
    ts->len = len;
    ts->hash_next = NULL;
    ts->alt_ident_tok = 0;
    memcpy(ts->str, str, len);
    ts->str[len] = '\0';
    /* hash チェーンに繋げない（tok_alloc_new の *pts = ts を省略） */
    return ts;
}

static int demote_cpp_keyword_to_ident(TokenSym *ts)
{
    if (ts->alt_ident_tok == 0)
        ts->alt_ident_tok = tok_alloc_demote(ts->str, ts->len)->tok;
    return ts->alt_ident_tok;
}
```

### 5.2 `tok_alloc_new()` に 1 行

```c
    ts->alt_ident_tok = 0;
```

（`ts->hash_next = NULL;` の近く）

### 5.3 `next_nomacro()` — `token_found:` 相当、`tok = ts->tok;` の直後

```c
        tok = ts->tok;
        if (!effective_cpp_lex(tcc_state) && is_cpp_only_keyword(tok))
            tok = demote_cpp_keyword_to_ident(ts);
```

---

## 6. A-3: `__cplusplus`

### 6.1 `tccpp.c` — `tcc_predefs()` 内、`__STDC__` の直前

```c
    if (s1->cpp && !is_asm) {
        cstr_printf(cs, "#define __cplusplus %dL\n", (int)TCC_CXX_VERSION);
    }
```

`.c` TU（`cpp==0`）では定義されない。

---

## 7. A-4: `extern "C" { }`

### 7.1 `tccgen.c` — `decl()` の `while (1)` 先頭

```c
        if (tcc_state->cpp && tok == TOK_EXTERN) {
            next();
            if (tok == TOK_STR) {
                const char *s = tokc.str.data;
                int len = tokc.str.size - 1;

                if (len == 1 && s[0] == 'C') {
                    next();
                    if (tok != '{')
                        tcc_error("Stage 1: extern C block only");
                    next();
                    tcc_state->extern_c++;
                    tcc_state->lex_c++;
                    while (tok != '}') {
                        if (tok == TOK_EOF)
                            tcc_error("unclosed extern C block");
                        decl(l);
                    }
                    next();
                    tcc_state->lex_c--;
                    tcc_state->extern_c--;
                    continue;
                }
                if (len == 3 && !memcmp(s, "C++", 3)) {
                    next();
                    tcc_error("Stage 1: extern C++ not supported");
                }
                tcc_error("unsupported linkage");
            } else {
                unget_tok(TOK_EXTERN);
            }
        }
```

### 7.2 `decl()` — `expect("declaration")` 前の修正

`extern "C"` ブロック終端で `decl()` が `}` を declaration 不足と誤認するのを防ぐ:

```c
                if (tok != TOK_EOF) {
                    if (tok == '}' && tcc_state->extern_c)
                        break;
                    expect("declaration");
                }
```

---

## 8. A-5: `class` 最小パース

### 8.1 `struct_decl` シグネチャ

```c
static void struct_decl(CType* type, int u, int is_class)
```

**全呼び出しを第 3 引数付きに更新**:

- `TOK_ENUM` → `struct_decl(&type1, VT_ENUM, 0);`
- `TOK_STRUCT` → `struct_decl(&type1, VT_STRUCT, 0);`
- `TOK_UNION` → `struct_decl(&type1, VT_UNION, 0);`

### 8.2 `parse_btype()` — `case TOK_CLASS:`

```c
        case TOK_CLASS:
            if (!tcc_state->cpp)
                tcc_error("class is C++ only");
            next();
            struct_decl(&type1, VT_STRUCT, 1);
            goto basic_type2;
```

（内部型は `VT_STRUCT`。`is_class=1` でデフォルト private）

### 8.3 `struct_decl` メンバループ

**開始時**:

```c
            int cur_access;
            cur_access = is_class ? ACCESS_PRIVATE : ACCESS_PUBLIC;
```

**アクセスラベル**（`parse_btype` より前）:

```c
                if (tok == TOK_PUBLIC || tok == TOK_PRIVATE || tok == TOK_PROTECTED) {
                    cur_access = (tok == TOK_PUBLIC) ? ACCESS_PUBLIC :
                                 (tok == TOK_PROTECTED) ? ACCESS_PROTECTED : ACCESS_PRIVATE;
                    next();
                    skip(':');    /* 必須。無いと public: の ':' で ';' expected */
                    continue;
                }
```

**メンバ関数型の禁止を class では緩和**:

```c
                        if (((type1.t & VT_BTYPE) == VT_FUNC && !is_class) ||
                            (type1.t & VT_BTYPE) == VT_VOID ||
                            (type1.t & VT_BTYPE) == VT_STORAGE)
                            tcc_error(...);
```

**メンバ関数本体 `{` は読み捨て**（`gen_function` 禁止）:

```c
                    if ((type1.t & VT_BTYPE) == VT_FUNC && tok == '{') {
                        skip_or_save_block(NULL);
                        next();
                        break;
                    }
```

**sym_push 時**:

```c
                        ss->a.access = cur_access;
```

---

## 9. インフラ（A-P / A-8）

### 9.1 `dev/test/` レイアウト

```
dev/test/
  smoke/hello.c       int main(void){ return 0; }
  smoke/empty.c       ;
  smoke/empty.cpp     ;
  repro_double6.c     （履歴.md 由来の double 引数回帰）
  run_all.bat         下記
  a2/keyword_gate.c
  a2/repeated_ident.c
  a2/keyword_lex.cpp
  a2/foo.cpp          ;
  a2/bar.c
  a2/mixed_link.bat
  a3/cplusplus.c      #error if __cplusplus
  a3/cplusplus.cpp    #ifndef __cplusplus #error
  a4/extern_c_block.cpp
  a5/class_min.cpp
  a5/keyword_gate.cpp
```

### 9.2 `dev/test/run_all.bat`

```batch
@echo off
setlocal
pushd "%~dp0"
set TCC=..\tcc.exe
set FAILED=0

for %%f in (smoke\*.c smoke\*.cpp a2\*.c a2\*.cpp a3\*.c a3\*.cpp a4\*.cpp a5\*.cpp) do (
    echo === %%f ===
    "%TCC%" -c "%%f" -o "%%~dpnf.o"
    if errorlevel 1 set FAILED=1
)

popd
exit /b %FAILED%
```

`mixed_link.bat` は for に含めない（手動: `call dev\test\a2\mixed_link.bat`）。

### 9.3 代表テストソース

**a2/keyword_gate.c**:
```c
int class = 1;
int main(void) { class = 2; return class; }
```

**a2/repeated_ident.c**:
```c
int class;
int main(void) { class = 2; class++; return class; }
```

**a3/cplusplus.cpp**:
```c
#ifndef __cplusplus
#error no __cplusplus
#endif
#if __cplusplus != 199711L
#error wrong __cplusplus
#endif
int x;
```

**a4/extern_c_block.cpp**:
```c
extern "C" {
    int c_add(int a, int b);
}
int c_add(int a, int b) { return a + b; }
int main() { return c_add(2, 3); }
```

**a5/class_min.cpp**:
```c
class Point {
    int x;
    int y;
public:
    void set(int a);
};
```

**a5/keyword_gate.cpp**:
```c
class Foo { int x; };
```

### 9.4 `build.bat`（要約）

1. VsDevCmd + `msbuild tcc.vcxproj` Release|x64  
2. `copy` → `dev\tcc.exe`  
3. smoke: `hello.c` コンパイル・実行  
4. `repro_double6.c` コンパイル・実行  
5. CUnit: `con_c_vs_test.vcxproj` + exe 実行  
6. `call dev\test\run_all.bat`

### 9.5 `dev/tcc_set.bat`

```batch
set "TCC=%TCC_ROOT%tcc.exe"
```

### 9.6 `.githooks/pre-commit.bat`

```batch
msbuild tcc.vcxproj /p:Configuration=Release /p:Platform=x64 /t:Build /v:quiet
dev\tcc.exe dev\test\repro_double6.c -o dev\test\repro_double6.exe
dev\test\repro_double6.exe
```

有効化: `git config core.hooksPath .githooks`

### 9.7 タグ

```batch
git tag clean-baseline-2026-05-24
```

（実装**前**のコミットに付けるのが本来。作業後に付けた場合は「変更前 HEAD」の目印として使用）

---

## 10. A-6: CPPUnit（TCC 駆動）

### 10.1 `test/cppuniut/cppuniut.cpp` 要点

- `GetModuleFileNameW` + パスを 4 階層上げて **リポジトリルート**を取得  
- `run_tcc_cmd`: `cmd /c cd /d "<root>" && dev\tcc.exe <args>`  
- `run_batch`: `cmd /c cd /d "<root>" && call dev\test\run_all.bat`  
- `TEST_METHOD`: smoke, a2〜a5 の `-c`、run_all

MSVC は **ハーネスのみ**コンパイル。テスト対象ソースは TCC がコンパイルする。

### 10.2 実行

```batch
msbuild test\cppuniut\cppuniut.vcxproj /p:Configuration=Debug /p:Platform=x64
cd test\vs_test
vstest.console.exe ..\cppuniut\x64\Debug\cppuniut.dll
```

期待: **10 tests passed**

---

## 11. A-7: CUnit

### 11.1 `test/vs_test/test_common.h`

```c
#ifndef TEST_COMMON_H
#define TEST_COMMON_H
#include <CUnit.h>
#define TEST_SUITE_ENTRY(name, setup, teardown, tests) \
  { (name), (setup), (teardown), (tests) }
#endif
```

### 11.2 `test/vs_test/vs_test_main.c`

- `test_no_cplusplus_macro`: `#ifdef __cplusplus` なら FAIL  
- `test_class_as_identifier`: `int class = 42;`  
- 旧 car テスト（意図的 FAIL）は **削除**

### 11.3 `con_c_vs_test.vcxproj`

- 全構成: `AdditionalIncludeDirectories` を  
  `..\..\..\..\tcc\dev\cunit` → **`..\..\dev\cunit`**
- `vs_test_main.c` に `<CompileAs>CompileAsC</CompileAs>`

---

## 12. A-9: MSVC 一括ビルド

`test/vs_test/build_vs_vs_test.bat` — cppuniut 4 構成 + vs_test.sln 8 構成。

---

## 13. 検証コマンド一覧

```batch
REM L1 単体
dev\tcc.exe -c dev\test\a5\class_min.cpp -o nul
dev\tcc.exe dev\test\a4\extern_c_block.cpp -o dev\test\a4\extern_c_block.exe
dev\test\a4\extern_c_block.exe
REM exit code 5 (= c_add(2,3)) が正常

REM 混合
dev\tcc.exe -c dev\test\a2\foo.cpp dev\test\a2\bar.c

REM プリプロ
dev\tcc.exe -E dev\test\a3\cplusplus.cpp | findstr __cplusplus

REM 一気通貫
build.bat
```

---

## 14. 既知の落とし穴（再実装チェックリスト）

| # | 症状 | 原因 | 対処 |
|---|------|------|------|
| 1 | `public:` で `';' expected` | `:` 未消費 | `skip(':')` after access label |
| 2 | `extern "C"` 閉じ `}` で declaration required | `decl()` が `}` でループ | `extern_c` 時 `tok=='}'` で break |
| 3 | `void set(...)` invalid type in class | struct 内 VT_FUNC 禁止 | `&& !is_class` 条件 |
| 4 | `-x c++` が効かない | `-x c` が `c++` にマッチ | `!optarg[1]` 追加 |
| 5 | `bar.c` で class エラー | キーワード降格なし | A-2 全体 + 混合テスト |
| 6 | 日本語化けビルドエラー | UTF-8 で tccpp.c 保存 | CP932 で読み書き |
| 7 | `int class; class=2` sym 不一致 | 毎回新 ident tok | `tok_alloc_demote` + `alt_ident_tok` キャッシュ |

---

## 15. ロールバックと再適用

### 15.1 本体のみ戻す

```batch
git checkout clean-baseline-2026-05-24 -- tcc.h libtcc.c tccpp.c tccgen.c
```

### 15.2 再適用順序

1. A-1 (`tcc.h` + `libtcc.c`)  
2. A-2 (`tcc.h` alt_ident + `tccpp.c`)  
3. A-3 (`tccpp.c` predefs)  
4. A-4 + A-5 (`tccgen.c`) — A-5 の `skip(':')` と `extern_c` break を忘れない  
5. `dev/test/` + `build.bat` + テストプロジェクト  
6. `build.bat` → vstest

### 15.3 差分確認

```batch
git diff tcc.h libtcc.c tccpp.c tccgen.c
```

---

## 16. 実装順序（DAG）

```
A-P → A-1 → A-2 ─┬→ A-4 ─┐
              A-3 ─┴→ A-5 ─┴→ A-6 → A-9
         A-1 → A-7 ─────────────→ A-9
         A-1 → A-8 ─────────────→ A-9
```

---

## 17. 関連ドキュメント

- [IMPLEMENTATION_GUIDE.md](IMPLEMENTATION_GUIDE.md) — 仕様の一次情報  
- [問題と原因.md](問題と原因.md) — struct_decl 内 gen_function 禁止の経緯  
- [レビューその4.md](レビューその4.md) — メンバ関数本体の扱い  
- [レビューその9.md](レビューその9.md) — Phase 1.5 指摘  
- [レビューその10.md](レビューその10.md) — Stage 2 / Critical 指摘  
- [実装済み.md](実装済み.md) — 現在の C++ 機能一覧  
- [PLAN.md](PLAN.md) — Stage 2 以降のロードマップ

---

## 18. Phase 1.5 + Stage 2（レビューその9 / その10 対応）— 2026-05-24

### 18.1 概要

[レビューその9.md](レビューその9.md) の Phase 1.5 残課題と、[レビューその10.md](レビューその10.md) の Stage 2 Critical（D/E/F/G）および即時項目（B / mixed_link / 一時ファイル削除）を実装した。

| 区分 | 内容 |
|------|------|
| Phase 1.5 | `struct_decl` インラインボディ `skip_member_semi`、`build.bat` Win32 Debug、`is_cpp_source` 統合、CPPUnit ルート解決、`.gitignore` |
| Stage 2 本体 | `VT_REFERENCE`、`Sym::parent_class`、TCC 独自マングル、`cpp_resolve_func_call`、修飾名 `::` |
| レビュー10 Critical | D/E/F/G（下表） |
| 即時 | B（ミッドブロック宣言回避）、`run_all.bat` + `mixed_link.bat`、作業用 `_*.c` 削除 |

### 18.2 レビューその10 Critical 対応

| ID | 問題 | 対応（`tccgen.c` 中心） |
|----|------|-------------------------|
| **D** | `extern "C" void foo();` が `decl()` 全体を巻き込む | `decl_once_flag` を立てて `decl(l)` 後に `break` |
| **E** | 1 つ目の関数がマングルされず呼び出し解決なし | 新規 `external_sym` でも `cpp_set_func_mangle_label`；`gfunc_call` 前に `cpp_resolve_func_call`；`sym_push` で互換 C++ 関数の重複定義を拒否 |
| **F** | `mangle_append_type` の `snprintf` オーバーフロー | `mangle_clamp_pos`；struct は `S<len>_<name>` |
| **G** | `cpp_qualified_class` / `cpp_cur_class` のリーク | `tccgen_init()` で NULL クリア |

### 18.3 マングル設計（TCC 独自・暫定）

- 形式: `__tcc_<関数名>_<引数型列>`（`asm_label` に格納）
- 型エンコード例: `i` int, `d` double, `p` ポインタ, `r` 参照（`VT_REFERENCE`）, `S<n>_<struct名>` struct
- `extern_c` 中はマングルしない（C リンケージ）
- 呼び出し時は実引数型からマングル名を組み立て、`prev_tok` チェーンで一致シンボルを検索

### 18.4 変更ファイル（代表）

| ファイル | 変更内容 |
|----------|----------|
| `tcc.h` | `VT_REFERENCE`, `Sym::parent_class` |
| `tccgen.c` | Stage 2 ブロック（マングル・参照・修飾名・D/E/F/G/B） |
| `libtcc.c` | `is_cpp_source()`、`guess_filetype` 統合 |
| `build.bat` | x64 Release + Win32 Debug、CUnit `pushd` 修正 |
| `tcc.vcxproj` | Debug\|Win32 に `ONE_SOURCE=0` 等 |
| `dev/test/a5/class_inline_body.cpp` | インライン定義スキップの回帰 |
| `dev/test/a6/*.cpp` | overload / ref_param / qualified / extern_c_decl |
| `dev/test/run_all.bat` | `mixed_link.bat` 呼び出し追加 |
| `dev/test/a2/mixed_link.bat` | TCC パス `..\..\tcc.exe` に修正 |
| `test/cppuniut/cppuniut.cpp` | a5/a6 テスト、15 TEST_METHOD |
| `test/vs_test/test_stage2_mangling.c` | CUnit Stage2 |
| `.gitignore` | `/_baseline_*.c`, `/_current_*.c`, `/_head_*.c`, `/libtcc.hex.txt` |

### 18.5 作業時の注意（今回も発生）

1. **`external_sym` の if/else 破損**: パッチ適用ミスで `if (!s)` と `else` が分離し、`main` の再定義エラーが発生。HEAD から関数全体を復元して Stage2 差分を再適用して解消。
2. **`overload.cpp` の UTF-8 BOM**: 先頭 BOM により `f` がトークン化されずパース失敗。BOM なし UTF-8 で再保存。
3. **CP932 ファイル**: `tccgen.c` は PowerShell + `Encoding 932` で編集（§2.1 参照）。

### 18.6 検証コマンド（Stage 2 追加分）

```batch
build.bat
dev\tcc.exe -c dev\test\a6\overload.cpp -o nul
dev\test\run_all.bat
cd test\vs_test
vstest.console.exe ..\cppuniut\x64\Debug\cppuniut.dll
pushd "x64\Debug Unicode" && con_c_vs_test.exe && popd
```

期待: L1 全緑、CPPUnit **15/15**、CUnit **5/5**。

### 18.7 未対応（レビューその10 残）

| 項目 | 状態 |
|------|------|
| P1: `libtcc.c` / `tccpp.c` 日本語エラーを ASCII 英文化 | ❌ 未対応（CP932 バイト列のまま） |
| rvalue 参照 `&&` の明示エラー化 | ❌ 未対応 |
| メンバ関数のコード生成 / `this` | **§19 で Stage 3 一部完了** |
| Itanium ABI 完全互換マングル | 非目標（TCC 独自形式を継続） |

---

## 19. Stage 3 — `this`・メンバ関数呼び出し・メンバ変数アクセス — 2026-05-25

### 19.1 概要

[PLAN.md](PLAN.md) / [IMPLEMENTATION_GUIDE.md](IMPLEMENTATION_GUIDE.md) の Stage 3 のうち、**コード生成が必要な最小セット**を `tccgen.c` に実装した。

| 区分 | 内容 | 状態 |
|------|------|------|
| **A** | メンバ関数内の `this` 仮引数（`gen_function`） | ○ |
| **B** | メンバ関数内のデータメンバ参照（`x = a` 等） | ○ |
| **C** | オブジェクト経由メンバ関数呼び出し（`p.set(3,4)`） | ○ |
| **D** | クラス名を型として使用（`Point p;`） | ○ |
| **E** | クラス外定義 `void Point::set(...)` | ○（Stage 2 `::` + typename 登録） |
| **F** | C++ TU の `main` リンク | ○（`main` / `wmain` はマングル除外） |
| **G** | デフォルト引数（パース保存 + 呼び出し時注入） | △ ヘルパー実装済み、クラス内は未接続 |
| **H** | インライン本体の遅延パース | △ ヘルパー実装済み、未接続（`skip_or_save_block` のまま） |

**代表テスト**: `dev/test/a7/member_call.cpp` — コンパイル・リンク・実行 **exit 0**

```cpp
class Point { int x; int y; public: void set(int a, int b); int getX(); };
void Point::set(int a, int b) { x = a; y = b; }
int Point::getX() { return x; }
int main() { Point p; p.set(3, 4); return p.getX() != 3; }
```

### 19.2 変更ファイル

| ファイル | 変更内容 |
|----------|----------|
| `tccgen.c` | Stage 3 本体（§19.3〜§19.5）。行番号は 2026-05-25 時点の目安 |
| `dev/test/a7/member_call.cpp` | Stage 3 代表 L1 テスト（新規） |
| `dev/test/run_all.bat` | a7 は `a7\member_call.cpp` のみ（調査用 `a7\*.cpp` は除外） |

**前提（Stage 2 済み）**: `tcc.h` の `Sym::parent_class`、`cpp_set_func_mangle_label`、`cpp_resolve_func_call`、`parse_cpp_scope_qualifier` 等が既にあること。

### 19.3 静的変数（`tccgen.c` 先頭付近、`cpp_cur_class` の直後）

`/* --- C++ Stage 3: this, member calls, default args --- */` コメント付きで追加:

```c
static SValue cpp_member_this;
static int cpp_member_this_pending;
static Sym *cpp_this_sym;
static Sym *cpp_cur_func_class;
```

| 変数 | 役割 |
|------|------|
| `cpp_member_this` | `.` / `->` でメンバ関数を選んだとき、呼び出し元オブジェクトのアドレス（`SValue`） |
| `cpp_member_this_pending` | 上記を `(` 呼び出し前に第 1 引数として注入するフラグ |
| `cpp_this_sym` | **現在コンパイル中のメンバ関数**の `this` 仮引数シンボル（`gen_function` 中のみ有効） |
| `cpp_cur_func_class` | 同上の所属クラス（メンバ名の暗黙ルックアップ用） |

`tccgen_init()` 末尾で NULL / 0 クリア（Stage 2 の `cpp_qualified_class` クリアと同ブロック）:

```c
    cpp_member_this_pending = 0;
    cpp_this_sym = NULL;
    cpp_cur_func_class = NULL;
```

### 19.4 ヘルパー関数（Stage 2 マングルブロックの直後に置く）

#### 19.4.1 `cpp_set_func_mangle_label` — Stage 3 追記（`main` リンク修正）

**問題**: C++ モードですべての関数が `__tcc_<name>_<args>` にマングルされ、`.cpp` の `main` が `__tcc_main_` になりリンク時 `undefined symbol 'main'`。

**修正**: エントリポイント名はマングルしない。

```c
static void cpp_set_func_mangle_label(Sym *sym, CType *type)
{
    char mbuf[256];
    TokenSym *ts;
    int len;
    const char *entry_name;

    if (!tcc_state->cpp || tcc_state->extern_c)
        return;
    if (!sym || (type->t & VT_BTYPE) != VT_FUNC)
        return;
    entry_name = get_tok_str(sym->v, NULL);
    if (!strcmp(entry_name, "main") || !strcmp(entry_name, "wmain"))
        return;
    len = cpp_build_func_mangle(sym->v, type, mbuf, sizeof mbuf);
    if (len <= 0)
        return;
    ts = tok_alloc(mbuf, len);
    sym->asm_label = ts->tok;
}
```

#### 19.4.2 `cpp_lookup_member_func` — クラス外シンボルへ解決

```c
static Sym *cpp_lookup_member_func(Sym *field, CType *obj_type)
{
    int v;
    Sym *s, *class_sym;

    if (!field || !obj_type || !obj_type->ref)
        return field;
    v = field->v & ~SYM_FIELD;
    class_sym = obj_type->ref;
    for (s = sym_find(v); s; s = s->prev_tok) {
        if ((s->type.t & VT_BTYPE) != VT_FUNC)
            continue;
        if (s->parent_class == class_sym)
            return s;
    }
    return field;
}
```

`p.set(...)` ではクラス内の `VT_FUNC` フィールドシンボルではなく、`void Point::set` として登録された **global 関数シンボル**（`parent_class == Point`）を `gfunc_call` 対象にする。

#### 19.4.3 `cpp_lookup_member_field` — メンバ関数内のデータメンバ

```c
static Sym *cpp_lookup_member_field(int v, Sym *class_sym)
{
    Sym *f;
    int v1;

    if (!class_sym)
        return NULL;
    v1 = v | SYM_FIELD;
    for (f = class_sym->next; f; f = f->next) {
        if (f->v == v1 && (f->type.t & VT_BTYPE) != VT_FUNC)
            return f;
    }
    return NULL;
}
```

#### 19.4.4 `cpp_push_member_var` — メンバ変数を評価スタックへ（**順序が重要**）

**誤り（以前）**: `gaddrof()` のみ、または `gaddrof()` の後に `indir()` — TCC の `gaddrof()` は型をポインタに変えないためオフセット計算が壊れる。

**正しい経路**: `this`（ポインタ）→ `indir()` でオブジェクト → `gaddrof()` でアドレス → オフセット加算 → LVAL

```c
static void cpp_push_member_var(Sym *field)
{
    int cumofs, qualifiers;

    if (!cpp_this_sym)
        tcc_error("invalid use of member name");
    cumofs = field->c;
    vset(&cpp_this_sym->type, cpp_this_sym->r, cpp_this_sym->c);
    vtop->sym = cpp_this_sym;
    indir();
    qualifiers = vtop->type.t & (VT_CONSTANT | VT_VOLATILE);
    gaddrof();
    vtop->type = char_pointer_type;
    vpushi(cumofs);
    gen_op('+');
    vtop->type = field->type;
    vtop->type.t |= qualifiers;
    if (!(vtop->type.t & VT_ARRAY))
        vtop->r |= VT_LVAL;
}
```

#### 19.4.5 デフォルト引数・インライン（**実装済み・未接続**）

将来接続用。現状 `member_call.cpp` では未使用。

| 関数 | 役割 |
|------|------|
| `cpp_save_default_arg(Sym *param)` | 関数宣言の `=` 以降を `TokenString` として `param->inline_func_str` に保存。`type_decl()` 内で C++ 時に呼ぶ（**5480 行付近**） |
| `cpp_apply_default_args(Sym *func, int *pnb_args, Sym **psa)` | 呼び出し時、不足引数をマクロ展開して `expr_eq` + `gfunc_param_typed` |
| `cpp_register_member_body(...)` | インライン本体 TokenString をフィールドシンボルへ |
| `cpp_finish_member_inlines(Sym *class_sym)` | クラス終了時に `inline_fns` へ登録（**呼び出し元未接続**） |

### 19.5 フックポイント（差分の当て方）

#### 19.5.1 `struct_layout` — `VT_FUNC` メンバをレイアウトから除外

```c
    for (f = type->ref->next; f; f = f->next) {
        if ((f->type.t & VT_BTYPE) == VT_FUNC)
            continue;
        /* ... 既存の size/align 計算 ... */
```

メンバ関数はデータではないため `f->c`（オフセット）を付けない。

#### 19.5.2 `struct_decl` — クラス typename 登録（**`tag_v` バグ修正**）

`struct_layout(type, &ad);` の直後、`if (is_class)` ブロック:

```c
            if (is_class) {
                int tag_v;
                CType ctype;

                tag_v = s->v & ~SYM_STRUCT;
                ctype = *type;
                ctype.t &= ~VT_STRUCT;
                sym_push(tag_v, &ctype, VT_TYPEDEF, 0);
            }
```

**過去バグ**: `sym_push(v, ...)` と書くとループ変数 `v`（最後のメンバ名 `set` 等）が typedef 名になり、`void Point::set` で再定義エラー。**必ず `tag_v = s->v & ~SYM_STRUCT` を使う**（`s` はクラスタグシンボル）。

#### 19.5.3 `parse_btype` — クラス名を型として解決（**`t = type->t` 必須**）

`default:` ラベル内、typedef チェックより前:

```c
            s = sym_find(tok);
            if (tcc_state->cpp) {
                Sym *stsym = struct_find(tok);
                if (stsym && (stsym->type.t & VT_BTYPE) == VT_STRUCT) {
                    next();
                    type->t = stsym->type.t;
                    type->ref = stsym;
                    typespec_found = 1;
                    st = bt = -2;
                    t = type->t;   /* ★ 必須: 無いと the_end で type->t = VT_INT に上書き */
                    break;
                }
            }
            if (!s || !(s->type.t & VT_TYPEDEF))
                goto the_end;
```

**過去バグ**: `type->t` だけ更新しローカル `t` が `VT_INT` のまま → ループ 2 回目で `the_end` → `Point p` が `int p` になる。

#### 19.5.4 `unary` / postfix — `.` と `->`（メンバ関数 vs データメンバ）

**データメンバ**（既存 C 構造体と同型）: `find_field` → `gaddrof` → オフセット加算 → LVAL

**メンバ関数**（C++）:

```c
        else if (tok == '.' || tok == TOK_ARROW) {
            int qualifiers, cumofs;
            CType obj_type;
            Sym *field;

            if (tok == TOK_ARROW)
                indir();
            qualifiers = vtop->type.t & (VT_CONSTANT | VT_VOLATILE);
            test_lvalue();
            next();
            field = find_field(&vtop->type, tok, &cumofs);
            if (tcc_state->cpp && (field->type.t & VT_BTYPE) == VT_FUNC) {
                Sym *fsym;

                obj_type = vtop->type;
                gaddrof();
                cpp_member_this = *vtop;
                vpop();
                fsym = cpp_lookup_member_func(field, &obj_type);
                vset(&fsym->type, fsym->r | VT_SYM, 0);
                vtop->sym = fsym;
                vtop->r &= ~VT_LVAL;
                cpp_member_this_pending = 1;
            } else {
                gaddrof();
                vtop->type = char_pointer_type;
                vpushi(cumofs);
                gen_op('+');
                /* ... field->type, LVAL ... */
            }
            next();
        }
```

**禁止**: メンバ関数分岐で `gaddrof()` の後に `indir()` を挟む（データメンバ側も `this_sv` + 二重 `indir()` は NG）。

#### 19.5.5 関数呼び出し `(` — `this` を第 1 引数として注入（**x64 ABI 順序**）

`cpp_resolve_func_call` の**後**、`gfunc_call(nb_args)` の**前**:

```c
            if (cpp_member_this_pending) {
                int na = nb_args;

                if (na == 0) {
                    vpushv(&cpp_member_this);
                    nb_args++;
                } else {
                    vtop++;
                    nb_args++;
                    memmove(vtop - nb_args + 1, vtop - nb_args,
                        na * sizeof(SValue));
                    vtop[-nb_args + 1] = cpp_member_this;
                }
                cpp_member_this_pending = 0;
            }
            gfunc_call(nb_args);
```

**スタックレイアウト**（`gfunc_call` 期待）: `vtop[-nb_args]` = 関数、`vtop[-nb_args+1]` = 第 1 引数（= `this`）、以降が実引数。

**過去バグ**:

| 誤った実装 | 症状 |
|------------|------|
| `vpushv(&cpp_member_this)` のみ（末尾 push） | x64 で `this` が RCX 以外のレジスタに入り AV（exit `0xC0000005`） |
| `memmove(vtop - nb_args - 1, vtop - nb_args, nb_args * ...)` のみ | 第 1 実引数が消える / vstack leak |
| `vswap()` ループ | 複数引数で順序が戻る |

**`na == 0`**: 引数なしメンバ呼び出し `p.foo()` — `vpushv` のみで `[func, this]` になる。

#### 19.5.6 `TOK_THIS`（`unary` の switch）

```c
    case TOK_THIS:
        if (!tcc_state->cpp || !cpp_this_sym)
            tcc_error("invalid use of 'this'");
        next();
        vset(&cpp_this_sym->type, cpp_this_sym->r, cpp_this_sym->c);
        vtop->sym = cpp_this_sym;
        break;
```

#### 19.5.7 識別子 — メンバ関数内の暗黙メンバルックアップ（`default:` / `tok_identifier:`）

`sym_find` が失敗し、かつ `cpp_cur_func_class` があるとき:

```c
        if (!s && tcc_state->cpp && cpp_cur_func_class) {
            Sym *mf = cpp_lookup_member_field(t, cpp_cur_func_class);
            if (mf) {
                cpp_push_member_var(mf);
                break;
            }
        }
```

`Point::set` 内の `x = a` で `x` をデータメンバとして解決。

#### 19.5.8 `gen_function` — `this` 仮引数の挿入

`sym->parent_class` があるメンバ関数の prolog 前（**一時的**に param リスト先頭へ挿入）:

```c
    this_param = NULL;
    saved_param_next = NULL;
    cpp_this_sym = NULL;
    cpp_cur_func_class = sym->parent_class;
    if (sym->parent_class && tcc_state->cpp) {
        CType pt;

        pt.t = VT_PTR;
        pt.ref = sym->parent_class;
        this_param = sym_malloc();
        this_param->v = TOK_THIS;
        this_param->type = pt;
        saved_param_next = sym->type.ref->next;
        this_param->next = saved_param_next;
        sym->type.ref->next = this_param;
        cpp_this_sym = this_param;
    }
    /* ... gfunc_prolog(sym); block(0); gfunc_epilog(); ... */
    if (this_param) {
        sym->type.ref->next = saved_param_next;
        sym_free(this_param);
    }
    cpp_this_sym = NULL;
    cpp_cur_func_class = NULL;
```

- 呼び出し側は §19.5.5 で `this` を注入。宣言側の param 型列には `this` を含めない（マングルは Stage 2 の `(int,int)` のまま）。
- `gfunc_prolog` が `this_param` を第 1 引数としてスタック/レジスタに割り当て、`cpp_this_sym->r/c` が有効になる。

#### 19.5.9 `struct_decl` — メンバ関数フィールドに `parent_class`

メンバ宣言ループ内（`sym_push(v | SYM_FIELD, ...)` の直後）:

```c
                        if ((type1.t & VT_BTYPE) == VT_FUNC)
                            ss->parent_class = s;
```

`s` はクラスタグシンボル（`sym_push(v | SYM_STRUCT, ...)` の結果）。

### 19.6 既知の落とし穴（ソース復元時に必読）

| # | 症状 | 真因 | 確認 |
|---|------|------|------|
| 1 | `.cpp` で `undefined symbol 'main'` | `main` も `__tcc_main_` にマングル | §19.4.1 |
| 2 | `Point p` が `int` 扱い | `parse_btype` で `t = type->t` 漏れ | §19.5.3 |
| 3 | `void Point::set` 再定義 / 型不一致 | typename 登録で `sym_push(v,...)`（メンバ名） | §19.5.2 |
| 4 | `member_call` AV / 戻り値不正 | `this` を引数スタック末尾に push | §19.5.5 |
| 5 | メンバ関数内 `x` アクセス AV | `cpp_push_member_var` の indir/gaddrof 順序 | §19.4.4 |
| 6 | データメンバ `.` アクセス AV | dot ハンドラで `gaddrof` 後 `indir` | §19.5.4 |
| 7 | `tccgen.c` パッチ後に 0 バイト | PowerShell `>` リダイレクトが UTF-16 化 | **必ず** `cmd /c "git cat-file blob ... > tccgen.c"` または CP932 `WriteAllLines` |
| 8 | `gfunc_call(nb_args)` 消失 | `if (cpp_member_this_pending)` を `else` に誤結合 | 復元後 grep で `gfunc_call(nb_args)` を確認 |

### 19.7 ソース復元手順（`tccgen.c` 喪失時）

1. **Git ベース**: `git restore tccgen.c` で HEAD（Stage 2 未コミットなら Stage 1 のみ）を取得。
2. **Stage 2 差分**: §18 を適用（マングル・`parent_class`・`::` 等）。
3. **Stage 3 差分**: 本 §19.3〜§19.5 を**この順**で適用:
   1. 静的変数 + `tccgen_init` クリア
   2. ヘルパー関数ブロック（§19.4）
   3. `struct_layout` / `struct_decl` / `parse_btype`
   4. `unary`（`TOK_THIS`、識別子、`.`/`->`、呼び出し `this` 注入）
   5. `gen_function`
4. **救済 blob**（2026-05-25 作業ツリー、Stage 3 込み・§19.5.5 修正前）:

   ```batch
   git fsck --unreachable
   git cat-file blob 8f20378c6ee5c32f87f51cee8165ffc5b5fffa41 > tccgen.c
   ```

   復元後 §19.4.1 / §19.4.4 / §19.5.2 / §19.5.3 / §19.5.4 / §19.5.5 を再適用。

5. **編集方法**: `tccgen.c` は **CP932**。PowerShell 例:

   ```powershell
   $enc = [System.Text.Encoding]::GetEncoding(932)
   $lines = [System.Collections.Generic.List[string]]::new()
   $lines.AddRange([string[]][System.IO.File]::ReadAllLines("tccgen.c", $enc))
   # ... Insert / InsertRange ...
   [System.IO.File]::WriteAllLines("tccgen.c", $lines.ToArray(), $enc)
   ```

   `InsertRange` には **`[string[]]@(...)`** を渡す（`System.Object[]` だと失敗し、空ファイル化の原因になる）。

### 19.8 検証コマンド

```batch
build.bat
dev\tcc.exe dev\test\a7\member_call.cpp -o dev\test\a7\member_call.exe
dev\test\a7\member_call.exe
echo exit=%ERRORLEVEL%
dev\tcc.exe dev\test\a7\main_only.cpp -o dev\test\a7\main_only.exe
dev\test\a7\main_only.exe
```

期待: いずれも **exit 0**。`build.bat` 全体も緑。

### 19.9 未対応（Stage 3 残）

| 項目 | 状態 |
|------|------|
| クラス内デフォルト引数（宣言時 `=`） | パースは `cpp_save_default_arg` のみ。クラスメンバ宣言ループへの接続要 |
| インライン関数本体の遅延コンパイル | `cpp_finish_member_inlines` 未呼び出し |
| `test/vs_test/test_stage3_members.c`（CUnit） | 未追加 |
| `typedef Point P;` / `typedef class` 一般化 | 未対応（`a7/typedef_class.cpp` 失敗） |
| `a7/` 調査用 `.cpp` 大量ファイル | `run_all.bat` からは除外済み。整理は任意 |

## 20. レビューその10 Critical バグ対応 (2026-05-25)

レビューその10で指摘されたCriticalバグ（Bug 1〜3およびShift-JIS文字化け）への対応履歴です。ソースがロールバックされた際はこの項目の内容を再実装してください。

### 20.1 Bug 1: `extern "C"` 単一宣言の暴走 (Bug D)

**【問題】**
`extern "C" void foo();` のようにブロック `{}` を伴わない単一宣言の場合、`decl(l)` 内の無限ループにより後続のすべての宣言が `extern "C"` スコープに巻き込まれてしまう問題がありました。

**【修正】**
`tcc_state->extern_c` 等のフラグを立てて `decl(l)` を呼ぶ前に、1回だけの宣言で抜けるためのフラグ `decl_once_flag` を導入しました。

**【元ソース】** (`tccgen.c`)
```c
                    next();
                    tcc_state->extern_c++;
                    tcc_state->lex_c++;
                    decl(l);
                    tcc_state->lex_c--;
                    tcc_state->extern_c--;
                    continue;
```

**【修正後ソース】** (`tccgen.c`)
```c
                    next();
                    tcc_state->extern_c++;
                    tcc_state->lex_c++;
                    decl_once_flag = 1; /* 追加 */
                    decl(l);
                    decl_once_flag = 0; /* 追加 */
                    tcc_state->lex_c--;
                    tcc_state->extern_c--;
                    continue;
```
および `decl()` ループの末尾:
```c
        if (decl_once_flag)
            break;
    }
    return 0;
}
```

### 20.2 Bug 3: `snprintf` のバッファ境界チェック漏れ (Bug F)

**【問題】**
`mangle_append_type` にて、マングル名の組み立て時に `snprintf` の戻り値チェックが甘く、`*pos` がバッファサイズを超えて増加し続けるバッファオーバーフローの危険がありました。

**【修正】**
`pos` を `buf_size - 1` にクランプするヘルパー関数 `mangle_clamp_pos` を導入しました。

**【修正後ソース】** (`tccgen.c`)
```c
static void mangle_clamp_pos(int buf_size, int *pos)
{
    if (*pos >= buf_size)
        *pos = buf_size - 1;
}

/* 各 snprintf 後に以下のようなチェックを追加 */
        wrote = snprintf(buf + *pos, buf_size - *pos, "S%d_%s", nlen, sn);
        if (wrote < 0 || wrote >= buf_size - *pos) {
            mangle_clamp_pos(buf_size, pos);
            return;
        }
        *pos += wrote;
```

### 20.3 Bug 2: オーバーロード解決のミスマッチ (Bug E)

**【問題】**
呼び出し側の引数からマングル名文字列を直接組み立て（例: `f(1.0)` -> `__tcc_f_d`）、関数の `asm_label` と文字列比較してオーバーロードを解決する実装になっていました。しかし、これではC++の**暗黙の型変換**（例: `f(short)` を渡して `f(int)` を呼ぶ）が発生した場合にマングル名が一致せず、リンクエラーまたは誤解決になります。

**【修正】**
文字列比較ではなく、関数シンボルの連なり（`s->prev_tok`）をイテレートし、各関数の仮引数と呼び出し側の実引数（`vtop`）の型互換性を調べて、最もマッチする（スコアが高い）関数を選ぶように `cpp_resolve_func_call` を抜本的に再実装しました。

**【元ソース】** (`tccgen.c`)
```c
static Sym *cpp_resolve_func_call(int v, int nb_args)
{
    char mbuf[256];
    Sym *s;
    const char *want;

    if (!tcc_state->cpp || tcc_state->extern_c)
        return sym_find(v);
    if (!cpp_build_call_mangle(v, nb_args, mbuf, sizeof mbuf))
        return sym_find(v);
    want = mbuf;
    for (s = sym_find(v); s; s = s->prev_tok) {
        if ((s->type.t & VT_BTYPE) == VT_FUNC && s->asm_label) {
            if (!strcmp(get_tok_str(s->asm_label, NULL), want))
                return s;
        }
    }
    return sym_find(v);
}
```

**【修正後ソース】** (`tccgen.c`)
```c
static Sym *cpp_resolve_func_call(int v, int nb_args)
{
    Sym *s, *best = NULL;
    int best_score = -1;

    if (!tcc_state->cpp || tcc_state->extern_c)
        return sym_find(v);

    for (s = sym_find(v); s; s = s->prev_tok) {
        Sym *p;
        int i, score = 0, match = 1;

        if ((s->type.t & VT_BTYPE) != VT_FUNC)
            continue;

        p = s->type.ref->next;
        for (i = 0; i < nb_args; i++) {
            CType *arg_type = &vtop[-nb_args + 1 + i].type;
            int p_bt, a_bt;

            if (!p || p->type.t == VT_VOID) {
                if (s->type.ref->f.func_type == FUNC_ELLIPSIS)
                    break;
                match = 0;
                break;
            }

            if (is_compatible_types(&p->type, arg_type)) {
                score += 10;
            } else {
                p_bt = p->type.t & VT_BTYPE;
                a_bt = arg_type->t & VT_BTYPE;
                if ((is_float(p_bt) || is_integer_btype(p_bt)) &&
                    (is_float(a_bt) || is_integer_btype(a_bt))) {
                    score += 1;
                } else if (p_bt == VT_PTR && a_bt == VT_PTR) {
                    score += 1;
                } else {
                    match = 0;
                    break;
                }
            }
            p = p->next;
        }

        if (match && p && p->type.t != VT_VOID)
            match = 0;

        if (match && score > best_score) {
            best_score = score;
            best = s;
        }
    }

    if (best)
        return best;
    return sym_find(v);
}
```
※この変更に伴い、`is_integer_btype` や `is_float` を前方参照できるように `tccgen.c` 先頭付近にプロトタイプ宣言を追加しました。

### 20.5 Bug 4 (Issue A): エラーメッセージのShift-JIS文字化け

**【問題】**
`libtcc.c` のエラーメッセージに CP932 (Shift-JIS) が使われており、C4819警告や他のソースとのエンコーディング不整合を引き起こしていました。

**【修正】**
該当文字列を UTF-8 で再記述し、警告を解消しました。

---

*本文書は Phase 1 の再現手順に加え、§18 の Stage 2、§19 の Stage 3 作業履歴を記録する。機能一覧は [実装済み.md](実装済み.md)、今後の着手は [次の実装.md](次の実装.md) / PLAN を参照。*
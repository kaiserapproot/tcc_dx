## 結論

**現時点では Request Changes、まだマージしない方がいいです。**

BUG-20/21/22 の方向性やテスト追加はかなり良いです。ただし PATCH 1 の仮想MI実装に、**コンパイラとして危険な「コンパイル成功するが誤コードになる」経路が少なくとも2つあります。**

特に **thunkシンボル生成**はマージ前に直すべきです。

1. **\[BLOCKER] thunk名が256文字を超えると、意図的に間違ったコードを生成します。** `cpp_new_virtual_thunk()` は名前生成が `buf[256]` を超えるとエラーにせず、派生実装 `impl` をそのまま返しています。しかもコメント自身が「this stays unadjusted」と認めています。 非primary基底 `B*` から `D::f()` を呼ぶケースでは、本来 `B* → D*` の this 補正が必要です。ここで raw `impl` に飛ばすと、**D::f() が B subobject を D 本体だと思って読み書きする**ため、典型的な silent memory corruption になります。同様に secondary vtable 名が256文字を超えた場合も、エラーではなく `continue` して生成を捨てています。 これは「pathologicalだから許容」できるフォールバックではありません。`tcc_error()` で停止するか、そもそもユーザー識別子を結合せず `__cpp_vthunk_<serial>` のような内部一意IDにしてください。
2. **\[BLOCKER] thunkシンボル名が一意ではありません。仮想関数オーバーロードで衝突する可能性が高いです。** 現在は `__cpp_vthunk_<Class>_<Base>_<method-name>` だけで生成しています。関数シグネチャも virtual slot 番号も入っていません。 一方、このtpp自身は「オーバーロード」を実装済み機能として明示しています。 例えば `B` が `virtual int f(int)` と `virtual int f(double)` を持ち、`D` が両方 override すると、両方とも概念上 `__cpp_vthunk_D_B_f` になります。さらにコメントでは local class が存在することまで考慮して thunk 発行をTU末尾に遅延していますから、別関数内に同名ローカルクラスが2個あるケースも衝突候補です。**slot番号または一意serialを必ず名前に入れるべき**です。私は `__cpp_vthunk_<serial>` を推します。
3. **\[HIGH] Win64 ABIの機械語を** **`tccgen.c`** **に直書きしています。少なくともガードが必要です。** thunk は明確に `sub rcx, imm32`、`movabs rax`、`jmp rax` のx86-64/Win64命令列です。 現在の仕様書が対象を「x86\_64 Windows」と限定しているので、**現ターゲットだけを見るなら実装自体は成立**します。 ただしPRは「Debug Win32 buildもgreen」と繰り返し記載しており、実際の仮想MIテストには `sizeof(D) == 24` というx64固定assertまであります。 つまり Win32 は「コンパイラ自体がビルドできただけ」で、thunk実行は検証していない可能性が高いです。最低でも `#if TCC_TARGET_X86_64 && PE` 相当のガードを入れ、非対応ターゲットでは明示エラーにすべきです。「Debug Win32 build green」を機能検証の根拠に使わない方がいいです。
4. *\[HIGH・要実機テスト] D→非primary B* の NULL 変換を追加してください。\*\* 今回 `gen_cast()` から polymorphic class のガードを外し、non-primary base へのオフセット調整を有効化しています。 C++では `D *p = 0; B *b = p;` は `b == 0` のままでなければなりません。単純に `p + base_offset` すると NULL が `0x10` 等になってしまいます。添付diffだけではその後の既存コード全体を確認できないので「確定バグ」とは断定しませんが、**現在の8本のvmiテストにはNULL upcastがありません**。これは必須ゲートです。`D *pd=0; B *pb=pd; if(pb != 0) return 1;` を、非仮想MIと仮想MIの両方に追加してください。
5. **\[HIGHだがスコープ判断可] BUG-22を「修正済み」とするには BUG-23 が近すぎます。** `helper(42)` は直った一方、`helper(v + 1)` はまだアクセス違反になります。PR自身もこれを「実用コードで頻出」「優先度 高」と認識しています。 仕様書にも「ビルド成功後0xC0000005」と明記されています。 これは今回導入した回帰ではないので、**このPRだけのmerge blockerにする必要は必ずしもありません**。ただ、Amateras C++対応を「完成」と扱うのはまだ早いです。普通の `update(x + member)` がクラッシュする状態なので、次PRの最優先で正しいです。
6. **\[MEDIUM/HIGH]** **`CINTERFACE`** **と D3D helper無効化を** **`__TINYC__`** **全体へ強制するのは影響範囲が広すぎます。** `_mingw.h` を読むだけで `CINTERFACE`, `D3D10_NO_HELPERS`, `D3D11_NO_HELPERS` が自動定義されます。 COM側でもTCC C++なら強制的にC形式へ落としています。 その結果、通常のC++ COMコードである `p->Release()` 等やD3D C++ helperを使うコードが、TCCだからという理由だけで別APIになります。Amaterasを通すための妥協としては理解できますが、\*\*Windows headers compatibilityというより「TCC限定C APIモード」\*\*です。専用互換マクロでopt-inにするか、仕様書にかなり目立つ形で書くべきです。
7. **\[MEDIUM] PATCH 3 の** **`.code-workspace`** **はPRから外すことを推奨します。** `../../../work_cross_platform/kaiser_system/amateras` というローカル環境依存パスをリポジトリへ入れています。 他の開発者には存在しない可能性が高く、さらに `cross.h` を `"c"` に固定しているため、今回わざわざ対応している C++ TU の IntelliSense を逆にCとして解析させます。個人用 `.code-workspace` にするかignore対象が妥当です。
8. **\[MEDIUM]** **`glu.h`** **517行全置換はレビュー性をかなり落としています。** 実質目的は `__TINYC__` 時に C++ class forward declaration をC側typedefへ振る程度なのに、PATCH統計では517行変更になっています。 改行コード変更などが原因なら、元のEOLを維持して意味のある数行だけのdiffに戻した方がいいです。SDKヘッダを丸ごと書き換えるdiffは、偶発変更を敵対的レビューで見抜きにくくします。

## 特に評価できるところ

テスト方針は以前よりかなり強くなっています。`a9/*.c` が実際にはゲートされていなかったことを直し、さらに「コンパイルに成功したら失敗」という negative gate を追加したのは正しい方向です。 また、仕様書で「ビルドNG」と「**ビルドは通るが動作が誤る**」を分離したのも重要です。

仮想MIについても、deep re-override、virtual dtor、downcast、diamond/virtual inheritanceを隠さず明記している点は良いです。 したがって問題は「無理に完成扱いしている」ことより、**未発見の silent miscompile がまだ残っている可能性**です。

### 私ならマージ条件をこうします

**最低限、① thunk名を serial/slot ベースに変更、② 名前長overflow時の silent fallback/continue を廃止、③ NULL upcastテスト追加、④ overloaded virtual on secondary base の実行テスト追加。**

そして可能なら、同じゲートで **同名local class × 2、255文字超識別子、x64 thunk実行**も入れます。

この4点を直せば、私はこのPRを \*\*「既知のC++サブセット制限を持つが、今回実装した範囲についてはマージ可能」\*\*まで引き上げられると評価します。現状は、特に1と2があるため **マージ拒否**です。

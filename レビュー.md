## 判定

**大幅に改善していますが、まだ実装着手前に3点修正が必要です。**

なお、`tcc_only.h` を入力にした再生成は再検証し、UTF-8/SHIFT\_JISとも現物とSHA-256が完全一致しました。前回の「`cross_tpp.h` を再生成できない」という指摘は入力ヘッダの取り違えで、rev.3の訂正が正しいです。

## 残る重要指摘

1. **\[P1] 3経路を同じ真偽値ヘルパーへ集約する設計は不十分です**

\[§3.1 (line 235)]\(E:/work/work\_github/tpp/tcc\_dx/amateras対応プラン.md:235)の `cpp_innermost_is_type(int v)` は「型か否か」しか返しません。しかし `parse_btype()` には「どの型が可視か」も必要です。

例えば合法な次のコードがあります。

```
struct X { int member; };

void f()
{
    typedef int X;
    X value = 1;
}
```

ここでは内側の `typedef int X` を使うべきです。真偽値ヘルパーが `true` を返した後、既存の \[struct\_find優先処理 (line 8629)]\(E:/work/work\_github/tpp/tcc\_dx/tccgen.c:8629)を残すと、外側の `struct X` と誤解釈します。

また、\[decl()の13079経路 (line 13079)]\(E:/work/work\_github/tpp/tcc\_dx/tccgen.c:13079)は `X::member` の修飾名検出です。これは計画自身が\[250行目 (line 250)]\(E:/work/work\_github/tpp/tcc\_dx/amateras対応プラン.md:250)で述べているように、非型名を無視する「type/namespace限定探索」が必要で、通常の隠蔽規則とは意味が違います。

**修正案:**

* 通常の型名探索は、可視になった `Sym*` または解決済み型を返すヘルパーにする。
* `parse_btype()` と `cpp_tok_starts_type_name()` はその結果を使う。
* `decl()` の修飾名探索は同じヘルパーへ機械的に置換せず、別の type-only lookup として扱う。
* 「内側typedefが外側classを隠す」テストを追加する。

2. **\[P1] 計画した負例とCモードテストは、現行テストランナーでは実行されません**

\[§3.2 (line 258)]\(E:/work/work\_github/tpp/tcc\_dx/amateras対応プラン.md:258)では `dev/test/a9/` に以下を追加するとしていますが、現行 \[run\_all.bat (line 15)]\(E:/work/work\_github/tpp/tcc\_dx/dev/test/run\_all.bat:15)は `a9\*.cpp` しか列挙しません。

* Dの `.c` テストは対象外。
* 「`tex t;` が失敗すること」を期待する負例は、通常のa9へ置くと単なるゲート失敗になります。
* `KNOWNFAIL` は「失敗し続けること」を検証せず、将来誤って成功しても検出できません。

**修正案:**

* `a9\*.c` をコンパイル・実行ゲートへ追加する。
* `a9\negative\*.cpp` などを設け、コンパイルが非0ならPASS、0ならFAILとする専用ループを追加する。
* 可能なら期待診断も確認し、無関係な構文エラーによる見かけ上のPASSを防ぐ。

3. **\[P1]** **`cross_tpp.h`** **の公開／内部専用がまだ未決定です**

\[§4.4 (line 330)]\(E:/work/work\_github/tpp/tcc\_dx/amateras対応プラン.md:330)は選択肢を明確化しましたが、依然として `(a)/(b)` を選んでいません。全体完了条件もその選択に依存しているため、現状は実装可能な確定プランではありません。

`inc/**` の配布物であり、利用者側を単純にするというamaterasの原則を考えると、特段の内部専用根拠がなければ **(a) 自己完結化** が妥当です。

ただし単純なinclude順変更の前に、`cross_msgbox.h`、`cross_global_value.h`、`win_define.h`間の依存を確認し、TCCとMSVCの両方で「`cross_tpp.h`のみinclude」を試験する必要があります。

## 軽微な指摘

* 冒頭の「推定のまま残した項目は無い」は、\[207行目 (line 207)]\(E:/work/work\_github/tpp/tcc\_dx/amateras対応プラン.md:207)の「MSVCでも同様のはず」や、残課題の「見込む」と矛盾します。「未確認事項は明記した」に弱めるのが正確です。
* amaterasの完了ゲートは改善しましたが、MSVC/TCCでそれぞれ何をコンパイルするか、対象ヘッダ・TU・マクロ・期待成果物までコマンド化すると再現性が上がります。

## 総評

前回のP0事項は解消され、生成物保護・ブランチ依存・STB修正位置・テスト観点も適切になりました。残る本質的な問題は、**通常の型名探索と修飾名のtype-only探索を同一視していること**です。ここを分離し、負例/Cテストを実際にゲートできる形へ直せば、着手可能な水準です。

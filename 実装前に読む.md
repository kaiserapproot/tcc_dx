### **Part A — レビュー用（実装前に読む）**

| **ガイド**  | **内容**                                           |
| :------- | :----------------------------------------------- |
| **A-0**  | 現状・実装原則・実際のテストパス                                 |
| **A-1**  | `cpp_mode` / 拡張子 / `-x c++`（`tcc.h`, `libtcc.c`） |
| **A-2**  | C++ キーワードの C モード無効化（`tccpp.c`）                   |
| **A-3**  | `__cplusplus 199711L`（`tccpp.c`）                 |
| **A-4**  | `extern "C"` 最小実装（`tccgen.c`）                    |
| **A-5**  | `class` 最小パース（`struct_decl` 拡張）                  |
| **A-6**  | CPPUnit テスト（`test/cppuniut/`）                    |
| **A-7**  | CUnit 整備・include パス修正                            |
| **A-8**  | `build.bat` / `dev/tcc.exe` / `tcc_set.bat`      |
| **A-9**  | `build_vs_vs_test.bat` + vstest                  |
| **A-10** | 実装順序・完了判定チェックリスト                                 |

各ガイドには次を含めています。

* 変更ファイルと行番号の目安
* 具体的なコード案（追加・変更前後）
* 完了判定用のコマンド・テストコード

### **Part B — 付録（従来どおり）**

libcunit 改造・ABI・ログテンプレート・メンバポインタチェックリストなど。

## **レビュー時の注意点**

1. **パス名** — `PLAN.md` の `tcc_cpp_unittest` ではなく、実在する `test/cppuniut/`、`con_c_vs_test.vcxproj` を記載しています。
2. **A-2 キーワードゲート** — `TOK_IDENT` への置き換え案は簡易版です。識別子として正しく扱うには `ts->tok` の扱いを要確認です。
3. **A-4** **`extern "C"`** — `parse_btype()` と `decl()` の両方に触れる案です。`TOK_EXTERN` の先読みで通常の `extern` と衝突しないか、レビューしてください。
4. **A-5** **`class`** — `Sym.inline_func_str` / 遅延パースは意図的にスコープ外と明記しています。

ご確認のうえ、修正や追加の希望があればお知らせください。問題なければ Part A の順（A-1 → A-10）で実装に進みます。

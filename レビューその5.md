v2.1 を読みました。**作業規律（A-P）の追加、A-3 注意書き、DAG、エンコーディング規約**などは大きな改善で、v2.0 の致命的な穴の多くが埋まっています。一方で **A-2 と A-4 にはまだ「実装すれば壊れる」レベルの未解決事項が残っています**。優先度順に指摘します。

***

## 🔴 Critical（このまま実装すると壊れる）

### 1. A-2「遅延登録方式」は混合コンパイル（`.c` + `.cpp` 同一 TCCState）で破綻する

プランは「`s1->cpp` のときだけ `register_cpp_keywords()` を呼ぶ」としていますが、`tok_alloc()` が登録するハッシュ表は **TCCState 全体で共有・1 回登録したら消えない**（`tccpp.c:3735` 周辺）。

```
tcc foo.cpp bar.c     ← 1 つの TCCState で順に compile
  ↓ foo.cpp: register_cpp_keywords() → "class" が TOK_CLASS として hash 登録
  ↓ bar.c:   s1->cpp=0 にしても、"class" は既に hash にあるので TOK_CLASS が返る
  → bar.c で int class; が再びエラー

```

つまり「シンプル禁止」とした **v2.0 の** **`tok = TOK_IDENT`** **系の後段処理が結局必要**。遅延登録だけでは不十分です。

**修正案**：

* (a) 常に登録する。`next_nomacro()` で `!s1->cpp && is_cpp_only_keyword(tok)` のとき、`ts` ごと **別の TokenSym に切り替える** か、`tok` と `tokc` を整合させて書き換える
* (b) 単一 TU シナリオに限定し、`tcc foo.cpp bar.c` を **明示的にサポート外**と plan に書く

このどちらかを A-2 で明文化しないと、最初の混合ビルドで再び壊れます。

### 2. A-4 の「単一宣言 `extern "C" void foo();`」が **未完成のまま記述**されている

449–469 行：

```
                    /* extern "C" 単一宣言: この decl 回だけ extern_c を有効化 */
                    {
                        int saved = tcc_state->extern_c;     // ← saved を読む箇所がない
                        tcc_state->extern_c++;
                        /* parse_btype に落ちる — 下の通常フローへ */
                        /* ※ extern は既に消費済み。adbase に VT_EXTERN を付与する小ヘルパが必要 */
                    }

```

* `saved` 未使用（restore がない）
* 「下の通常フローへ落ちる」と書かれているが、コード上 **fall-through する書き方になっていない**（`continue` も `break` もない）
* 「adbase に VT\_EXTERN を付与する小ヘルパが必要」が **TBD のまま**

→ A-R 表で「**`decl()`** **のみに確定**」と書いてあるのに、肝心の単一宣言パスが **未確定**。これが「実装中に方針変更してソース破損」の最頻パターンです（履歴.md と同型）。

**修正案**：Stage 1 の最小スコープを：

| 構文                            | 対応                                  |
| :---------------------------- | :---------------------------------- |
| `extern "C" { ... }`          | **対応**                              |
| `extern "C" void foo();` 単一宣言 | **Stage 1 では未対応**（パースエラーまたは黙って通すだけ） |

として、`saved` ブロックを削除。これだけで A-4 が確定します。単一宣言が必要なら Stage 2 で別途。

### 3. `extern "C" { #include <windows.h> }` で C++ キーワードがヘッダ内に侵入する

`#include` は同一 TU の中で展開され、`s1->cpp` はリセットされません。つまり：

```
/* foo.cpp — s1->cpp=1 */
extern "C" { #include <windows.h> }   /* windows.h 内も s1->cpp=1 のまま */

```

`windows.h` の中に `class` や `this` という識別子（変数名・マクロ）が 1 つでもあれば、A-2/A-5 が動いた時点で**ビルド不能**。`extern "C"` はリンケージ指定で、字句解析モードを変えません。

**A-4 完了判定の** **`windows_smoke.exe`** **は、この事実を踏まえないと必ず失敗します**。

**修正案**：

* `extern "C" { ... }` ブロック内で **C++ キーワードを一時的に識別子として扱う**フラグを追加（`tcc_state->lex_as_c++ = 0`）
* もしくは Stage 1 では `windows_smoke.cpp` をテストから外し、`extern_c.cpp`（自前 C 関数呼び出しのみ）に限定

事前に SDK ヘッダ内の `\b(class|this|true|false|namespace|operator|virtual|public|private|protected)\b` を grep しておくのが必須（A-2 の grep は TCC 本体だけで、`dev/include/` を除外している L352）。**`dev/include/`** **こそ grep すべき対象です**。

***

## 🟡 Important（実装時に確実にハマる）

### 4. A-5「C モードで `TOK_CLASS` を見たら `goto the_end`」は A-2 と矛盾

A-2 が正しく動けば C モードで `tok == TOK_CLASS` にはなりません（識別子トークンになる）。`goto the_end` 分岐は **絶対に到達しないデッドコード** か、**A-2 が壊れたときの保険**かのどちらか不明。

A-2 のアプローチに合わせて：

* A-2 を「常時登録 + 後段書き換え」にした → `goto the_end` は **誤り**（TOK\_CLASS は来る）
* A-2 を「遅延登録」にした → `goto the_end` は **デッドコード**（到達しない）

どちらにせよ書き直しが必要。

### 5. `build.bat` smoke が初回失敗する

A-8-1 の `build.bat`：

```
dev\tcc.exe dev\test\smoke\hello.c -o dev\test\smoke\hello.exe
if errorlevel 1 exit /b 1

```

`dev\test\smoke\hello.c` の **作成手順がプラン内にない**。A-P P-6 完了判定にも入っていない。初回 `build.bat` 実行で必ず失敗 → 開発者が `build.bat` を信用しなくなる。

**修正案**：A-P P-5 に「以下のファイルを作る」リストを明記：

* `dev/test/smoke/hello.c` （最小 `printf("hello\n");`）
* `dev/test/smoke/empty.c`、`empty.cpp`
* `dev/test/repro_double6.c`（履歴.md からコピー）
* `dev/test/run_all.bat`（最初は `exit /b 0` の空シェル）

### 6. pre-commit フックの `sh` シェバング on Windows

`.githooks/pre-commit` を `#!/bin/sh` で書いていますが、Git for Windows 同梱の bash が必要。チームで PowerShell only な人は気づかず commit が通ります。

**修正案**：`.cmd` 版も用意するか、`pre-commit.bat` をフック先にリダイレクトする README を A-P に書く。

### 7. `con_c_vs_test.exe` を「ビルド済みなら実行」では回帰検出にならない

```
if exist "test\vs_test\x64\Debug Unicode\con_c_vs_test.exe" (
  "...\con_c_vs_test.exe"
)

```

`if exist` で **静かにスキップ**されるので、ビルドが古いまま回帰したら気付かない。

**修正案**：`build.bat` で常に `msbuild test\vs_test\con_c_vs_test.vcxproj` を走らせる。

### 8. A-3 で「Windows SDK 失敗時のエスカレーション」が**事前**でなく**事後**になっている

> 「`199711L` で失敗したら 1. ログ記録 2. 必要なら `201103L` に上げる 3. 実装前に smoke する」

これは **失敗ベースの判断**で、実装が進んだ後に値変更すると `__cplusplus < 199711` 前提コードを書いている下流に影響します。

**修正案**：A-3 着手時に「`dev/tcc.exe -E -D__cplusplus=199711L <windows.h ラッパー>`」で SDK 取り込みを **先に検証**し、合わなければ最初から `201103L` で固定する。値を後から変えない。

***

## 🟢 Minor（読者が誤解する／品質低下）

### 9. Part B Stage 4 例の `new BasicTests()` は scope 外

```
TestNode *tests[] = { new BasicTests(), new AdvancedTests(), NULL };

```

`operator new` は Stage 1–5 のどこにも実装計画がない。読者が「Stage 4 までに `new` も動く」と誤読する。注記が必要：「`new` の代わりに静的インスタンスかスタック割当を使う」。

### 10. A-10 の Mermaid 図

GitHub やモダンビューアでは描画されますが、`type IMPLEMENTATION_GUIDE.md` などの素直なテキスト閲覧では読めません。ASCII art の DAG も併記すると親切。

### 11. A-2 grep の除外条件

```
--glob "!dev/include/**" --glob "!tcctok*.h"

```

**dev/include を除外している**のが致命的（上記 #3）。`dev/include/**` は **含めて** grep すべき対象です。

### 12. A-P P-1 の `git push` を無条件で書いている

> `git push origin clean-baseline-2026-05-24   REM リモートがある場合`

「リモートがある場合」とは書かれていますが、本リポジトリは現在 master のみで origin push を意図しているか不明。`git remote -v` で確認手順を入れたほうが安全。

### 13. A-1 章番号が前後している

A-1-1 → A-1-6 → A-1-7 → A-1-2 → A-1-3 … の順で書かれており、上から読む人が混乱します。番号を 1-1→1-2→1-3→1-4→1-5→1-6→1-7 に整理（または挿入箇所を「6 と 7 は最後に追加」と注記）。

### 14. `tcctok.h` 分割の影響範囲が `tcc.h` に未言及

A-2 で `tcctok.h` 分割／除外を提案していますが、`tcc.h:1193` でも `#include "tcctok.h"` が使われており、ここは **enum 定義** として全トークンが必要です。「enum 定義は全件、ハッシュ登録のみ選別」という機構が必要。プランに `tcc.h` 側の扱いを 1 行追加すべき。

***

## 📋 改訂提案サマリ

| 優先    | 章       | 必要なアクション                                                                        |
| :---- | :------ | :------------------------------------------------------------------------------ |
| 🔴 1  | A-2     | 「遅延登録 + 後段書き換え」のハイブリッドに変更。または `.c`/`.cpp` 混合非対応を明記                              |
| 🔴 2  | A-4     | 単一宣言（`extern "C" int foo();`）を **Stage 1 スコープ外**に移し、ブロック構文のみに                   |
| 🔴 3  | A-4     | `extern "C" { #include <windows.h> }` のヘッダ内 C++ キーワード問題を明示。dev/include を事前 grep |
| 🟡 4  | A-5     | `goto the_end` 分岐を A-2 方針に整合                                                    |
| 🟡 5  | A-P/A-8 | 初回 `build.bat` で参照する全ファイルの作成手順を P-5 に列挙                                         |
| 🟡 6  | A-P     | pre-commit を `.bat` でも提供、または README で bash 依存を明記                                |
| 🟡 7  | A-8     | `con_c_vs_test.vcxproj` の msbuild も `build.bat` に組み込み                           |
| 🟡 8  | A-3     | `__cplusplus` 値の決定を事前 smoke で確定（事後変更禁止）                                         |
| 🟢 9  | Part B  | Stage 4 例の `new` を静的割当に書き換え（または注記）                                              |
| 🟢 11 | A-2     | grep から `dev/include` を除外しない                                                    |
| 🟢 13 | A-1     | サブ章番号を昇順に整理                                                                     |
| 🟢 14 | A-2     | `tcc.h:1193` の enum 包含への影響を 1 行追記                                               |

***

## 全体評価

v2.0 比で **「規律」「依存関係」「失敗時切り分け」「TCC 駆動テスト」の 4 大欠陥は解消**しており、構成としては大幅に改善されています。

ただし **A-2 と A-4 の技術的核心部分** が「決定」と書かれているのに実コードレベルで詰め切れておらず、ここを実装着手前に潰さないと **また同じ「最初の壁で破綻」が起きます**。

特に：

* **A-2 の混合 TU 問題**（最重要）
* **A-4 の単一宣言ブロック削除**（最少コードで確定）
* **A-3/A-4 の SDK ヘッダ事前 smoke**（実コードで検証）

この 3 点だけ先に解消すれば、Phase 1 はかなり安全に走れる見込みです。

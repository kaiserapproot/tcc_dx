# ビルド履歴

## 2026-05-16: MSVC (Visual Studio 2022) で TCC をビルドできるようにした

参考: https://gaiger-programming.blogspot.com/2017/06/build-tiny-c-compiler-by-vc-in-visual.html

### 問題1: LNK2005（重複定義）と LNK2001/LNK2019（未解決）が大量発生

#### 症状
Debug|Win32 でビルドすると、`__tcc_error`、`_tcc_add_file` などが `libtcc.obj` と `tcc.obj` の両方で定義されているという LNK2005 が大量に出る一方、`_tcc_state`、`_pstrcpy`、`_dynarray_add` などが未解決という LNK2001/LNK2019 も同時に出る。

#### 原因
TCC のビルド戦略の混在。TCC には2つの戦略がある:

- **ONE_SOURCE=1（デフォルト）**: [tcc.c:21-28](tcc.c#L21-L28) で `tcc.c` が `libtcc.c` を `#include`、[libtcc.c:21-65](libtcc.c#L21-L65) で `libtcc.c` が `tccpp.c`, `tccgen.c`, `tccdbg.c`, `tccasm.c`, `tccelf.c`, `tccrun.c`, `i386-gen.c`, `i386-link.c`, `i386-asm.c`, `tccpe.c` を `#include`。`tcc.c` 1つだけコンパイルすればよい。[tcc.h:352-359](tcc.h#L352-L359) により `ST_FUNC`/`ST_DATA` が `static` になる。
- **ONE_SOURCE=0**: すべての `.c` を個別に翻訳単位としてコンパイル＆リンク。`ST_FUNC` は空（外部リンケージ）、`ST_DATA` は `extern` になる。

`tcc.vcxproj` の Debug|Win32 構成では `ONE_SOURCE` が未定義（デフォルト=1）だったのに、`tccpp.c`, `tccgen.c` などすべての .c も `ClCompile` 対象にしていた。結果:

- `libtcc.obj` ← `libtcc.c` が他の .c を取り込み、全シンボルが定義される
- `tcc.obj` ← `tcc.c` も `libtcc.c` を取り込み、全シンボルが定義される
- → **LNK2005（重複定義）**
- 同時に独立ビルドされた `tccpp.obj` などは内部で `ST_FUNC=static` となり、`tcc_state` 等への参照が外側から解決できず → **LNK2001/LNK2019（未解決）**

#### 解決
Debug|Win32 と Release|Win32 のプリプロセッサ定義に `ONE_SOURCE=0` を追加し、x64 構成と整合させた。

### 問題2: `_tcc_debug_*`, `_tcc_tcov_*`, `_relocate` が未解決

#### 症状
`ONE_SOURCE=0` 追加後も `tccgen.obj`, `tccelf.obj`, `tccpp.obj`, `tccasm.obj` などから `_tcc_debug_start`, `_tcc_debug_new`, `_tcc_tcov_*`, `_relocate` 等が未解決と報告される。

#### 原因
`ONE_SOURCE=1` の場合、これらのシンボルを定義する [tccdbg.c](tccdbg.c) と [i386-link.c](i386-link.c) は `libtcc.c` から `#include` されるが、`ONE_SOURCE=0` にした以上、個別の翻訳単位として `tcc.vcxproj` に登録しないとリンクされない。

| 未解決シンボル | 定義ファイル |
|---|---|
| `tcc_debug_*`, `tcc_add_debug_info`, `tcc_tcov_*` | `tccdbg.c` |
| `relocate` | `i386-link.c` |

#### 解決
`tcc.vcxproj` の `<ItemGroup>` に以下を追加:

```xml
<ClCompile Include="i386-link.c" />
<ClCompile Include="tccdbg.c" />
```

`tcctools.c` は [tcc.c:29](tcc.c#L29) で常時 `#include` されているため、vcxproj には追加しないこと（追加すると LNK2005 になる）。

## 2026-05-23: x86-64 で glOrtho/glFrustum 等の double 6 引数関数が壊れる ABI バグの修正

### 一行サマリ

**TCC を x86-64 (Windows PE) でビルドして使うと、`double` 引数を 4 個以上取る外部関数 (`glOrtho`, `glFrustum`, `gluPerspective` 等) を呼んだとき、引数 1 以降の XMM レジスタ配置がずれ、引数の値が壊れる。** float→double 暗黙昇格や明示 `(double)` キャストでも発生。double 変数 / リテラル直渡しなら発生しない。これは TCC の `gfunc_call` で SSE float 引数を XMM レジスタへ転送する命令の ModR/M バイトに **XMM 番号 (`arg*8`) が抜けていた** のが原因。

### 影響範囲

| ターゲット | PE フォーマット | 浮動小数点 ABI | このバグ |
|------------|----------------|----------------|---------|
| **x86-64 (Windows)** | PE32+ | XMM0-XMM3 + スタック | **発生する** |
| i386 (Windows) | PE32 | x87 FPU スタック経由 | 発生しない |
| Linux x86-64 (SysV ABI) | ELF | XMM レジスタ | 別の場所だが類似ロジックあり |

`x86_64-gen.c` の Windows PE 用 `gfunc_call` (`#ifdef TCC_TARGET_PE` ブロック) で発生。i386 ターゲットは x87 経由なので無関係。

### 症状 (実測ログ)

OpenGL の `glOrtho(-15.96, +15.96, -1.815, +21.025, -45.72, +45.64)` を呼ぶと、生成される `GL_PROJECTION_MATRIX` は：

| 要素 | 期待式 | 期待値 | 実測値 (バグあり) | 判定 |
|------|--------|--------|------------------|------|
| m[0]  = 2/(r-l)         | 2/31.92  | 0.063  | **0.141** (= 2/14.18) | ✗ X 方向だけ異常 |
| m[12] = -(r+l)/(r-l)    | 0/31.92  | 0.000  | **1.257**            | ✗ |
| m[5]  = 2/(t-b)         | 2/22.84  | 0.088  | 0.088                | ✓ Y 方向は正常 |
| m[13] = -(t+b)/(t-b)    | -19.21/22.84 | -0.841 | -0.841          | ✓ |
| m[10] = -2/(f-n)        | -2/91.36 | -0.022 | -0.022               | ✓ Z 方向も正常 |
| m[14] = -(f+n)/(f-n)    | 0.08/91.36 | 0.001 | 0.001               | ✓ |

m[0] と m[12] から逆算すると、`glOrtho` が実際に受け取った引数は：

- `left`   = -16.0 (≒ -15.96、正しい)
- `right`  = **-1.825 (= bottom の値!)**
- `bottom` = -1.815 (正しい)
- `top`    = +21.025 (正しい)

第 2 引数 (right) に第 3 引数 (bottom) の値が混入している。結果として `glOrtho` の X 範囲が極端に狭くなり、メッシュが画面右上に潰れて描画される。

### 検出方法

OpenGL 環境では:

```c
float pm[16];
glGetFloatv(GL_PROJECTION_MATRIX, pm);
printf("m[0]=%.3f m[12]=%.3f\n", pm[0], pm[12]);
```

期待値 (`2/(right-left)` と `-(right+left)/(right-left)`) と一致しなければ本バグ確定。

汎用テスト (任意の `double` 6 引数関数で確認可):

```c
#include <stdio.h>
void test6(double a, double b, double c, double d, double e, double f) {
    printf("a=%f b=%f c=%f d=%f e=%f f=%f\n", a, b, c, d, e, f);
}
int main(void) {
    float fa=-15.96f, fb=15.96f, fc=-1.815f, fd=21.025f, fe=-45.72f, ff=45.64f;
    test6(fa, fb + 0.0f, fc, fd, fe, ff);  /* 引数 1 に float 式が混じるとバグる */
    return 0;
}
```

- バグあり: `b=-1.815` (arg2 の値が混入)
- 修正後:  `b=15.96`  (正常)

### 根本原因 (TCC ソースレベル)

#### 関係ファイル

`x86_64-gen.c` の Windows PE 用 `gfunc_call` (`#ifdef TCC_TARGET_PE` のブロック)。
コミット時のリポジトリでは **802-908 行付近**。

#### バグの仕組み

TCC の `gfunc_call` は引数を **逆順** (`arg = nb_args-1, ..., 1, 0`) に処理し、各引数を「対応する XMM[arg] レジスタへロード」→「整数レジスタへ複製コピー (`movq %xmm[arg], %r[d]`)」する。Windows x64 ABI では `double` 引数は XMM0〜XMM3 へ渡せばよく整数レジスタへの複製は本来不要だが、TCC は variadic 互換のため両方に入れる設計。

問題のあったコードは `movq %xmm[arg], %r[d]` 命令の **ModR/M バイト**を組み立てる部分。ModR/M の reg フィールド (bits 3-5) に **XMM レジスタ番号 (= `arg`)** を `arg*8` でシフトして埋め込むべきところ、その項が欠落して常に `XMM0` を指定した命令になっていた。

実機械語の差分 (引数 6 個の `double` 関数呼び出し):

| 引数 | 期待 ModR/M | 期待命令 | 実際 ModR/M (バグあり) | 実際命令 |
|------|-------------|----------|----------------------|----------|
| arg3 (top)    | `0xd9` (reg=3, r/m=1+B) | `movq r9, xmm3`  | `0xc1` (reg=0, r/m=1+B) | `movq r9, xmm0` |
| arg2 (bottom) | `0xd0` (reg=2, r/m=0+B) | `movq r8, xmm2`  | `0xc0` (reg=0, r/m=0+B) | `movq r8, xmm0` |
| arg1 (right)  | `0xc9` (reg=1, r/m=3)   | `movq r11, xmm1` | `0xc3` (reg=0, r/m=3+B) | `movq r11, xmm0` |
| arg0 (left)   | `0xc2` (reg=0, r/m=2)   | `movq r10, xmm0` | `0xc2` (reg=0, r/m=2+B) | `movq r10, xmm0` ← ここだけ偶然一致 |

`gv(RC_XMM0 << arg)` で「XMM[arg] へロード」しているはずなのに、その後の整数レジスタ複製命令で常に `xmm0` を参照するため、`gv` で値を入れた XMM[arg] が「使われずに」次のイテレーションで上書きされる連鎖反応が発生し、結果的に各引数が隣のレジスタの値で塗りつぶされる。

### 修正前のソース (バグあり)

`x86_64-gen.c` の `gfunc_call` 内、`#ifdef TCC_TARGET_PE` ブロックの SSE float 引数処理 (`is_sse_float` の分岐の `arg < REGN` 側):

```c
/* === 修正前 (バグあり) === */
if (is_sse_float(vtop->type.t)) {
    if (tcc_state->nosse)
        tcc_error("SSE disabled");
    if (arg >= REGN) {
        gv(RC_XMM0);
        /* movq %xmm0, j*8(%rsp) */
        gen_offs_sp(0xd60f66, 0x100, arg*8);
    } else {
        /* Load directly to xmmN register */
        gv(RC_XMM0 << arg);
        d = arg_prepare_reg(arg);
        /* mov %xmmN, %rxx */
        o(0x66);
        orex(1, d, 0, 0x7e0f);
        /* ★ ここがバグ: arg*8 が抜けていて常に 0xc0 + REG_VALUE(d) になっていた。
         *   ModR/M の reg フィールド (bits 3-5) に XMM 番号が入らないため、
         *   生成される命令は常に movq r<d>, xmm0 になる */
        o(0xc0 + REG_VALUE(d));        /* 欠落: arg*8 が無い */
    }
}
```

### 修正後のソース (修正済み・最新版)

```c
/* === 修正後 (リポジトリ最新版) === */
if (is_sse_float(vtop->type.t)) {
    if (tcc_state->nosse)
        tcc_error("SSE disabled");
    if (arg >= REGN) {
        gv(RC_XMM0);
        /* movq %xmm0, j*8(%rsp) */
        gen_offs_sp(0xd60f66, 0x100, arg*8);
    } else {
        /* Load directly to xmmN register */
        gv(RC_XMM0 << arg);
        d = arg_prepare_reg(arg);
        /* mov %xmmN, %rxx */
        o(0x66);
        orex(1, d, 0, 0x7e0f);
        /* ★ 修正: arg*8 を含むことで ModR/M の reg フィールドに XMM 番号 (arg)
         *   が正しく埋め込まれ、movq %xmm[arg], %r<d> が生成される */
        o(0xc0 + arg*8 + REG_VALUE(d));
    }
}
```

### 修正パッチ (diff 形式)

```diff
--- a/x86_64-gen.c
+++ b/x86_64-gen.c
@@ -885,7 +885,7 @@
                 /* mov %xmmN, %rxx */
                 o(0x66);
                 orex(1,d,0, 0x7e0f);
-                o(0xc0 + REG_VALUE(d));
+                o(0xc0 + arg*8 + REG_VALUE(d));
             }
         } else {
```

**変更は 1 行のみ**。`arg*8` を `0xc0 + ... + REG_VALUE(d)` の中央に加える。

### TCC を再ダウンロードした場合の修正再適用手順

新しい TCC ソースを `git clone` または zip でダウンロードしたら以下の手順で確認・修正:

#### Step 1: バグの有無を確認

```bash
grep -n "arg.0xc0.*REG_VALUE.d" x86_64-gen.c
```

期待出力:
- ヒットが `0xc0 + arg*8 + REG_VALUE(d)` を含んでいれば **既に修正済み**。何もしなくてよい。
- `0xc0 + REG_VALUE(d)` だけで `arg*8` が無ければ **バグあり**。Step 2 へ。

#### Step 2: バグがあれば 1 行修正

`x86_64-gen.c` で `gfunc_call` の Windows PE 版 (#ifdef TCC_TARGET_PE のブロック) 内の `is_sse_float` 分岐を探し、`arg < REGN` の `else` 側 (約 880-890 行) で以下を変更:

```diff
-                o(0xc0 + REG_VALUE(d));
+                o(0xc0 + arg*8 + REG_VALUE(d));
```

#### Step 3: amateras 環境との互換のため tcc.h を修正

amateras プロジェクト (`amateras/ext/tcc/lib/libtcc1-64.a`) との互換性を保つため、`tcc.h` の `TCC_LIBTCC1` のデフォルトを修正:

修正前:
```c
/* tcc.h 321 行付近 */
#ifndef TCC_LIBTCC1
# define TCC_LIBTCC1 "libtcc1.a"
#endif
```

修正後:
```c
#ifndef TCC_LIBTCC1
# ifdef TCC_TARGET_X86_64
#  define TCC_LIBTCC1 "libtcc1-64.a"
# else
#  define TCC_LIBTCC1 "libtcc1.a"
# endif
#endif
```

#### Step 4: Visual Studio 2022 で x64 Release ビルド

```powershell
$msbuild = "C:\Program Files\Microsoft Visual Studio\2022\Professional\MSBuild\Current\Bin\MSBuild.exe"
& $msbuild tcc.vcxproj /p:Configuration=Release /p:Platform=x64 /v:minimal /nologo /t:Rebuild
```

成果物: `x64\Release\tcc.exe` (340KB, PE32+ x86-64)

#### Step 5: 検証用最小コードでバグ修正を確認

```c
/* repro_double6.c */
#include <stdio.h>
void test6(double a, double b, double c, double d, double e, double f) {
    printf("a=%f b=%f c=%f d=%f e=%f f=%f\n", a, b, c, d, e, f);
}
int main(void) {
    float fa=-15.96f, fb=15.96f, fc=-1.815f, fd=21.025f, fe=-45.72f, ff=45.64f;
    test6(fa, fb + 0.0f, fc, fd, fe, ff);
    return 0;
}
```

ビルド・実行:

```bash
x64/Release/tcc.exe repro_double6.c -o repro_double6.exe
./repro_double6.exe
```

期待出力 (修正済み):
```
a=-15.965160 b=15.965160 c=-1.815001 d=21.025000 e=-45.720001 f=45.639999
```

NG (バグあり):
```
a=-15.965160 b=-1.815001 ← b に c の値が混入
```

### amateras プロジェクト側に必要な追加修正

新版 TCC を amateras (`amateras/ext/tcc/tcc.exe`) に置き換える場合、新版 TCC には別のリグレッション (`static __inline__` 関数を TU ローカルにせず global symbol として出力する) があり、`windows.h` を複数 TU で include すると `_InterlockedAnd` 等が defined twice エラーになる。

回避策として `amateras/ext/tcc/include/_mingw.h` の以下の箇所を修正:

修正前:
```c
#if !defined(__MINGW_INTRIN_INLINE) && (defined(__GNUC__) || defined(__TINYC__))
#define __MINGW_INTRIN_INLINE extern __inline__ __attribute__((__always_inline__,__gnu_inline__))
#endif
```

修正後:
```c
#if !defined(__MINGW_INTRIN_INLINE) && (defined(__GNUC__) || defined(__TINYC__))
# ifdef __TINYC__
/* TCC は static __inline__ を global symbol 化するリグレッションがあるため、
 * intrinsic ラッパーの本体定義をスキップして宣言だけ通す。
 * MMD/OpenGL コードはこれら intrinsic を直接呼ばないので未定義でも問題ない。 */
#  define __INTRINSIC_DEFINED__InterlockedAnd       1
#  define __INTRINSIC_DEFINED__InterlockedOr        1
#  define __INTRINSIC_DEFINED__InterlockedXor       1
#  define __INTRINSIC_DEFINED__InterlockedAnd64     1
#  define __INTRINSIC_DEFINED__InterlockedOr64      1
#  define __INTRINSIC_DEFINED__InterlockedXor64     1
#  define __INTRINSIC_DEFINED__InterlockedAnd8      1
#  define __INTRINSIC_DEFINED__InterlockedOr8       1
#  define __INTRINSIC_DEFINED__InterlockedXor8      1
#  define __INTRINSIC_DEFINED__InterlockedAnd16     1
#  define __INTRINSIC_DEFINED__InterlockedOr16      1
#  define __INTRINSIC_DEFINED__InterlockedXor16     1
#  define __MINGW_INTRIN_INLINE static __inline__
# else
#  define __MINGW_INTRIN_INLINE extern __inline__ __attribute__((__always_inline__,__gnu_inline__))
# endif
#endif
```

### Double が絡んで問題になりやすい関数 (要注意リスト)

x86-64 ターゲットで `double` を多数 (特に 4 個を超えて) 取る関数全般。バグ未修正の TCC ではすべて影響を受ける可能性あり:

- **OpenGL/GLU**: `glOrtho`, `glFrustum`, `gluPerspective`, `gluLookAt`, `gluOrtho2D`
- **OpenGL の `d` サフィックス系**: `glTranslated`, `glRotated`, `glScaled`, `glColor3d/4d`, `glVertex3d/4d`
- **GLUT**: `glutSolidSphere`, `glutSolidTorus`
- **数学関数**: `pow`, `atan2`, `fmin`, `fmax`, `fma` (3 引数以下なら通常は安全だが、float 式混入時は要注意)

回避方針 (推奨順):
1. `f` 接尾辞の float 版を使う (`glTranslatef`, `glRotatef` 等)
2. 自前で行列計算して `glLoadMatrixf` (ポインタ 1 引数なのでバグ無関係)
3. すべての引数を一旦 `double` 変数に格納してから渡す

### バックアップ (置き換え時に作成)

- `amateras/ext/tcc/tcc.exe.bak_buggy_2025-09-08` ← 旧版バグあり tcc.exe (497KB, 2025-09-08 ビルド)
- `amateras/ext/tcc/include/_mingw.h.bak` ← 元の `_mingw.h`

### 関連ドキュメント

- `kaiser_system/amateras/base_inc/問題と原因.md` セクション 7 — エンドユーザー向け詳細解説 (再現テスト、回避策、テンプレートコード)
- `kaiser_system/tcc_repro/repro_double6.c` — 4 ケースのテストコード (float→double 暗黙昇格 / 明示キャスト / double リテラル / double 変数)
- `kaiser_system/tcc_repro/mini.c` — 最小再現コード (引数 1 に float 式)

### 検証環境

- Windows 10/11 x64
- Visual Studio 2022 Professional (MSBuild v143)
- TCC バージョン: 0.9.28rc
- 再現性: **100%** (同じ条件で必ず同じバグ)

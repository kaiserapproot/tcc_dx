# 問題と原因

## 事象

- Class を追加すると `tcc.exe` が正常に生成されなくなった。
- 具体的には、`tccgen.c` の変更中に `tccgen.c` 本体が破損・不完全な状態になり、リンク時に多数の未解決外部シンボルが発生した。
- 元の変更では `Sym.inline_func_str` を追加し、`struct_decl()` で `skip_or_save_block(&method_str)` を使ってクラス内インライン本体を保存し、`gen_inline_functions()` で遅延生成する設計を試みていた。
- しかしその実装は途中で壊れてしまい、`tccgen.c` は部分的に置き換えられたりプレースホルダ化されたりしたため、`tccgen_compile`／`tccgen_init`／`tccgen_finish` などの本来のシンボルが欠落した。
- 現在は `tccgen.c` を upstream に戻したが、当初の修正を安全に再実装できていない可能性が高い。

## 原因

1. `tccgen.c` の修正が途中で壊れ、最終的に正しい実装が入っていなかった。
   - 元のプランでは `Sym.inline_func_str` を使ってクラス内インライン本体を保存し、`gen_inline_functions()` で遅延生成する想定だった。
   - しかし途中で `tccgen.c` を upstream 版に置き換えたため、当初の修正内容が現在のソースに反映されていない。

2. `tccgen.c` を一度プレースホルダ／破損状態にしたことで、`tccgen_*` 系のシンボル定義が消え、リンクエラーが発生した。
   - ユーザーのビルドログに出ていた未解決の外部シンボルは、`tccgen.c` に本来あるべき実装が欠落していたことを示す。

3. プラン自体が `tccgen.c` の大規模な修正を前提にしていたが、実装の最小単位化・段階検証が不足していた。
   - 変更箇所を小さく分割して逐次ビルド確認するべきだった。
   - 特に MSVC 環境では、ソースファイルのエンコーディングやプロジェクトへの登録状況が重要だった。

## このプランの問題点

- `tccgen.c` を壊した状態で進めたため、問題の本質が「設計ミス」ではなく「ソース破損」になった。
- `Sym.inline_func_str` 追加だけで満足し、実際に `struct_decl()`／`gen_inline_functions()` を完全に結合できていなかった。
- `tccgen.c` の修正を upstream ベースに再適用する際の差分管理ができていなかった。
- MSVC ビルド前の単体確認が不足し、実装を確かめずに次のステップへ進んだ。
- 破損修復の途中で `tccgen.c` を丸ごと差し替えるという手法が、かえって復旧を難しくした。

## 新しいプラン

1. `tccgen.c` を upstream CLEAN に戻し、現状の作業をリセットする。
2. `tcc.h` の `Sym.inline_func_str` 追加が正しいかだけを確認する。
3. `tccgen.c` の `struct_decl()` と `gen_inline_functions()` のみに、最小限の差分を再実装する。
4. 変更後は必ず `cl` / `msbuild` でビルド確認を行う。
5. `Class` 追加を再現する最小テストを作成し、`tcc.exe` の生成と実行を確認する。
6. 必要であればソースを UTF-8(BOM) に統一し、MSVC の `C4819`/文字化け問題を先に解消する。
7. ファイル名が `.c` の場合は従来の C ビルドを行う挙動を維持する。
8. ファイル名が `.cpp`, `.cc`, `.hpp` の場合は C++ の予約語と機能を有効にする。
9. `.cpp`, `.cc`, `.hpp` の場合には `__cplusplus` マクロが有効になるようにする。
10. 同様に `extern "C" {` が正しく機能するようにする。

## 結論

今回の失敗は、Class機能そのものの設計ではなく、`tccgen.c` の修正適用とビルド検証が途中で破綻したことに起因する。
新しいプランではまず「正しいベースに戻す」「最小差分で再実装する」「ビルドを必ず通す」を徹底します。

---

# BUG-33: 静的メンバが TU をまたげない（2026-08-22 修正）

**発見日**: 2026-08-22（G4 完了で SimpleString.cpp が完走し、次の停止
`cudebug.h:29 TestUtility::trimFileName(...)` で露出）
**状態**: **修正済み**。回帰テスト `dev/test/a9/link/bug33_link_*.cpp`（クロス TU）/
`dev/test/a9/bug33_static_inline.cpp`
**重大度**: 高（`.h` に宣言・`.cpp` に定義という C++ の通常構成で静的メンバが使えない）

## 33a: 宣言だけの静的メンバが解決できない

`cpp_lookup_static_member` はフィールドを見つけた後、**既存のグローバル Sym** しか
探さず、無ければ NULL を返して `static member not found` になっていた。
`TestUtility::trimFileName` は `TestUtility.h` で宣言され `TestUtility.cpp` で定義される
——BUG-30 が通常メンバで直した状況の静的メンバ版。

**修正**: 見つからなければ extern 参照を作る（関数は BUG-30 の
`cpp_make_member_func_extern`、データは `external_sym` に `VT_EXTERN`）。
静的データメンバはクラス外定義側が `static` を書かないので、
extern 側も **VT_STATIC を落とす**（付けたままだと再定義扱い/内部リンケージになる）。

## 33b: 静的メンバ関数の定義が内部リンケージになる

C++ の `static` メンバ関数であることを、ホイストしたグローバルの型に **VT_STATIC を
コピー**して記録していた。しかし tcc の他の部分にとって VT_STATIC は
**内部リンケージ**を意味するため、定義がファイルローカルになり別 TU から呼べない
（実測: `undefined symbol '__tcc_Util__trim_pc'`）。同一 TU 内で完結する既存テスト
ばかりだったため潜伏していた。

**修正**: `FuncAttr` に `func_static_member` ビットを新設し、
「`this` を渡さない」判定は `gen_function` でそのフラグを見るようにした。
グローバルの型からは VT_STATIC を外す（フィールド側は従来どおり VT_STATIC を保持
——インスタンスレイアウトから外すのはそちら）。インライン定義の経路は
`VT_STORAGE` を落とす時点で VT_STATIC も消えていたため、そちらにもフラグを立てる
（従来はインラインの静的メンバに暗黙 `this` が付く別の誤りがあった）。

---

# BUG-32: デフォルト引数の再生が壊れている 3 件（2026-08-22 修正）

**発見日**: 2026-08-22（G4 `new`/`delete` 実装後に SimpleString.cpp が末尾まで進み、
`resize()` の `erase(n)` で露出）
**状態**: **修正済み**。回帰テスト `dev/test/a9/bug32_default_overload.cpp` /
`bug32_default_twice.cpp`
**重大度**: 高（32b はコンパイル不能、32c は誤オーバーロード = silent miscompile、
32a は use-after-free）

## 32a: 保存済みトークン列が `end_macro()` に破壊される

`cpp_apply_default_args` は `begin_macro(sa->inline_func_str, 1)` と、パラメータ Sym に
保存してある**原本**を渡していた。`end_macro()` は渡された列を必ず壊す
（`alloc==1` なら `tok_str_free`、`alloc==0` なら `len = 0`）ため、**同じデフォルト引数を
使う 2 回目以降の呼び出しは解放済みメモリを読む**。既存テストが各デフォルトを 1 回しか
使っていなかったため潜伏していた（実測でも 2 回目が偶然通ってしまう = 発現が非決定的）。

**修正**: `tok_str_dup_for_default()` で複製してから再生する（`gen_function` が
mem-initializer で採っているのと同じ方式）。

## 32b: 再生が呼び出し直後のトークンを消す

`begin_macro()` は現在のトークンを保存しない。**引数変換を遅延する経路**
（オーバーロードされた呼び出し）では、デフォルト引数を適用する時点で呼び出しの `)` を
既に `next()` で消費済みなので、再生後に `tok` が**再生列の終端**になり、呼び出しの次の
トークンが失われる。症状はファイル末尾での `';' が必要です（"<eof>" が見つかりました）`。

**修正**: `unget_tok` と同じ方式で、再生前に現在トークンを `tok_str_add_tok` で退避し、
再生後に `begin_macro`+`next` で戻す。

## 32c: オーバーロード解決がデフォルト引数を考慮しない

候補判定が**完全な引数個数一致**を要求していたため、`erase(size_type pos, size_type n = 9)`
は `erase(n)`（1 引数）の候補にならず、無関係な `erase(iterator)` に解決されていた
（実測では整数がポインタへ暗黙変換され警告のみ）。

**修正**: 残りのパラメータが**すべてデフォルト引数を持つ**候補も可とする。デフォルトで
埋まる引数はスコアに加算しないので、引数個数が完全一致する候補が同点では勝つ。

---

# BUG-31: 静的データメンバのクラス外定義がクラスを次の関数定義へ漏らす（2026-08-22 修正）

**発見日**: 2026-08-22（BUG-30 の最小再現を作る過程で、オーバーロードが 1 つも無い形でも
壊れることに気付いて分離）
**状態**: **修正済み**（本ブランチ）。回帰テスト `dev/test/a9/bug31_static_member_leak.cpp`
**重大度**: 高（silent miscompile。テンプレートもオーバーロードも関係しない素の C++ コードが対象）

## 事象（最小再現）

```cpp
class S { public: static const int npos; int m; };
const int S::npos = 99;
void probe(S& s) { s.m = 7; }
int main() { S s; s.m = 0; probe(s); return s.m; }   // 7 のはずが 0
```

コンパイルも実行も通るのに `probe()` の代入が呼び出し元へ反映されない。

## 真因

`Class::member` の宣言子は `parse_cpp_scope_qualifier` で `cpp_qualified_class` を
セットする。関数**定義**（`decl()` の `tok == '{'` 分岐）と関数**宣言**
（`external_sym` 分岐）は使用後に NULL へ戻すが、**静的データメンバのクラス外定義**は
`decl_initializer_alloc` へ抜けるためセットしたままだった。

結果、その TU で**次に現れた関数定義**が `cpp_pending_member_class` / `parent_class`
にそのクラスを持つ「メンバ関数」として登録され、`gen_function` が暗黙の `this`
引数を挿入する。呼び出し側は宣言どおりの引数しか積まないので、第 1 引数が `this`
スロットに入り、以降の引数が 1 つずつずれる。

CPPUnit 本体（SimpleString.cpp）では `npos` 定義の次も修飾付きメンバ定義が続くため
`cpp_qualified_class` が上書きされ、症状が隠れていた。自由関数が続くと露出する。

## 修正

`decl()` の宣言子ループ末尾（関数定義分岐は `break` で通らない）で
`cpp_qualified_class = NULL;` を行い、関数以外の宣言子で確実に後始末する。

---

# BUG-30: メンバ関数の前方参照呼び出しが壊れる（2026-08-22 修正）

**発見日**: 2026-08-22（CPPUnit プラン G-CAST 完了後、SimpleString.cpp の停止地点が
33 行目→38 行目へ進んだことで露出）
**状態**: **修正済み**（ガイド **G-OVL** = [cppunit対応プラン.md](cppunit対応プラン.md)）
**重大度**: 高（オーバーロードが 1 つでも**実行時クラッシュ**、複数なら誤呼び出し。
`.h` に宣言・`.cpp` に定義という C++ の通常構成がそのまま該当する）

> **当初の起票より範囲が広かった**: 起票時は「同名オーバーロードの前方参照」と
> 記述したが、切り分けの結果 **オーバーロードが無い単一メンバでも壊れていた**
> （下記「症状 2」）。本節は実測に合わせて記述を改めている。

## 事象

```
sample/cppunit/SimpleString.cpp:38: error: 関数への引数が多すぎます
```

該当行は `assign((size_type)0, (value_type)0);`。`SimpleString::assign` は 5 つの
オーバーロードを持ち、2 引数の `assign(size_type n, value_type c)` が正解だが、
1 引数の `assign(const SimpleString& s)`（クラス内で**最初に宣言**されたもの）へ
解決されていた。

**症状 2（切り分け中に判明・より重大）**: オーバーロードが**無くても**壊れる。

```cpp
class C { public: int v; int f(int a); };
int probe(C& c) { return c.f(5); }   // 定義より前の呼び出し
int C::f(int a) { return v + a; }
```

これは**コンパイル・リンクとも通り、実行時にアクセス違反**になっていた
（`ovl12`。メンバ本体からの unqualified 呼び出し `ovl14` も同様）。

## 真因（最小再現 17 本の二分探索で確定）

呼び出し先の解決が**グローバル Sym チェーン**（`sym_find` → `prev_tok`）だけを
見ていた。メンバは定義（本体）が現れた時点で初めてグローバル Sym を持つため、
まだ定義されていないメンバは:

1. **呼び出し先が存在しない** → `cpp_lookup_member_func` が field をそのまま返し、
   呼び出し側は**リンケージを持たない Sym** に対して call を発行 → 実行時クラッシュ。
2. **オーバーロード候補にならない** → クラス内の最初の宣言へ解決される
   （引数の数が合わなければ「引数が多すぎます」、合えば黙って誤呼び出し）。

| 条件 | 結果 | 再現 |
|---|---|---|
| 全オーバーロードの定義が呼び出しより**前** | ✅ 正しく解決 | `ovl7` |
| 定義が呼び出しより**後** | ❌ 誤解決 | `ovl11` |
| 同 TU に定義が無い（宣言のみ） | ❌ 誤解決 | `ovl10` |
| オーバーロード無し・定義が後 | ❌ **実行時クラッシュ** | `ovl12` / `ovl14` |
| unqualified / `obj.f()` / `this->f()` | 3 形とも同じ | `ovl5` / `ovl6` |

切り分けで**否定された**仮説（いずれも単独では再現しない）: `size_t` 対 `unsigned int`、
インライン `operator=` 本体からの同名呼び出し、ctor オーバーロード群の存在、
unqualified 呼び出し経路（BUG-22 の `this` 補完）そのもの。
なお切り分け中に**別の独立バグ [BUG-31](#bug-31-静的データメンバのクラス外定義がクラスを次の関数定義へ漏らす2026-08-22-修正)** を発見し、先に修正した。

## 修正

1. **呼び出し先の生成**: `cpp_lookup_member_func` が候補を見つけられなかったとき、
   field を返す代わりに `cpp_make_member_func_extern` で **extern 参照**を作る
   （`VT_EXTERN` 付き。無いと後続の定義が `patch_type` で「再定義」になる）。
   リンク名は BUG-14 のクラス修飾マングルなので、同 TU / 別 TU いずれの定義とも結び付く。
2. **候補集合**: `cpp_resolve_member_func_call` を新設し、メンバ呼び出しの再解決を
   **クラスのメンバチェーン（宣言側 field、基底も再帰）**から行う。スコアリングは
   `cpp_resolve_func_call` と同一（`cpp_arg_matches_param` / 完全 arity 一致）で、
   既に正しく解決できていたケースの挙動は変えない。見つからなければ従来の
   グローバル経由解決へフォールバックする。
3. **引数変換の遅延**: `cpp_call_has_overloads` がグローバルしか数えず「候補 1 個」と
   判断していたため、再解決の前に最初の宣言の引数型へ変換（または引数超過エラー）
   されていた。クラス宣言側の同名メンバ数も数えるようにした。
4. 解決後の実体引き当ては **完全シグネチャ一致のみ**の
   `cpp_member_func_global_exact` を使う。`cpp_lookup_member_func` の arity
   フォールバックをここで使うと、**別オーバーロード用に作った extern** を掴んで
   黙って誤関数を呼ぶ（実測済み）。

---

# BUG-1: `default_static.cpp` 修正調査記録（2026-05-30）

## 事象

`dev/test/a7/default_static.cpp` がコンパイルエラー `too few arguments to function` で失敗していた。

```cpp
class Foo { public: static int bar(int x = 10); };
int Foo::bar(int x) { return x; }
int main() { return Foo::bar() == 10 ? 0 : 2; }
```

表面的には「静的メンバ関数の呼び出し側にデフォルト引数が適用されない」だけに見えたが、調査の結果、**Stage 3〜4 にまたがる 5 つの相互に関連するバグ**が見つかった。

## 個別バグと原因

### BUG-1a: クラス宣言のデフォルト引数が定義側に伝播していない

- **症状**: クラス内宣言 `static int bar(int x = 10);` でパラメータの `inline_func_str` にデフォルト値が保存されるが、クラス外定義 `int Foo::bar(int x)` のパラメータは別 Sym オブジェクトなので、その `inline_func_str` は NULL のまま。呼び出し時に `cpp_apply_default_args` がデフォルトを見つけられない。
- **原因**: `cpp_save_default_arg` は `type_decl` 経由でクラス宣言時にしか呼ばれない。クラス外定義は `external_sym` 経由で新規 Sym を作るため、宣言側との紐付けが必要だった。
- **修正**: `cpp_inherit_decl_defaults` 関数を新設し、`decl()` の `cpp_qualified_class` 設定箇所で呼び出して field sym から global sym へデフォルト引数 TokenString をコピー。

### BUG-1b: デフォルト引数の TokenString が二重解放される

- **症状**: BUG-1a 修正後、デフォルト引数を使うテストで `';' が必要 ("<eof>" が見つかりました)` などの解析エラーが発生。
- **原因**: `cpp_apply_default_args` の `begin_macro(sa->inline_func_str, 1)` + `end_macro()` は TokenString を解放してしまう。同一ポインタを field sym と global sym で共有すると、二度目の呼び出しでダングリングポインタを参照する。
- **修正**: `tok_str_dup_for_default` でディープコピーを作成。global sym 側にコピーを保持し、`end_macro(alloc=1)` で解放されても元（field sym 側）は無傷。

### BUG-1c: 静的メンバ関数のクラス外定義に `VT_STATIC` が伝播していない（**本命**）

- **症状**: BUG-1a/b 修正後も `Foo::bar(5)` が引数を正しく受け取れず、戻り値が garbage（毎回異なるアドレス様の値）。`Foo::bar()`（引数なしで定数返却）は動くため、引数渡しだけが壊れている。
- **原因**: クラス外定義 `int Foo::bar(int x)` には `static` キーワードが書けないため、global sym の `type.t` に `VT_STATIC` フラグが入らない。`gen_function` の判定 `if (sym->parent_class && !(sym->type.t & VT_STATIC))` で誤って暗黙の `this` パラメータを挿入していた。結果として x64 ABI で arg0=RCX が `this`（garbage）、arg1=RDX が `x`（未初期化）となり、関数は `x` を返却するつもりが garbage RDX を返していた。
- **修正**: `cpp_inherit_decl_defaults` で `VT_STATIC` フラグも伝播。

### BUG-1d: `cpp_member_this_pending` の `memmove` インデックスが 1 ずれている

- **症状**: BUG-1a/b 修正後、`default_arg.cpp`（非静的メンバ関数 `f.set()` でデフォルト引数）が実行時にゼロでない値を返す。引数バイト列が破壊されていた。
- **原因**: vstack の引数を 1 スロット押し上げて先頭に `this` を挿入する処理で `memmove(vtop - nb_args + 1, vtop - nb_args, na * sizeof(SValue))` となっており、ソース位置が `vtop - na - 1`（最初の引数より下、無関係なスロット）を指していた。
- **修正**: `memmove(vtop - nb_args + 2, vtop - nb_args + 1, na * sizeof(SValue))` に修正。args[0..na-1] を args[1..na] に正しく一段シフトする。

### BUG-1e: `::` ハンドラが三項演算子の `:` を誤認識

- **症状**: `return x == 0 ? 0 : 1;` のような三項演算子で `error: identifier が必要です` が発生（クラス未使用でも発生する純粋な既存バグ）。
- **原因**: post-operator ループの `tok == ':'` ブランチが、まず `vtop->sym` が struct かを検査して `expect("identifier")` を発火し、その後で 2 つ目の `:` をチェックしていた。三項演算子の `:` は struct コンテキスト外なので、`vtop->sym` が NULL でエラー発火。
- **修正**: 先に 2 つ目の `:` を `next()` で peek してから struct チェックに進む順序に変更。`::` でない場合は `unget_tok(':')` で巻き戻す。

## 教訓

1. **表面症状の裏に複数バグが隠れる**: 1 つのコンパイルエラーを解いても、その下に別の codegen バグが連鎖していることがある。修正のたびに最小再現テストで縦割り検証することが重要。
2. **段階的バイセクト**: 各修正を一時的に無効化／有効化して「どの修正が何を直し、何を壊しているか」を切り分けた。デバッガが使えない TCC のような環境では、テスト分割と一時コメントアウトが最強の武器。
3. **既存の動作テストを必ず再実行**: 自分の変更で `default_arg.cpp` を一時的に壊した（解析エラー）が、リバートして検証したことで「これは私が壊した」と確信できた。逆に `typedef_class.cpp` のリグレッションは私の修正と無関係（バイセクトで確認済み）と切り分けられた。
4. **VT_STATIC のような暗黙の意味論**: storage class フラグはクラス外定義では構文に現れないため、parser 側で**明示的にコピー伝播**する仕組みが必要。同様の問題は `inline`, `virtual`, `const` などでも起きうる。

## 残課題（次回 BUG-2 以降）

- **typedef_class.cpp** の `typedef Point P; P p;` がリグレッション中（私の変更と無関係の先行バグ）。実装履歴.MD §19.4 で「実装済み」と記載されていたが現在は失敗。
- ~~**inline_member.cpp**（BUG-2）~~ → **完了**（2026-05-30、f94caf1）
- ~~**文レベル `Counter::count = 5;`**（BUG-3）~~ → **完了**（2026-05-30、f94caf1）
- ~~**コンストラクタ初期化子リスト codegen**（FEAT-4A）~~ → **完了**（2026-05-30、f94caf1。`: Base(b)` は skip のみ）

---

# BUG-2: `inline_member.cpp` 修正調査記録（2026-05-30）

## 事象

`dev/test/a7/inline_member.cpp` がクラス内インライン定義のメンバ関数を呼び出すと、BUG-1 修正後はコンパイル成功するが、**実行時に Access Violation（0xC0000005）でクラッシュ**するようになっていた。

```cpp
class Calc {
public:
    int add(int a, int b) { return a + b; }
};
int main() {
    Calc c;
    return c.add(3, 4) == 7 ? 0 : 1;
}
```

## 原因

`struct_decl` のクラス本体パース処理で、メンバ関数のインライン本体 `{ ... }` を **完全に捨てていた**:

```c
if ((type1.t & VT_BTYPE) == VT_FUNC && tok == '{') {
    skip_or_save_block(NULL);   /* NULL: トークン保存しないでスキップ */
    skip_member_semi = 1;
    break;
}
```

その結果:
1. field sym にはプロトタイプ情報のみ残る（マシンコードなし）
2. `cpp_finish_member_inlines` は既存実装ありだが、**どこからも呼ばれていなかった**
3. 呼び出し側 `c.add(3, 4)` で `cpp_lookup_member_func` がグローバル sym を見つけられず、field sym を返す
4. field sym は relocation 対象アドレス 0 を持つので、call 命令で 0 番地にジャンプ → クラッシュ

つまり、**インライン定義の遅延コード生成パスが完全に断線していた**。

## 修正

3 箇所を接続:

### 修正 1: `struct_decl` でインライン本体を保存（[tccgen.c:5276-5287](e:/work/work_github/tpp/tcc_dx/tccgen.c#L5276-L5287)）

```c
if ((type1.t & VT_BTYPE) == VT_FUNC && tok == '{') {
    TokenString *body = NULL;
    skip_or_save_block(&body);                /* &body: トークン列を保存 */
    if (tcc_state->cpp && v && body)
        cpp_register_member_body(ss, s, &type1, body);
    skip_member_semi = 1;
    break;
}
```

`cpp_register_member_body` は既存実装で、field sym の `inline_func_str` に body をセットするだけのシンプルな関数。

### 修正 2: クラス本体パース完了時に `cpp_finish_member_inlines` を呼ぶ（[tccgen.c:5310-5316](e:/work/work_github/tpp/tcc_dx/tccgen.c#L5310-L5316)）

```c
if (is_class) {
    cpp_finish_member_inlines(s);
    cpp_cur_class = NULL;
}
```

`cpp_finish_member_inlines` は既存実装で、以下を行う:
1. 各 field の `inline_func_str` を走査
2. 対応するグローバル sym を `external_sym(f->v & ~SYM_FIELD, ...)` で作成
3. `parent_class` と `VT_INLINE` をセット、`cpp_set_func_mangle_label` でマングル
4. `inline_fns` に登録 → 後で `gen_inline_functions` がコード生成

### 修正 3: `ss` の未初期化警告対策（[tccgen.c:4992](e:/work/work_github/tpp/tcc_dx/tccgen.c#L4992)）

MSVC が「ss が未初期化の可能性」を `error C4703` で報告したため、宣言時に `NULL` で初期化:

```c
Sym *s, *ss = NULL, **ps;
```

実際には `if (v)` ブロック内で必ず代入されるが、`type1.t & VT_BTYPE == VT_FUNC && tok == '{'` のチェックパスとの**到達経路の証明が MSVC のフロー解析で取れない**ため、明示初期化が必要だった。

## ハマりどころと教訓

1. **「コンパイル成功」と「実行成功」は別物**: BUG-1 修正後はパースエラーが消えたため一見動いているように見えたが、生成バイナリは未定義シンボルを呼んでいた。実行テストまで含めないとクラスのインライン関数のような遅延処理は壊れていることに気づけない。

2. **既存のヘルパー関数を信じすぎない**: `cpp_register_member_body` / `cpp_finish_member_inlines` は **両方とも既に実装済み**だったが、`struct_decl` のフローから一切呼ばれていない「孤立コード」だった。grep でヘルパー名を検索しても、呼び出し箇所が 0 件なら机上の存在に過ぎない。

3. **MSVC のフロー解析は条件付き代入を許さない**: `if (v) { ss = sym_push(...); }` の後で `ss` を使うコードは clang/gcc では警告レベルだが、MSVC `/W3` 以上では `C4703` でエラー扱い。先頭で `= NULL` 初期化が無難。

## 残課題

- typedef_class.cpp は依然リグレッション中（BUG-1 で確認済み、私の変更とは無関係）
- ~~**BUG-3** 文レベル `Counter::count = 5;`~~ → 完了
- ~~**FEAT-4A** コンストラクタ初期化子リスト codegen~~ → 完了（後述）

---

# BUG-3: 文レベル `Counter::count = 5;` 修正調査記録（2026-05-30）

## 事象

文レベル（ステートメント先頭）の `Class::member` 代入が parse error になっていた。式中（`return` の中、カンマ式の中など）では動くため、`static_member.cpp` ではカンマ式 `return Counter::count = 5, Counter::get() - 5;` で回避されていた。

```cpp
class Counter { public: static int count; };
int Counter::count = 0;
int main() {
    Counter::count = 5;   // ← `':' の前に式が必要` (修正前)
    return Counter::count - 5;
}
```

## 原因

`cpp_unget_scoped_expr` 関数に **2 つの独立したバグ**があった。`block` 関数のステートメントループは、`Class::member` 形式の文を見つけるとこの関数を呼んで「式として処理するか」を判定し、true なら `gexpr()` に流す。両バグでこの判定または巻き戻し（peek + unget）が崩れていた。

### BUG-3a: 2 つ目の `:` をチェックしていない

```c
cls_tok = tok;
next();
if (tok != ':') { unget_tok(cls_tok); return 0; }
next();                                /* ← 1 つ目の `:` を消費、何でも進めている */
if (!(tok >= TOK_IDENT)) { ... }       /* ← 突然「次は識別子」を要求 */
```

`Counter::count` の場合、1 つ目の `:` 消費後 `next()` した tok は **2 つ目の `:`** で、識別子ではない。よって false が返り、`decl(VT_LOCAL)` 経由で `Counter` を typedef としてパースし、続く `::` で別エラー（`';' が必要`）になる。

これは BUG-1e と類似の構造的バグだが、関数が違うため別箇所の独立した修正が必要だった。

### BUG-3b: `mem_tok` を明示的に unget すると重複保存される

修正 BUG-3a だけ適用後、別エラー `';' が必要 ("count" が見つかりました)` が発生。トレースしたところ、`unget_tok` の暗黙挙動が原因と判明した。

`unget_tok(last_tok)` のセマンティクスは「**現在の `tok` を自動でバッファに保存し**、新 `tok` を `last_tok` にする」というもの。元のコード：

```c
mem_tok = tok;                /* tok == count(=mem_tok) の状態 */
unget_tok(mem_tok);           /* 現tok=count を保存、tok=count（無駄な再代入） */
unget_tok(':');               /* 現tok=count を再度保存 ← ★ count が 2 回積まれる */
unget_tok(':');
unget_tok(cls_tok);
```

結果、再生成されるトークン列は `Counter, :, :, count, count, =, 5, ;`（**count が 2 回**）。`gexpr` が `Counter::count` まで正常に解析した後、もう 1 つ余分な `count` トークンが残り、`skip(';')` で `; が必要 (count が見つかりました)` が発火する。

## 修正

**[tccgen.c:386-415](e:/work/work_github/tpp/tcc_dx/tccgen.c#L386-L415)** — 2 行の修正:

1. 2 つ目の `:` を明示的にチェック（`next()` 直後に `if (tok != ':')` 追加）
2. `unget_tok(mem_tok)` を削除（自動保存で十分なため）

```c
next();
if (tok != ':') {              /* ← BUG-3a: 第 2 ':' 確認 */
    unget_tok(':');
    unget_tok(cls_tok);
    return 0;
}
next();
if (!(tok >= TOK_IDENT)) { ... }
mem_tok = tok;
/* BUG-3b: unget_tok(mem_tok) を呼ばない。
 * tok == mem_tok の状態で次の unget_tok(':') が
 * 現tok を自動保存するので、count は 1 度だけ積まれる */
unget_tok(':');
unget_tok(':');
unget_tok(cls_tok);
return 1;
```

## ハマりどころと教訓

1. **`unget_tok` の暗黙の副作用に注意**: API が「引数を unget する」と読めるが、実際は「**現在の `tok` を保存し**、引数を新 tok にする」。複数連続して呼ぶときは「最後の現 tok」が落ち続けることを意識しないと、無意識に同じ token を二重に積む。今回の `unget_tok(mem_tok)` は「ペアの可視性のため」入れていたと推察されるが、結果として重複バグの温床になった。

2. **エラーメッセージの変化を追跡する**: 1 回目の修正で `':' の前に式が必要` → `';' が必要 ("count" が見つかりました)` と症状が変わった。これは「BUG-3a は直ったが別バグが顔を出した」のシグナル。トレースを諦めずに次のエラーを潰したことで BUG-3b に到達できた。

3. **「peek + unget」パターンは LIFO**: 多段の `unget_tok` は LIFO で出てくる。順序は「最後に unget したものが最初に next() で返る」。`Counter, :, :, count` を再生成したい場合は `count, :, :, Counter` の順に unget…するように見えるが、現 tok 自動保存があるので **最初の 1 つを省ける**。

## 検証

- `Counter::count = 5;` 単独文: 修正前コンパイル失敗 → 修正後 exit 0
- `Foo::get();` 単独文: 修正前コンパイル失敗 → 修正後 exit 0
- 既存の `static_member.cpp` カンマ式パス: リグレッションなし
- Stage 3/4 全 13 テスト PASS

## 残課題

- typedef_class.cpp は依然リグレッション中（私の変更とは無関係）
- ~~**FEAT-4A** コンストラクタ初期化子リスト codegen~~ → 完了（後述）（`: Base(b), m(d) { }` の実行時初期化）

---

# FEAT-4A: コンストラクタ初期化子リスト codegen 実装記録（2026-05-30）

## 目的

```cpp
class Foo {
public:
    int a, b;
    Foo(int x, int y) : a(x), b(y) {}    // ← この init list を codegen
};
int main() {
    Foo f;
    f.Foo(3, 4);
    return f.a + f.b - 7;
}
```

`: a(x), b(y)` を実行時に `this->a = x; this->b = y;` 相当のマシンコードに変換する。

## 実装した 5 つの修正

実装してみると、表面的な codegen の問題ではなく**5 つの独立した問題**が連鎖していた。

### 修正 1: ctor の戻り型省略 parse（既存欠落）

`class Foo { Foo(int x); };` の `Foo(int x)` は戻り型省略の ctor 宣言だが、parse_btype が typedef 名 `Foo` を**戻り型として吸い取り**、`Foo (int x)` を「Foo を返す関数」と誤解釈していた（`')' が必要`、`x` 識別子エラー）。

**修正**: メンバループ先頭で `tok == (class name) && next() == '('` を peek し、ctor 候補なら `btype = void` を仮定して parse_btype を skip。[tccgen.c:5230-5253](e:/work/work_github/tpp/tcc_dx/tccgen.c#L5230-L5253)

### 修正 2: ctor グローバル sym と class typedef の衝突

`cpp_finish_member_inlines` がインライン ctor を `Foo` トークンで `external_sym` 化すると、**class typedef sym `Foo` と衝突**（「再定義の型が互換性がない: 'Foo'」エラー）。

**修正**: ctor 専用のマングル名 `__cpp_ctor_<Class>` を `cpp_ctor_name_tok()` で生成し、グローバル sym 名にする。lookup 側 (`cpp_lookup_member_func`) も `cpp_is_ctor_field()` で ctor を判定し同じマングル名で `sym_find`。[tccgen.c:476-528](e:/work/work_github/tpp/tcc_dx/tccgen.c#L476-L528)

### 修正 3: bit field 解析が ctor の `:` を消費

`Foo() : a(5)` の `:` 直後 `cpp_save_mem_init_list` を呼ぶ前に、struct_decl の `if (tok == ':') { ... expr_const(); }` ブロックが**先に `:` を bit field width 指定子として食べる**。結果、`a(5)` が `expr_const()` で評価され「`a` 暗黙宣言」「constant expression が必要」エラー。

**修正**: bit field 解析を `is_ctor_decl` でガード（`if (tok == ':' && !is_ctor_decl)`）。[tccgen.c:5289](e:/work/work_github/tpp/tcc_dx/tccgen.c#L5289)

### 修正 4: init_list の展開ロジック

`gen_function` の `block(0)` 直前に展開コードを挿入。各 `name(expr)` を以下に変換:

- `cpp_push_member_var(field)` で `this->name` を lvalue として vtop に push
- `expr_eq()` で expr を解析・push
- `vstore()` で代入、`vpop()` で結果破棄

Base クラス初期化 `Base(b)` は POD 範囲外なので **paren count で skip** する（Stage 4 制限）。

init list は ctor が複数回呼ばれる可能性があるので `tok_str_dup_for_default` でディープコピーしてから `begin_macro` する。`cpp_finish_member_inlines` で field→global sym へ `cpp_mem_init_list` を伝搬。[tccgen.c:9484-9544](e:/work/work_github/tpp/tcc_dx/tccgen.c#L9484-L9544)

### 修正 5: `block(0)` の `{` 状態復元

`gen_inline_functions` は body macro を pushしてから `next()` で `{` を読んで `gen_function(sym)` を呼ぶ。私の expansion 内で `begin_macro(init_copy)` → `next()` で init_list を進めると、最後 `tok = TOK_EOF`。`end_macro()` で macro layer を pop しても、**`tok` は TOK_EOF のまま**で `block(0)` に入り、「`}` の前に式が必要」エラー。

**修正**: `end_macro()` の後 `unget_tok('{')` で **body の `{` を仮想的に再現**。次の `next()` で `block` が `{` を正しく受け取る。[tccgen.c:9540-9544](e:/work/work_github/tpp/tcc_dx/tccgen.c#L9540-L9544)

## ハマりどころと教訓

1. **「機能を 1 つ実装」は連鎖バグを呼ぶ**: 目的は「codegen 追加」だけのつもりが、ctor parse → 名前衝突 → bit field 衝突 → tok 状態管理 と 5 つの異質な問題を順に剥がす羽目になった。Stage 4 の ctor サポートは表面的（parse stub）だっただけで、本番の動作にはまだ多くの未解決領域がある。

2. **debug printf は最強の切り分けツール**: 私の expansion ロジックが「実行されていない」のを発見できたのは `fprintf(fp, ...)` で痕跡を残したから。Shift_JIS ソースに `fprintf` を埋めるのは慎重に必要（識別子と数値だけで日本語コメントなし）。

3. **typedef とメンバ関数の名前空間衝突は C++ 実装の宿命**: C++ では class `Foo` と ctor `Foo` が同名でも文法的に区別されるが、TCC のグローバル sym table は単一名前空間。マングル名で逃げるのは安全策だが、`f.Foo()` のような明示呼び出しでは lookup 側でも翻訳が必要になる。

4. **macro stack と tok 状態の duality**: `begin_macro`/`end_macro` は `macro_ptr` を切り替えるが、現在の `tok` は別途管理されている。expansion 完了後の状態は「macro layer は pop されたが tok は前回の最後」という奇妙な状態になる。`unget_tok` でうまく次の `next()` のために整える必要がある。

## テスト

新規/変更したテスト:
- `dev/test/a8/ctor_noarg.cpp`: `Foo() : a(5) {}` + `f.Foo()` 明示呼び出し
- `dev/test/a8/ctor_init_inline.cpp`: `Foo(int x) : a(x) {}` 1 引数
- `dev/test/a8/ctor_init_two.cpp`: `Foo(int x, int y) : a(x), b(y) {}` 2 引数

すべて exit 0。

**Stage 3/4 全 13 テスト PASS**、smoke / repro_double6 リグレッションなし、x64 Release + Debug Win32 両ビルド成功。

## 制限と残課題

- ~~`Foo f(5);` 形式~~ → **FEAT-4B で実装済み**（2026-05-31）。ローカル変数のみ。
- ~~Base クラス初期化子 `: Base(b)`~~ → **FEAT-4D で実装済み**（2026-05-31）
- 暗黙のデフォルト ctor、自動デストラクタ、コピー ctor などはすべて未実装。

## 残課題（全体）

- ~~typedef_class.cpp リグレッション（BUG-4）~~ → **修正済み**（2026-05-31）
- ~~デストラクタ（FEAT-4E）~~ → **完了**（2026-05-31）。ブロック終了時の自動呼び出し + 明示 `f.~Foo()`
- ~~ctor の暗黙呼び出し `Foo f(5);`~~ → FEAT-4B 完了。`Foo f;` の暗黙デフォルト ctor は未対応
- Stage 5: 仮想関数 / VTable、メンバポインタ

---

# FEAT-4B: 変数宣言での暗黙コンストラクタ呼び出し 実装記録（2026-05-31）

## 目的

```cpp
class Foo {
public:
    int a;
    Foo(int x) : a(x) {}
};
int main() {
    Foo f(5);           // ← 標準 C++ 書法
    return f.a - 5;
}
```

`Foo f(args);` を `Foo f; f.Foo(args);` 相当のコードに展開する。

## 実装概要

**方式**: §4.5 の unget 方式を採用（当初 §4.4 の vstack 直接構築案から変更）。

1. `decl()` の宣言ループで `cpp_find_ctor_field(btype.ref)` により ctor 存在を確認
2. `ident` の次が `(` かつ中身が空でなければ ctor 呼び出し形式と判定
3. `decl_initializer_alloc` で通常のローカル変数領域を確保
4. `unget_tok` で `var . Ctor (` を逆順に積み、`expr_eq()` で FEAT-4A のメンバ呼び出し経路に流す
5. 空 `()` の `Foo f();` は most vexing parse として unget で復元し従来の関数宣言パスへ

**主要箇所**: [tccgen.c:9810-9856](tccgen.c#L9810-L9856)

## 制限

- **ローカル変数のみ**（`l == VT_LOCAL`）。グローバル `Foo g(5);` は未対応
- **クラス内インライン定義 ctor のみ**（クラス外定義は FEAT-4C）
- ctor オーバーロード（同名 ctor 複数）はクラスメンバ登録段階でブロック（4B とは独立）

## テスト

| ファイル | 検証内容 | 結果 |
|----------|----------|------|
| `feat4b_basic.cpp` | `Foo f(5);` 単一引数 | exit 0 |
| `feat4b_two_args.cpp` | `Pt p(3, 4);` 2 引数 | exit 0 |
| `feat4b_no_ctor.cpp` | ctor なしクラス | exit 0 |
| `feat4b_multi_decl.cpp` | `Foo a(1), b(2), c(3);` | exit 0 |

公式 Stage 3/4 テスト 17 本すべて exit 0（2026-05-31 確認）。

## 残課題

- `build.bat` フルゲート（smoke / repro_double6 / CUnit）はマージ前に要確認
- `run_all.bat` は `a8\*.cpp` 全件を拾うため調査用ソースで失敗しうる

---

# FEAT-4C: クラス外定義コンストラクタ 実装記録（2026-05-31）

## 目的

```cpp
class Foo { public: int a; Foo(int x); };
Foo::Foo(int x) : a(x) {}
int main() { Foo f(5); return f.a - 5; }
```

## 実装概要

1. **`cpp_peek_out_of_class_ctor()`** — `Class::Class(` を `decl()` 先頭で peek（`::` 二重 peek による tok 壊れを回避）
2. **戻り型省略** — peek 成功時 `btype.t = VT_VOID`、`cpp_qualified_class` を設定して `parse_btype` を skip
3. **`external_sym`** — ctor 名は `__cpp_ctor_<Class>`（FEAT-4A と同じマングル）
4. **init list** — `tok == ':'` 時に `cpp_save_mem_init_list(sym)` → FEAT-4A の `gen_function` 展開を再利用

**主要箇所**: [tccgen.c](tccgen.c) `cpp_peek_out_of_class_ctor`, `decl()` 関数定義ブロック

## テスト

| ファイル | 検証内容 | 結果 |
|----------|----------|------|
| `feat4c_basic.cpp` | 外定義 + init list + `Foo f(5)` | exit 0 |
| `feat4c_two_args.cpp` | 2 引数 + init list | exit 0 |
| `feat4c_no_init.cpp` | init list なし | exit 0 |

---

# FEAT-4D: Base クラス初期化子 codegen 実装記録（2026-05-31）

## 目的

```cpp
class Base { public: int v; Base(int x) : v(x) {} };
class Derived : public Base {
public:
    int m;
    Derived(int b, int d) : Base(b), m(d) {}
};
int main() {
    Derived d(3, 4);
    return d.v + d.m - 7;
}
```

## 実装概要

1. **`cpp_find_base_field()`** — 派生クラスの匿名 base subobject を base 名トークンで検索
2. **`cpp_emit_base_ctor_call()`** — `: Base(args)` を `__cpp_ctor_Base(&base_subobject, args)` に展開
3. **`gen_function` init list ループ** — POD メンバ代入と base ctor 呼び出しを分岐

**主要箇所**: [tccgen.c](tccgen.c) `cpp_find_base_field`, `cpp_emit_base_ctor_call`, `gen_function` mem-init 展開

## テスト

| ファイル | 検証内容 | 結果 |
|----------|----------|------|
| `feat4d_base_init.cpp` | `: Base(b), m(d)` | exit 0 |
| `feat4d_base_body.cpp` | `: Base(b)` + 本体代入 | exit 0 |

---

# BUG-4: クラス型 typedef `typedef Point P; P p;` 実装記録（2026-05-31）

## 症状

```cpp
class Point { public: int x; };
typedef Point P;
P p;   // error: ';' expected before "p" または P の再定義エラー
```

`typedef int I; I x;` は成功するが、`typedef` の元型が struct/class 名の場合のみ失敗。

## 原因

C++ モード `parse_btype()` の default 分岐で、クラス名を `struct_find()` 経由で型として取り込む際に

```c
type->t = stsym->type.t;
t = type->t;
```

と **`VT_TYPEDEF` 等の storage 修飾子を捨てていた**。

その結果 `typedef Point P;` 宣言時に `P` が typedef ではなく `Point` 型の**グローバル変数**として登録され、以降 `P p;` で `parse_btype(P)` が typedef 解決できず K&R 的に `int P` と解釈 → 再定義エラー。

## 修正

```c
type->t = stsym->type.t | (t & VT_STORAGE);
```

`typedef` / `extern` / `static` 等の storage ビットを struct 型とマージしてから `t` に反映。

## テスト

| ファイル | 検証内容 | 結果 |
|----------|----------|------|
| `dev/test/a7/typedef_class.cpp` | `typedef Point P; P p;` | exit 0 |
| CUnit `test_stage3_typedef_class_compile` | compile-only | PASS |

---

# FEAT-4E: デストラクタ parse + 自動呼び出し 実装記録（2026-05-31）

## 目的

```cpp
class Foo {
public:
    Foo(int x) : a(x) {}
    ~Foo() { /* cleanup */ }
};
int main() {
    { Foo f(1); }  // ブロック終了時に ~Foo() を自動呼び出し
    return 0;
}
```

## 実装概要

1. **`~Class()` parse** — `struct_decl` で ctor と同様に `VT_VOID` + 内部 field 名 `__cpp_dtor_fld_<Class>`
2. **`__cpp_dtor_<Class>` マングル** — `cpp_finish_member_inlines` / `cpp_lookup_member_func` でグローバル sym に接続
3. **`cpp_call_scope_dtors`** — `prev_scope()` でブロック終了時、ローカル class オブジェクトの dtor を宣言逆順で emit
4. **明示呼び出し `f.~Foo()`** — postfix `.` 経路で `~` + クラス名を dtor field に解決

## 制限

- **`return` / `break` 経路** — 自動 dtor 未対応（`prev_scope` のみ）。`return` 前に明示 `f.~Foo()` は可
- **グローバル / 関数スコープ終端** — 関数末尾 `return` では未呼び出し（内側ブロック `{}` は可）
- **派生クラスの base dtor 連鎖** — 未対応

## テスト

| ファイル | 検証内容 | 結果 |
|----------|----------|------|
| `feat4e_basic.cpp` | ctor + ブロック終了時 dtor | exit 0 |
| `feat4e_explicit.cpp` | `f.~Foo()` 明示呼び出し | exit 0 |
| `feat4e_nested.cpp` | 複数ローカル dtor 順序 | exit 0 |

---

# FEAT-5A: 仮想関数 / VTable 実装記録（Phase 1/2、2026-06-03）

## 前提

FEAT-5A は「未着手」と記載されていたが、実際には [tccgen.c](tccgen.c) に**実装の大部分（`virtual` parse、slot 割当、vptr 挿入、vtable emit、ローカル vptr 初期化、仮想呼び出し dispatch）が既に存在し配線済み**で、テストも `dev/test/a8/feat5a_*.cpp` に 7 本あった。だが仮想呼び出しを行うと **tcc.exe が 0xC0000005 でクラッシュ**していた。

## 目的のコード

```cpp
class Foo { public: Foo() {} virtual int foo() { return 42; } };
int main() { Foo f; return f.foo() - 42; }   // f.foo() / p->foo() を動かす
```

## 症状の遷移と確定原因（独立した 3 バグ）

症状は「コンパイラがクラッシュ」→「生成プログラムがクラッシュ」と段階的に移動し、根本原因は相互に独立した 3 つだった。トレース `fprintf` を仕込んで到達点を追う FEAT-4A 流のバイセクトで切り分けた。

### BUG-5A-a: `indir()` に NULL ref のポインタ型を渡しコンパイラ即死（**本命1**）

- **症状**: `cpp_prepare_virtual_member_call` 内の最初の `indir()` で 0xC0000005。トレースは「after gaddrof」までで停止。
- **原因**: vtable をたどるため `pt.t = VT_VOID | VT_PTR; pt.ref = NULL;` という手製のポインタ型を `indir()` に渡していた。`indir()` ([tccgen.c](tccgen.c) `ST_FUNC void indir`) は末尾で必ず `vtop->type = *pointed_type(&vtop->type)` を実行し、`pointed_type()` は `&type->ref->type` を返す。**`ref == NULL` で `NULL->type` を参照**して即死。`VT_VOID|VT_PTR` は `VT_VOID==0` なので実体は `VT_PTR`、`indir()` 冒頭の「ポインタか?」判定を通過してしまいガードに掛からない。
- **副次バグ**: 最終ステップ `vtop->type = field->type; vtop->r &= ~VT_LVAL;` は、レジスタに入っているのが「スロットのアドレス」なのにそれを関数本体アドレスとして扱い、**1 段足りない間接参照**で誤った呼び先になる潜在バグでもあった。
- **修正**: `mk_pointer()` で正規のポインタ型（`ref` を確保）を構築し、`T(***)()→T(**)()→T(*)()` と段階的にたどって**最終的に関数ポインタの lvalue を vtop に残す**方式に書き換え。これは通常の関数ポインタ呼び出し `fp()` と同一表現で、呼び出しハンドラの「ポインタ→関数」変換パスがそのまま間接呼び出しを生成する。スロット添字も `vpushi(slot)` による型付きポインタ演算（自動で PTR_SIZE 倍）に変更。

### BUG-5A-b: 残存 `.sym` による誤オーバーロード解決でコンパイラ死（**本命2**）

- **症状**: BUG-5A-a 修正後、`cpp_prepare_virtual_member_call` は完走するが、呼び出しハンドラ側で 0xC0000005。
- **原因**: `vpushv(&cpp_member_this)` が `this`（オブジェクト）の `SValue` を丸ごとコピーするため `.sym` も引き継ぐ。呼び出しハンドラ [tccgen.c](tccgen.c) の `if (... vtop[-nb_args].sym && ... == VT_FUNC) cpp_resolve_func_call(vtop[-nb_args].sym->v, ...)` が、関数ポインタ値に残った**stale な Sym** を参照して死亡。
- **修正**: dispatch 完了後に `vtop->sym = NULL` を設定。これは名前付き関数ではなく間接呼び出しなので、オーバーロード解決経路に入れてはならない。

### BUG-5A-c: `cpp_init_local_vptr` の封印 + `vstore` オペランド逆順（実行時クラッシュ）

- **症状**: BUG-5A-a/b 修正で **コンパイルは成功**するようになったが、生成プログラムが実行時 0xC0000005。原因は**ローカルオブジェクトの vptr が未初期化**（object[0] がゴミ）のまま仮想呼び出しすること。
- **原因 1（封印）**: `decl()` のローカル変数確保部 ([tccgen.c](tccgen.c)) で `cpp_init_local_vptr(sym)` の呼び出しが `#if 0 /* debug */` で無効化されていた。
- **原因 2（逆順）**: 封印解除すると今度は `cpp_init_local_vptr` 自身がコンパイラを落とした。`vstore()` の規約は **`vtop[-1]` = 格納先 lvalue、`vtop` = 格納する値** だが、当該関数は `vpushsym(&vtable)`（値）→ `vset(object)`（lvalue）の順で積んでおり**逆**。結果、vtable のある rodata 側を格納先と誤認して書き込もうとした。加えて格納型も `pt.ref = NULL` の void* だった。
- **修正**: 呼び出しの `#if 0` を解除。`cpp_init_local_vptr` を「宛先 lvalue（`vset(VT_LOCAL|VT_LVAL, obj_sym->c)`）を先 → 値（`vpushsym(&vtable)`）を後」に是正し、void* は `mk_pointer` で正規化。これで object[0] に `&vtable` が書かれ、dispatch が正しい vtable をたどる。

## 修正箇所（[tccgen.c](tccgen.c) 3 箇所）

1. `cpp_prepare_virtual_member_call` — 正規ポインタ型での vtable 走査 + `.sym` クリア（BUG-5A-a, b）
2. `cpp_init_local_vptr` — `vstore` オペランド順是正 + void* 正規化（BUG-5A-c）
3. `decl()` ローカル変数確保 — `cpp_init_local_vptr` 呼び出しの `#if 0` 解除（BUG-5A-c）

## テスト

| ファイル | 検証内容 | 結果 |
|----------|----------|------|
| `feat5a_class_only.cpp` | `virtual` 宣言のみ | exit 0 |
| `feat5a_local.cpp` | ローカル obj 生成（vptr 初期化） | exit 0 |
| `feat5a_basic.cpp` / `feat5a_call.cpp` | `f.foo()` 値オブジェクト動的 dispatch | exit 0 |
| `feat5a_ptr.cpp` | `p->foo()` ポインタ動的 dispatch | exit 0 |
| `feat5a_global.cpp` | グローバル obj | **未対応**（Phase 4。実行時クラッシュ） |
| `feat5a_override.cpp` | 派生上書き + `(Base*)` キャスト | **未対応**（Phase 3。コンパイルエラー `'Base' は宣言されていません`） |

公式 Stage3/4 全 25 本 exit 0（回帰なし、2026-06-03）。

## ハマりどころと教訓

1. **手製の型を codegen ヘルパー（`indir`/`vstore`）に渡すなら必ず `mk_pointer` で `ref` を確保する**。`ref == NULL` のポインタ型は `pointed_type()` 経由で確実にコンパイラを落とす。FEAT-4A から続く「`unget`/vstack の暗黙規約」と同類の地雷。
2. **`vstore()` のオペランド順は「宛先 lvalue が下、値が上」**。FEAT-4A のメンバ代入と同じ並びを守る。逆順は rodata 破壊や実行時クラッシュになる。
3. **`#if 0 /* debug */` で封印された配線は「未実装」ではなく「壊れて封印された実装」**。封印を解くと別バグ（今回は `vstore` 逆順）が顔を出す。FEAT-4B/4A と同じく、封印コードは鵜呑みにせず実テストで検証する。
4. **症状はコンパイル時クラッシュ→実行時クラッシュへ移動する**。各修正後に必ずビルド＋実行まで通し、「どのバグが何を直し次の何を露出させたか」をバイセクトで追うのが有効だった。

## 残課題（Phase 3 以降）

- **Phase 3**: `(Base*)&d` のキャストでクラス名が型解決されない（virtual と独立の可能性）。解消後、派生上書きの動的ディスパッチ（`p->foo()==2`）と `cpp_assign_virtual_slots` の継承 slot 引き継ぎを実行時検証。
- **Phase 4**: グローバルオブジェクトの vptr 静的初期化（data セクションに vtable アドレスを relocation で埋める）。`cpp_init_local_vptr` はローカル専用。
- `build.bat` フルゲート（smoke / repro_double6 / Win32 Debug / CUnit）はマージ前に要実行。
## FEAT-5A Phase 3/4 追記（2026-06-05）

### BUG-5A-d: `check_fields` が ctor のクラス名トークンを破壊（Phase 3）

- **症状**: `class Derived : public Base` のあと `Base *p` や `(Base*)&d` で `'Base' は宣言されていません`。
- **原因**: `check_fields(check=0)` が派生クラスの埋め込み base を再帰走査し、Base の ctor メンバ（クラス名トークン）に対して `ts->tok ^= SYM_FIELD` を実行。Base 単独では 1 回、派生定義後は 2 回 XOR されトークンが壊れる。
- **修正**: C++ では `VT_FUNC` メンバを `check_fields` から除外。埋め込み base（`parent_class`）への再帰も常にスキップ。

### BUG-5A-e: グローバル polymorphic オブジェクトの vptr（Phase 4）

- **症状**: `Foo g;`（virtual あり）でコンパイラまたは実行時クラッシュ。
- **原因**: `.bss` / common に `greloca` すると tcc が落ちる。vptr 未初期化だと実行時クラッシュ。
- **修正**: `cpp_needs_vptr` で `.data` セクションに配置し、`cpp_init_global_vptr` で vtable への reloc を埋める。

---

# BUG-6: メンバ関数内の `this` が未初期化 Sym r/c 経由で壊れる（2026-06-11）

## 事象

FEAT-6A-ext の実行テスト確認（残作業）で、`feat6a_vec2*.cpp` 8 本と
`feat6a_ext_sub_member/sub_const/full` が **0xC0000005（access violation）** で
クラッシュ。直近 3 マージ（cb70fd2 / 429f522 / 6ef790d）の全ビルドで再現する一方、
「どのテストが落ちるか」がビルドごとに変わる（ドキュメントの過去の
「exit 0 確認済み」とも食い違う）という揮発性の症状だった。

## 切り分け

- `dev\tcc.exe`（2026-06-06 18:34）が最終コミット 3cb7173（18:47、tccgen.c 変更）より
  **古い**ことを発見 → 再ビルドしても症状は残存（バイナリ鮮度は無関係と確定）。
- worktree で cb70fd2 / 429f522 をビルドし比較 → どのコミットでも落ちる組が変わるだけ。
- 最小再現: 「struct 返却メンバ関数」+「本体で `this` 経由のメンバ読み」の組のみ落ちる。
  定数だけ書く `copy()` は落ちない。
- dumpbin /disasm で確定: `V::copy` 先頭が `mov [rbp+10h],rcx`（this 退避）の直後に
  **`mov eax,[rax]`** — this を `[rbp+10h]` からロードせず **未初期化 RAX を間接参照**。

## 原因

`gen_function()` が `this` 仮引数を注入する際:

```c
this_param = sym_malloc();
this_param->v = TOK_THIS;
this_param->type = pt;
/* r と c は未設定のまま！ */
cpp_this_sym = this_param;
```

`sym_malloc()` は **Sym プールの再利用でゼロ初期化されない**。`cpp_push_member_var()` と
unary の `TOK_THIS` パスは `vset(&cpp_this_sym->type, cpp_this_sym->r, cpp_this_sym->c)` と
r/c を直接使うため:

- 再利用 Sym がたまたま直前の関数の第 1 引数（`r=VT_LOCAL|VT_LVAL, c=0x10`）なら**偶然正しく動く**
- struct 返却型の処理で Sym プールの解放順が変わると `r=0`（= レジスタ 0 = **RAX**）等の
  ゴミを引き、`mov eax,[rax]` を生成してクラッシュ

つまり Stage 3 以降のメンバアクセスは全て**未定義動作の上で偶然動いていた**。
コミット間・ビルド間で合否が揺れたのはこのため。

## 修正（branch `fix/cpp-this-uninit-rc`、tccgen.c の gen_function のみ）

`gfunc_prolog()` は this を含む全パラメータを正しい storage（r/c）付きで
`local_stack` に push 済みなので、prolog 直後に差し替える:

```c
if (this_param) {
    Sym *this_local = sym_find(TOK_THIS);
    if (this_local)
        cpp_this_sym = this_local;
}
```

## 検証

- クラッシュしていた 12 本（`feat6a_vec2*` 8 本 + `feat6a_ext_sub_member/sub_const/full` + 最小再現）全て exit 0
- `build.bat` フルゲート緑（Release x64 / Debug Win32 / smoke / repro_double6 / CUnit 14/14 / run_all）
- a7・a8 公式テスト全 exit 0（回帰なし）

## 教訓

1. **`sym_malloc()` は初期化しない**。新規 Sym を作るときは使うフィールド（特に r/c）を必ず明示設定する。
2. **「以前 exit 0 だった」は未定義動作の偶然かもしれない**。コンパイル only のゲート（`run_all.bat` の `-c`）では実行時の codegen バグを検出できない。実行テストをゲートに含める仕組みが必要。
3. クラッシュ exe は WER ダイアログでハングして見える。`HKCU\...\Windows Error Reporting\DontShowUI=1`（調査後に戻す）で exit code 取得が安定する。

## レビュー対応（2026-06-11）

PR レビュー指摘を受けて同ブランチで追加修正:

1. **fail-fast 化**: `sym_find(TOK_THIS)` が NULL の場合、サイレントに無視せず
   `tcc_error("internal error: 'this' parameter not pushed by prolog")` で即死させる。
2. **`this_param->r/c` の明示初期化**: sym_malloc 直後に `r = 0; c = 0;` を設定し
   「未初期化の残骸」という地雷自体を除去（プレースホルダであることをコメント明記。
   正式な storage は prolog が push したローカル）。
3. **前提条件のコメント明文化**: `sym_find(TOK_THIS)` は「prolog 直後・本体パース前」に
   実行されるため、解決先は直前に push されたパラメータのみで shadowing は発生し得ない。

レビューで挙がった中長期課題（本 PR スコープ外、要フォローアップ）:

- **sym_malloc 初期化ポリシー**: debug ビルドでの poison（`memset(sym, 0xCD, ...)`）
  またはゼロ初期化ヘルパ `sym_new_clean()` の導入。同種の未初期化バグの未然防止。
- **`cpp_this_sym` グローバル状態の設計見直し**: 再帰・ネスト・inline 展開で
  状態リークの温床になりうる。コンテキスト構造体への移動を中長期で検討。
- **`run_all.bat` への実行テストゲート組み込み**: 現状 compile-only（`tcc -c`）のため
  実行時 codegen バグを検出できない（BUG-6 が長期間潜伏した直接原因）。

## 今回の調査で見つかった残課題（別タスク）

| テスト | 症状 | 原因見込み |
|--------|------|-----------|
| `feat6a_ref_arg` | EXIT=-2 | **テスト自体のバグ**（`return x + o.x + o.y;` に `+ y` 欠落。BUG-6 時代は this がゴミで偶然 b を指し 10 になり exit 0 に見えていた） |
| `feat6b_overload` / `_rev` | EXIT=±100 | const/非 const オーバーロードが両呼び出しとも**後に定義された方**に解決される（`cpp_find_field_for_call` / グローバル sym 上書きの疑い） |
| `feat6a_big_struct` | EXIT=32763 | 8 バイト超 struct 返却メンバ呼び出しで sret ポインタと this の引数順が不整合の疑い（調査用テスト、公式ゲート外） |

---

# BUG-8: `this` の明示デリファレンスでコンパイラが AV（2026-06-11）

## 事象

FEAT-6A-ext2（代入 operator）着手時、`operator=` の正準形 `return *this;` を含む
クラス外定義をコンパイルすると **tcc.exe 自体が 0xC0000005 でクラッシュ**。
最小化の結果、operator とは無関係で、メンバ関数内の以下が全て落ちる（ベースライン
master でも再現する既存バグ）:

- `this->v`（明示の `->` メンバアクセス）
- `(*this).v`
- `return *this;`（参照返却・値返却とも）

`return this;`（ポインタのまま）や暗黙のメンバアクセス `v = o.v;` は落ちない。
また `Acc* p = this;` に「互換性のないポインタ型」警告が出ていた（同根の症状）。

## 切り分け

`dev/test/a9/manual/ext2_t1〜t16.cpp` の 16 本で二分探索:

| 確認 | 結果 |
|------|------|
| 宣言のみ（t1） | OK |
| `return o;`（参照仮引数を返す、t7） | OK |
| `return this;`（t8） | OK（警告つき） |
| `Acc* p = this; return *p;`（t11） | OK |
| `return *this;`（t10/t13） | **AV** |
| `this->v`（t16） | **AV** |
| ベースライン master ビルドで t13/t16 | **AV**（既存バグと確定） |

## 原因

`gen_function()` の `this` 型構築が **ポインタ CType の不変条件を破っていた**:

```c
pt.t = VT_PTR;
pt.ref = sym->parent_class;   /* クラスのタグ Sym を直接指す */
```

正しいポインタ型は `ref` が「指し先型を持つ要素 Sym」を指す（`mk_pointer()` が作る形）。
タグ Sym を直接指すと `pointed_type(this) == &parent_class->type` となるが、
`struct_decl()` はタグ Sym 自身の type を `type1.ref = NULL` で push するため
（tccgen.c L6574〜6576）、**指し先が `{VT_STRUCT, ref=NULL}`** になる。

- `indir()` 自体は落ちない（type をコピーするだけ）
- その後の **field 走査**（`find_field` 等）が `type->ref->next` を辿って **NULL deref**
- `cpp_push_member_var()`（暗黙メンバアクセス）は `field->c` の cumofs を直接使い
  `type.ref` を見ないため、これまで露見しなかった
- `Acc* p = this;` の警告も、指し先比較が `{ref=NULL}` vs `{ref=tag}` で不一致になるため

## 修正（branch `fix/cpp-this-deref`、gen_function のみ）

`mk_pointer()` で正規のポインタ型を構築する:

```c
pt.t = VT_STRUCT;
pt.ref = sym->parent_class;
mk_pointer(&pt);
if (sym->type.ref->f.func_const)
    pt.t |= VT_CONSTANT;   /* const の位置は従来踏襲（ポインタ側） */
```

`VT_CONSTANT` の位置を従来（ポインタ側）のまま維持し、feat6b の const 判定への
影響をゼロにしている（指し先側へ移すのは FEAT-6B-P2 で検討）。

## 検証

- t5/t8/t10/t11/t13/t14/t16 全て AV 解消（t8/t11 の警告も消滅）
- 回帰テスト `dev/test/a9/bug8_this_deref.cpp`（`this->v` + `(*this).v` + 参照/値 `return *this`）exit 0
- `run_all.bat` の出力がベースラインと**完全一致**（Compare-Object で確認、回帰ゼロ。
  既知の a8 調査用ソース失敗はベースラインでも同一）
- Release x64 / Debug Win32 ビルド緑、CUnit 14/14、smoke / repro_double6 通過

## 教訓

1. **CType の不変条件（`VT_PTR` の `ref` は要素 Sym）を手組みで破らない**。ポインタ型は必ず `mk_pointer()` で作る。
2. 「一部の構文だけ通る」のは経路ごとに型の使い方が違うサイン（暗黙メンバアクセスは cumofs 直接、明示は field 走査）。
3. 謎の「互換性のないポインタ型」警告は型構築バグの早期シグナルだった。警告を放置しない。

---

# FEAT-6A-ext3 実装中の発見（2026-06-11）

## 発見 1: `.cpp` の空 `()` が `FUNC_OLD` で arity overload が壊れる

`feat6a_ext3_arity.cpp`（単項 `operator-()` + 二項 `operator-(const Acc&)` 共存）で
2 つ目のクラス外定義が **「再定義: '__cpp_op_minus'」**（`patch_type`）。

- 真因: `post_type()` で空 `()` は K&R 旧式 `FUNC_OLD`（= 引数任意）になる（[tccgen.c](tccgen.c) L7456〜）。
  `is_compatible_func()` は **FUNC_OLD を任意のシグネチャと「互換」扱い**するため、
  `external_sym()` の overload 分岐（非互換なら新 sym push）に入らず patch へ流れる。
- 修正: C++ TU では `l = tcc_state->cpp ? FUNC_NEW : FUNC_OLD;` — **空 `()` を `(void)` 扱い**
  （ISO C++ のセマンティクスそのもの）。`.c` は従来通り。
- 影響範囲に注意: `.h` は親 TU の cpp を継承するため、`.cpp` から include される
  C ヘッダの `int foo();` も `(void)` になる。SDK ヘッダは完全プロトタイプのため実害なし
  （a7/a8/a9 実行 58 本 + CUnit + mixed_link で回帰なしを確認）。

## 発見 2: `cpp_lookup_member_func` がシグネチャ無視（BUG-7 と同根）

arity テストで `-a`（単項）と `a - b`（二項）の**両方が後に定義された方**の
グローバル sym に解決された（EXIT=1）。

- 真因: `cpp_lookup_member_func()` は `sym_find(v)` の prev_tok チェーンを
  `parent_class` と `func_const` だけでフィルタし、**最初にヒットした sym を返す**。
  同名 overload が複数定義されると常に後勝ち。これは BUG-7
  （`feat6b_overload` の const overload 誤解決）と**同じメカニズム**。
- 今回の対処: **arity（param 個数）フィルタを追加**（`cpp_func_param_count`）。
  単項／二項の共存はこれで解決。
- 残課題（= BUG-7 本体）: 同 arity で param **型**だけ違う overload
  （`set(int)` / `set(double)` 等）のクラス外定義は依然として後勝ち。
  `is_compatible_func` ベースの完全シグネチャ照合を BUG-7 修正で導入すべき。

---

# BUG-7: const/非 const オーバーロードが後に定義された方に解決される（2026-06-12 修正）

## 事象

`feat6b_overload.cpp` / `feat6b_overload_rev.cpp`（`get()` と `get() const` を
クラス外定義し、非 const オブジェクト・const オブジェクトから 1 回ずつ呼ぶ）が
EXIT=+100 / -100。**両呼び出しとも「後に定義された方」**の関数が実行される。

## 調査過程（デバッグ出力 + dumpbin /disasm）

1. obj 内のマングルシンボルは `__tcc_get_` / `__tcc_get__C` の **2 つとも存在**
   — 定義 push（`external_sym` の overload 分岐）は正常。
2. `cpp_lookup_member_func` のデバッグ出力では **両呼び出しとも `want_const=0`**
   — `cf.get()` でも const フィールドが選ばれていない。
3. `cpp_find_field_for_call` のデバッグ出力で `cf` の `type->t=0x7`
   （VT_STRUCT のみ、**VT_CONSTANT 脱落**）。同コードの `.c` 版は `0x107` で正常。
4. それでも lookup は `__tcc_get_`（非 const）を返すのに、disasm では両 call とも
   const 本体へ — postfix `(` の **`cpp_resolve_func_call` 再解決**が
   lookup の選択を**上書き**していた。

## 真因（2 件の合わせ技）

### 真因 1: `parse_btype` の C++ クラス名パスで先行 `const` が脱落

[tccgen.c](tccgen.c) の `parse_btype()` default 分岐（クラス名を型として認識する
cpp パス）で:

```c
type->t = stsym->type.t | (t & VT_STORAGE);   /* ← VT_CONSTANT を捨てる */
```

`const Foo cf` の `const` は先に `t` に蓄積されるが、ストレージビットしか
引き継がれない。後置（`Foo const cf`）は the_end 側で合流するため脱落しない。
typedef パスは `parse_btype_qualify()` で正しく処理しており、クラス名パスだけの欠落。

→ `cf` の型から const が消える → `cpp_find_field_for_call` の `obj_const=0`
→ const フィールドが選ばれない（`feat6b_const_obj` は const 版しか無いため
fallback で偶然 exit 0 に見えていた）。

### 真因 2: postfix `(` の `cpp_resolve_func_call` が parent_class / func_const 無視

`cpp_prepare_member_func_call` → `cpp_lookup_member_func` が parent_class +
func_const + arity で正しい sym を選んでも、postfix `(` ハンドラの

```c
Sym *resolved = cpp_resolve_func_call(vtop[-nb_args].sym->v, nb_args);
vtop[-nb_args].sym = resolved;   /* ← 無条件で上書き */
```

が**トークン名だけで prev_tok チェーンを再走査**する。`get()` overload は
両方 0 引数なので両方 match（score 同点）→ **チェーン先頭 = 後に定義された方**
が勝つ。これが「後勝ち」の直接原因。

## 修正（branch `fix/cpp-bug7`）

1. `parse_btype()` クラス名パス: 修飾子を保持

```c
type->t = stsym->type.t
    | (t & (VT_STORAGE | VT_CONSTANT | VT_VOLATILE));
```

2. `cpp_resolve_func_call(v, nb_args, cur)` に現在バインド済み sym `cur` を渡し、
   候補を `parent_class` 一致 + `func_const` 一致でフィルタ
   （メンバ呼び出しの再解決がクラス境界・const 境界を越えない）。
   free 関数呼び出しは `cur->parent_class == NULL` 同士で従来挙動。

## テスト

- `feat6b_overload.cpp` / `feat6b_overload_rev.cpp` — exit 0（修正前 ±100）
- 新規 `bug7_const_inline.cpp` — インライン本体の const overload + 先行 const 保持
- 新規 `bug7_const_param.cpp` — `const Foo *p` 経由の const overload 呼び出し
- 回帰: `run_all.bat` / `build.bat` で新規 NG なし（既知 sm3/sm6/t_assign のみ）

## 教訓

1. 「後に定義された方に解決」は **解決ロジックが 2 段ある**サイン。
   片方（lookup）を直しても、後段（再解決）が上書きしていないか必ず確認する。
2. 修飾子付き型のバグは「警告が出ない方の言語モード」（今回 .cpp）を疑い、
   同一コードを `.c` と `.cpp` で並走させて type->t を比較するのが最短。
3. 既知失敗（`feat6b_const_obj` が exit 0）も**偶然成立**のことがある。
   overload が片側しか無いテストは const 判定の検証にならない。

## 残課題（BUG-7 から分離）

- ~~同 arity・**param 型違い**のクラス外 overload（`set(int)` / `set(double)`）は
  依然として後勝ち。`is_compatible_func` ベースの完全シグネチャ照合が必要~~
  → **2026-06-12 実装済み**（下記「完全シグネチャ照合」節参照）。

---

# 完全シグネチャ照合 + 引数変換遅延（2026-06-12 実装）

## 事象（実装前）

同 arity・param 型違いの overload（`set(int)` / `set(double)`）をクラス外定義
すると、**両呼び出しとも後に定義された方**に解決される（`sig_member_types.cpp`
で EXIT=100 を確認）。

## 真因（こちらも 2 段）

1. `cpp_lookup_member_func` が parent_class / const / **arity まで**しか
   見ないため、初期バインドが後勝ち。
2. より深刻: postfix `(` では **引数パース中に `gfunc_param_typed()` が
   初期バインドのプロトタイプへ即時キャスト**する。`set(2.5)` が
   `set(int)` に仮バインドされると 2.5 → 2 に変換された**後**で再解決が走り、
   int になった引数は `set(int)` に 10 点でマッチ → 誤った overload に確定。
   変換で情報が落ちるため、再解決だけでは原理的に直せない。

## 実装

1. **`cpp_lookup_member_func` 2 パス化**:
   - パス 1: `is_compatible_types(&s->type, &field->type)`（→ `is_compatible_func`。
     戻り値型・param 型・func_const を全て照合）でフィールドのシグネチャと
     完全一致するグローバル sym を返す。
   - パス 2（フォールバック）: 従来の const + arity フィルタ
     （ctor/dtor 内部プロトなど型比較が成立しないケース用）。
2. **引数変換の遅延**（postfix `(`）:
   - 呼び出し先が overload 集合（同 v・同 parent_class・同 func_const の
     候補が 2 つ以上 = 新ヘルパ `cpp_call_has_overloads`）のとき、
     パース中の `gfunc_param_typed()` をスキップ。
   - `cpp_resolve_func_call` が**生の引数型**でスコアリングして解決した後、
     `vrotb(nb_args)` を nb_args 回（最深の引数が top に来る → 変換 → 1 周で
     元の並びに戻る）で各引数を解決済みプロトタイプへ変換。
   - デフォルト引数の適用（`cpp_apply_default_args`）も解決後に移動
     （解決された側のデフォルト値を使うのが正しいセマンティクス）。
   - スコープ外条件（従来動作維持）: struct 返却（sret スロットが
     nb_args に混ざる）、`reverse_funcargs`、関数ポインタ経由
     （`VT_SYM` なし）、`extern_c`。

## テスト

- `sig_member_types.cpp` — `set(int)` / `set(double)` クラス外定義、正順
- `sig_member_types_rev.cpp` — 宣言・定義とも逆順
- `sig_free_types.cpp` — free 関数 `pick(int)` / `pick(double)` / `pick(const char*)`
- 回帰: a7/a8/a9 実行（既知失敗のみ）、`run_all.bat`（既知 sm3/sm6/t_assign のみ）、
  `build.bat` フルゲート、CUnit 全 pass

## 制限（既知）

- スコアは「完全一致 10 / 数値変換・ポインタ変換 1」の 2 段のみ。
  C++ の順位付け（昇格 > 変換）や曖昧エラーは未実装 — 同点はチェーン先頭
  （後に定義された方）が勝つ。
- struct 返却 overload・関数ポインタ経由・`reverse_funcargs` 時は
  従来通り即時変換（型違い overload は解決されない）。

## 教訓

1. overload 解決バグは「解決ロジック」だけでなく**解決前に走る型変換**を疑う。
   単一パスコンパイラでは変換タイミングが実質的な解決タイミングになる。
2. `vrotb(n)` を n 回で「全要素を 1 度ずつ top に出して元に戻す」イディオムは
   非 top の vstack 要素へ後追い変換を掛ける安全な方法。

---

# FEAT-CTOR-OV: 基底 mem-initializer の ctor 解決と引数変換（2026-06-12）

## 事象

ctor オーバーロード自体は完全シグネチャ照合（12g）で既に動作していたが、
派生 ctor の mem-initializer `: B(2.5)` だけが:

1. 基底に overload ctor があると **最初の ctor フィールドに誤解決**（EXIT=-102）
2. overload が無くても **引数が未変換**（`: B(2.9)` で double の生ビットが
   int param に渡り EXIT=858993457 のゴミ）

## 真因

`cpp_emit_base_ctor_call()` は独自経路で、(a) 引数パース**前**に
`cpp_find_ctor_field`（最初の ctor）→ `cpp_lookup_member_func` で解決を確定、
(b) `gfunc_param_typed()` を一切呼ばず生の SValue を `gfunc_call` に渡していた。
postfix `(` 経路に入った再解決・変換遅延・型付き変換がどれも効かない。

## 修正（branch `feat/cpp-ctor-overload`）

引数パース後に `cpp_resolve_func_call(ctor_global->v, na, ctor_global)` で
生の引数型から解決し直し、`vrotb(na)` × na 回で各引数を解決済みプロトタイプへ
`gfunc_param_typed` 変換。デフォルト引数が残れば `cpp_apply_default_args`。
this 挿入（base_this）より前に行うので rotate 対象は args のみ。

## 教訓

- 「メイン経路を直しても独自経路が残る」— 呼び出し系の修正は
  postfix `(` / `cpp_finish_member_call` / `cpp_emit_base_ctor_call` の
  **3 経路すべて**を確認すること。
- 引数変換漏れは overload が無くても潜在バグ（型が一致している間だけ動く）。

---

# FEAT-4F 実装中の発見（2026-06-12）

## 発見 1: struct ローカル参照 `P &r = a;` が実行時ハング（既存問題）

FEAT-4F のテスト作成中に発見。**ctor の有無と無関係**に、struct 型の
ローカル参照宣言を含むコードが実行時に無限ループする:

```cpp
class P { public: int v; };
int main() { P a; a.v = 7; P &r = a; r.v += 1; return a.v - 8; }  /* HANG */
```

- 修正前の tcc でも同症状（FEAT-4F 起因ではない）。
- スカラ参照（`int &r = x;`）は FEAT-6A-ext の参照基盤で動作する。
  struct 参照はバインド経路が未接続の可能性が高い。
- 対処: **BUG-9 として修正済み（下記セクション参照、2026-06-12）**。
  テスト `ctor4f_skip.cpp` からは参照行を除外した。

## 発見 2: TEMP の stale exe による偽 AV

ハングした旧バイナリ（参照入りテスト）のプロセスが TEMP の exe を
ロックしたまま残り、`tcc -o` が**黙って書き込み失敗** → 古い exe を
実行して AV/ハングが再現し続けた。コピー先や出力名を変えると直る。

- 教訓: テストの失敗が「コード修正後も全く変わらない」場合、
  出力 exe が本当に更新されたか（タイムスタンプ／別名出力）を確認する。
  ハング調査の後は `Stop-Process` 後に**出力ファイル名も変える**こと。

---

# BUG-9: ローカル参照宣言が値コピーになる（2026-06-12 修正）

## 症状

```cpp
class P { public: int v; };
int main() { P a; a.v = 7; P &r = a; r.v += 1; return a.v - 8; }  /* AV/ハング */
```

```cpp
int main() { int x; x = 7; int &r = x; r += 1; return x - 8; }    /* EXIT=-1 + 警告 */
```

struct 参照だけでなく**スカラのローカル参照宣言も壊れていた**
（FEAT-6A-ext で動いていたのは参照「引数」と参照「戻り値」のみ）。

## 真因（disasm で確認）

参照のローカル宣言初期化が `init_putv()` の通常代入経路
（`vset` → `vswap` → `vstore`）をそのまま通り、**アドレス格納（バインド）が
どこにも無かった**:

1. **struct**: `vstore()` がソース VT_STRUCT を見て memcpy 経路に入り、
   `a` の中身 4 バイトを参照スロット（8 バイト）へ **movs コピー**。
   `r.v` は「7 + スタックごみ」をポインタとして deref → AV/ハング。
2. **スカラ**: `gen_assign_cast` の int→ptr 暗黙変換（警告つき）で
   `x` の**値**がポインタスロットに入る → deref でごみアクセス。

既存の参照バインド機構（`gen_cast()` L4873 の lvalue→T& バインド）は
引数渡し・return 専用で、宣言初期化には接続されていなかった。

## 修正（tccgen.c、2 箇所）

1. **`init_putv()` ローカル枝**: dest 型が VT_REFERENCE なら、初期化子が
   バインド可能な lvalue（`cpp_can_bind_lvalue_to_reference`）であることを
   確認して `gaddrof()` → ポインタとして格納。store 時は両辺から
   VT_REFERENCE を外す（残すと vstore の「参照経由代入」経路 L5487 に
   入ってしまう）。バインド不能なら `tcc_error`。
2. **identifier push（unary の TOK_IDENT 末尾）**: 参照「変数」は使用時に
   常に referent を表すので `indir()` で自動 deref。
   関数シンボルは VT_REFERENCE が「参照を返す」の意味なので
   `(type.t & VT_BTYPE) != VT_FUNC` で除外。

(2) により `r += 1`（スカラ参照の直接使用）、`incp(r)`（参照変数を参照引数へ
再バインド）、`return r;`（参照戻し）、`&r`（referent のアドレス）が
すべて正しくなる。メンバアクセス時の deref（postfix `.` の L8748）は
型から VT_REFERENCE が消えるため発火しなくなるだけで無害。

## テスト

- `dev/test/a9/bug9_a.cpp` — struct ローカル参照（旧ハング）
- `dev/test/a9/bug9_b.cpp` — スカラローカル参照（旧 EXIT=-1）
- `dev/test/a9/bug9_c.cpp` — 複合（参照変数→参照引数、参照戻り値への
  バインド、参照経由代入、`&ref`、`pick(a) += 3`）

build.bat フルゲート緑（既知のベースライン失敗 a8/sm3・sm6・t_assign のみ）。

## 教訓

- 「参照が動く」は経路ごとに別物 — **引数 / 戻り値 / 宣言初期化 / 直接使用**
  の 4 経路を個別に確認すること。動いていたのは前者 2 つだけだった。
- ハング系は dumpbin /disasm が最速。movs（struct copy）が一目で見えた。

---

# BUG-10: FEAT-4G 初期実装のクラッシュ群（2026-07-18 修正）

## 事象

FEAT-4G（グローバルオブジェクトの自動 ctor/dtor、`.init_array`/`.fini_array`）の
初期実装で、グローバル ctor を含む `.cpp` のコンパイルで tcc.exe 自体が
AV（0xC0000005）でクラッシュ。修正過程で計 8 個の独立バグが見つかった。
`feat4g_basic/args/dtor/multi.cpp` 全 4 本と `CTOR→MAIN→DTOR` 順序検証で解決確認。

## 個別バグと原因（コンパイル時クラッシュ）

### BUG-10a: dynarray_add にスタック上の一時構造体を渡していた（本命）

- **症状**: グローバル ctor 登録ありの TU で thunk 生成時に AV。トレースで
  `ent->is_dtor = 1886404911`（ゴミ）を確認。
- **原因**: `dynarray_add(&cpp_global_dyns, &nb, &ent)` はポインタを**そのまま格納**
  する（libtcc.c L525、構造体コピーはしない）。ローカル変数 `&ent` を渡した
  ためダングリング化。さらに読み出し側が `cpp_global_dyns[i]`（構造体配列）
  として参照し型不一致の二重バグ。
- **修正**: 配列を `CppGlobalDynEntry **` に変更し、各エントリを `tcc_malloc`
  でヒープ確保。`dynarray_reset` の各要素 free と整合。

### BUG-10b: 保存 TokenString に TOK_EOF 終端がない

- **症状**: ctor 引数付き `P g(17);` で引数再生（`begin_macro`）中に AV。
- **原因**: `cpp_save_paren_expr_tokens` が終端を付けずに返していた。他の保存
  箇所（default 引数 L1784、FEAT-4B init_str L11316 等）はすべて
  `tok_str_add(ts, TOK_EOF)` 済み。再生がバッファ外のゴミトークンを読む。
- **修正**: 終端 `TOK_EOF` を追加。`end_macro()` 後の余分な `next()` も削除
  （gen_inline_functions の再生パターンに一致させた）。

### BUG-10c: ctor 引数 TokenString の二重解放

- **症状**: BUG-10b 修正後、リンク時に注入起動コードのコンパイルが
  `<string>:38: 無認識の文字 \x01` で失敗（ヒープ破壊の後遺症）。
- **原因**: `begin_macro(arg_toks, 1)` の alloc=1 は所有権移譲で、
  `end_macro()` が `tok_str_free` まで行う（tccpp.c L1131）。thunk 側の手動
  `tok_str_free(ent->ctor_args)` が二重解放（BUG-1b と同型）。
- **修正**: 手動 free を削除。

### BUG-10d: 注入起動コードの CString が NUL 終端されていない

- **症状**: BUG-10c 修正後も `\x01` エラーがテストによりランダムに発生。
- **原因**: `cstr_cat(&cstr, "...", -1)` の len=-1 は **NUL を含めない**
  （`len = strlen + 1 + len`、tccpp.c L374）。`tcc_compile` 側の
  `strlen(str)` がヒープの後続ゴミまで読み込む。発生の有無は直前の
  ヒープ状態依存だった。
- **修正**: 最終 cat を len=0（strlen+1、NUL 込み）に変更。

## 個別バグと原因（リンク時・実行時）

### BUG-10e: リンク時に `s1->cpp` が 0 に戻っていて起動コードが注入されない

- **症状**: リンク成功するが ctor が実行されない（従来 crt1 経路のまま）。
- **原因**: `tcc_compile()` は `s1->cpp` を保存/復元する（libtcc.c L810/L850）
  ため、リンカ（pe_add_runtime）実行時は常に 0。
- **修正**: `TCCState.cpp_global_ctors` フラグを新設し、
  `cpp_finish_global_dyns` で thunk 発行時に 1 にする。tccpe.c / tccelf.c の
  ゲートをこのフラグに変更。

### BUG-10f: エントリシンボル名のアンダースコア規約不一致

- **症状**: 起動関数 `__tcc_cpp_start` がエントリとして解決されない。
- **原因**: pe_add_runtime は `start_symbol + 1` で先頭 `_` を 1 つ剥がす規約
  （`"__start"` テーブル名 ↔ crt1 実体 `_start`）。注入関数も同じ規約に
  従う必要があった。
- **修正**: 注入 C 関数名を `_tcc_cpp_start` に変更（テーブル名は
  `"__tcc_cpp_start"` のまま）。

### BUG-10g: thunk が gen_inline_functions に上書きされる

- **症状**: 実行時 AV。dumpbin /disasm で `.init_array` の指す位置に thunk が
  無く、ctor 本体と unwind データが重なっていた。
- **原因**: `o()`/`g()` は `ind` を進めるだけで `cur_text_section->data_offset`
  を更新しない（同期は gen_function 終端の仕事）。thunk 発行後に同期しない
  まま `gen_inline_functions` が走り、**同じオフセットから遅延メンバ本体を
  生成して thunk を上書き**していた。
- **修正**: thunk 発行後に `cur_text_section->data_offset = ind;` を追加。

### BUG-10h: thunk が win64 の 32 バイト home 領域を確保していない（実行時 AV 本命）

- **症状**: WER の障害オフセット 0x10CA = run_init ループ内の
  `mov [rbp-8],rax`（スタック書き込みで AV）。
- **原因**: thunk は `push rbp; mov rbp,rsp; call ctor` のみで
  `sub rsp,0x20` が無い。win64 では **callee が register 引数を呼び出し元の
  shadow 領域に home する**（ctor prolog の `mov [rbp+10h],rcx`）ため、
  thunk の保存済み rbp が上書きされ、`leave` で壊れた rbp が呼び出し元
  （run_init）に戻る。
- **修正**: thunk prologue に `sub rsp,0x20`（`o(0x20ec8348)`）を追加。

### BUG-10i: グローバル変数 push の加算値に `obj_sym->c` を渡していた

- **症状**: ctor の this が `&g + 3` になる（disasm で `lea rax,[403007]`、
  実体は 403004）。
- **原因**: グローバル変数 Sym の `c` は **ELF シンボル番号**（セクション
  オフセットではない）。`vset(..., obj_sym->c)` で加算値として二重計上。
  ずれ幅 3 = シンボル番号と一致。
- **修正**: 加算値 0 で `vset` し、`vtop->sym` のリロケーションに任せる
  （ctor / dtor 両経路）。

## 付随修正

- tccpe.c の `.init_array` 探索を `find_section`（無ければ**作成**してしまう）
  から新公開の `have_section`（参照のみ、tccelf.c L335 を ST_FUNC 化）に変更。
  グローバル ctor の無い C++ リンクに空セクションが混入するのを防止。
- `build.bat` の CUnit 実行を `.\con_c_vs_test.exe` に修正（カレント実行の
  `.\` 必須ルール準拠）。

## 実装中の発見（未修正・別タスク）

- **`extern "C" { ... }` ブロックの後に `class` が来るとパースエラー**
  （`型は暗黙 int` → `';' が必要`）。グローバル ctor 無しの最小ケースでも
  再現するため FEAT-4G とは無関係の既存問題。ブロック終了時の `lex_c--` で
  C++ キーワードが復元されていない疑い。class を先に宣言すれば回避可能。
- **クラス外 dtor 定義 `P::~P() {}` は未対応**（`expected member name after ::`）。
  FEAT-4E はインライン定義のみ。

## 教訓

- `dynarray_add` は**ポインタ登録**専用。構造体を積むならヒープ確保が必須
  （シグネチャだけ見て構造体コピーと誤解しない）。
- TokenString の保存→再生には決まり型がある: **TOK_EOF 終端 + begin_macro
  alloc=1 は所有権移譲（手動 free 禁止）+ end_macro 後に next() しない**。
  既存の gen_inline_functions / default 引数経路を必ず先に読むこと。
- 手書き機械語で関数を作るなら win64 ABI の **shadow space (0x20)** と
  `data_offset` 同期を忘れない。gen_function がやっていることを省略した分
  だけ自前で背負う。
- 実行時 AV は **WER イベントログの障害オフセット + dumpbin /disasm** の
  組み合わせが最速だった（デバッガ無し環境でも特定可能）。

---

# BUG-11: extern "C" 領域の直後の `class` がパースエラー（2026-07-19 修正）

## 事象

`extern "C" { ... }` ブロックまたは `extern "C" <単一宣言>;` の**直後**に
`class` 宣言を書くと、`型は暗黙 int になります` →
`';' が必要です（"P" が見つかりました）` でコンパイルエラーになる。

```cpp
extern "C" { int puts(const char *s); }

class P {   // ← ここでエラー
    ...
};
```

C ヘッダ相当の宣言を先頭に置く頻出パターンが書けなかった。
BUG-10（FEAT-4G）作業中の実行順検証テスト作成時に発見。

## 原因

**extern "C" 領域終端の 1 トークン先読みが `lex_c` 有効のまま字句解析される。**

- ブロック形式: 閉じ `}` を消費する `next()`（decl() 内）が `lex_c--` の
  **前**にあり、`}` の次のトークンを C モードで読む。
- 単一宣言形式: `decl(l)`（decl_once_flag=1）が `;` 消費時に次トークンを
  先読みした時点ではまだ `lex_c == 1`。

いずれも次トークンが C++ キーワードだった場合、A-2 の降格機構
（`is_cpp_only_keyword` → `alt_ident_tok` の降格双子、tccpp.c L2794）で
**識別子化された降格双子トークン**が `tok` に残り、`lex_c` を戻しても
再解釈されない。`class` が普通の識別子として構文解析に入り上記エラーになる。

## 修正（tccgen.c）

新ヘルパ `cpp_repromote_stale_lookahead()`:

- `tok` が識別子（`>= TOK_IDENT`）なら `get_tok_str` で名前を取り
  `tok_alloc(name, len)` で引き直す。**降格双子はハッシュチェーンに
  連結されない**（`tok_alloc_demote` の設計）ため、名前検索は必ず
  **元のキーワードトークン**を返す。通常の識別子は自分自身に解決され無害。
- extern "C" のブロック形式・単一宣言形式の両方で、`lex_c--` 直後に呼ぶ。

あわせてブロック開始側も対称化: `{` を消費する `next()` の**前**に
`lex_c++` するよう移動（ブロック先頭トークンも C モードで読む）。

## テスト

- `dev/test/a9/bug11_externc_class.cpp` — ブロック形式・単一宣言形式の
  両方の直後に class、msvcrt 実関数（abs/atoi）呼び出し込みで exit 0
- 回帰: `dev/test/a4/extern_c_block.cpp` compile OK、feat4g 5 本 exit 0、
  発見時の再現形（extern "C" 単一宣言 → class → グローバル ctor/dtor）で
  `CTOR→MAIN→DTOR` 出力順を確認

## 教訓

- lex モードフラグの切り替えは**先読みトークンと同期しない**。トークンを
  消費する `next()` とフラグ増減の順序は「これから読むトークンはどちらの
  モードか」で決めること。
- 降格双子がチェーン非連結である設計（A-2 v2.4）が、名前引き直しによる
  再昇格をそのまま可能にした。設計メモの「なぜ」が後のバグ修正の道具になる。

---

# BUG-17: polymorphic 基底を持つ派生クラスに vptr が二重挿入される（2026-08-08 修正）

## 事象

`class D : public B` で **B が polymorphic**（仮想関数あり）の場合、D にも
独自の vptr が無条件で挿入され、B subobject が 8 バイト後ろへずれていた
（`sizeof(D)` が標準レイアウト + 8）。仮想ディスパッチは偶然動くが、
**基底ポインタ経由のデータアクセスと非仮想基底メソッドが壊れる**。

```cpp
class B { public: int x; virtual int f(); int getx() { return x; } };
class D : public B { public: int y; virtual int f(); };
D o; o.x = 10;
B *pb = (B *)&o;
pb->x;      // ゴミ（B レイアウトの x 位置 = D 内では B subobject の内側 vptr）
pb->getx(); // 同上
```

## 真因

`cpp_insert_vptr_field` が「クラスが polymorphic なら挿入」だけで、
**先頭基底が既に vptr を持つ場合の共有**（標準的な primary-base sharing）を
していなかった。旧レイアウトは `[D の vptr][B subobject（内側に未初期化の
B vptr）]...` となり、(Base*) キャストは offset 0 維持（MI Phase 1 の仮想
ガード）なので、B* 経由のメンバオフセットが全部 8 バイトずれる。
仮想呼び出しだけは「D の vptr がスロット番号互換の D vtable を指す」ため
偶然正しく動き、既存テストの基底がデータメンバを持たなかったため潜伏。

## 修正

`cpp_insert_vptr_field` で**先頭（primary）基底が polymorphic なら挿入を
スキップ**し、派生は基底の vptr スロット（offset 0）を共有する（そこには
派生自身の vtable が書かれる）。これで基底 subobject は offset 0 に戻り、
アップキャスト・データアクセス・非仮想メソッドの全経路が整合する。
仮想 MI Phase 2（複数 vptr + thunk）の前提となるレイアウト修正。

## テスト

- `dev/test/a9/vmi_si_poly_data.cpp` — 単一継承 + polymorphic 基底 +
  データメンバ。`sizeof(D) == 24`（vptr 1 個）の compile-time assert と
  `pb->x` / `pb->getx()` / 仮想ディスパッチ。修正前は compile 段階で
  assert 失敗（sizeof=32）を実測。

## 教訓

- 「仮想ディスパッチが通る」ことはレイアウトが正しいことを意味しない。
  スロット番号互換の vtable を指していれば呼び出しは偶然成立する。
- レイアウト系の機能テストには **compile-time の sizeof assert**
  （負の配列サイズ）を混ぜると、実行前に破綻を検出できる。

---

# BUG-18: `ptr->virtual()` で cpp_member_this のレジスタが vtable ロードに破壊される（2026-08-08 修正）

## 事象

ポインタ経由の仮想呼び出し `p->vf()` で、メンバを読む仮想関数が
**ゴミの値を返す**（`this` がおかしい）。オブジェクト lvalue 経由の
`o.vf()` は正常。定数を返す仮想関数では観測されないため長く潜伏
（feat5a_ptr 等の既存テストはすべて定数返し）。

## 真因

`cpp_prepare_virtual_member_call` が `this`（オブジェクトアドレス）を
**vstack 外の `cpp_member_this` に先に退避**してから vtable ロード列
（indir → +slot → indir）を codegen していた。`p->vf()` 経路では `this` が
**レジスタ保持の SValue** になるが、vstack から消えた時点でレジスタ
アロケータの保護対象外となり、vtable ロード列が同じレジスタを再割り当て
して破壊。後で注入される `this` 引数がゴミになる。
`o.vf()` は `this` が VT_LOCAL ベース（再計算可能な lea 形）のため無事。

## 修正

順序を反転: `vdup()` でアドレスの複製を **vstack 上に残したまま**
vtable ロード列を流し（vstack 上の参照はアロケータが保護する）、
ロード完了後に `vswap()` → `cpp_member_this = *vtop` → `vpop()` で
**最後に**捕獲する。注入時点では `this` が再び vstack に載るため、
関数ポインタの gv でも保護される。

## テスト

- `dev/test/a9/vmi_no_override.cpp` ほか vmi_* 全般（`pb->fb()` が
  メンバを読む）。診断（zz_vmi_diag）で「vtable エントリ・vptr は正しいのに
  `pb->fb()` だけ 200+ゴミ」を実測して特定した。

## 教訓

- `cpp_member_this` に SValue を退避してから追加の codegen を挟むのは
  **レジスタ保持値では不正**。vstack に載っていない SValue のレジスタは
  いつでも再割り当てされうる。
- 仮想関数のテストは**定数返しだけでは不十分**。`this` 経由のメンバ読み
  書きを必ず含めること（BUG-15/16 の「8 バイト超を必ず入れる」と同種）。

---

# BUG-19: 派生メソッド本体からの継承メンバ参照が基底オフセットを失う（2026-08-08 修正）

## 事象

派生クラスのメソッド本体で**継承メンバを暗黙参照**（`b` のように this 無し）
すると、基底 subobject が非ゼロオフセットの場合（MI の第 2 基底、vptr 挿入で
ずれた基底）に**別メンバの値を読む**。

```cpp
class A { public: int a; virtual int fa(); };
class B { public: int b; virtual int fb(); };
class D : public A, public B {
public:
    int d;
    virtual int fb() { return b + d; }  // b が a の位置（this+8）から読まれた
};
```

## 真因

`cpp_push_member_var` が `cumofs = field->c` と**宣言クラス内オフセット**を
そのまま使っていた。`cpp_lookup_member_field` は `find_field` で累積
オフセットを計算しているのに**捨てて**フィールド Sym だけ返す。基底が
offset 0 の場合（従来の単一継承・MI 第 1 基底）は両者が一致するため潜伏。

## 修正

新設 `cpp_field_cumofs_in_class`（フィールド**同一性**で照合する再帰walk —
名前照合ではないのでオーバーロード/シャドウイングの誤選択がない）で
`this` のクラスから見た累積オフセットを求め、`cpp_push_member_var` の
`cumofs` を置き換える。到達不能時は従来の `field->c` にフォールバック。

## テスト

- `dev/test/a9/vmi_basic.cpp` / `vmi_ctor.cpp` / `vmi_second_write.cpp` 等
  （派生 override 本体が第 2 基底のメンバを読む）。診断（zz_vmi_diag3）で
  「thunk 経由も直接呼び出しも同じ誤値 904」→ 呼び出し機構ではなく
  本体のメンバ解決が真因、と切り分けた。

## 教訓

- 累積オフセットを計算している既存ヘルパ（find_field）の結果を**捨てて
  いる**箇所は同型バグの温床。戻り値だけでなく out-param も追うこと。
- 「thunk が悪い」と見えた failing 群の共通項を疑い、**thunk を通らない
  直接呼び出しで同じ誤値**を出して仮説を棄却できた。診断は段階的に。

---

# BUG-16: 8 バイト超の基底 subobject への基底 ctor 呼び出しが値コピーになる（2026-07-31 修正）

## 事象

基底クラスが 8 バイトを超えると、派生 ctor の明示 mem-init `: A(x)` で
呼ばれた基底 ctor の書き込みが**基底 subobject に反映されない**。

```cpp
class A { public: int a, b, c, d; A(int x){ a=x; b=x+1; c=x+2; d=x+3; } };  // 16 バイト
class D : public A { public: int e; D(int x) : A(x) { e = x*10; } };
D o(1);   // o.a〜o.d は未初期化のゴミ（A(1) はコピーを書き換えて捨てた）
```

## 真因

**BUG-15 と同一の根**。`cpp_emit_base_ctor_call` は基底 subobject のアドレスを
`cpp_push_member_var` + `gaddrof()` で作るが、`gaddrof()` は**型を struct のまま
残す**（アドレス値になっても `VT_STRUCT`）。そのため `gfunc_call` が by-value
構造体引数として扱い、Win64 ABI で 8 バイト超の構造体をテンポラリへ memcpy して
その address を渡す。基底 ctor はコピーを初期化し、実体は未初期化のまま。

BUG-15 の修正はメンバ関数呼び出しの 4 経路だけを直しており、**基底 ctor 経路は
取り残されていた**。既存 `mi_ctor.cpp` の基底が 4/8 バイトのため潜伏。

## 影響範囲（実測で確認）

BUG-15 の修正はメンバ関数呼び出しの 4 経路だけを直しており、`gaddrof()` で
`this` を作る**残り 4 経路がすべて取り残されていた**。パターン単位で洗い直し、
それぞれ再現テストで修正前 `[RUN FAIL]` を実測してから修正した。

| 経路 | 症状 | 再現テスト |
|------|------|-----------|
| `cpp_emit_base_ctor_call`（明示 mem-init `: A(x)`） | 基底の初期化が全部消える | `mi_large_base_ctor.cpp` |
| `cpp_emit_global_ctor_call`（FEAT-4G グローバル ctor） | グローバルオブジェクトの初期化が全部消える | `bug16_global_large_ctor.cpp` |
| `cpp_emit_local_dtor`（FEAT-4E ローカル自動 dtor） | `this` がコピーのアドレス。`~Node(){ unlink(this); }` 等が壊れる | `bug16_dtor_this_identity.cpp` |
| `cpp_emit_global_dtor_call`（FEAT-4G グローバル dtor） | 同上。**専用テストなし**（main 復帰後に走るため観測が困難）。同一パターンとして同時修正 | — |

## 修正

`gaddrof()` の直後に `mk_pointer(&vtop->type)` を追加して `this` を
ポインタ値にする（上記 4 経路）。新設した暗黙基底 ctor / 自動基底 dtor の
発行経路にも同じ `mk_pointer` を入れてある。

なお 8 バイト以下のオブジェクトが従来から動いていたのは偶然で、Win64 の
by-value 経路がレジスタに載る場合は `gaddrof()` が作った**アドレス値そのもの**を
渡していたため。8 バイト超だけが「コピーを staging してそのアドレスを渡す」
分岐に落ち、実体と切り離されていた。

## 教訓

- BUG-15 の修正時に「`gaddrof()` の後に `this` を作っている箇所」を横断的に
  洗い出していれば同時に潰せた。**同型バグは経路単位ではなくパターン単位で
  grep する**こと（`gaddrof` + `gfunc_call` の組み合わせ）。
- 8 バイト以下のテストしか無い機能は、この種のバグを構造的に見逃す。
  新機能のテストには**必ず 8 バイト超のケースを 1 本入れる**。
- 「動いているから正しい」は危険。≤8 バイトが通っていたのは ABI 分岐の偶然で、
  設計としては最初から誤りだった。

---

# BUG-15: 8 バイト超オブジェクトのメンバ関数書き込みが失われる（2026-07-30 修正）

## 事象

メンバ関数が `this->member` に**書き込む**と、オブジェクトが 8 バイトを
超える（int 3 個 = 12 バイト、double 2 個 = 16 バイト等）場合に**書き込みが
呼び出し側に反映されない**。読み取りは正常。多重継承（MI）のメソッドテスト
中に発覚したが、**MI 非依存の既存バグ**（ext6/master ベースラインで再現）。

```cpp
class D { public: int a, b, d; void setA(int x){ a = x; } };
D o; o.a = 0; o.setA(11); // o.a は 11 にならず 0 のまま（>8 バイトのため）
```

既存テストは ≤8 バイト（1〜2 メンバ）のオブジェクトしか使っておらず、
long く潜伏していた。

## 原因

`o.method()` の `this` は `cpp_prepare_member_func_call`（および仮想・PMF・
dtor の 3 経路）で `gaddrof()` によりオブジェクトのアドレスとして得るが、
**その値の型が struct T のまま**（`mk_pointer` していない）だった。`this` は
vstack に arg0 として挿入され `gfunc_call` が渡すが、**型が struct なので
by-value 構造体引数として扱われる**。Win64 ABI では 8 バイト超の構造体
by-value は**隠しポインタ経由でコピーを渡す**（`gfunc_call` がオブジェクトを
一時領域に memcpy し、そのコピーのアドレスを渡す）。結果、メソッドは
**コピーを変更**し、呼び出し側の実体は不変になる。8 バイト以下は
レジスタ渡しで（偶然）アドレスが正しく渡り、動いていた。

切り分け:
- 非メンバ関数 `void f(D* p, int x){ p->a = x; }` は 12 バイトでも正常
  （明示 `D*` はポインタ渡し）。C++ の `this` 機構固有。
- メソッド内で `&a` を返して呼び出し側の `&o.a` と比較すると **8 バイト超で
  不一致**（this がコピーを指す）。

## 修正（tccgen.c、4 箇所）

`cpp_member_this = *vtop;` の直前（`gaddrof()` 後）に `mk_pointer(&vtop->type)`
を追加し、`this` を **T\* ポインタ値**にする。これで `gfunc_call` は 8 バイトの
ポインタ引数として渡す。対象 4 経路:
`cpp_prepare_member_func_call`（通常メソッド・演算子）、
`cpp_prepare_virtual_member_call`（仮想）、`cpp_emit_mptr_pmf_invoke`（PMF）、
postfix `.` の明示 dtor 呼び出し。

## テスト

- `dev/test/a9/bug15_large_this.cpp` — 12 バイト / 16 バイトクラスの setter・
  多メンバ書き込み・getter
- `dev/test/a9/bug15_large_virtual_pmf.cpp` — 仮想メソッド書き込み・PMF 書き込み

## 回帰

≤8 バイトのオブジェクト（既存 feat6a/6b 等）exit 0、feat5a/5b（仮想・PMF）
exit 0、build.bat ゲート真の exit 0。

## 教訓

- vstack に「アドレスだが型は struct」の値を置くと `gfunc_call` が by-value
  構造体として扱う。アドレスを渡したいなら型もポインタにする（`mk_pointer`）。
- **既存テストが ≤8 バイトのオブジェクトに偏っていた**ため、Win64 の struct
  by-value 境界（8 バイト）を跨ぐ書き込みバグを見逃していた。12e 実行ゲートに
  8 バイト超オブジェクトのテストを含めることで今後は捕捉できる（本修正で追加）。

---

# BUG-14: 同名メンバ関数のマングル衝突（クラス名欠落）（2026-07-25 修正）

## 事象

- 無関係な 2 クラスが同名・同シグネチャのメソッドを持つと、その呼び出しで
  tcc がクラッシュ（例: `class A{int f();}; class B{int f();};` で
  `a.f(); b.f();`）。
- 派生クラスが基底の仮想関数を**同名で override** すると、基底クラスの
  実体オブジェクトの仮想呼び出しが**派生の override に誤ディスパッチ**する
  （`Base b; b.who();` が `Derived::who` を呼ぶ）。通常呼び出し・仮想 PMF の
  両方で発生。FEAT-5C（仮想 PMF）の検証中に発覚。

## 原因

`cpp_build_func_mangle` が生成するリンク名 `__tcc_<name>_<args>` に
**クラス名が含まれない**。よって `Base::who` と `Derived::who`、あるいは
`A::f` と `B::f` が同一 asm_label になる。さらに `external_sym` は
`is_compatible_types`（クラスを見ない）が真だと別クラスのメンバ関数でも
**同一 Sym に merge**（`patch_storage`）してしまう。結果、片方の本体・
vtable relocation 先が他方に上書きされる（非仮想=クラッシュ、仮想=誤スロット）。
ctor/dtor が無事なのは既に `__cpp_ctor_<C>` / `__cpp_dtor_<C>` という
クラス修飾トークンで登録されているため。

## 修正方針の選択（Approach A を破棄 → B を採用）

- **Approach A（不採用）**: 通常メンバも `__cpp_m_<Class>_<member>` という
  クラス修飾**トークン**で登録・解決。しかし `sym->v == メンバ名トークン`
  を前提とする箇所（静的メンバ解決 `cpp_lookup_static_member`、デフォルト
  引数継承 `cpp_inherit_decl_defaults`、const 解決）が軒並み壊れ、
  whack-a-mole になったため破棄。
- **Approach B（採用）**: グローバル Sym の**トークンは bare のまま維持**
  （既存の field 照合・静的・const・デフォルト引数が無変更で動く）。代わりに
  1. `external_sym` が、登録中クラス（新設グローバル `cpp_pending_member_class`）
     と既存 Sym の `parent_class` が異なるメンバ関数を**別 Sym にフォーク**
     （overload の非互換フォークと同じ経路に条件を追加）。
  2. `cpp_build_func_mangle` に `Sym *cls` を渡し、メンバ関数の asm_label を
     `__tcc_<Class>__<name>_<args>` と**クラス修飾**（自由関数は NULL で従来どおり）。
  呼び出し・vtable・静的の各 lookup は元々 `parent_class` で照合しているため
  追加変更不要。`cpp_pending_member_class` はインライン登録
  （`cpp_finish_member_inlines`）とクラス外定義の 2 箇所でのみ set/reset する。

## テスト

- `dev/test/a9/bug14_same_method_name.cpp` — 無関係 2 クラスの同名メソッド
  （インライン + クラス外の両方、旧クラッシュ）
- `dev/test/a9/bug14_virtual_base_object.cpp` — Base 実体が Base::who、Derived
  実体が Derived::who、Base* 経由の多態も Derived を呼ぶ（旧誤ディスパッチ）

## 回帰

build.bat ゲート緑（**12e 実行ゲートで a9 全 78 本の実行 exit 0 を機械検証**、
Approach A の回帰はこのゲートが捕捉した）。feat5a/5b/6a/6b、静的メンバ、
const、デフォルト引数 実行 exit 0。

## 教訓

- C++ のメンバ関数はクラスも含めて識別すべき。名前＋シグネチャだけの
  マングル/merge はクラス跨ぎで衝突する。ctor/dtor だけ修飾していたのは
  不完全だった。
- 「トークンを変える」対「Sym 同一性＋リンク名を変える」は影響範囲が桁違い。
  `sym->v` を広く前提にしているコードでは、トークン不変の Approach B が安全。
- 実行テストゲート（12e）が Approach A の隠れ回帰を機械的に捕捉した。
  compile-only では見逃していた。

---

# BUG-13: 無名引数を持つメンバ関数の呼び出しで tcc がクラッシュ（2026-07-25 修正）

## 事象

無名引数を持つメンバ関数（例: 後置演算子の定石 `int operator++(int)`、
あるいは単に `int m(int) { ... }`）を**定義して呼び出す**と、`tcc.exe`
自身が AV（0xC0000005）でクラッシュする。定義のみ（呼び出さない）なら
通る。自由関数 `int f(int) {}` は定義時にパースエラーで拒否されるため
再現しない（メンバ限定の現象）。

FEAT-6A-ext4（後置 `a++`）の実装中に、後置演算子の標準シグネチャ
`operator++(int)` が無名ダミー引数を使うことで発覚。切り分けの結果、
`operator++(int x)`（named）や明示・暗黙呼び出しは正常で、**無名引数
そのもの**が真因と判明（ext4 のフックとは無関係）。

## 原因

`post_type()` は無名引数を `v == SYM_FIELD`（名前トークン無し）で積む。
関数**定義**の宣言子検査（tccgen.c 12456 付近の `expect("identifier")`）が
無名引数を弾くが、これは自由関数の定義パスでしか走らない。メンバ本体は
`skip_or_save_block` で保存され、後から `gen_function` → `gfunc_prolog`
経由で emit されるため、この検査を通らない。

`gfunc_prolog` は各引数を `sym_push(sym->v & ~SYM_FIELD, ...)` で積む。
無名引数は `v & ~SYM_FIELD == 0` なので `sym_push(0, ...)` となり、
`sym_push` 内の名前記録分岐

```c
if (!(v & SYM_FIELD) && (v & ~SYM_STRUCT) < SYM_FIRST_ANOM) {
    ts = table_ident[(v & ~SYM_STRUCT) - TOK_IDENT];   // v=0 → table_ident[-256]
```

が `table_ident[0 - TOK_IDENT]`（負インデックス）を触って AV する。

## 修正（tccgen.c）

メンバ本体登録 `cpp_finish_member_inlines()` に `cpp_name_unnamed_params()`
を追加し、**登録前に無名引数へ新規の匿名トークン id（`anon_sym++`）を
合成付与**する。`anon_sym` は `SYM_FIRST_ANOM`（0x10000000）以上なので、
`gfunc_prolog` の `sym_push` は上記の名前記録分岐（`< SYM_FIRST_ANOM`）に
入らず table_ident を触らない。引数の型は無変更なので、オーバーロード
解決・マングルには影響しない（参照不可能な引数名だけが変わる）。

`sym_push` 本体へ下限ガードを足す案（core 改変）ではなく、C++ メンバ
登録側の局所修正を選択（自由関数の従来動作は不変のまま）。

## スコープ / 制限

- 対象は**クラス内インライン定義**のメンバ。クラス外定義
  `int C::m(int) {}` は通常の定義パス（12456）に乗るため無名引数は
  従来どおりエラー（クラッシュではない）。後置演算子はインライン定義が
  一般的なので実用上の問題は小さい。

## 追補: BUG-13-P2 — 自由関数の無名引数を C++ で許容（2026-07-25）

BUG-13 はメンバ限定の修正だったため、自由関数の無名引数
（例: 非メンバ後置 `operator++(T&, int)` の定石）は定義パス 12456 の
`expect("identifier")` で従来どおり拒否されていた（クラッシュではない）。
C++ は関数定義でも無名引数を許容する規格なので、12456 の検査を

```c
if (!(sym->v & ~SYM_FIELD)) {
    if (tcc_state->cpp)
        sym->v = (anon_sym++) | SYM_FIELD;   // C++: 無名引数へ匿名 id
    else
        expect("identifier");                // C: K&R 規則で従来どおり拒否
}
```

に変更。BUG-13 の `cpp_name_unnamed_params` と同型の匿名 id 付与で、
`gfunc_prolog` の `sym_push` が table_ident を触らない経路に入る。
`.c` は K&R 規則を維持（回帰は CUnit / run_all の `.c` 群で担保）。
テスト: `dev/test/a9/bug13_p2_free_unnamed.cpp`（第一引数のみ無名 /
両方無名）、`feat6a_ext4_free.cpp` を無名ダミーの標準形へ更新。

## テスト

- `dev/test/a9/bug13_unnamed_member.cpp` — `int add10(int)` 明示呼び出し
- `dev/test/a9/bug13_unnamed_operator.cpp` — `operator++(int)` /
  `operator--(int)` を `a.operator++(0)` で明示呼び出し（暗黙 `a++` は
  FEAT-6A-ext4）

## 教訓

- 自由関数が定義時に弾く不正入力（無名引数）を、メンバ本体は別経路で
  受理してしまう。「自由関数で拒否される＝メンバでも安全」とは限らない。
  遅延 emit（inline_func_str → gen_function）は宣言子検査を迂回する。
- `sym_push` の名前記録分岐は上限（`< SYM_FIRST_ANOM`）だけ見て下限
  （`>= TOK_IDENT`）を見ていない。`v < TOK_IDENT` の sym を渡すと配列外。

---

# BUG-12: 8 バイト超 struct 返却メンバ呼び出しの this/sret 引数順不整合（2026-07-22 修正）

## 事象

`feat6a_big_struct.cpp`（`int a,b,c;` の 12 バイト struct を返すメンバ関数
`Big::make()`）が実行時に異常終了（EXIT=32762）。ロードマップ 12d の調査項目。

## 原因

Win64 x64 ABI で **struct 返却がレジスタに収まらない（`using_regs` が偽 =
8 バイト超または非 2 冪）場合、隠しポインタ引数（sret）が予約する arg0/RCX
は、メンバ関数の `this` より優先される**。`gen_function` は `this` を
`sym->type.ref->next`（型リストの**先頭**）に挿入するだけで、sret 用の
枠は `gfunc_prolog`（x86_64-gen.c）が型リスト走査の**前**に別途確保する
ため、コード生成側（callee）は自動的に `RCX=sret, RDX=this` になる。

一方、呼び出し側（caller）のメンバ呼び出し共通処理は 2 箇所とも
「`this` を常に arg0 に挿入」というロジックで、sret ポインタが既に arg0
として積まれていてもお構いなしにそれを arg1 へ押し出していた。結果、
呼び出し側は `RCX=this, RDX=sret` を送り、callee の期待
（`RCX=sret, RDX=this`）と食い違う。

- `cpp_finish_member_call()`（演算子オーバーロード等の共通経路、L6377）
- postfix `(` 汎用呼び出し内 `cpp_member_this_pending` ブロック（L9380、
  `obj.method(args)` の実呼び出しはこちら。今回のクラッシュの直接原因）

8 バイト以下・2 冪サイズの struct（`Vec2` 等）はレジスタ返却
（`ret_nregs > 0`）になり sret 自体が発生しないため、既存テストでは
再現していなかった（Stage 6 完了時点で未検証のまま残っていた領域）。

## 修正（tccgen.c、2 箇所）

両呼び出し経路とも、`this` の挿入位置を条件分岐:

```c
int has_sret = (s->type.t & VT_BTYPE) == VT_STRUCT && ret_nregs == 0;
int insert_at = has_sret ? 1 : 0;   /* sret あり: this は arg1 へ */
```

- `has_sret` が偽（通常のメンバ呼び出し）: 従来どおり `this` を position 0
  （最前列 = arg0/RCX）へ挿入。挙動変化なし。
- `has_sret` が真: sret ポインタ（既に積まれている `na` 個中の先頭要素）は
  そのまま arg0 に残し、`this` を position 1 に挿入。`memmove` の移動量を
  `na - insert_at` に変更し、以降の要素（ユーザー引数）だけをずらす。

## テスト

- `dev/test/a9/feat6a_big_struct.cpp`（既存、初めて exit 0 化）— 引数なし
  メンバ呼び出し `x.make()`
- `dev/test/a9/bug12_big_struct_args.cpp`（新規）— クラス外定義 + ユーザー
  引数 1 個（`this`/`sret` の後ろに来る R8 が正しくずれることを確認）
- `dev/test/a9/bug12_big_struct_op.cpp`（新規）— メンバ `operator+` が
  12 バイト struct を返す形（`cpp_finish_member_call` 経路を直接検証）

## 回帰

feat6a_vec2*・feat6a_ext2_full・feat6a_ext3_full・feat5a_*・feat5b_*
（vtable / PMF 呼び出しも同じ汎用呼び出し経路を通るため確認）・
feat4b_two_args・ctor_ov_inline 実行 exit 0。build.bat ゲート緑
（赤は既知の a8 sm3/sm6/t_assign のみ）。

## 教訓

- 「レジスタに収まる struct 返却」と「収まらない struct 返却（sret）」は
  ABI 的に**別の呼び出し規約**であり、片方だけ通したテストで「struct 返却
  は動く」と判断してはいけない。サイズ境界（8 バイト / 2 冪）をまたぐ
  テストを必ず用意すること。
- callee 側（`gfunc_prolog`）が暗黙に行っている「隠し引数の挿入順」を、
  caller 側の汎用コードが**知らずに**別の順序を組み立てると典型的にズレる。
  ABI の暗黙ルールは呼び出し側・定義側の両方で明示的に揃える必要がある。

---

# FEAT-4E-P2: クラス外 dtor 定義 `P::~P() {}` 実装記録（2026-07-19）

## 事象（実装前）

`P::~P() { ... }` が `expected member name after ::` でコンパイルエラー。
FEAT-4E はインライン定義のみ対応だった（BUG-10 作業中に発見、ロードマップ 20）。

## 原因

- `parse_cpp_scope_qualifier()` は `::` の次に識別子（`>= TOK_IDENT`）しか
  受けず、単字トークン `~` で即エラー。
- decl() 先頭の `cpp_peek_out_of_class_ctor()`（`Cls :: Cls (` 検出）も
  `~` を挟む形を認識しないため、qualified-class 宣言パス（btype VT_VOID）
  に入らなかった。

## 修正（tccgen.c、2 箇所 + 前方宣言）

1. `parse_cpp_scope_qualifier()` — `::` の直後が `~` なら次の識別子が
   クラス名と一致することを検査し、`*v` を **FEAT-4E の既存マングル名
   `__cpp_dtor_<Cls>`**（`cpp_dtor_name_tok`）に差し替えて返す。
   以降は通常の関数定義パスがそのまま動き、`external_sym` /
   `cpp_set_func_mangle_label` / `parent_class` 設定 / `gen_function` は
   インライン dtor（`cpp_finish_member_inlines`）と同一のシンボルに合流する。
   自動 dtor（FEAT-4E ブロック終端 / FEAT-4G fini thunk）・明示 `q.~Q()` の
   呼び出し側は `cpp_lookup_member_func` → `__cpp_dtor_<Cls>` 検索なので
   無修正でリンクする。
2. `cpp_peek_out_of_class_ctor()` — `Cls :: ~ Cls (` も受理（`tilde`
   フラグ + unget 復元に `~` を追加）。
3. 付随修正: 同関数の `mem != cls` 失敗パスにあった **`unget_tok(mem)` の
   二重復元**（tok が mem のまま mem を push するため mem が 2 回読める
   状態になる潜在バグ）を除去。既存テストでこのパスを通る形は無く挙動
   変化なし（ゲートで確認）。

## 前提と制限

- クラス内に `~P();` 宣言（dtor field 生成）があることが前提。
  **C++ の規格どおり**（暗黙宣言 dtor のクラス外定義は不可）。
  宣言なしで OOC 定義だけ書くと呼ばれない孤立シンボルになる（エラーにはしない）。

## テスト

- `dev/test/a9/feat4e_p2_basic.cpp` — OOC dtor + ブロック終端自動呼び出し（カウンタ検証）
- `dev/test/a9/feat4e_p2_explicit.cpp` — 明示 `q.~Q()` が OOC 本体に束縛
- `dev/test/a9/feat4e_p2_global.cpp` — FEAT-4G グローバルの fini thunk が OOC dtor にリンク
- 回帰: feat4c 2 本・feat4e 3 本・ctor4f_dtor・feat4g_dtor・bug11 exit 0、
  build.bat ゲート緑（赤は既知の a8 sm3/sm6/t_assign のみ）
差分を読み終えました。Stage 1 修正に加え **Stage 2 (マングリング / 参照型 / 修飾名 / オーバーロード) も着手** されており、想定スコープを超えていますので、両方をレビューします。

# 前回指摘事項の対応状況

| #  | 内容                                         | 対応                                                                       |
| :- | :----------------------------------------- | :----------------------------------------------------------------------- |
| 1  | Shift-JIS 文字列化（libtcc.c / tccpp.c の日本語文字列） | ❌ **未対応**。差分上、依然として `"%s: �F���ł��Ȃ��t�@�C���^�C�v�ł�"` などのまま            |
| 2  | `struct_decl` インライン本体スキップのバグ               | ✅ 修正済み（`skip_member_semi` フラグで `skip(';')` をバイパス）                        |
| 3  | `struct` のメンバ関数を禁止                         | ⚠️ コメント追記のみ（Stage 1 制約として明記）                                             |
| 4  | `build.bat` の Debug ビルド                    | ✅ Win32 Debug + x64 Release を errorlevel 連動で両方走らせるよう修正                   |
| 5  | `mixed_link.bat` 未統合                       | ❌ **未対応**。`run_all.bat` は `a2\*.cpp` をループするだけで `mixed_link.bat` は呼ばれていない |
| 6  | `is_cpp_source` / `guess_filetype` 拡張子重複   | ✅ `guess_filetype` から `is_cpp_source(filename)` を呼ぶ形に統一                  |
| 7  | CPPUnit のリポジトリ root 解決の脆さ                  | ✅ `TCC_REPO_ROOT` env 優先 + `dev\tcc.exe` を見つけるまで親方向に登る方式へ改善              |

# Stage 1 残課題

## A. Shift-JIS 文字列化（再掲・最重要）

`libtcc.c` / `tccpp.c` の日本語エラーメッセージが **CP932 バイト列で保存されたまま**です。HEAD は UTF-8 BOM 付きで `E8 AA 8D ...`（UTF-8「認」）。現在は `94 46 8E AF ...`（Shift-JIS「認」）。

CLAUDE.md 規約「既存 TCC ソース：ASCII 維持・日本語コメント不可」に明確に違反します。Stage 2 のコード追加で diff が膨らむ前に **別コミットで英語化** を強く推奨します（後回しにすると、本来 Stage 2 のレビューに集中したい diff にノイズが混入します）。

## B. `int skip_member_semi = 0;` のミッドブロック宣言

[tccgen.c:4718](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L4718):

```
if (!parse_btype(&btype, &ad1, 0)) { ... continue; }
int skip_member_semi = 0;        // ← MSVC C90 ルール違反
while (1) { ... }

```

ブロック先頭への移動を。CLAUDE.md：「新規変数はブロック先頭にまとめて宣言」。

## C. 作業ディレクトリにレビュー用一時ファイルが残っている

`_baseline_libtcc.c` / `_baseline_tccpp.c` / `_current_libtcc.c` / `_current_tccpp.c` / `_head_libtcc.c` / `libtcc.hex.txt` が残置。`.gitignore` にも入っていません。コミット直前に削除を。

# Stage 2 新規実装の問題

## D. **致命的**：`extern "C" void foo();` の単一形が次の宣言も巻き込む

[tccgen.c:8892-8898](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L8892-L8898):

```
if (tok != '{') {
    tcc_state->extern_c++;
    tcc_state->lex_c++;
    decl(l);           // ← decl() は EOF まで「ループ」する
    tcc_state->lex_c--;
    tcc_state->extern_c--;
    continue;
}

```

`decl(l)` は内部で `while(1)` で複数の宣言を消費します。単一形のはずが、後続のすべての宣言が `extern_c=1` のスコープで処理されてしまいます。

[extern\_c\_decl.cpp](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/dev/test/a6/extern_c_decl.cpp) の `extern "C" void foo(); void foo() {}` を例にとると、2 行目の `void foo() {}` も extern\_c 下で処理され、本来 Stage 2 でマングルされるべき関数定義のマングルが抑止されます。

→ 単一宣言だけ消費するインライン版を作るか、`decl()` に「1 個処理したら戻る」モードを追加してください。

## E. **致命的**：マングリングが overload 検出時にしか走らない

[tccgen.c:1447-1457](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L1447-L1457) で、`external_sym` は **既存シンボルと型不一致時のみ** 新規 push + マングルします。1 つ目の宣言はマングルされず素の名前で登録されます。

```
int f(int);     // 'f' で登録（マングル無し）
int f(double);  // '__tcc_f_d' で登録
f(1);           // → 'f' を呼ぶ（OK）
f(1.0);         // → 'f' を暗黙キャストで呼ぶ（バグ：本来は __tcc_f_d）

```

呼び出し側のオーバーロード解決が無いため、 **オーバーロードは外見上コンパイル成功しても実行時にミスマッチ** します。テスト [overload.cpp](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/dev/test/a6/overload.cpp) は `int main() { return 0; }` で `f` を呼ばないので、この問題は隠れます。

加えて [sym\_push の改変](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L827-L835) は **同一シグネチャの再定義もサイレント許容** します：

```
if (s->prev_tok && sym_scope(s->prev_tok) == s->sym_scope) {
    if (!(tcc_state->cpp && (type->t & VT_BTYPE) == VT_FUNC))
        tcc_error("redefinition: '%s'", ...);
}

```

`is_compatible_types(&s->type, type)` のチェックが必要です（互換なら従来どおりエラー）。

→ 設計を見直してください。本筋は「**宣言時に常にマングル**」「呼び出し側で引数型からマングル名を導出」です。今の中途半端な実装は、潜在的なリンク不整合を導入するだけになります。

## F. **致命的**：`mangle_append_type` でバッファオーバーフロー

[tccgen.c:208-211](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L208-L211):

```
case VT_STRUCT:
    if (*pos + 2 < buf_size) {
        const char *sn = get_tok_str(ty.ref->v & ~SYM_STRUCT, NULL);
        *pos += snprintf(buf + *pos, buf_size - *pos, "S%s", sn);
    }

```

* `snprintf` の戻り値は **書き込まれた長さではなく書こうとした長さ**。`*pos` が `buf_size` を超え得ます。
* 次回呼び出し時 `buf_size - *pos` が **負数**。これが `size_t` に格上げされて巨大な値となり、`snprintf` が `buf` 外へ書きます（CWE-787）。

→ クランプを毎回必須に：`int wrote = snprintf(...); if (wrote < 0 || wrote >= buf_size - *pos) { *pos = buf_size - 1; break; } *pos += wrote;`

加えて Itanium ABI のように `<len><name>` 形式にしないと、struct `foo` と struct `Sfoo` を曖昧に表現してしまいます（衝突）。

## G. グローバル `cpp_qualified_class` / `cpp_cur_class` が longjmp で漏れる

[tccgen.c:167-168](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L167-L168) の 2 つのグローバルは、parse 中に `tcc_error()` で `longjmp` した場合に **クリアされません**。次のコンパイル単位に状態がリークします（同じ `TCCState` で連続コンパイルする場合に顕在化）。

→ `tccgen_init()` で 0 クリア、または `setjmp` ブロックの先頭で必ずリセット。

## H. `parse_cpp_scope_qualifier` の戻り後に `unget_tok(':')` 経由でビットフィールドと干渉

[tccgen.c:248-255](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L248-L255):

```
if (!tcc_state->cpp || tok != ':') return 0;
next();
if (tok != ':') {
    unget_tok(':');
    return 0;
}

```

`int x : 3;` のビットフィールド構文（`x` の後で `:` が来る）でこの関数が呼ばれた場合、`:` を一度消費して unget で戻します。動作的には正しい（`unget_tok` は現在の `tok`/`tokc` を退避してから `:` をセット）ですが、`*v` の意味が呼び出し元で破壊されないか、また `parse_cpp_scope_qualifier` が **`tcc_state->cpp`** **のときだけ呼ばれる前提** に違反していないかを再確認してください。`type_decl` の identifier パスから呼ばれるため、C モードでも到達します（最初の `tcc_state->cpp` で抜けますが、呼び出し自体は無条件）。

## I. 参照型 `&` で rvalue ref `&&` がパースを通る

[tccgen.c:5399-5421](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L5399-L5421) は `while (tok == '&')` で複数の `&` を許容します。`int && x;` を「`int & & x;`」とサイレントに受理し、`int*` がもう一段 pointer 化された型が作られます。C++98 に rvalue ref は無いため、`tcc_error("rvalue reference requires C++11")` か、少なくとも明示エラーを推奨。

## J. テスト側の問題

1. **`test_stage2_mangling.c`** がリポジトリ root を `..\\..\\..\\..\\dev\\tcc.exe` の固定相対パスで参照しています。CPPUnit ハーネスで作った頑健な解決（D の env+up-walk）と一貫させてください。
2. テスト名は「mangling」ですが、コンパイル成功だけを確認しており、**マングル名そのものを検証していません**。たとえば `dumpbin /symbols overload.o | findstr __tcc_f_` の grep が含まれるべきです。これが無いと E の致命的バグを検出できません。
3. [overload.cpp](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/dev/test/a6/overload.cpp) が `main` から `f` を呼んでいないため、リンク不整合が隠れます。`return f(1) + (int)f(1.0);` 程度に変更を。
4. [qualified.cpp](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/dev/test/a6/qualified.cpp) は `Point::set` の宣言と外部定義のみ。`Point p; p.set(3);` を呼んで Stage 3 で必要になる呼び出し側を仕込んでおくと、後の繋ぎ込みが楽です（今呼ぶと現状落ちるので、要 Stage 3 まで保留）。

## K. `vs_test_main.c` の `extern CU_SuiteInfo stage2_suites[];` 前置

[vs\_test\_main.c:1-3](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/test/vs_test/vs_test_main.c#L1-L3):

```
#include "test_common.h"
extern CU_SuiteInfo stage2_suites[];
#include <Basic.h>

```

ヘッダ間に `extern` 宣言を挟むのは可読性が悪く、後続テストが増えるたびに散らかります。`test_suites.h` のような集約ヘッダに移すのが良いでしょう。

# 軽微

* [tcc.vcxproj](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tcc.vcxproj#L77) Debug|Win32 に `TCC_TARGET_PE;TCC_TARGET_X86_64;_CRT_SECURE_NO_WARNINGS;ONE_SOURCE=0` を追加 — Debug|Win32 だけ。Release|x64 は元から正しく定義されていますか? Property Sheet で共通化を検討。
* [cppuniut.cpp](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/test/cppuniut/cppuniut.cpp#L1) 先頭に UTF-8 BOM が付きました（マルチバイト処理目的なら OK）。
* [.gitignore](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/.gitignore#L245-L246) `dev/test/**/*.exe`, `dev/test/**/*.o` 追加は良い改善。
* `dev/tcc.exe` がリポジトリ追跡。CI 配布物として意識的にコミットしているなら良いですが、開発中はビルド差分が毎回出ます。

# 優先対応サマリ

| 優先度          | 項目                                                                 |
| :----------- | :----------------------------------------------------------------- |
| **Critical** | D（extern "C" 単一形の暴走）、E（マングルの不完全実装）、F（snprintf オーバーフロー）             |
| **High**     | A（Shift-JIS）、G（グローバル状態リーク）                                         |
| **Med**      | B（mid-block decl）、I（rvalue ref）、J（マングル名の実テスト不足）、5（mixed\_link.bat） |
| **Low**      | C（temp ファイル）、H、K、軽微項目                                              |

**強い推奨**：Stage 2 は **D と E を直さない限り「コンパイルは通るが動かない」コード** が量産されます。一度 Stage 2 のコミットを巻き戻し、A（文字列）の整理 → Stage 1 のクリーンコミット → Stage 2 を「マングルは常時 ON、overload は呼び出し側まで含めて end-to-end」で再設計、というシーケンスを検討してください。`PLAN.md` の Stage 2 設計表は「リンク可能なシンボルを作る層」と明記しており、現在の実装はその要件を満たしていません。

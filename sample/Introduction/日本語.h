// ===================================================
// ファイルの中身をここに入れてください（ヘッダ）
// ===================================================
#include <stdio.h>

// ===================================================
// 書き換えルール（前書き換え）
// ===================================================

// 動き・型
#define 戻り値はありません      void
#define 整数                    int
#define 文字列として            char*
#define 表示する                printf
#define もどる                  return

// ブロック
#define ここが始まりです        {
#define ここまでです            }
#define こちらです              {

// 構造体
#define 中身は                  struct
#define からっぽ                {0}

// プログラムの始まり
// 「始めます」と書くと、コンピュータはここからプログラムを動かす
#define 始めます                main
#define 引数はありません        (void)

// 先に名前だけ知らせる（前方宣言）
// 作りかたどうしが互いを参照するため、先に名前だけ宣言しておく
#define 先に知らせる(名前)      typedef struct 名前 名前
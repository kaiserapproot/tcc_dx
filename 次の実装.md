# G7 着手: BUG-43/44/45 実行時クラッシュ修正（2026-08-23・一部完了）

**状態**: **一部完了**。ライブラリ 12 TU の -c 通過は既に達成済み（BUG-42
時点）。今回は G7 ドライバ (`sample/cppunit/all_test_tpp.cpp`, N=17) を
新規作成し**実際に実行**したところ、コンパイルは通っても**実行時に
クラッシュ/ハングする既存バグ 3 件**を新規発見・修正した
（いずれも本日 G7 開始前の `dev\tcc.exe` でも再現する既存バグ）。
**作業ブランチ**: `feat/cppunit-build-gate`（ベース: `master` = BUG-42 マージ済み）
**参照**: [問題と原因.md](問題と原因.md) BUG-43 / BUG-44 / BUG-45

- **BUG-43**: クラス外定義の仮想関数（`void Mux::lock() {}` をクラス本体の
  外で定義）が vtable 構築時点（クラス閉じ括弧）ではまだグローバルが存在せず
  スロット NULL のまま発行 → 初回呼び出しでクラッシュ。BUG-30 式の
  extern フォールバックで解決。
- **BUG-44**: `TestResult(Mutex* mutex = 0);` のように**全パラメータに
  デフォルトを持つ 1 引数以上の ctor**が `cpp_class_has_default_ctor`
  の「厳密ゼロ引数」判定から漏れ、`TestResult r;` の**コンストラクタ呼び出し
  自体が丸ごとスキップ**（メンバ未初期化のまま使われクラッシュ）。
  ゼロ引数で呼べるかの判定に変更。副次的に `cpp_inherit_decl_defaults`
  （クラス外定義へのデフォルト値伝播）がコンストラクタで**最初に見つかった
  別オーバーロード**を誤って拾うバグも発覚・修正。
- **BUG-45**: 同クラスの static メンバ関数への**非修飾・前方（定義より前）
  呼び出し**（`assign()` 内から `frobSize()`）が、BUG-22 の this 注入から
  static として除外された後**どの経路にも拾われず** C の「暗黙関数宣言」
  （無型 K&R）に落ち、呼び出し規約の不一致で不正アドレスへジャンプ。
  **診断手法**: 生成 exe に tcc 自身の `tcc_crash_net_veh` と同じ VEH
  技法を **TCC 自身でコンパイルするハーネス**として組み込み（ABI/名前修飾の
  不一致を排除）、クラッシュを 1 回だけ捕捉。`CONTEXT` の RIP/RSP を
  直接読み取り `RIP == RSP`（生アドレスへの飛び先ずれの典型パターン）を確認、
  probe マーカーで `frobSize()` の本体に一度も到達していないことも確認して
  原因を確定。修正は `cpp_lookup_static_member`（BUG-33 の extern 生成）
  経由に統一。
- 副次的に **G-FCAST-N**（`T(a1,...,an)` 多引数の関数形式キャスト temporary
  — `SimpleString.h:118 return SimpleString(*this, pos, n);`）、
  **vtable シンボルの WEAK 化**（クラス定義を見た全 TU が同じ vtable を
  発行するため複数 TU リンク時に多重定義エラーになっていたのを解消）、
  **暗黙メンバ ctor/dtor**（mem-init リストが挙げないクラス型データ
  メンバを黙示的に構築・逆順破棄）も実装。
- **CPPUnit 実測**: ライブラリ 12 TU 全て -c 通過（変わらず）。
  G7 ドライバは **BUG-43/44/45 修正後、17 テスト中 2 件まで進行**
  （test_case_lifecycle / test_case_failure_record が PASS）。
  **test #3（test_assert_equals）の直前、`TestResult` のデストラクタが
  `SimpleList`（m_failures、push_back 済みで要素 1 件）を反復走査する箇所で
  ハング**（クラッシュではなく無限ループの疑い。診断は次回に持ち越し
  — VEH ハーネスと probe マーカーで「`assign()`→`frobSize()` 呼び出しは
  一度も走らない」「pass() 出力後、外側 runBare の 'after runTest' 直前で
  停止」まで特定済み。SimpleList::insert/push_back 自体のアルゴリズムは
  教科書通りの循環双方向リストで正しいはずだが、この実データでの走行は
  本セッション初めて）。

**次の候補**: SimpleList push_back→iterate のハング調査 → G7 ドライバ
N=17 完走 → G7 完了

---

# 局所クラス（BUG-42）— CPPUnit ライブラリ 12/12 TU 達成（2026-08-23 完了）

**完了日**: 2026-08-23（`local_class.cpp` exit 0、build.bat フルゲート
0 failure / 0 crash、Debug|Win32 緑）
**作業ブランチ**: `fix/cpp-testrunner-255`（ベース: `master` = BUG-41 済み）
**参照**: [問題と原因.md](問題と原因.md) BUG-42

- TestRunner.cpp:255 = `SIMPLE_AUTO_PTR` 擬似テンプレートマクロ（関数内
  クラス定義 + 即時宣言子）。真因はインライン本体の TU 終端再生時に
  局所クラスのタグ/フィールドが local_stack で解放済み（ダングリング）。
- `cpp_class_sym_push` を追加し、クラス定義を構成する Sym（タグ・基底匿名
  フィールド・メンバフィールド・vptr）を C++ かつ局所スコープでは
  global_stack へ格納。sym_scope=0 のままにして BUG-37 複製対象から除外。
  **制限（tpp仕様.md 記録）**: タグ名が関数終了後もファイルスコープに残る。
- **CPPUnit 実測**: **ライブラリ 12/12 TU 全て -c 通過**。残は
  all_test.cpp:212（ドライバ、`',' が必要です（"*" がありました）`）のみ
  → G7（ビルドゲート）で対応。

**次の候補**: all_test.cpp:212 の切り分け → G7（cppunit ビルドゲート統合）

---

# 混在戻り値の多重定義 defer + BUG-41（2026-08-23 完了）

**完了日**: 2026-08-23（`overload_mixed_return.cpp` / `nested_member_call.cpp`
exit 0、build.bat フルゲート 0 failure / 0 crash、Debug|Win32 緑）
**作業ブランチ**: `fix/cpp-simplelist-19`（ベース: `master` = BUG-40 済み）
**参照**: [問題と原因.md](問題と原因.md) BUG-41

- **SimpleList.cpp:19** `insert(begin(), n, value);` の停止（関数への引数が
  多すぎます）を解消。真因は defer 条件の struct 戻り値除外: 初期バインドの
  `iterator insert(it,v)` が struct を返すため引数変換が遅延されず、2 引数
  プロトタイプへ即時変換 → 3 個目で過剰エラー。**sret スロット構築を再解決後へ
  移動**（解決済みオーバーロードの戻り型で gfunc_sret → スロット値を
  `vrott(nb_args+1)` で先頭引数位置へ回転）し、struct 戻り値でも defer 可能に。
- **BUG-41 発見・修正**（既存バグ、この形で初露出）: 引数リスト内の入れ子
  メンバ呼び出しが外側の `cpp_member_this_pending` を消費し、外側呼び出しが
  this 無しで発行され全引数が 1 スロット左詰め（callee プリントで
  this == pos を実測、AV）。'(' ハンドラで退避・復元。
- **CPPUnit 実測**: **ライブラリ 11/12 TU**（+SimpleList）。残:
  TestRunner.cpp:255（SimpleAutoPtr `m_ptr`）と all_test.cpp:212（ドライバ、
  G7 対象）。

**次の候補**: TestRunner.cpp:255 → G7

---

# BUG-40 + クラス型の関数形式キャスト（2026-08-23 完了）

**完了日**: 2026-08-23（`extern_c_macro_keyword.cpp` / `func_cast_class.cpp`
exit 0、build.bat フルゲート 0 failure / 0 crash、Debug|Win32 緑）
**作業ブランチ**: `fix/cpp-testcase-88`（ベース: `master` = const mem-init 済み）
**参照**: [問題と原因.md](問題と原因.md) BUG-40

- **BUG-40**: TestCase.cpp:88 の真因は extern "C" 終端処理の順序
  （`next()` → `lex_c--`）。next() が次の `#define` 行を跨いでプリプロセス
  するとマクロ本体が C 字句で貯蔵され、`cu_CATCH_ALL` = `if (false)` の
  `false` が識別子化していた。`lex_c--` を next() の前に移動。
- **クラス型の関数形式キャスト** `T(expr)`: BUG-40 修正後に露出した
  TestCase.cpp:159 `return cu_String(buf);`。G-CAST の明示拒否を解除し、
  ctor を持つクラスの 1 引数形のみ G-CONV の一時構築機構
  （`cpp_try_class_conversion`）へ接続。同クラス引数はそのまま値、
  nocode_wanted 下は型差し替えのみ。ctor なしクラス / `T()` / 多引数は
  従来どおり拒否（`a9/negative/gcast_class_type.cpp` は ctor なしのため
  無変更で有効）。
- **CPPUnit 実測**: **10/12 TU**（+TestCase）。残 2 TU:
  SimpleList.cpp:19（3 引数 insert）、TestRunner.cpp:255（SimpleAutoPtr
  `m_ptr`）。all_test.cpp:212 はドライバ（G7 対象）。

**次の候補**: 残 2 TU → G7

---

# const メンバの mem-init 初期化（2026-08-23 完了）

**完了日**: 2026-08-23（`const_member_init.cpp` exit 0 + negative 1 本、
build.bat フルゲート 0 failure / 0 crash、Debug|Win32 緑）
**作業ブランチ**: `fix/cpp-const-member-init`（ベース: `master` = new 暗黙コピー済み）

- RepeatedTest.h `const int m_timesRepeat;` + `: m_timesRepeat(repeat)`
  （RepeatedTest.cpp:13）。mem-init 展開は `this->a = x` 相当の vstore で、
  const メンバの VT_CONSTANT が read-only 検査に当たっていた。
  **初期化は代入ではない**ので、mem-init のストア 1 箇所だけ修飾を剥がす
  （`cpp_push_member_var` 直後に `vtop->type.t &= ~VT_CONSTANT`）。
- 本体での const メンバ代入は従来どおりエラー
  （`a9/negative/const_member_assign.cpp` で担保）。
- **CPPUnit 実測**: **9/12 TU**（+RepeatedTest）。残 3 TU:
  SimpleList.cpp:19（3 引数 insert）、TestCase.cpp:88（`false` フィールド）、
  TestRunner.cpp:255（SimpleAutoPtr `m_ptr`）。

**次の候補**: 残 3 TU の個別調査 → G7

---

# new の暗黙コピー構築 `new T(obj)`（2026-08-23 完了）

**完了日**: 2026-08-23（`new_implicit_copy.cpp` 実行値検証 exit 0、
build.bat フルゲート 0 failure / 0 crash、Debug|Win32 緑）
**作業ブランチ**: `feat/cpp-testresult-64`（ベース: `master` = 修飾基底呼び出し済み）

- TestResult.cpp:64 `m_failures.push_back(new TestFailure(*failure));` の形。
  TestFailure はユーザー ctor が `(Test*, ...)` のみで、コピー ctor は**暗黙**。
  `cpp_emit_heap_ctor_call` に 2 分岐を追加:
  1. ctor なしクラス: `new T(obj)`（obj が T）なら struct コピー、他は従来エラー。
  2. ctor ありクラス: 解決候補の第 1 パラメータが同クラス引数を受けられない
     （= viable なユーザー ctor がない）とき、malloc 済み領域へ struct コピー。
     ソースは同クラスなので **vptr もコピーで正しい**（仮想クラスで実測）。
- **注意（実装上の学び）**: `cpp_resolve_func_call` は不成立時に NULL でなく
  `sym_find(v)` へフォールバックする仕様。「viable がない」判定は戻り値の
  NULL 検査ではなく解決候補のパラメータ照合で行うこと。
- **CPPUnit 実測**: **8/12 TU**（+TestResult）。残 4 TU:
  SimpleList.cpp:19（3 引数 insert）、TestCase.cpp:88（`false` フィールド）、
  TestRunner.cpp:255（SimpleAutoPtr `m_ptr`）、RepeatedTest.cpp:13（const 代入）。

**次の候補**: 残 4 TU の個別調査 → G7

---

# 修飾基底メンバ呼び出し + BUG-38/39（2026-08-23 完了）

**完了日**: 2026-08-23（`qual_base_call.cpp` / `alias_scoped_decl.cpp` 実行値検証 exit 0、
build.bat フルゲート 0 failure / 0 crash、Debug|Win32 も緑）
**作業ブランチ**: `feat/cpp-qualified-base-call`（ベース: `master` = OOC ネストクラス済み）
**参照**: [問題と原因.md](問題と原因.md) BUG-38 / BUG-39

- **`Base::method(args)`（メンバ本体内の明示・非仮想基底呼び出し）**: TestSetup.cpp:51
  `TestDecorator::run(m_result);` の形。postfix `::` の静的メンバ経路の手前に分岐を追加:
  現在のクラスから到達可能な非静的メンバ関数なら *this を積んで
  `cpp_prepare_member_func_call`（宣言クラス実装への直接バインド + this の基底調整）
  + `cpp_member_this_pending = 1`。指名クラスの**自レベル宣言を優先**して
  フィールドを選ぶ（チェーンは基底が先頭のため）。
- **BUG-38 発見・修正**: `cpp_score_member_overloads` の同点先勝ちで基底の同名が
  派生 override に勝っていた（16 vs 31 の実測で発覚）。名前隠蔽を実装
  （自レベルに宣言があれば基底へ降下しない）。非仮想シャドーイング一般の
  silent miscompile も同時に解消。
- **BUG-39 発見・修正**: `cpp_unget_scoped_expr` が typedef 別名を見ず
  `cu_List::iterator p;` を式に誤送（TestResult.cpp:25）。別名→タグ解決を追加。
- **CPPUnit 実測**: **7/12 TU**（+TestSetup）。TestResult :25 → :64
  （`const TestFailure` → `Test*` 変換 = 次層）へ前進。残 5 TU:
  SimpleList.cpp:19（3 引数 insert）、TestCase.cpp:88（`false` フィールド）、
  TestResult.cpp:64、TestRunner.cpp:255（SimpleAutoPtr `m_ptr`）、
  RepeatedTest.cpp:13（const 代入）。

**次の候補**: 残 5 TU の個別調査 → G7

---

# ネストクラスのクラス外定義 + 多段静的メンバ式（2026-08-23 完了）

**完了日**: 2026-08-23（`ooc_nested_class.cpp` 実行値検証 exit 0、build.bat フルゲート緑）
**作業ブランチ**: `feat/cpp-ooc-nested-class`（ベース: `master` = G-CONV/BUG-37 済み）

- TestRunner.cpp:31 の形 `class TestRunner::Utility { ... };`（前方宣言済み
  ネストクラスのクラス外本体定義）。struct_decl のタグ読み取り直後に
  `Outer::Inner`（多段可）を受理し、外側クラスを `cpp_enclosing_class` に設定
  （G3 スコープ走査・G3-P4 の二重修飾メンバ定義がそのまま効く）。
- postfix の静的メンバ式に**ネスト降下**を追加: `Runner::Utility::add(...)` の
  中間名がネストクラスなら降下して次の `:: member` を読む。
- **CPPUnit 実測**: TestRunner の停止が :31 → :255（SimpleAutoPtr マクロ層）へ前進。

---

# CPPUnit G-CONV: 変換 ctor の暗黙適用 + BUG-37（2026-08-23 完了）

**完了日**: 2026-08-23（`gconv_*.cpp` 2 本 + negative 1 本、build.bat ゲート真の exit 0）
**作業ブランチ**: `feat/cpp-converting-ctor`（ベース: `master` = BUG-36 マージ済み）
**参照**: [cppunit対応プラン.md](cppunit対応プラン.md) G-CONV、[問題と原因.md](問題と原因.md) BUG-37

- **変換フックは 1 箇所の設計**: `gen_assign_cast` と `vstore` の先頭で、宛先が
  クラス T（または T への参照）かつソース非互換のとき、T の viable な 1 引数 ctor
  （残りデフォルト充足可）で**スタック上の一時**を構築し vtop を差し替え。以降は
  既存の struct コピー / 参照束縛がそのまま動く。1 段のみ・深度 4 で打ち切り・
  失敗時はオペランド無傷で従来診断。
- **ctor 解決の同格化**: BUG-30 の宣言側候補集合（`cpp_resolve_member_func_call` /
  `cpp_call_has_overloads`）にマングルトークン `__cpp_ctor_C` ↔ クラス名フィールドの
  対応を追加。ヘッダ宣言のみの ctor が前方参照で解決・extern 化される。
  `cpp_resolve_func_call` にも BUG-32c と同じデフォルト引数 viability を追加。
- **採点バグ修正**: `const Str&` 引数が汎用 ptr/ptr 規則で `const char*` を
  score 1 で受け、`Str(const char*)` より copy-ctor が勝っていた。クラス参照は
  同クラス（10）か派生→基底の束縛可能（5）のみマッチに変更。
- **BUG-37 を発見・修正**（同コミット）: `sym_copy_ref` の STRUCT 降下判定が
  変数 Sym の `sym_scope` を見ており、パラメータ Sym の共用体汚染で
  **ファイルスコープのタグを複製** → 「Iterator を Iterator に変換できません」。
  判定を**タグの scope** に変更（BUG-35 と同族の共用体誤読）。
- **CPPUnit 実測**: **6/12 TU**（+TestFailure / TestRegistry / TestSuite）。残 6 TU:
  SimpleList.cpp:19（3 引数 insert の解決 = 次層）、TestCase.cpp:88 `フィールドが
  みつかりません: false`、TestResult/TestSetup の `static member not found` 新形、
  TestRunner.cpp:31 修飾基底名、RepeatedTest.cpp:13 const 代入。

**次の候補**: 残 6 TU の各停止を個別調査（修飾基底名は小、他は要切り分け）→ G7

---

# BUG-36: ネスト型宣言が外側クラスの匿名メンバになる（2026-08-23 完了）

**完了日**: 2026-08-23（6 変種 + 回帰テスト exit 0、build.bat ゲート真の exit 0）
**作業ブランチ**: `fix/cpp-bug36-nested-type-decl`（ベース: `master` = G6 マージ済み）
**参照**: [問題と原因.md](問題と原因.md) BUG-36

- G6 後に 6 TU を止めていた `m_mutex 重複` の真因。C++ 機能とは無関係で、
  宣言子なしの名前付きタグ宣言（`struct Inner {...};`）が ms-extensions の
  匿名メンバ経路に落ち、**外側クラスのレイアウトを黙って肥大**させつつ
  `check_fields` の再帰で同名メンバを誤検出していた。
- 修正は 1 分岐: C++ モードではメンバを作らず型宣言のみ（無名 `struct {...};` の
  匿名メンバ拡張は不変）。回帰テストで sizeof / 匿名平坦化 / 同名メンバ共存を実行値検証。
- **CPPUnit 実測（修正後）**: `m_mutex` 系は全 TU から消滅、3/12 のまま各 TU が
  より深い位置へ前進。残る停止は:
  1. **変換 ctor 経由の暗黙変換**（`const char*`→SimpleString、`Node*`→Iterator。
     コピー初期化・return 値・引数渡し）— 4 TU（SimpleList / TestCase /
     TestRegistry / TestSuite）。**次の本命**
  2. SimpleString.h:149 `関数への引数が多すぎます`（コピー ctor
     `SimpleString(const SimpleString&, pos=0, n=npos)` へのデフォルト引数付き
     1 引数呼び出し `SimpleString s(lhs)` の解決）— 1 TU（TestFailure）
  3. `static member not found` の新形（TestResult.cpp:25 / TestSetup.cpp:51）— 2 TU
  4. TestRunner.cpp:31 `expected base class name`（**修飾名の基底クラス**
     `class X : public Outer::Inner` — 基底句パーサが struct_find 直呼び）— 1 TU
  5. RepeatedTest.cpp:13 `assignment of read-only location`（const 関連）— 1 TU

**次の候補**: 上記 1（変換 ctor の暗黙適用 — 最大 5 TU を解放しうる）→ 4（修飾基底名、小）→ 3/5 調査

---

# CPPUnit G6: 仮想デストラクタ + complete-object delete（2026-08-22 完了）

**完了日**: 2026-08-22（`g6_*.cpp` 3 本 exit 0、build.bat ゲート真の exit 0 =
`vmi_*` 8 本無変更で通過 = vtable レイアウト invariant 検証込み）
**作業ブランチ**: `feat/cpp-virtual-dtor`（ベース: `master` = クラッシュ防止 C1〜C4 マージ済み）
**参照**: [cppunit対応プラン.md](cppunit対応プラン.md) G6、BUG-24

- **パース**: `virtual ~B()` — dtor 経路は parse_btype をバイパスするため `virtual` を
  先に消費して `ad1.f.func_virtual` に接続（post_type がプロトタイプへコピー）。
- **スロット**: dtor は**位置ベース override**（フィールド名 `__cpp_dtor_fld_<Class>` が
  クラス固有で名前照合が効かない）。primary チェーンを辿る
  `cpp_find_virtual_dtor_in_chain` で基底スロットを継承し、**基底が virtual なら派生
  dtor はキーワード無しでも暗黙 virtual**（C++ の規則どおり）。セカンダリ vtable の
  override 判定にも同じ位置ベース分岐を追加。
- **offset-to-top（プラン採用案どおり）**: vtable の関数スロット列の**前**に
  ポインタ幅 signed で配置（primary=0、secondary=−subobject offset を
  `write64le/write32le` で直書き）。**vtable シンボルはスロット 0 を指したまま**なので
  既存の vptr 書き込み・仮想呼び出し・thunk は 1 バイトも変わらない。
- **delete**: 静的型の dtor が virtual の場合のみ G6 経路 — NULL ガード（vptr 読みより
  必ず先）→ `complete = p + vptr[-1]` を先に計算・退避 → vtable 経由で dtor
  （this 調整は既存 thunk がそのまま効く）→ `free(complete)`。
  非仮想 dtor / POD は G4 直呼び経路を完全維持。明示 `p->~B()` も仮想ディスパッチ化。
- **検証**: 単一継承の順序（state=12）、**MI の非 primary 基底 `delete b`**（`b != d` を
  確認した上で桁送り state=123 = D→~B→~A の順でのみ成立、heap 破壊なし）、
  仮想 dtor の `delete NULL` no-op、非仮想/POD の直呼び回帰、`sizeof` の vptr 加算、
  明示 `p->~B()` の動的ディスパッチ。
- **制限**: 深い入れ子のセカンダリ基底の offset-to-top は中間クラス相対
  （Phase 2 の dispatch と同じ既存制限。CPPUnit は MI 不使用）。

**CPPUnit 実測**: **3/12 TU**（+TestDecorator）。残る 9 TU の停止は G6 で新たに露出した
次層で、主に 3 種:
1. **`m_mutex` 重複メンバ誤検出**（TestResult.h:164）— ネストクラス `AutoMutexLock` と
   外側 `TestResult` が**同名メンバ**を持つと外側の `check_fields` が重複と誤報
   （BUG-36 候補。6 TU がこれ）
2. SimpleString.h:149 `関数への引数が多すぎます`（非メンバ `operator+` 系、2 TU）
3. SimpleList.cpp:19 `Iterator へ変換できません`（`return m_node->next;` —
   Node* → Iterator の**変換 ctor 経由の暗黙変換**未対応）

**次の候補**: BUG-36（check_fields のネスト同名メンバ）→ 2/3 の調査 → G7

---

# CPPUnit G5: 純粋仮想 + 抽象クラス（2026-08-22 完了）

**完了日**: 2026-08-22（`g5_pure_virtual.cpp` exit 0、negative 2 本、build.bat ゲート
真の exit 0、Release|x64 は /t:Rebuild）
**作業ブランチ**: `feat/cpp-pure-virtual`（ベース: `master` = BUG-34 マージ済み）
**参照**: [cppunit対応プラン.md](cppunit対応プラン.md) G5

- `FuncAttr.func_pure` による **metadata 方式**（プラン Blocker 4 の本質）。
  `struct_decl` のメンバ宣言子で `virtual R f() = 0;` を受理する
  （`= 0` の 0 リテラルのみ。C++11 の `= default` / `= delete` は明示エラー）。
- **抽象判定は最終 vtable レイアウトの走査**（`cpp_class_is_abstract`）:
  スロットごとに `cpp_find_virtual_by_slot` で最派生の宣言を引き、まだ pure なら抽象。
  これで継承が自然に効く（`B : A` が override しなければ B も抽象、
  override した `C : B` は具象）。
- **拒否**: 抽象クラスのオブジェクト宣言（`decl_initializer_alloc` の入口 = ローカル /
  グローバル / static が必ず通る唯一の場所）と `new`。ポインタ・参照は可。
- **プランからの逸脱を明記**: 未 override スロットの abort スタブは**発行していない**
  （NULL のまま）。理由と根拠はプラン G5 節に追記済み。
- **実装中に既存バグ [BUG-34] を発見**（祖父母の仮想を孫が override すると別スロット）。
  G5 のプラン必須テスト「継承経由の抽象維持」が踏んだため、先に別ブランチで修正。

**判明した次の障害（実測）**:
- CPPUnit 12 TU のうち 10 TU は `Test.h:32 virtual ~Test() {}` = **BUG-24 / G6** で停止。
- `SimpleList.cpp` は **tcc 自身がスタックオーバーフロー**（0xC00000FD）。
  BUG-34 マージ前・後・G5 の 3 バイナリすべてで同じ終了コードを実測しており、
  **G5 とも BUG-34 とも無関係な既存の無限再帰**。G7 の前に要調査（BUG-35 候補）。

**次の候補**: G6 仮想デストラクタ（10 TU を解放）→ SimpleList.cpp の無限再帰調査 → G7

---

# CPPUnit G4: `new` / `delete`（2026-08-22 完了）

**完了日**: 2026-08-22（`g4_*.cpp` 4 本 + C 回帰 1 本 exit 0、negative 2 本、
build.bat ゲート真の exit 0、Release|x64 は /t:Rebuild）
**作業ブランチ**: `feat/cpp-new-delete`（ベース: `master` = BUG-32 マージ済み）
**参照**: [cppunit対応プラン.md](cppunit対応プラン.md) G4

- **スコープは実測から確定**（プラン G4 の「G0 の形別分類で分岐」規定に従う。
  `sample/cppunit` を grep）: `new Class(args)` / `new Class()` が 13 箇所、
  `new POD[n]` が 4 箇所（すべて `char`）、**scalar POD の `new` は 0 件**。
  よって scalar POD（`new int`）は実装せず **明示エラー**にした
  （default-init と value-init の取り違えを黙って作らないため）。
  ctor/dtor を持つクラスの `new C[n]` / `delete[]` も要素数の保持が必要なので明示エラー。
- 実装（`tcctok.h` に `TOK_NEW` / `TOK_DELETE`、`is_cpp_only_keyword` 登録 →
  `.c` では識別子。トークン番号がずれるため **/t:Rebuild**）:
  - `new`: `malloc(sizeof)` → **一時スロットへ退避**（入れ子 `new` と BUG-23 対策）→
    **vptr 初期化**（ヒープ版 `cpp_init_heap_vptr`。ヒープオブジェクトには宣言地点が
    無いので、ローカル/グローバルが宣言時に受け取る vptr をここで書かないと
    ポインタ経由の仮想呼び出しがゴミを踏む）→ ctor 呼び出し
    （`cpp_emit_base_ctor_call` と同じくパース後にオーバーロード解決 → 引数変換）
  - `delete`: 一時スロットへ退避 → **NULL ガード**（`delete 0` / NULL は no-op）→
    dtor（**非仮想の直呼び**。仮想 dtor / complete-object 補正は G6）→ `free`
  - `malloc` / `free` は `tccgen_init` で作る `void*(...)` / `void(...)` の
    K&R プロトタイプ経由（`func_old_type` の `int` 返却だと x86_64 でポインタが切れる）
- **実装中に既存バグ [BUG-32] を発見**（デフォルト引数再生の 3 件）。別ブランチで先に修正。
- **CPPUnit 実測**: SimpleString.cpp の停止地点が 67 行目 → **同ファイルを完走**し、
  次は `cudebug.h:29` の `TestUtility::trimFileName(...)`（別 TU 定義の**静的メンバ**が
  解決できない = BUG-30 と同型の静的メンバ経路）。

**次の候補**: BUG-33（静的メンバの前方参照 = BUG-30 の静的版）→ G5 純粋仮想 →
G6 仮想 dtor → G7 フルゲート

---

# CPPUnit G-OVL / BUG-30: メンバ関数の前方参照呼び出し（2026-08-22 完了）

**完了日**: 2026-08-22（`govl_*.cpp` 3 本 + クロス TU リンク 1 本 exit 0、negative 1 本、
build.bat ゲート真の exit 0）
**作業ブランチ**: `fix/cpp-bug30-overload-forward`（ベース: `master` = BUG-31 修正マージ済み）
**参照**: [問題と原因.md](問題と原因.md) BUG-30、[cppunit対応プラン.md](cppunit対応プラン.md) G-OVL

- **起票より範囲が広かった**: プラン起票時は「同名オーバーロードの前方参照」だったが、
  切り分けで **オーバーロードが無い単一メンバでも実行時クラッシュ**していたことが判明
  （`ovl12` / `ovl14`）。`.h` 宣言＋`.cpp` 定義という通常構成が丸ごと該当する重大バグ。
- 修正 4 点（詳細は BUG-30 節）:
  1. 未定義メンバへの呼び出しに **extern 参照を生成**（`cpp_make_member_func_extern`。
     `VT_EXTERN` が必須 — 無いと後続の定義が `patch_type` で「再定義」になる）
  2. 再解決の候補集合を**クラス宣言側のメンバチェーン**から作成
     （`cpp_resolve_member_func_call` / `cpp_score_member_overloads`）
  3. `cpp_call_has_overloads` が宣言側も数えるように（引数変換の遅延判定）
  4. 解決後の実体引き当ては**完全シグネチャ一致のみ**（`cpp_member_func_global_exact`）。
     arity フォールバックを使うと別オーバーロード用の extern を掴んで誤呼び出しになる
     （実測で踏んだ）
- **切り分け中に独立バグ [BUG-31] を発見**（静的データメンバのクラス外定義が
  `cpp_qualified_class` を漏らし、次の関数定義に暗黙 `this` が付く）。先に別ブランチで修正。
- **負の対照**: 新規 4 本すべて修正前バイナリで失敗（`govl_same_arity` はアクセス違反、
  他は compile fail / link fail）、修正後 exit 0。
- **CPPUnit 実測**: SimpleString.cpp の停止地点が 38 行目 → **67 行目 `delete`**（= G4 領域）
  へ前進。ヘッダ・ctor・assign 群を通過。

**次の候補**: G4 `new` / `delete`（中）→ G5 純粋仮想 → G6 仮想 dtor → G7 フルゲート

---

# CPPUnit G-CAST: 関数形式キャスト `T(expr)`（2026-08-22 完了）

**完了日**: 2026-08-22（`gcast_*.cpp` 2 本 + `gcast_c_decl.c` exit 0、negative 3 本、build.bat ゲート真の exit 0）
**作業ブランチ**: `feat/cpp-functional-cast`（ベース: `master` = G3 マージ済み）
**参照**: [cppunit対応プラン.md](cppunit対応プラン.md) G-CAST

- `unary()` の先頭（switch の前）に 1 フックのみ。式頭のトークンが**型名**
  （基本型キーワード / typedef / G3 のクラス内 typedef）で、**直後が `(`** の
  ときだけ関数形式キャストとして扱い、既存の `(T)expr` キャスト機構
  （`parse_btype` → `gen_cast`）へ接続する。新しいキャスト実装は書いていない。
- 修飾形 `S::size_type(-1)` も対応（3 トークン先読み）。`S::get()` のような
  **静的メンバ呼び出しとの区別は「`::` の次の名前がそのクラスで型か」**で行うため、
  既存の静的メンバ経路は不変（`g3p3_static_expr` で回帰確認）。
- 明示拒否（silent miscompile を作らない方針）: クラス型 `Foo(1)`（ctor 選択が
  必要 = G4 領域）、`T()` value-init、`T(a, b)`。
- C モード非回帰: フックは `s1->cpp` ゲート。`.c` の `int (x);` は従来どおり
  **変数宣言**（`gcast_c_decl.c` で担保）。
- **CPPUnit 実測**: SimpleString.cpp:33 `size_type(-1)` を突破。次の停止は
  同ファイル **38 行目**（下記の新規露出項目）。

**次の候補**: 下記「新規露出」の G-OVL（メンバ関数の unqualified 呼び出しの
overload 解決）→ G4 `new`/`delete`

---

# CPPUnit G3: クラス内 typedef と修飾型名（2026-08-22 完了）

**完了日**: 2026-08-22（P0〜P5 の 6 コミット、新規テスト 15 本 + negative 5 本、
build.bat ゲート真の exit 0、**最終ゲート = SimpleString.h / SimpleList.h の
include-only TU が原本のまま `-c` 通過**）
**作業ブランチ**: `feat/cpp-class-typedef`（ベース: `master` = G-OP マージ済み）
**参照**: [cppunit対応プラン.md](cppunit対応プラン.md) G3（P0 監査表は同プランに追記済み）

- **P0（監査 → 別チェーン確定）**: member チェーン walker を全列挙し、局所 skip で
  安全にできない consumer（brace 初期化子 3 箇所 / DWARF 3 パス / SysV ABI 分類 /
  find_field 系 / cpp_find_base_field）を根拠に、typedef は `Sym.cpp_class_typedefs`
  独立リストへ。既存 walker は 1 箇所も変更していない。
- **P1**: struct_decl で VT_TYPEDEF 受理（type_size 前 = 不完全型 typedef 合法）。
  `Sym.cpp_enclosing_class` 追加。`cpp_cur_class` を save/restore 化（ネストクラス
  終端で外側を失う既存問題の修正）。ネストクラス名も外側のリストへ登録。
- **P2**: `cpp_lookup_class_type`（自クラス → 直接基底の再帰 merge。「指定する型」
  比較で A::T=int + B::T=int は許容、異型は ambiguity エラー、先勝ち禁止）+
  `cpp_unqualified_class_type_find`（cpp_cur_class / cpp_cur_func_class /
  cpp_qualified_class 起点 → 外側クラスへ）。block ローカルの非型隠蔽は既存経路。
- **P3**: parse_btype の `cls ::` unget 箇所を拡張し修飾型名を消費（多段対応、
  2 段目以降の不在は明示エラーで enclosing へ逃げない）。文頭 `C::T x;` は
  cpp_unget_scoped_expr に型判定を追加して decl 経路へ。静的メンバ式は不変。
- **P4**: `parse_cpp_scope_qualifier` ループ化で `L::It::operator++(int)` 受理。
  中間クラス名は直前の修飾クラスの nested 登録を優先解決。
- **P5**: デフォルト引数を `tokens + 定義クラス`（param->parent_class）で保存し、
  再生時に owning class 復元 + `cpp_default_arg_replay` フラグで
  「呼び出し点ローカル捕捉禁止（**負の対照実測**: 修正前 exit 1）/ owning class の
  static 解決 / this 依存の暗黙メンバ解決オフ」。
- **CPPUnit 実測**: SimpleString.cpp は 33 行目 `size_type(-1)` = **G-CAST 対象**で
  停止（ヘッダ typedef 群は全通過）。次のガイドはプラン順で **G-CAST → G4**。

**次の候補**: G-CAST 関数形式キャスト `T(expr)`（小、G3 依存クリア済み）、
G4 `new` / `delete`（中）

---

# CPPUnit G-OP: 単項 `operator*` 呼び出し + `operator->`（2026-08-22 完了）

**完了日**: 2026-08-22（`gop_*.cpp` 5 本 + `gop_c_deref.c` exit 0、negative 2 本、build.bat ゲート真の exit 0）
**作業ブランチ**: `feat/cpp-op-deref-arrow`（ベース: `master` = G2 マージ済み）
**参照**: [cppunit対応プラン.md](cppunit対応プラン.md) G-OP、[実装済み.md](実装済み.md) §7 表

- 実装は 3 箇所のみ（既存 ext3 単項基盤の流用）:
  1. `cpp_operator_suffix` に `TOK_ARROW` → "arrow"（宣言・クラス外定義・明示
     `a.operator->()` が ext1〜6 と同経路に乗る。`*` の suffix "mul" は既存）。
  2. `unary()` の単項 `*`: `cpp_try_member_unop('*') → cpp_try_free_unop('*')` →
     失敗時のみ従来 `indir()`（両フックとも VT_STRUCT ガード済みでポインタ/C 不変。
     二項 `a*b` との arity 分離も ext3 の want_args 機構で既存動作）。
  3. postfix `->`: 左辺がクラス型なら `cpp_try_member_unop(TOK_ARROW)` で
     `(x.operator->())` を発行してから従来 `indir()` へ —— **1 段のみ**適用、
     非ポインタ返却は `indir()` の "pointer が必要です" で拒否（再帰適用は
     スコープ外 = プランどおり）。`->` は ISO どおり**メンバのみ**（free 形なし）。
- `T& operator*() const` の **lvalue 返却**は ext2 の参照返却機構がそのまま効き、
  読み `(*it).v` / 書き `(*it).v=..` / 丸ごと代入 `*it = c` / `&(*it)` /
  cast オペランド `(P*)*w` すべて実行値検証で exit 0。
  Proxy 形（rev.7 High 2: `operator->` が this と無関係なオブジェクトを返す）も検証。
- **発見（既存ギャップ、G-OP スコープ外として記録）**: 「ポインタへの参照」の
  直接宣言子 `void*& operator*()` / `int*& f()` は G-OP 以前から `identifier が
  必要です` で不可。SimpleList の実物はクラス内 typedef（`reference`）経由で
  この形に到達するため **G3 で再評価**する。
- CPPUnit の各 TU は依然 SimpleString.h:18（クラス内 typedef = G3）で停止
  （G-OP の対象構文は G0 smoke で単体確認済みのもの）。

**次の候補**: G3 クラス内 typedef + 修飾型名（大物・P0 監査 + P1〜P5 の 5 コミット、
`SimpleString.h:18` を突破する本丸）。G3 中に `*&` 宣言子ギャップの扱いも判断

---

# CPPUnit G2: `friend class X;` 限定受理（2026-08-22 完了）

**完了日**: 2026-08-22（`friend_class.cpp` + `a2/friend_ident.c` exit 0、negative `friend_func.cpp`、build.bat ゲート真の exit 0、Release|x64 + Debug|Win32 とも **/t:Rebuild** で全リビルド）
**作業ブランチ**: `feat/cpp-friend-skip`（ベース: `master` = G1 マージ済み）
**参照**: [cppunit対応プラン.md](cppunit対応プラン.md) G2、[tpp仕様.md](tpp仕様.md) §2 / §3.1

- `tcctok.h` に `DEF(TOK_FRIEND, "friend")`（LF 維持のためバイト保持編集）、
  `tccpp.c` の `is_cpp_only_keyword` に登録（C モードでは識別子降格 — A-2 機構）。
- `struct_decl` メンバループで **`friend class Identifier;` の形のみ**読み捨て受理。
  friend 関数宣言（`friend int fn(C&);` 等）は宣言自体が消える silent miscompile に
  なるため `tcc_error`（プラン High 2 対応の限定スコープ）。
- tcctok.h 変更でトークン番号がずれるため両構成とも /t:Rebuild（f14b534 の教訓）。
- CPPUnit 実測: 唯一の使用箇所 TestRunner.cpp:34 `friend class Logger;`。同 TU は
  include 先の SimpleString.h:18（クラス内 typedef = G3 対象）で停止し、G1 時点から
  後退なし（friend 起因の停止は解消）。

**次の候補**: G-OP `operator*`/`operator->`（`wip_g1_g2_gop_full.patch` に着手断片）、
G3 クラス内 typedef（大物・P0 監査 + P1〜P5）

---

# CPPUnit G1: 先頭 `::` グローバルスコープ修飾（2026-08-22 完了）

**完了日**: 2026-08-22（`gscope_*.cpp` 6 本 + `gscope_c_ternary.c` exit 0、negative 1 本、build.bat ゲート真の exit 0、Release|x64 + Debug|Win32 両ビルド緑）
**作業ブランチ**: `fix/cpp-global-scope-op`（ベース: `master` = プラン rev.7 マージ済み）
**参照**: [cppunit対応プラン.md](cppunit対応プラン.md) G1、[tpp仕様.md](tpp仕様.md) §2

- **正式 lookup で実装**（プランのフォールバック「shadow 検出で tcc_error」ではない方）:
  `cpp_global_scope_find()` が `external_sym` と同じ `prev_tok` 遡りでグローバル束縛のみを
  返す。ホイストされたメンバ関数体・クラス static（`parent_class` 付き）もスキップ。
  shadow テスト `int x=1; int main(){int x=2; return ::x==1?0:1;}` は exit 0。
- **型位置**: `parse_btype` に `case ':'` を新設。グローバル側に型（typedef / tag）が
  あるときだけ pending フラグ + `continue` で既存 default ケースを再利用（型構築ロジックの
  複製なし）。型が無ければ `::` を unget して式パーサへ委譲（`::x = 1;` 文を壊さない）。
- **式位置**: `unary()` に `case ':'` を新設し `tok_identifier` へ合流。フラグ有効時は
  BUG-21/BUG-22 の暗黙メンバ解決をバイパス（`::name` がメンバへ再束縛されない）。
  未宣言の `::name` は暗黙関数宣言に落とさず明示エラー。
- **既存バグ 3 件を露出・修正**（`::` が新たに出現可能になったため）:
  1. `cpp_unget_scoped_expr` がキーワード `return` を cls_tok として受理し
     `return ::gfn()` を丸ごと gexpr に流していた → cls_tok を `TOK_UIDENT` 以上に限定。
  2. postfix の静的メンバ検出（`X::m`）が `a ? v : ::b` のコロン 3 連続を `::` と誤認 →
     3 個目の `:` をピークして「三項の `:` + `::`」に分割。
  3. `expr_cond` の GNU 拡張 `a ?: b` 判定が `a ? ::b : c` を省略形と誤認 →
     C++ TU のみ 2 個目の `:` をピークして判別（C の `?:` は不変）。
- **効果（実測）**: CPPUnit の最初の停止地点 cuconfig.h:178（`typedef ::SimpleString
  cu_String;`）を突破。次の停止は SimpleString.h:18 クラス内 typedef（= G3 対象、
  プラン §0 の想定どおり）。
- スコープ外（プランどおり）: 宣言子位置の `::C::m`（CPPUnit で不使用を grep 確認）、
  `::` 付きの overload 解決の特殊ケース。

**次の候補**: G2 `friend class` 限定受理（極小）、G-OP `operator*`/`operator->`（WIP パッチ
`wip_g1_g2_gop_full.patch` に着手時の断片あり）、G3 クラス内 typedef（大物・5 コミット）

---

# 仮想 MI Phase 2: 複数 vptr + セカンダリ vtable + this 調整 thunk（2026-08-08 完了）

**完了日**: 2026-08-08（`vmi_*.cpp` 8 本 exit 0、build.bat ゲート真の exit 0、Release|x64 + Debug|Win32 両ビルド緑）
**作業ブランチ**: `feat/cpp-virtual-mi`（ベース: `master` = 暗黙基底 ctor/dtor マージ済み）
**参照**: [実装済み.md](実装済み.md) 多重継承 Phase 2 行、ロードマップ 29。[問題と原因.md](問題と原因.md) BUG-17 / BUG-18 / BUG-19

- **レイアウト（BUG-17 修正が土台）**: `cpp_insert_vptr_field` を「先頭（primary）
  基底が polymorphic なら挿入しない」に変更し、派生は基底の vptr スロット
  （offset 0）を共有（そこに派生自身の vtable を書く）。旧実装は無条件挿入で
  基底が 8 バイトずれ、B* 経由のデータ/非仮想メソッドが壊れていた（単一継承でも）。
- **セカンダリ vtable**: 非 primary の polymorphic 基底 subobject ごとに
  `__cpp_vtbl2_<D>_<B>` を rodata に発行（基底のスロット番号）。エントリは
  派生が override するスロット（名前照合、primary のスロット継承と同方式）は
  **this 調整 thunk**、非 override スロットは基底実装へ**直接**（this は既に
  subobject を指すため調整不要）。vtable トークンは基底 anon フィールドの
  `cpp_vtable_tok` に保存。
- **thunk**: `sub rcx, off; movabs rax, &impl; jmp rax`（19 バイト、reloc は
  `R_DATA_PTR` のみでターゲット非依存マクロで書ける）。**発行は TU 末尾に遅延**
  （`cpp_finish_virtual_thunks`、`tccgen_compile` の decl 直後）— struct_decl は
  ローカルクラスで関数本体の途中でも走るため、`ind` への即時発行は関数コードを
  破壊しうる。シンボルは登録時に `external_global_sym` で先行作成し reloc を張る。
- **vptr 初期化の複数化**: ローカル（`cpp_init_local_vptr_rec`）/ グローバル
  （`cpp_init_global_vptr_rec`）とも、primary（offset 0）+ 非 primary 基底の
  vptr を再帰的に全部書く。ゲートは新設 `cpp_class_needs_vptr_init`（自身の
  vtable が無くても第 2 基底が polymorphic なら真、`.data` 強制も同条件）。
- **ディスパッチ**: `cpp_prepare_virtual_member_call` に this 調整を追加 —
  非 primary 基底で宣言された仮想メンバは**その基底のスロット番号**なので、
  this を subobject へ動かして **subobject の vptr（セカンダリ vtable）経由**で
  呼ぶ。アップキャスト 3 経路（`gen_cast` ポインタ / 参照バインド /
  `cpp_prepare_member_func_call`）の `!cpp_type_has_virtual` ガードを解除
  （新レイアウトでは `cpp_base_subobject_offset` の f->c が真のオフセット）。
- **実装中に発見・修正した既存バグ 2 件**（詳細 [問題と原因.md](問題と原因.md)）:
  - **BUG-18**: `p->vf()` で `cpp_member_this` を vstack 外へ先に退避してから
    vtable ロードを codegen するとレジスタが再割り当てされ this がゴミに。
    `vdup` で vstack 上に残し**最後に**捕獲する順序へ修正。既存テストが
    定数返しの仮想関数のみだったため潜伏していた。
  - **BUG-19**: 派生メソッド本体の暗黙メンバ参照が `field->c`（宣言クラス内
    オフセット）のままで、非ゼロオフセット基底のメンバが別スロットから
    読まれていた。`cpp_field_cumofs_in_class`（フィールド同一性照合）で
    累積オフセット化。
- **検証**: `vmi_*.cpp` 8 本（両基底 poly / override 無し / 書き込み thunk /
  第 1 基底非 poly / 参照アップキャスト / 単一継承データ（BUG-17 回帰・
  sizeof assert）/ グローバル / ctor 連携）すべて exit 0。**負の対照**:
  tccgen.c のみ stash した修正前バイナリで 8 本全 FAIL（RUN FAIL 6 +
  COMPILE FAIL 2）を実測。
- **制限（ドキュメント化）**: 菱形継承・仮想継承未対応。深い再 override
  （E : D : A,B で E が B の仮想を新規 override）はセカンダリ vtable が
  中間クラス（D-as-B）のまま。仮想 dtor の基底ポインタ経由ディスパッチ
  未対応（dtor フィールド名がクラスごとに異なり名前照合に乗らない）。
  ダウンキャスト（B*→D*）はポインタ調整なし。

**次の候補**: 宣言なしクラスの暗黙 ctor/dtor 合成、メンバ変数の ctor/dtor 連鎖、
特殊 operator（`() -> , new delete`）、仮想 dtor のディスパッチ、菱形/仮想継承

---

# 暗黙の基底 ctor / 自動基底 dtor + BUG-16（2026-07-31 完了）

**完了日**: 2026-07-31（新規テスト 5 本 exit 0、build.bat ゲート真の exit 0、Release|x64 + Debug|Win32 両ビルド緑）
**作業ブランチ**: `feat/cpp-mi-implicit-base-ctor`（ベース: `master` = 多重継承 Phase 1 マージ済み）
**参照**: [実装済み.md](実装済み.md) 多重継承行、ロードマップ 26b / 28。MI Phase 1 が残した制限

- **暗黙の基底 ctor**: 派生 ctor が mem-init リストに書かなかった基底（リスト自体が
  無い `D(int x) { ... }` を含む）を、宣言順にデフォルト構築する。
  接続点は `gen_function` の mem-init 展開ブロックの**直前**（C++ は基底が完全に
  構築されてからメンバ初期化子が走るため、リストより前に出す必要がある）。
  明示済み基底は `cpp_collect_explicit_bases` が保存済みトークン列を
  `begin_macro` で 1 度リプレイして収集し、二重呼び出しを防ぐ。
- **自動基底 dtor**: 派生 dtor 本体の後に基底 dtor を**逆順**で呼ぶ。
  挿入位置は `gsym(rsym)`（`return` 経路の合流点）と `nocode_wanted = 0` の後、
  `pop_local_syms` の前（`cpp_this_sym` / `local_stack` がまだ生きている必要がある）。
  単方向リストの逆順は `cpp_emit_base_dtor_calls` の末尾再帰前置で得ている。
- **BUG-16（実装中に発見・実測）**: BUG-15 の修正がメンバ呼び出し 4 経路のみで、
  `gaddrof()` で `this` を作る**残り 4 経路が取り残されていた**（基底 ctor /
  グローバル ctor / ローカル dtor / グローバル dtor）。8 バイト超で
  by-value コピーに落ち、初期化が丸ごと消える・`this` がコピーを指す。
  既存テストの対象が 4/8 バイトのため潜伏。詳細は
  [問題と原因.md](問題と原因.md) BUG-16。
- **検証**: 新規 7 本すべて、`tccgen.c` のみ stash した修正前バイナリで
  `[RUN FAIL]`、修正後で exit 0（負の対照を実測）。テストが無意味に
  通っていないことを機械的に確認済み。
- **制限（ドキュメント化）**:
  - 派生クラス自身が ctor / dtor を宣言していることが前提。宣言が無いクラスの
    暗黙 ctor/dtor 合成は未対応（`cpp_class_has_default_ctor` /
    `cpp_find_dtor_field` が NULL を返し、そもそも呼び出しが発行されない）。
  - 基底に 0 引数 ctor が無い場合は黙って無呼び出し（C++ はエラーだが、
    既存ソース保護を優先）。
  - メンバ変数（基底以外）の ctor/dtor 自動連鎖は対象外。
  - 菱形継承・仮想 MI（Phase 2）は従来どおり未対応。

**次の候補**: 仮想 MI（Phase 2: 複数 vptr + thunk）、宣言なしクラスの暗黙 ctor/dtor 合成、
メンバ変数の ctor/dtor 連鎖、特殊 operator（`() -> , new delete`）

---

# 多重継承（非仮想）Phase 1（2026-07-30 完了）

**完了日**: 2026-07-30（`mi_basic/ctor/upcast.cpp` 3 本 exit 0、build.bat ゲート真の exit 0）  
**作業ブランチ**: `feat/cpp-multiple-inheritance`（ベース: `fix/cpp-bug15-large-this`）  
**参照**: [実装済み.md](実装済み.md) 多重継承行、ロードマップ 26。前提 BUG-15

- Step 1（複数基底パース/レイアウト）: `: A, B` を loop 化し宣言順で先頭に並べる
  （A→offset0, B→offset sizeof(A)）。データメンバは既存 cumofs 経路でそのまま動く。
- Step 2（非先頭基底メソッドの `this` 調整）: 新規 `cpp_base_subobject_offset` で
  `obj + offset(base)` に調整。BUG-15 修正で書き込みも反映。
- Step 3（基底 ctor）: 明示 mem-init `: A(), B()` が `cpp_find_base_field` +
  `cpp_emit_base_ctor_call` で複数基底とも動作（暗黙基底 ctor / 自動基底 dtor は
  単一継承と同じく未対応の既存制限）。
- Step 4（アップキャスト）: `cpp_can_bind_lvalue_to_reference` を派生→基底に拡張、
  `gen_cast`（参照バインド + ポインタキャスト）と局所参照バインド（BUG-9 経路）で
  基底サブオブジェクトへポインタ調整。D→B&（引数/局所）、D*→B*（明示/暗黙）動作。
- **仮想ガード**: 途中、仮想クラスの `(Base*)&d` を壊す回帰を入れて発覚
  （vptr で base offset が非ゼロになるため）。全 MI 調整サイトに
  `!cpp_type_has_virtual()` ガードを追加し、仮想クラスは従来の offset 0 を維持
  （仮想 MI は Phase 2）。この回帰の見逃しで実行ゲートのクラッシュ未検出も判明・修正。
- 制限: 仮想関数を持つ MI（Phase 2）、菱形継承、暗黙基底 ctor / 自動基底 dtor、
  暗黙 D*→B* 変換時の警告（結果は正しい）。

**次の候補**: 仮想 MI（Phase 2: 複数 vptr + thunk）、暗黙基底 ctor/dtor、11+ コミットの master マージ

---

# BUG-15: 8 バイト超オブジェクトのメンバ関数書き込みが失われる（2026-07-30 修正）

**完了日**: 2026-07-30（`bug15_large_this.cpp` / `bug15_large_virtual_pmf.cpp` 2 本 exit 0、build.bat ゲート真の exit 0）  
**作業ブランチ**: `fix/cpp-bug15-large-this`（ベース: `feat/cpp-feat6a-ext6-bitwise`）  
**参照**: [問題と原因.md](問題と原因.md) BUG-15。多重継承テスト中に発覚した既存バグ

- 真因: `this` を `gaddrof()` で得た後、**型が struct T のまま**（`mk_pointer`
  していない）だったため、`gfunc_call` が by-value 構造体引数として扱い、Win64
  で 8 バイト超の構造体をコピー渡し → メソッドがコピーを変更し呼び出し側の実体は
  不変。読み取りは正常、既存テストが ≤8 バイトのため潜伏していた。
- 修正: `cpp_member_this = *vtop` の直前で `mk_pointer(&vtop->type)` を追加し
  `this` を T* ポインタ値にする。4 経路（通常メソッド／仮想／PMF／dtor）。
- 重大度が高い（大きいオブジェクトのメンバ書き込みが黙って壊れる一般バグ）。
  MI 非依存で、MI の前提として先行修正。

**次の候補**: 多重継承（Phase 1 の続き。本修正で MI メソッド書き込みが可能に）

---

# FEAT-6A-ext6: ビット/シフト/剰余演算子 `% & | ^ << >>` + 複合代入（2026-07-29 完了）

**完了日**: 2026-07-29（`feat6a_ext6_bitwise/compound/stream.cpp` 3 本 exit 0、build.bat ゲート真の exit 0）  
**作業ブランチ**: `feat/cpp-feat6a-ext6-bitwise`（ベース: `feat/cpp-bool`）  
**参照**: [実装済み.md](実装済み.md) operator 表、ロードマップ 24

- 二項 `% & | ^ << >>` は `expr_infix`（ext5 と同フック）、複合代入
  `%= &= |= ^= <<= >>=` は `expr_eq` の struct `TOK_ASSIGN` 経路（ext2 と同）に
  そのまま乗る。実装は `cpp_operator_suffix` に **12 ケース追加のみ**
  （`%`/`&`/`|`/`^`/`TOK_SHL`/`TOK_SAR` と `TOK_A_MOD`〜`TOK_A_SAR`）。
- メンバ／非メンバ、`T&` 返却の複合代入、stream 風 `s << a << b << c` チェーンが動作。
- これで C の二項演算子はほぼ全てオーバーロード可能に（残: `operator()`、
  `operator->`、`operator,`、`new`/`delete` 等の特殊 operator）。

**次の候補**: 多重継承（大物）、特殊 operator（`() -> , new delete`）、ブランチ群の master マージ

---

# FEAT-BOOL: `bool` 型 / `true` `false` リテラル（2026-07-29 完了）

**完了日**: 2026-07-29（`featbool_basic/operator.cpp` 2 本 + `a2/bool_ident.c` exit 0、build.bat ゲート真の exit 0）  
**作業ブランチ**: `feat/cpp-bool`（ベース: `feat/cpp-feat6a-ext5-relational`）  
**参照**: [実装済み.md](実装済み.md) operator/型 表、ロードマップ 23。FEAT-6A-ext5 の定石 `bool` 返却を可能にする

- 既存の A-2 降格スキームに乗せる最小実装:
  - `tcctok.h` に `DEF(TOK_BOOL2, "bool")` を追加し、`is_cpp_only_keyword` に
    登録（**C++ 専用キーワード**＝C モードでは `lex_c` により識別子へ降格。
    C の `bool` は `<stdbool.h>` マクロ or ユーザー識別子なので破壊しない）。
  - `parse_btype` で `TOK_BOOL2` を `_Bool`(VT_BOOL) と同型に。
  - `unary()` で `TOK_TRUE`/`TOK_FALSE` を定数 1/0（VT_BOOL）に。C++ のみ到達
    （C では降格されこの switch に来ない）。
- 効果: `bool operator==(...) const { ... }` など**定石の比較演算子**が書け、
  `bool b = (a < c);`、`if (true)` 等が動作。
- 回帰: `.c` で `int bool = 5;` `int true = 3;` が識別子として通る
  （`a2/bool_ident.c`）。既存キーワードゲート（CUnit "class as identifier"）緑。

**次の候補**: 残 operator（`% & | ^ << >>` と複合代入）、多重継承、ブランチ群の master マージ

---

# FEAT-6A-ext5: 比較演算子オーバーロード `== != < > <= >=`（2026-07-29 完了）

**完了日**: 2026-07-29（`feat6a_ext5_eq/rel/free/full.cpp` 4 本 exit 0、build.bat ゲート真の exit 0）  
**作業ブランチ**: `feat/cpp-feat6a-ext5-relational`（ベース: `feat/cpp-feat5c-virtual-pmf`）  
**参照**: [実装済み.md](実装済み.md) operator 表、ロードマップ 22

- 比較演算子は二項なので、既存の `expr_infix`（9834）の
  `cpp_try_member_binop` / `cpp_try_free_binop` フックに**そのまま乗る**。
  実装は `cpp_operator_suffix` に `TOK_EQ`→"eq"、`TOK_NE`→"ne"、`TOK_LT`→"lt"、
  `TOK_GT`→"gt"、`TOK_LE`→"le"、`TOK_GE`→"ge" の 6 ケース追加**のみ**。
- メンバ／非メンバ、`if` 条件での利用、配列走査での min（`operator<`）など動作。
- 制限: 返り値は `int`。C++ の `bool` 型と `true`/`false` リテラルは未対応
  （`_Bool` は動くが `bool` 綴りは未登録、`true`/`false` は unary 未処理）。
  次の候補として **FEAT-BOOL（`bool`/`true`/`false`）** を推奨（比較演算子を
  定石の `bool` 返却で書けるようにする小機能）。

**次の候補**: FEAT-BOOL（`bool`/`true`/`false`）、比較以外の残 operator（`% & | ^ << >>` 等）、多重継承

---

# FEAT-5C: 仮想メンバ関数ポインタ（仮想 PMF）（2026-07-25 完了）

**完了日**: 2026-07-25（`feat5c_virtual_pmf/args/single.cpp` 3 本 exit 0、build.bat ゲート真の exit 0）  
**作業ブランチ**: `feat/cpp-feat5c-virtual-pmf`（ベース: `fix/cpp-bug14-inline-virtual-symbol`）  
**参照**: [実装済み.md](実装済み.md) 仮想 PMF 行、ロードマップ 16、前提 [BUG-14](問題と原因.md)

- FEAT-5B（非仮想 PMF）の拡張。PMF は具体アドレスではなく **member を型に
  記号的に保持**（`pm->type.ref->c` = member token）し、invoke 時に field を
  引く方式なので、仮想 field なら vtable ディスパッチへ切り替えるだけで済む。
- 変更（2 箇所の仮想拒否解除のみ）:
  - `&Class::vfunc`（address-of、8749 の拒否解除）: 値は非仮想と同じく class
    実装アドレス（invoke では無視、null テスト用に非ゼロ）。member token は型へ。
  - `cpp_emit_mptr_pmf_invoke`（invoke）: 仮想 field なら `vpushv(obj)` +
    `cpp_prepare_virtual_member_call(field, &obj->type)` に委譲（通常の
    `obj.vfunc()` と同一の vtable[slot] ロード）。前方宣言を追加。
- 発見: 仮想 PMF 検証中、Base 実体オブジェクトの仮想呼び出しが Derived の
  override に誤ディスパッチする既存バグ（[BUG-14](問題と原因.md) 同名メンバの
  マングル衝突）を発見。**BUG-14 を先行修正**してから本 FEAT をフル検証・完成。
- 多態（`(basePtr->*pmf)()` → 派生）・単一クラス・引数付き・base/derived 実体、
  全ケース exit 0。

**次の候補**: 12e 残（sym_malloc / cpp_this_sym・駆動バグ待ち）、多重継承 PMF、後置 `[]` 以外の未実装 operator など

---

# BUG-14: 同名メンバ関数のマングル衝突（2026-07-25 修正）

**完了日**: 2026-07-25（`bug14_same_method_name.cpp` / `bug14_virtual_base_object.cpp` 2 本 exit 0、build.bat ゲート緑=真の exit 0）  
**作業ブランチ**: `fix/cpp-bug14-inline-virtual-symbol`（ベース: `test/cpp-12e-exec-gate`）  
**参照**: [問題と原因.md](問題と原因.md) BUG-14。FEAT-5C（仮想 PMF）検証中に発覚

- 真因: `cpp_build_func_mangle` のリンク名にクラス名が無く、`Base::who` と
  `Derived::who`（や無関係 2 クラスの `f()`）が同一 asm_label に。加えて
  `external_sym` が同名同シグネチャのメンバを別クラスでも同一 Sym に merge。
  → 本体・vtable relocation が上書きされ、非仮想=クラッシュ / 仮想=誤ディスパッチ。
- 修正（Approach B）: Sym トークンは bare 維持（field 照合・静的・const・
  デフォルト引数を壊さない）。`external_sym` を `cpp_pending_member_class` で
  クラス違いは別 Sym にフォーク + `cpp_build_func_mangle` に `Sym *cls` を渡し
  asm_label を `__tcc_<Class>__<name>_<args>` にクラス修飾。lookup は既に
  parent_class 照合なので追加変更なし。
- Approach A（クラス修飾トークン）は `sym->v==メンバ名` 前提の箇所を軒並み
  壊したため破棄（12e 実行ゲートが回帰を捕捉）。

**次の候補**: FEAT-5C（仮想 PMF。本修正で基盤が正しくなったので再開）、16 継続

---

# 12e-P1: run_all.bat 実行テストゲート（2026-07-25 完了）

**完了日**: 2026-07-25（`run_all.bat` を compile+実行の 2 フェーズ化、build.bat が真の exit 0=健全を返すよう改善）  
**作業ブランチ**: `test/cpp-12e-exec-gate`（ベース: `fix/cpp-bug13-p2-free-unnamed-param`）  
**参照**: ロードマップ 12e、[問題と原因.md](問題と原因.md) BUG-12（compile-only が実行時バグを見逃した実例）

- 動機: 従来 `run_all.bat` は `-c` の **compile-only**。BUG-12（`feat6a_big_struct`
  が compile 通過・実行時に異常終了）を見逃していた。加えて build.bat は
  既知ベースライン（a8 `sm3`/`sm6`/`t_assign` の compile 失敗）で常に exit 1 を
  返し、実 regression と区別できなかった。
- 変更（`run_all.bat`、tccgen.c 非改変）:
  - **Phase 1（compile）**: 全ソースを `-c`。既知の未実装ギャップ（静的メンバ
    `Counter::x=v` 代入未対応の `sm3`/`sm6`/`t_assign`）を `KNOWNFAIL` リストで
    **非ゲート化**（情報表示のみ）。新規 compile 失敗のみ FAILED に計上。
  - **Phase 2（execute・新規）**: exit 0 が保証された集合（a9 全 78 + a7 メンバ
    4 + smoke hello）を**リンク＋実行し exit 0 を必須**。exe は `%TEMP%` 出力で
    作業ツリーを汚さない。実行時 regression を `[RUN FAIL]` で明示・計上。
- 効果: `run_all.bat` が健全時に **exit 0** を返し、build.bat（既存の
  `call run_all.bat` ゲート）が**真の green（exit 0=健全 / exit 1=実 regression）**
  になった。BUG-12 型の実行時 regression を機械的に捕捉。
- 検証: 健全時 exit 0、非ゼロ終了テスト混入で `[RUN FAIL]`＋exit 1 を確認
  （自己テストで実証・削除済み）。build.bat フルで exit 0 到達。
- スコープ: 12e の「基盤整備」のうち **実行テストゲート**を実装。残る
  `sym_malloc` 初期化ポリシー / `cpp_this_sym` 設計見直しは、駆動する具体バグが
  無い投機的 core 改変のため**据え置き**（必要が生じた時点で着手）。

**次の候補**: 16（FEAT-5C 仮想 PMF・任意）、12e 残（sym_malloc / cpp_this_sym・駆動バグ待ち）

---

# BUG-13-P2: 自由関数の無名引数を C++ で許容（2026-07-25 完了）

**完了日**: 2026-07-25（`bug13_p2_free_unnamed.cpp` 新規 + `feat6a_ext4_free.cpp` を無名ダミー標準形へ更新、exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `fix/cpp-bug13-p2-free-unnamed-param`（ベース: `feat/cpp-feat6a-ext4-postop`）  
**参照**: [問題と原因.md](問題と原因.md) BUG-13 追補

- BUG-13 はメンバ限定だったため、自由関数の無名引数（非メンバ後置
  `operator++(T&, int)` の定石）は定義パス 12456 の `expect("identifier")` で
  拒否されていた。C++ は関数定義でも無名引数を許容するので、C++ モード時のみ
  無名引数へ匿名 id（`anon_sym++`）を付与するよう変更（`.c` は K&R 規則維持）。
- これで非メンバ後置 operator も標準の無名ダミーで書けるようになり、
  FEAT-6A-ext4 の「非メンバは named 必須」制限を解消。

**次の候補**: 12e（基盤整備）、16（FEAT-5C 仮想 PMF）

---

# FEAT-6A-ext4: 後置 operator `a++` / `a--`（`operator++(int)`）（2026-07-25 完了）

**完了日**: 2026-07-25（`feat6a_ext4_post_inc/post_dec/prepost/free/full.cpp` 5 本 exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `feat/cpp-feat6a-ext4-postop`（ベース: `fix/cpp-bug13-unnamed-member-param`）  
**参照**: [実装済み.md](実装済み.md) §14、ロードマップ 21、前提 [BUG-13](#bug-13-無名引数を持つメンバ関数の呼び出しでtccがクラッシュ2026-07-25完了)

- 前置基盤（FEAT-6A-ext3）の素直な拡張。前置＝メンバ 0 引数／非メンバ 1 引数、
  後置＝メンバ 1 引数（ダミー `int`）／非メンバ 2 引数を **arity** で分離
  （マングルは引数型付きなので `operator++()` と `operator++(int)` は別シンボル）。
- 新規 `cpp_try_member_postop`（arity1 メンバへダミー `int 0` を 1 引数で
  `cpp_finish_member_call`）と `cpp_try_free_postop`（`operator++(T&,int)` を
  2 引数解決）。接続点は postfix ループ `inc(1,tok)` 直前に `VT_STRUCT`
  ガード付きフックのみ（`.c`／スカラは従来経路を 1 バイトも変えず）。
- 発見: 後置の定石 `operator++(int)` が**無名ダミー引数**を使うため、
  無名メンバ引数で tcc がクラッシュする既存バグ（[BUG-13](#bug-13-無名引数を持つメンバ関数の呼び出しでtccがクラッシュ2026-07-25完了)）を露出。
  BUG-13 を先行修正してから本 FEAT を完成させた。
- 制限（当初）: 非メンバ後置 operator のダミーは named 必須だったが、
  **[BUG-13-P2](#bug-13-p2-自由関数の無名引数をcで許容2026-07-25完了) で解消**（自由関数の無名引数を C++ で許容）。

**次の候補**: 12e（基盤整備）、16（FEAT-5C 仮想 PMF）

---

# BUG-13: 無名引数を持つメンバ関数の呼び出しで tcc がクラッシュ（2026-07-25 完了）

**完了日**: 2026-07-25（`bug13_unnamed_member.cpp` / `bug13_unnamed_operator.cpp` 2 本 exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `fix/cpp-bug13-unnamed-member-param`（ベース: `fix/cpp-bug12-sret-this`）  
**参照**: [問題と原因.md](問題と原因.md) BUG-13

- 真因: `post_type` は無名引数を `v==SYM_FIELD`（名前トークン無し）で積む。
  自由関数は定義時の宣言子検査（`expect("identifier")`, 12456）で弾くが、
  メンバ本体は `skip_or_save_block` で保存され `gen_function`→`gfunc_prolog`
  で後から emit されるため検査を通らない。`gfunc_prolog` の
  `sym_push(v & ~SYM_FIELD)` が無名だと `sym_push(0,...)` となり、名前記録
  分岐が `table_ident[0 - TOK_IDENT]`（負 index）を触って AV。
- 修正: `cpp_finish_member_inlines` の登録前に `cpp_name_unnamed_params` で
  無名引数へ匿名トークン id（`anon_sym++`, `SYM_FIRST_ANOM` 以上）を合成付与。
  `sym_push` は匿名分岐に入り table_ident を触らない。型は無変更で
  オーバーロード解決・マングルに影響なし。
- 制限: クラス外定義 `int C::m(int){}` は通常定義パス（12456）に乗るため
  無名引数は従来どおりエラー（クラッシュではない）。

**次の候補**: FEAT-6A-ext4（後置 operator。本バグ修正が前提）

---

# BUG-12: 8 バイト超 struct 返却メンバ呼び出しの this/sret 引数順不整合（2026-07-22 完了）

**完了日**: 2026-07-22（`feat6a_big_struct.cpp` 初 exit 0、`bug12_big_struct_op/args.cpp` 新規 2 本 exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `fix/cpp-bug12-sret-this`  
**参照**: [問題と原因.md](問題と原因.md) BUG-12、ロードマップ 12d

- 真因: Win64 ABI で struct 返却がレジスタに収まらない（sret）場合、
  隠しポインタが arg0/RCX を占有し `this` は arg1/RDX に押し出される
  （`gfunc_prolog` の型リスト走査前の暗黙挿入）。呼び出し側 2 箇所
  （`cpp_finish_member_call`、postfix `(` の `cpp_member_this_pending`
  ブロック）は無条件に `this` を arg0 へ挿入しており、sret を arg1 へ
  押し出して食い違っていた。
- 修正: sret が積まれている場合は `this` の挿入位置を position 1 に変更
  （sret はそのまま arg0 に留める）。8 バイト以下/2 冪サイズの struct
  （レジスタ返却）は分岐に入らず無変化。
- 演算子オーバーロード経路（`cpp_finish_member_call`）も同型のバグを
  含んでいたため同時に修正（`bug12_big_struct_op.cpp` で検証）。

**次の候補**: 12e（基盤整備）、16（FEAT-5C 仮想 PMF）、後置 `a++`（`operator++(int)`）

---

# FEAT-4E-P2: クラス外 dtor 定義 `P::~P() {}`（2026-07-19 完了）

**完了日**: 2026-07-19（`feat4e_p2_basic/explicit/global.cpp` 3 本 exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `feat/cpp-ooc-dtor`  
**参照**: [問題と原因.md](問題と原因.md) FEAT-4E-P2 節、[実装済み.md](実装済み.md) §7

- `parse_cpp_scope_qualifier` に `~` を追加し、`*v` を FEAT-4E の既存
  マングル名 `__cpp_dtor_<Cls>` に差し替えるだけで、通常の関数定義パスが
  インライン dtor と同一シンボルに合流。呼び出し側は無修正。
- `cpp_peek_out_of_class_ctor` を `Cls :: ~ Cls (` にも対応（+ 同関数の
  `mem != cls` 失敗パスの unget 二重復元という潜在バグを除去）。
- 前提: クラス内 `~P();` 宣言が必要（C++ 規格どおり）。

**次の候補**: 12d（`feat6a_big_struct` sret 引数順調査）、12e（基盤整備）、16（FEAT-5C 仮想 PMF）、後置 `a++`（`operator++(int)`）

---

# BUG-11: extern "C" 領域直後の `class` パースエラー（2026-07-19 完了）

**完了日**: 2026-07-19（`bug11_externc_class.cpp` exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `fix/cpp-externc-class`  
**参照**: [問題と原因.md](問題と原因.md) BUG-11

- 真因: extern "C" 終端（ブロック `}` / 単一宣言 `;`）の 1 トークン先読みが
  `lex_c` 有効のまま字句解析され、C++ キーワードが降格双子のまま残留。
- 修正: `cpp_repromote_stale_lookahead()` 新設（降格双子はハッシュチェーン
  非連結なので `tok_alloc` の名前引き直しで元キーワードに戻る）。
  ブロック開始側も `lex_c++` を `{` 消費前に移動して対称化。

**次の候補**: ロードマップ 20（クラス外 dtor 定義 `P::~P()`）、12d（big_struct 調査）、12e（基盤整備）

---

# FEAT-4G: グローバル自動 ctor/dtor（2026-07-18 完了）

**完了日**: 2026-07-18（`feat4g_basic/args/dtor/multi.cpp` 4 本 exit 0、
`CTOR→MAIN→DTOR` 出力順確認、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `feat/cpp-feat4g`  
**参照**: [実装済み.md](実装済み.md) §16、[問題と原因.md](問題と原因.md) BUG-10

- 方式: decl() 登録 → `cpp_finish_global_dyns` で `.init_array`/`.fini_array`
  thunk 発行 → PE リンク時に `_tcc_cpp_start` 起動コードを C ソース注入して
  entry 差し替え（crt1.o 再ビルド回避）。
- 初期実装はコンパイル時/実行時 AV を計 8 個抱えていた（dynarray 誤用、
  TOK_EOF 終端漏れ、二重解放、NUL 終端漏れ、リンク時 `s1->cpp`=0、
  エントリ名規約、`data_offset` 未同期による thunk 上書き、win64 shadow
  space 欠落 + シンボル番号の加算値誤用）。詳細は BUG-10。
- 発見事項（未修正・別タスク）: `extern "C" {}` ブロック後の `class` が
  パースエラー / クラス外 dtor 定義 `P::~P()` 未対応。

---

# BUG-9: ローカル参照宣言の値コピー（2026-06-12 完了）

**完了日**: 2026-06-12（`bug9_a/b/c.cpp` 3 本 exit 0、build.bat 緑）  
**作業ブランチ**: `fix/cpp-bug9-struct-ref`  
**参照**: [問題と原因.md](問題と原因.md) BUG-9 節

- 真因: 参照のローカル宣言初期化にバインド（アドレス格納）経路が無く、
  struct は memcpy・スカラは値格納になっていた。struct だけでなく
  **スカラ参照宣言も壊れていた**（動いていたのは参照引数・参照戻り値のみ）。
- 修正: `init_putv()` ローカル枝に参照バインド追加 +
  unary identifier push で参照変数を自動 deref（関数シンボルは除外）。

---

# 次の実装: FEAT-4F（暗黙デフォルト ctor `Foo f;`）

**FEAT-4F 完了日**: 2026-06-12（`ctor4f_*.cpp` 5 本 exit 0）  
**プラン作成日**: 2026-06-12  
**作業ブランチ**: `feat/cpp-feat4f`（ベース: `feat/cpp-ctor-overload`）  
**参照**: [実装済み.md](実装済み.md) §8、FEAT-4B（`Foo f(args)` 書き換え）

## 1. 概要

ユーザー定義のデフォルト ctor `P()` を持つクラスのローカル変数宣言
`P f;` で、ctor を自動的に呼ぶ。現在は確保のみで ctor が呼ばれない
（Stage 4 制限）。

## 2. 設計

FEAT-4B の書き換え方式をそのまま流用する。decl() の FEAT-4B フック
（識別子の次が `(` かを見る分岐）の else 側に分岐を追加:

- 条件: `tok == ';' || tok == ','`、storage が
  `VT_STATIC | VT_EXTERN | VT_TYPEDEF` を含まない、
  新ヘルパ `cpp_class_has_default_ctor()`（0 引数 ctor フィールドの存在）が真。
- 処理: `decl_initializer_alloc` で確保後、`unget` LIFO で
  `var . Ctor ( )` を合成して `expr_eq()` → `vpop()`。
  `,` なら continue で次の宣言子（`P a, b;` の b にも再適用）。

## 3. スコープ外（ドキュメント化）

- グローバル `P g;`（FEAT-4G `.init_array` 待ち）
- `static P s;`（init-once ガード未実装のため除外）
- 全引数デフォルト値の ctor `P(int a = 5)` での暗黙呼び出し
- 派生クラスに ctor が無い場合の基底 ctor 自動合成
- ctor を持つが 0 引数 ctor が無いクラスの `P f;` は従来通り無警告で確保のみ
  （C++ ではエラーだが既存テスト保護を優先）

## 4. テスト

- `ctor4f_basic.cpp` — `P a;` で ctor 実行
- `ctor4f_overload.cpp` — `P()` / `P(int)` 共存で `P a; P b(3);`
- `ctor4f_multi.cpp` — `P a, b;` 複数宣言子
- `ctor4f_dtor.cpp` — デフォルト ctor + dtor 連携（FEAT-4E 回帰）
- `ctor4f_skip.cpp` — `P *p; P &r = a; P c = a;` が暗黙呼び出しを**しない**こと

---

# （アーカイブ）FEAT-CTOR-OV（ctor オーバーロード）

**FEAT-CTOR-OV 完了日**: 2026-06-12（`ctor_ov_*.cpp` / `ctor_base_conv.cpp` 5 本 exit 0）  
**プラン作成日**: 2026-06-12  
**作業ブランチ**: `feat/cpp-ctor-overload`（ベース: `feat/cpp-sig-match`）  
**参照**: [実装済み.md](実装済み.md) §8、[問題と原因.md](問題と原因.md) 完全シグネチャ照合節

## 調査結果（2026-06-12）

「ctor オーバーロードはクラスメンバ登録段階でブロック」という既知の制限は
**陳腐化**していた。完全シグネチャ照合 + 引数変換遅延（`feat/cpp-sig-match`）
の導入により、以下は**既に動作**する:

- インライン ctor overload（`P(int)` / `P(double)` 本体クラス内）
- クラス外定義 ctor overload（`P::P(int)` / `P::P(double)`）
- mem-initializer 付きクラス外 ctor overload
- インライン / クラス外 混在
- デフォルト ctor `P()` + 引数付き `P(int)` の共存 + 明示 `a.P()` 呼び出し

`Foo f(args);` は FEAT-4B で `f.Foo(args)` に書き換えられ postfix `.`/`(` を
通るため、シグネチャ照合・変換遅延・再解決がそのまま効く。

## 残る欠陥（本 FEAT のスコープ）

`cpp_emit_base_ctor_call()`（派生 ctor の mem-initializer `: Base(args)`）のみ
独自経路で、2 つの欠陥がある:

1. **引数パース前に解決確定**: `cpp_find_ctor_field()`（最初の ctor フィールド）
   → `cpp_lookup_member_func()` で先に global を選び、引数の型を見ない。
   基底に overload ctor があると誤解決（probe: `: B(2.5)` が `B(int)` に行き EXIT=-102）。
2. **`gfunc_param_typed()` を一切呼ばない**: overload が無くても
   `: B(2.9)` の double が int param へ未変換のまま渡る（EXIT=858993457 のゴミ）。

## 実装ステップ

- P1: `cpp_emit_base_ctor_call` — 引数パース**後**に `cpp_resolve_func_call`
  （生の引数型でスコア、cur=初期 ctor global で parent_class/const フィルタ）
  → vtop[-na] の sym/type を差し替え。
- P2: 解決済みプロトタイプへ `vrotb(na)` × na 回で各引数を `gfunc_param_typed`
  変換（args は func の上に na 個、this 挿入前なので rotate 対象は args のみ）。
  デフォルト引数が残れば `cpp_apply_default_args`。
- P3: テスト `dev/test/a9/ctor_ov_*.cpp`（インライン / クラス外 / mem-init /
  基底 overload / 基底単独 double→int 変換）。
- P4: 回帰（a7/a8/a9 + run_all + build.bat）+ ドキュメント + 1 コミット。

## 落とし穴

- vstack 配置は `[func, arg1..argN]`。`vrotb(na)` は top na 個（= args のみ）を
  回すこと。this 挿入後に回すと this が混ざる。
- `cpp_resolve_func_call` の `cur` は `__cpp_ctor_<Base>` トークンの global。
  parent_class は base_class と一致している前提（external_sym 定義パスで設定済み）。

---

# （アーカイブ）FEAT-6A-ext3（単項 operator `!` `-` `~` `++` `--`）

**FEAT-6A-ext3 完了日**: 2026-06-11（`feat6a_ext3_*.cpp` 6 本実行 exit 0）  
**現在の次タスク**: §6 ロードマップ参照（FEAT-6B-P2 / FEAT-4F など。BUG-7・完全シグネチャ照合は 2026-06-12 完了）  
**作業ブランチ**: `feat/cpp-feat6a-ext3`（ベース: `feat/cpp-feat6a-ext2`）  
**参照**: [実装済み.md](実装済み.md) §14、[問題と原因.md](問題と原因.md) BUG-8

> **実装中の発見 2 点**（詳細 [問題と原因.md](問題と原因.md) FEAT-6A-ext3 節）:
> 1. `.cpp` の空 `()` が `FUNC_OLD`（K&R 引数任意）だったため arity overload が「互換」誤判定 → cpp ゲートで `FUNC_NEW`（= `(void)`）に変更
> 2. `cpp_lookup_member_func` がシグネチャ無視で先勝ち解決（**BUG-7 と同根**）→ arity フィルタ追加（param 型レベルの選別は BUG-7 で対応）

## 1. 概要とスコープ

メンバ／非メンバの**単項 operator** を実装する。対象: `!` `-`（単項マイナス） `~` 前置 `++` `--`。

| 目標 | 内容 |
|------|------|
| G1 | メンバ単項 operator（0 引数）宣言・定義・暗黙呼び出し `-a` `!a` `~a` `++a` `--a` |
| G2 | 非メンバ単項 operator（1 引数）フォールバック |
| G3 | **arity 解決**: 同名 `operator-` の単項（0 引数）／二項（1 引数）共存 |
| G4 | 非 struct オペランドの従来動作を完全維持（`.c` 回帰ゼロ） |

**非目標**: 後置 `a++`（`operator++(int)` ダミー引数）、単項 `*`（deref）、`operator()`、virtual operator。

## 2. 設計

- `cpp_operator_suffix` に `!`→"not"、`~`→"compl"、`TOK_INC`→"inc"、`TOK_DEC`→"dec" 追加（`-` は既存 "minus" を共用、arity で区別）
- **`cpp_find_operator_member` に `want_args` パラメータ追加**（呼び出し側: binop/subscript=1、unop=0）。arity 一致候補を優先、無ければ従来の any-arity にフォールバック（既存動作保護）
- **新規 `cpp_try_member_unop(op_tok)`**: vtop が struct → 0 引数メンバ呼び出し（`cpp_finish_member_call(s, NULL, 0)`）
- **新規 `cpp_try_free_unop(op_tok)`**: `cpp_resolve_free_func_call(v, 1)` で 1 引数解決（`cpp_try_free_binop` の 1 引数版）
- `unary()` の `'!'` / `'~'` / `'-'` / `TOK_INC` / `TOK_DEC`（前置）各 case に「メンバ → 非メンバ → 従来処理」フック

## 3. 落とし穴

| 症状 | 対策 |
|------|------|
| `-x`（int/float）回帰 | フックは `VT_STRUCT` ガードで即 return 0 |
| 二項 `a - b` が単項 field を拾う | `want_args=1` の arity 優先 |
| 明示 `.operator-()` の arity | `cpp_find_field_for_call` は未変更（混在クラスでは先勝ち。制限として記録） |
| 定数畳み込み経路 | struct は const 式に入らないため影響なし |

## 4. テスト（`dev/test/a9/`）

| ファイル | 検証 |
|---------|------|
| `feat6a_ext3_neg.cpp` | メンバ `operator-()` + `-a` |
| `feat6a_ext3_not.cpp` | メンバ `operator!()` + `!a`（if 文での利用含む） |
| `feat6a_ext3_arity.cpp` | 単項／二項 `operator-` 共存 + 両呼び出し |
| `feat6a_ext3_incdec.cpp` | 前置 `++a` / `--a`（`T&` 返却） |
| `feat6a_ext3_free.cpp` | 非メンバ `operator-(const T&)` / `operator!(const T&)` |
| `feat6a_ext3_full.cpp` | 統合 |

---

# （アーカイブ）FEAT-6A-ext2（代入系 operator `=` / `+=`）

**FEAT-6A-ext2 完了日**: 2026-06-11（`feat6a_ext2_*.cpp` 6 本実行 exit 0）  
**現在の次タスク**: §9 ロードマップ参照（BUG-7 候補 / FEAT-6A-ext3 単項 operator など）  
**作業ブランチ**: `feat/cpp-feat6a-ext2`（ベース: `fix/cpp-this-deref`）  
**参照**: [実装済み.md](実装済み.md) §14–15、[TCC_CPP.md](TCC_CPP.md) Stage 6、[問題と原因.md](問題と原因.md) BUG-8

> **着手時に BUG-8 を発見・修正**（2026-06-11）: `operator=` の正準形 `return *this;` で tcc 自体が AV。真因は `gen_function` の this 型構築（`pointed_type` が `{VT_STRUCT, ref=NULL}` を返す）。別ブランチ `fix/cpp-this-deref` で修正済み — [問題と原因.md](問題と原因.md) BUG-8 参照。  
> **FEAT-6A-ext 完了**（2026-06-11）: 非メンバ operator / `operator[]`、実行テスト exit 0 + BUG-6 修正。詳細はアーカイブ節参照。

---

## 1. 概要

Stage 6 の続きとして **代入演算子のオーバーロード**を実装する。対象は (1) メンバ `operator=`、(2) 複合代入 `operator+=`（メンバ／非メンバ）、(3) 同テーブルで安価に対応できる `-=` `*=` `/=`。

既存基盤（合成トークン `__cpp_op_*`、`cpp_try_member_binop`、`cpp_try_free_binop`、参照返却）をそのまま再利用し、新しい接続点は **`expr_eq()`** のみ（代入は右結合のため `expr_infix` を通らない）。

### 1.1 目的のコード（完了時）

```cpp
class Acc {
public:
    int v;
    Acc& operator=(const Acc& o) { v = o.v; return *this; }
    Acc& operator+=(const Acc& o) { v += o.v; return *this; }
};

int main() {
    Acc a, b, c;
    a.v = 1; b.v = 2; c.v = 3;
    a = b;          /* メンバ operator= */
    a += c;         /* メンバ operator+= */
    b = a = c;      /* T& 返却によるチェーン */
    return a.v + b.v - 6;   /* 3 + 3 - 6 = 0 */
}
```

### 1.2 現状の挙動（FEAT-6A-ext 完了後）

| 構文 | 結果 |
|------|------|
| `a = b`（struct、operator= なし） | ✅ memcpy コピー（`vstore`） |
| `a = b`（struct、メンバ `operator=` あり） | **operator= を呼ばない**（memcpy のまま） |
| `a += b`（struct） | **コンパイルエラー**（`gen_op` が struct を拒否） |
| `operator=` / `operator+=` の宣言 | **parse error**（`cpp_operator_suffix` 未登録 → `unsupported operator`） |
| `a + b` メンバ／非メンバ | ✅ FEAT-6A / 6A-ext |
| `obj[i]` | ✅ FEAT-6A-ext |

---

## 2. 目標と非目標

### 2.1 目標（FEAT-6A-ext2 スコープ）

| # | 内容 |
|---|------|
| G1 | メンバ `operator=` の宣言・定義・`a = b` 暗黙呼び出し（メンバのみ — C++ 標準） |
| G2 | `operator=` 未宣言クラスの `a = b` は **従来 memcpy を完全維持**（回帰ゼロ） |
| G3 | メンバ `operator+=` `-=` `*=` `/=` の宣言・定義・暗黙呼び出し |
| G4 | 非メンバ `operator+=` 等（メンバ解決失敗後のフォールバック） |
| G5 | `T&` 返却によるチェーン `a = b = c` |
| G6 | `feat6a_ext2_full.cpp` exit 0 |
| G7 | 既存 `feat6a_*` / `feat6b_*` / a7 / a8 回帰維持、`.c` 回帰ゼロ |

### 2.2 非目標（別 FEAT へ）

| 内容 | 理由 / 別タスク |
|------|----------------|
| コピー初期化 `Acc c = a;` での operator= 呼び出し | C++ 的にも初期化は copy ctor の領域（operator= は呼ばれないのが正しい）。現状の memcpy 初期化を維持 |
| 暗黙の copy-assignment operator 合成 | 未宣言時は従来 memcpy = 実質同等。合成不要 |
| `%=` `&=` `|=` `^=` `<<=` `>>=` | 需要が出たら `cpp_operator_suffix` にケース追加のみ |
| 単項 operator（`!` `-` `++` `--`） | FEAT-6A-ext3 |
| `operator()` | 将来 |
| const メソッド内メンバ書き込み拒否 | FEAT-6B-P2（任意） |
| BUG-7（const オーバーロード誤解決） | 別ブランチ（ロードマップ 12b） |

---

## 3. 現状コードの分析

### 3.1 トークンと既存テーブル

- 字句解析は `+=` を **単一トークン `TOK_A_ADD`（0xb0）** にする（[tcc.h](tcc.h) L1166〜）。`-=`/`*=`/`/=` は `TOK_A_SUB`/`TOK_A_MUL`/`TOK_A_DIV`。
- `TOK_ASSIGN(t)` マクロが複合代入の判定、`TOK_ASSIGN_OP(t)` が `+=` → `'+'` 変換（[tcc.h](tcc.h) L1177〜）。
- `cpp_operator_suffix()`（[tccgen.c](tccgen.c) L608）に case を足せば、宣言パース `cpp_parse_operator_token()`（L638）は **変更不要**で `operator=` / `operator+=` を受理する（`tok` がそのまま `'='` / `TOK_A_ADD` で来る）。

### 3.2 接続点は `expr_eq()`（L9241）

```c
static void expr_eq(void)
{
    expr_cond();
    if ((t = tok) == '=' || TOK_ASSIGN(t)) {
        test_lvalue();
        next();
        if (t == '=') {
            expr_eq();
        } else {
            vdup();
            expr_eq();
            gen_op(TOK_ASSIGN_OP(t));   /* struct はここで落ちる */
        }
        vstore();                        /* struct = struct は memcpy */
    }
}
```

- 代入は右結合のため `expr_infix`（L9030）には来ない。**operator 解決のフックは `expr_eq` に置く**。
- RHS 評価後の vstack は `[lhs(lvalue), rhs]` — `cpp_try_member_binop`（L6210）が期待するレイアウトと **同一**。そのまま再利用できる。
- `cpp_try_member_binop` は LHS が `VT_STRUCT` でない／メンバ未発見なら **vstack 無変更で 0 を返す** → 失敗時に従来 `vstore()` へ安全にフォールバックできる。

### 3.3 設計方針

| 経路 | 方針 |
|------|------|
| `t == '='` | RHS 評価後に `cpp_try_member_binop('=')`。成功 → `vstore()` をスキップ（vtop は operator= の返却値）。失敗 → 従来 `vstore()`（memcpy 維持）。**非メンバ `operator=` は試さない**（C++ 標準でメンバ限定） |
| `TOK_ASSIGN(t)` かつ LHS が `VT_STRUCT` | `vdup()`/`gen_op` を**迂回**して RHS 評価 → `cpp_try_member_binop(t)` → 失敗時 `cpp_try_free_binop(t)` → 両方失敗なら `tcc_error`（従来も struct `+=` はエラーなので回帰なし） |
| `TOK_ASSIGN(t)` かつ LHS が非 struct | **従来経路を 1 バイトも変えない**（`vdup` → `gen_op` → `vstore`） |

- マングル: `__tcc___cpp_op_assign_<args>` / `__tcc___cpp_op_plus_assign_<args>`（既存 `cpp_build_func_mangle` がそのまま動く）。
- `T&` 返却・`const T&` 引数は FEAT-6A-P2 / 6A-ext の基盤を再利用。チェーンは `expr_eq` の右結合再帰で自然に成立。

### 3.4 再利用できる既存経路

| 経路 | 用途 |
|------|------|
| `cpp_operator_suffix` / `cpp_operator_field_tok`（L608〜） | suffix 追加のみ |
| `cpp_parse_operator_token` / `cpp_parse_operator_decl_name`（L638〜） | 変更不要 |
| `cpp_try_member_binop`（L6210） | `=` / `+=` メンバ解決（変更不要の見込み） |
| `cpp_try_free_binop`（L6106） | 非メンバ `+=` 解決（変更不要の見込み） |
| `cpp_find_operator_member` / `cpp_prepare_member_func_call` / `cpp_finish_member_call` | メンバ呼び出し |
| 参照返却（`s->type.t & VT_REFERENCE` → `indir()`、L6101） | `T&` 返却 + チェーン |

---

## 4. 実装ステップ（フェーズ分割）

各フェーズ末: x64 Release ビルド緑 + 既存 a9 PASS。  
**規律**: 本 FEAT 全体で 1 feature branch = 1 コミット。壊れた状態でコミットしない。

| Phase | 内容 | 観測可能な進捗 |
|-------|------|----------------|
| **P0** | 事前 grep + 現状エラーの記録 | 現象記録 |
| **P1** | メンバ `operator=` | `feat6a_ext2_assign*.cpp` exit 0 |
| **P2** | メンバ複合代入 `+=` `-=` `*=` `/=` | `feat6a_ext2_pa_member.cpp` exit 0 |
| **P3** | 非メンバ複合代入 | `feat6a_ext2_pa_free.cpp` exit 0 |
| **P4** | 統合 + 回帰 + ドキュメント | `feat6a_ext2_full.cpp` exit 0 |

### Step 0（P0）: 事前調査

```batch
findstr /n "cpp_operator_suffix cpp_try_member_binop cpp_try_free_binop" tccgen.c
findstr /n "expr_eq" tccgen.c
findstr /n "TOK_A_ADD TOK_ASSIGN" tcc.h
dir /s /b dev\test\a9\*.cpp
```

- `operator=` 宣言を含む 5 行 `.cpp` で現状の `unsupported operator` を確認・記録。
- struct `a = b`（operator= なし）の memcpy 動作を再確認（回帰基準）。

### Step 1（P1）: メンバ `operator=`

| Step | 内容 |
|------|------|
| P1-1 | `cpp_operator_suffix` に `case '=': return "assign";` 追加 |
| P1-2 | クラス内宣言・クラス外定義 `Acc& Acc::operator=(const Acc&)` のマングル確認 |
| P1-3 | `expr_eq` — `t == '='` の RHS 評価後に `cpp_try_member_binop('=')`、成功時 `vstore` スキップ |
| P1-4 | `T&` 返却 + チェーン `a = b = c` 確認 |

合格: `feat6a_ext2_assign.cpp` + `feat6a_ext2_chain.cpp` exit 0。`feat6a_ext2_memcpy.cpp`（operator= なし）回帰。

### Step 2（P2）: メンバ複合代入

| Step | 内容 |
|------|------|
| P2-1 | `cpp_operator_suffix` に `TOK_A_ADD` → `"plus_assign"`、`TOK_A_SUB` → `"minus_assign"`、`TOK_A_MUL` → `"mul_assign"`、`TOK_A_DIV` → `"div_assign"` |
| P2-2 | `expr_eq` — `TOK_ASSIGN(t)` かつ LHS `VT_STRUCT` の分岐新設（`vdup`/`gen_op` 迂回 → RHS 評価 → `cpp_try_member_binop(t)`） |
| P2-3 | 失敗時 `tcc_error`（P3 で free フォールバック挿入予定の位置） |

合格: `feat6a_ext2_pa_member.cpp` exit 0。`.c` の `x += 1` / `int` 系回帰なし（smoke + CUnit）。

### Step 3（P3）: 非メンバ複合代入

- P2-3 の位置に `cpp_try_free_binop(t)` フォールバックを挿入（メンバ → 非メンバ → エラー）。
- 合格: `feat6a_ext2_pa_free.cpp` exit 0。

### Step 4（P4）: 統合・完了

- `feat6a_ext2_full.cpp` — §1.1 完全版 + `+=` 統合
- Release\|x64 + Debug\|Win32 + `build.bat`
- [実装済み.md](実装済み.md) §7 / §14 更新
- [問題と原因.md](問題と原因.md) に問題があれば追記

**コミットメッセージ例**: `feat(cpp): FEAT-6A-ext2 assignment operators`

---

## 5. テスト計画

配置: `dev/test/a9/`（`run_all.bat` の `a9\*.cpp` glob に自動包含。compile-only ゲート + 手動 link/実行で exit 0 確認）

| 優先 | ファイル | Phase | 検証内容 |
|------|---------|-------|---------|
| 1 | `feat6a_ext2_assign.cpp` | P1 | メンバ `operator=` + `a = b`（int メンバ書き換えで検証） |
| 2 | `feat6a_ext2_chain.cpp` | P1 | `T&` 返却 + `a = b = c` |
| 3 | `feat6a_ext2_memcpy.cpp` | P1 | **operator= なし**クラスの `a = b` が従来 memcpy（回帰） |
| 4 | `feat6a_ext2_pa_member.cpp` | P2 | メンバ `operator+=` / `operator-=` |
| 5 | `feat6a_ext2_pa_free.cpp` | P3 | 非メンバ `operator+=(A&, const A&)` |
| 6 | `feat6a_ext2_full.cpp` | P4 | §1.1 完全版 |

**回帰**: 既存 a9 全本（feat6a 8 + feat6a_ext 6 + feat6b 5）+ a8/a7 + smoke + repro_double6 + CUnit（`.c` の通常代入・複合代入）。

---

## 6. 主要タッチポイント

| 関数 / 箇所 | 行 | 変更内容 |
|-------------|-----|---------|
| `cpp_operator_suffix` | L608 | `'='` + `TOK_A_ADD`〜`TOK_A_DIV` の case 追加 |
| **`expr_eq`** | L9241 | `=` フック + struct 複合代入分岐（**本 FEAT の中核、最小差分**） |
| `cpp_try_member_binop` | L6210 | 変更なしの見込み（回帰保護） |
| `cpp_try_free_binop` | L6106 | 変更なしの見込み |
| `cpp_parse_operator_token` | L638 | 変更不要（suffix テーブル参照のみ） |

**触らない**: `expr_infix`、`vstore` 本体、`struct_decl()` 内 `gen_function()`、x86_64 `arg*8` ABI。

---

## 7. 想定される落とし穴

| 症状 | 原因 | 対策 |
|------|------|------|
| `.c` / 非 struct の `x += 1` 回帰 | `expr_eq` の分岐が広すぎ | struct 分岐は `tcc_state->cpp && (vtop->type.t & VT_BTYPE) == VT_STRUCT` で厳密ガード。判定は **`vdup()` より前**（RHS 評価前に LHS 型は確定済み） |
| operator= なし struct の `a = b` が壊れる | フック挿入位置ミス | `cpp_try_member_binop` は失敗時 vstack 無変更 → 失敗時は必ず従来 `vstore()` に到達させる。`feat6a_ext2_memcpy.cpp` で常時検証 |
| `a = b = c` で値が消える | operator= が `void` 返却 / `vstore` スキップ漏れ | 成功時は `cpp_finish_member_call` の返却値を vtop に残し `vstore` を呼ばない（1 値だけ残す） |
| `test_lvalue()` が operator= 経路で誤動作 | フック位置 | `test_lvalue()` は既存位置のまま（operator= でも `this` 取得に lvalue 必須） |
| 一時オブジェクト RHS（`a += b + c`）で落ちる | `const T&` バインド | FEAT-6A-P2 の参照バインド経路を通ることを確認（`feat6a_ext_free_plus` と同型） |
| 複合代入の二重評価 | `vdup` 残留 | struct 経路では `vdup()` を呼ばない（operator 呼び出しは LHS を 1 回だけ評価） |
| マングル不一致 | decl/call でトークン名不一致 | `get_tok_str(v)` を trace（`__cpp_op_assign` / `__cpp_op_plus_assign`） |
| `operator=` の free 関数宣言が通ってしまう | 宣言側ノーチェック | 許容（呼び出し側でメンバのみ解決）。余裕があれば宣言時 `tcc_error` |
| BUG-7（const overload 誤解決）と干渉 | `cpp_find_operator_member` 共用 | 本 FEAT では const overload の operator= を**テストに含めない**（BUG-7 修正後に追加） |

---

## 8. 完了判定基準

- [x] `operator=` / `operator+=` 宣言・クラス外定義 compile OK
- [x] `a = b`（operator= あり）が operator 呼び出し、なしが memcpy（両テスト exit 0）
- [x] `a = b = c` チェーン exit 0（`(a += b) += c` も exit 0）
- [x] メンバ／非メンバ `a += b` exit 0
- [x] `feat6a_ext2_full.cpp` exit 0
- [x] 既存 a9/a8/a7 + smoke + repro_double6 + CUnit 回帰なし（a9 実行 20 本 exit 0。run_all の a8 調査用 sm3/sm6/t_assign 失敗はベースラインと同一の既知問題）
- [x] x64 Release + Debug Win32 ビルド成功
- [x] [実装済み.md](実装済み.md) 更新
- [x] feature branch 上で 1 コミット（前提の BUG-8 は `fix/cpp-this-deref` で別コミット）

---

## 9. ロードマップ（FEAT-6A-ext2 以降）

| 順 | 機能 | 状態 | 内容 |
|----|------|------|------|
| 12 | FEAT-6A-ext | **完了** | 非メンバ operator / `operator[]`（実行 exit 0、2026-06-11） |
| 12a | BUG-6 | **完了（2026-06-11）** | `this` 仮引数 Sym の r/c 未初期化クラッシュ修正 |
| 12b | BUG-7 | **完了（2026-06-12）** | const オーバーロード誤解決。真因 2 件: parse_btype クラス名パスの const 脱落 + `cpp_resolve_func_call` の無差別再解決（`fix/cpp-bug7`） |
| 12g | 完全シグネチャ照合 | **完了（2026-06-12）** | 同 arity・param 型違い overload。`cpp_lookup_member_func` 2 パス化 + postfix `(` の引数変換遅延（`feat/cpp-sig-match`）。残: スコア順位付け・曖昧エラー |
| 12h | FEAT-CTOR-OV | **完了（2026-06-12）** | ctor オーバーロード。本体は 12g で成立済みと判明。`cpp_emit_base_ctor_call` のパース後解決 + 引数変換を修正（`feat/cpp-ctor-overload`） |
| 17 | FEAT-4F | **完了（2026-06-12）** | 暗黙デフォルト ctor `P a;`。decl の 4B フック拡張で `a.P()` に書き換え（`feat/cpp-feat4f`）。発見: struct ローカル参照 `P &r = a;` が実行時ハング（→ BUG-9） |
| 12i | BUG-9 | **完了（2026-06-12）** | ローカル参照宣言の値コピー（struct ハング / スカラ誤動作）。`init_putv` 参照バインド + identifier 自動 deref（`fix/cpp-bug9-struct-ref`） |
| 12c | テスト修正 | **完了（2026-06-12）** | `feat6a_ref_arg.cpp` の `+ y` 欠落を修正、exit 0（`fix/cpp-12c-ref-arg-test`） |
| 12d | BUG-12 | **完了（2026-07-22）** | `feat6a_big_struct` 8 バイト超 sret メンバ呼び出しの引数順不整合。`cpp_finish_member_call` + postfix `(` 汎用呼び出しの 2 箇所を修正（`fix/cpp-bug12-sret-this`） |
| 12e | 基盤整備 | **一部完了（2026-07-25）** | `run_all.bat` 実行テストゲート完了（compile+実行の 2 フェーズ化、build.bat 真の exit 0 化）（`test/cpp-12e-exec-gate`）。残: sym_malloc 初期化ポリシー、`cpp_this_sym` 設計見直し（駆動バグ待ちで据え置き） |
| 12f | BUG-8 | **完了（2026-06-11）** | `this` 明示デリファレンス（`this->x` / `*this`）AV 修正（`fix/cpp-this-deref`） |
| 13 | FEAT-6A-ext2 | **完了（2026-06-11）** | 代入 operator（`=` `+=` `-=` `*=` `/=`）。実行 exit 0 |
| 14 | FEAT-6A-ext3 | **完了（2026-06-11）** | 単項 operator（`!` `-` `~` 前置 `++` `--`）+ 空 `()` = `(void)` 化 + `cpp_lookup_member_func` arity フィルタ。後置 `++(int)` は 21 で対応 |
| 15 | FEAT-6B-P2 | **完了（2026-06-12）** | const メソッド内書き込み拒否。`this` の VT_CONSTANT を被指示型へ移動 + `verify_assign_cast` C++ エラー化（const T& 宛先は除外）（`feat/cpp-feat6b-p2`） |
| 16 | FEAT-5C | **完了（2026-07-25）** | 仮想 PMF。invoke 時に仮想 field を `cpp_prepare_virtual_member_call` へ委譲。前提 BUG-14（`feat/cpp-feat5c-virtual-pmf`） |
| 21c | BUG-14 | **完了（2026-07-25）** | 同名メンバ関数のマングル衝突（クラス名欠落 + external_sym の跨クラス merge）。asm_label をクラス修飾 + 別 Sym フォーク（`fix/cpp-bug14-inline-virtual-symbol`） |
| 22 | FEAT-6A-ext5 | **完了（2026-07-29）** | 比較演算子 `== != < > <= >=`。`cpp_operator_suffix` に 6 ケース追加のみ（既存 expr_infix binop フックに乗る）。返り値 int（`feat/cpp-feat6a-ext5-relational`） |
| 23 | FEAT-BOOL | **完了（2026-07-29）** | `bool` 型（`_Bool` 別名、C++ 専用キーワードで C 降格）+ `true`/`false` リテラル。比較演算子を bool 返却で書ける（`feat/cpp-bool`） |
| 24 | FEAT-6A-ext6 | **完了（2026-07-29）** | ビット/シフト/剰余 `% & \| ^ << >>` + 複合代入。`cpp_operator_suffix` に 12 ケース追加のみ（`feat/cpp-feat6a-ext6-bitwise`） |
| 25 | BUG-15 | **完了（2026-07-30）** | 8 バイト超オブジェクトのメンバ書き込みが失われる（`this` が by-value struct 渡しでコピーされる）。`this` を `mk_pointer` で T* 化（4 経路）（`fix/cpp-bug15-large-this`） |
| 26 | 多重継承 | **Phase 1 完了（2026-07-30）** | 非仮想 MI: 複数基底パース/レイアウト/this 調整/明示基底 ctor/アップキャスト（D→B&, D*→B*）。仮想 MI は Phase 2（`feat/cpp-multiple-inheritance`）。前提 BUG-15 |
| 26b | 暗黙基底 ctor/dtor | **完了（2026-07-31）** | mem-init に無い基底のデフォルト構築（宣言順）+ 派生 dtor 後の基底 dtor 逆順呼び出し。単一/多重継承とも（`feat/cpp-mi-implicit-base-ctor`） |
| 28 | BUG-16 | **完了（2026-07-31）** | 8 バイト超の基底 subobject への基底 ctor/dtor 呼び出しが by-value コピーになる（BUG-15 と同型の `mk_pointer` 欠落）（`feat/cpp-mi-implicit-base-ctor`） |
| 27 | 実行ゲート crash 検出 | **完了（2026-07-30）** | run_all.bat の exec ゲートが `errorlevel 1` で負の終了コード（クラッシュ）を見逃していたのを `neq 0` に修正（`feat/cpp-multiple-inheritance`） |
| 29 | 仮想 MI Phase 2 | **完了（2026-08-08）** | primary vptr 共有レイアウト + セカンダリ vtable `__cpp_vtbl2_*` + this 調整 thunk（TU 末尾遅延発行）+ 複数 vptr 初期化 + アップキャスト仮想ガード解除。`vmi_*.cpp` 8 本（`feat/cpp-virtual-mi`）。前提 BUG-17 |
| 29a | BUG-17 | **完了（2026-08-08）** | polymorphic 基底を持つ派生への vptr 二重挿入（基底 8 バイトずれ、B* 経由データ/非仮想メソッド破壊）。primary 基底の vptr 共有で修正（`feat/cpp-virtual-mi`） |
| 29b | BUG-18 | **完了（2026-08-08）** | `p->vf()` の this レジスタが vtable ロードの codegen で再割り当てされゴミ化。vdup で vstack 保護し最後に捕獲（`feat/cpp-virtual-mi`） |
| 29c | BUG-19 | **完了（2026-08-08）** | 派生メソッド本体の継承メンバ参照が基底オフセット抜きの `field->c` 直読み。`cpp_field_cumofs_in_class` で累積化（`feat/cpp-virtual-mi`） |
| 18 | FEAT-4G | **完了（2026-07-18）** | グローバル自動 ctor/dtor（`.init_array` thunk + PE 起動コード注入）。実装時の 8 バグは BUG-10 参照（`feat/cpp-feat4g`） |
| 19 | BUG-11 | **完了（2026-07-19）** | extern "C" 領域直後の `class` パースエラー。先読みトークンの降格残留を `cpp_repromote_stale_lookahead()` で再昇格（`fix/cpp-externc-class`） |
| 20 | FEAT-4E-P2 | **完了（2026-07-19）** | クラス外 dtor 定義 `P::~P() {}`。`parse_cpp_scope_qualifier` の `~` 対応 + OOC peek 拡張で既存 `__cpp_dtor_*` に合流（`feat/cpp-ooc-dtor`） |
| 12d | BUG-12 | **完了（2026-07-22）** | 8 バイト超 sret メンバ呼び出しの this/sret 引数順（`fix/cpp-bug12-sret-this`） |
| 21a | BUG-13 | **完了（2026-07-25）** | 無名引数メンバ関数呼び出しの AV（`sym_push` の table_ident 負 index）。`cpp_name_unnamed_params` で無名引数へ匿名 id 付与（`fix/cpp-bug13-unnamed-member-param`） |
| 21 | FEAT-6A-ext4 | **完了（2026-07-25）** | 後置 operator `a++` / `a--`（`operator++(int)`）。arity で前置と分離、postfix ループにフック。前提 BUG-13（`feat/cpp-feat6a-ext4-postop`） |
| 21b | BUG-13-P2 | **完了（2026-07-25）** | 自由関数の無名引数を C++ で許容（12456 検査を cpp のみ匿名 id 付与へ）。非メンバ後置 operator の named 必須制限を解消（`fix/cpp-bug13-p2-free-unnamed-param`） |

---

## 10. 一文まとめ

**FEAT-6A-ext2** では、`cpp_operator_suffix` に `=`/`+=` 系 suffix を追加し、代入の唯一の経路である `expr_eq()` に「struct 限定でメンバ → 非メンバ → 従来処理」のフックを最小差分で挿入して、既存の `cpp_try_member_binop` / `cpp_try_free_binop` / 参照返却基盤をそのまま代入演算子に転用する。

---

# （アーカイブ）FEAT-6A-ext — 非メンバ operator / `operator[]`

**完了**（2026-06-11、実行テスト exit 0 + BUG-6 修正）: `cpp_try_free_binop`（非メンバ二項）、`cpp_try_cpp_subscript`（メンバ／非メンバ `operator[]`、非メンバは **TCC 拡張**）、`expr_infix` / postfix `[` 接続。テスト `feat6a_ext_*.cpp` 6 本 exit 0。クラッシュの真因は BUG-6（`this` 仮引数 Sym の r/c 未初期化）— [問題と原因.md](問題と原因.md) 参照。詳細は [実装済み.md](実装済み.md) §14。

---

# （アーカイブ）FEAT-6A-P2 — struct 返却 operator + const T& 引数

**完了**（2026-06-06）: `cpp_finish_member_call` を postfix `(` の sret 実装に揃え、`const T&` 仮引数の宣言・実引数変換を接続。テスト `feat6a_vec2_int.cpp`, `feat6a_vec2_binop.cpp`, `feat6a_vec2_explicit.cpp`, `feat6a_ref_arg.cpp`, `feat6a_vec2.cpp` exit 0。詳細は [実装済み.md](実装済み.md) §14 Phase 2。

---

# （アーカイブ）FEAT-6B — const メンバ関数

**完了**（2026-06-06）: `FuncAttr.func_const`、`post_type()` trailing const、`cpp_find_field_for_call()`、マングル `_C`、`const T* this`。テスト `feat6b_this`, `feat6b_const_obj`, `feat6b_defn`, `feat6b_overload`, `feat6b_overload_rev` exit 0。制限: const メソッド内メンバ書き込み拒否は未接続。詳細は [実装済み.md](実装済み.md) §15。

---

# （アーカイブ）FEAT-6A — 演算子オーバーロード Phase 1

**完了**（2026-06-06）: 合成トークン `__cpp_op_plus` 等、postfix `.operator+` / `expr_infix` の `a + b` 変換。テスト `feat6a_call.cpp`, `feat6a_min.cpp`, `feat6a_op_plus.cpp` exit 0。制限: int 返却 + by-value 引数 → **FEAT-6A-P2**。詳細は [実装済み.md](実装済み.md) §14。

---

# （アーカイブ）FEAT-5B — メンバポインタ

**完了**（2026-06-06）: `VT_MPTR`、DMP、非仮想 PMF、`feat5b_*.cpp` 4 本 exit 0。詳細は [実装済み.md](実装済み.md) §13。

---

# （アーカイブ）FEAT-5A — 仮想関数

**完了**（2026-06-05）: 仮想関数 Phase 1〜4。`feat5a_*.cpp` 7 本。

---

# （アーカイブ）FEAT-4D / 4C / 4B

**FEAT-4D 完了**: Base 初期化子 `: Base(args)` codegen。  
**FEAT-4C 完了**: クラス外 ctor 定義 `Foo::Foo(int) : a(x) {}`。  
**FEAT-4B 完了**: 暗黙 ctor 呼び出し `Foo f(args);`。詳細は [実装済み.md](実装済み.md) §10.2。

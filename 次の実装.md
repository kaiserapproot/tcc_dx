Phase 1（IMPLEMENTATION\_GUIDE の A-P 〜 A-9 / **Stage 1 基盤**）は完了しています。コンパイラ本体として次に進むのは **[PLAN.md](vscode-file://vscode-app/d:/bin/cursor/resources/app/out/vs/code/electron-sandbox/workbench/PLAN.md)** **の Stage 2** です。

***

## **次の本体実装：Stage 2 — 名前修飾とスコープ**

[PLAN.md](vscode-file://vscode-app/d:/bin/cursor/resources/app/out/vs/code/electron-sandbox/workbench/PLAN.md) の「Phase 3：Stage 2 実装（マングリング＆スコープ）」に相当します（5〜8h 想定）。

### **主な内容**

| **項目**           | **内容**                                             |
| :--------------- | :------------------------------------------------- |
| **軽量マングリング**     | 関数シグネチャ（名前＋引数型）から内部シンボル名を生成（Itanium 風または TCC 独自形式） |
| **関数オーバーロード**    | `int f(int)` と `int f(double)` の区別                 |
| **参照型** **`&`**  | 型修飾子としてパースし、マングルに含める                               |
| **クラススコープ**      | `Sym` 拡張（例: `parent_class`）、メンバの分類・名前解決            |
| **修飾名** **`::`** | クラス外メンバ定義 `Point::set(...)` など（Stage 1 では未対応と明記）   |

主な変更先は **`tccgen.c`**（`decl()` のシンボル登録、`type_decl()` 等）。

### **Stage 1 からの「つなぎ」項目（Stage 2 で扱う想定）**

[IMPLEMENTATION\_GUIDE.md](vscode-file://vscode-app/d:/bin/cursor/resources/app/out/vs/code/electron-sandbox/workbench/IMPLEMENTATION_GUIDE.md) で Stage 1 から先送りしたものも、Stage 2 前後で入る想定です。

* `extern "C" void foo();` の**単一宣言**形式
* `extern "C++" { ... }`
* `extern "C" { #include <windows.h> }`（`windows_smoke.cpp`）
* クラス外定義 `void Point::set(...) { ... }`

***

## **その次（ロードマップ全体）**

| **順** | **Stage**            | **内容**                                                 |
| :---- | :------------------- | :----------------------------------------------------- |
| **2** | マングリング＆スコープ          | 上記（**次**）                                              |
| **3** | `this`・メンバ関数・デフォルト引数 | `obj.method()` の**コード生成**、インライン定義の本実装（Stage 1 は読み捨てのみ） |
| **4** | 単一継承・初期化子リスト・静的メンバ   | `class Derived : public Base` 等                        |
| **5** | メンバポインタ              | `&Class::member`、`obj.*p`、PMF                          |

***

## **テスト側で次に増やすもの**

[PLAN.md](vscode-file://vscode-app/d:/bin/cursor/resources/app/out/vs/code/electron-sandbox/workbench/PLAN.md) では Stage ごとにテストファイルが定義されています。

* **Stage 2**: `test/vs_test/test_stage2_mangling.c`（CUnit）、`cpp_test_stage2.cpp`（CPPUnit）
* **Stage 3**: `test_stage3_members.c` など

TCC 駆動では `dev/test/a6/` 以降のディレクトリを増やす運用（[実装履歴.MD](vscode-file://vscode-app/d:/bin/cursor/resources/app/out/vs/code/electron-sandbox/workbench/%E5%AE%9F%E8%A3%85%E5%B1%A5%E6%AD%B4.MD) の sequencing 規約）も想定されています。

***

## **着手前の推奨（実装以外）**

Phase 1 の変更がまだ **未コミット** なら、Stage 2 に入る前に:

1. Phase 1 を **1 コミット**（ガイド P-3）
2. `build.bat` / CPPUnit / CUnit が緑のままであることの確認

***

## **一文まとめ**

**次は Stage 2（名前修飾・オーバーロード・クラススコープ・`::`）で、C++ として「リンク可能なシンボル」を作る層です。** その次の Stage 3 で、いまパースだけのメンバ関数に `this` とコード生成が付きます。

Stage 2 の詳細設計を IMPLEMENTATION\_GUIDE の Part B や PLAN の §3.1 から起こす作業が、実装面での「次の一手」になります。

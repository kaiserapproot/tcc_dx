# 次の実装: FEAT-6A-ext（非メンバ operator / operator[]）

**FEAT-6A-P2 完了日**: 2026-06-06  
**現在の次タスク**: **FEAT-6A-ext**（非メンバ operator、`operator[]`）  
**参照**: [実装済み.md](実装済み.md) §14、[TCC_CPP.md](TCC_CPP.md) Stage 6、[問題と原因.md](問題と原因.md)

> **FEAT-6A-P2 完了**（2026-06-06）: struct 返却 operator、`const T&` 引数。テスト `feat6a_vec2*.cpp` 5 本 + Phase 1 回帰 exit 0。  
> **FEAT-6B 完了**（2026-06-06）: trailing `const`、`cpp_find_field_for_call()`、マングル `_C`。  
> **FEAT-6A Phase 1 完了**（2026-06-06）: メンバ二項 operator、`a + b` 暗黙変換。

---

## 1. 概要

FEAT-6A Phase 1 でパース・リンクまで通るが **実行時に未検証**だった 2 点を解消し、Stage 6 の代表例 `Vec2 operator+(const Vec2&)` を end-to-end で動かす。

| # | 制限（Phase 1） | 現象 |
|---|----------------|------|
| L1 | **struct 返却** operator | compile/link OK だが `a + b` または代入で **ランタイムハング**（または異常終了） |
| L2 | **`const T&` 引数** | 宣言・呼び出しで type/parse エラー（`struct or union expected` 等） |

### 1.1 目的のコード（完了時）

```cpp
class Vec2 {
public:
    int x, y;
    Vec2 operator+(const Vec2& o);
};

Vec2 Vec2::operator+(const Vec2& o) {
    Vec2 r;
    r.x = x + o.x;
    r.y = y + o.y;
    return r;
}

int main() {
    Vec2 a, b, c;
    a.x = 1; a.y = 2;
    b.x = 3; b.y = 4;
    c = a + b;              /* a.operator+(b) */
    return c.x + c.y - 10;  /* 0 */
}
```

### 1.2 現状の挙動（FEAT-6B 完了後）

| 構文 | 結果 |
|------|------|
| `int operator+(Vec2 o)` + `a + b` | ✅ exit 0（`feat6a_op_plus.cpp`） |
| `Vec2 operator+(Vec2 o)` + `a + b` | compile OK、**実行ハング**（未修正） |
| `int operator+(const Vec2& o)` | **parse/type エラー**（未対応） |
| `Vec2 operator+(const Vec2& o)` + `a.operator+(b)` | 未検証（L1+L2 複合） |
| 非 operator メンバ `Vec2 make()` | 要確認（比較用ベースライン） |

---

## 2. 目標と非目標

### 2.1 目標（FEAT-6A-P2 スコープ）

| # | 内容 |
|---|------|
| G1 | struct 返却メンバ operator の codegen（Win x64 sret / 戻り値領域） |
| G2 | 暗黙変換 `a + b` で struct 返却 operator を呼び出し、結果を lvalue に代入 |
| G3 | `const T&` をメンバ operator の仮引数として宣言・呼び出し可能 |
| G4 | 明示呼び出し `a.operator+(b)` も §1.1 と同等に動作 |
| G5 | `feat6a_vec2.cpp` exit 0 |
| G6 | 既存 `feat6a_*.cpp` / `feat6b_*.cpp` 回帰維持 |

### 2.2 非目標（別 FEAT へ）

| 内容 | 別タスク |
|------|---------|
| 代入系 operator（`=` `+=`） | FEAT-6A-ext |
| `operator[]` / 非メンバ `operator+` | FEAT-6A-ext |
| virtual operator | FEAT-5A 連携（任意） |
| `-` `*` `/` の struct 返却テスト追加 | Phase 2 完了後の任意拡張 |
| const メソッド内メンバ書き込み拒否 | FEAT-6B 残課題 |

---

## 3. 現状コードの分析

### 3.1 二つの呼び出し経路（L1 の核心）

FEAT-6A ではメンバ operator 呼び出しに **2 経路**がある。

| 経路 | 入口 | struct 返却の sret 処理 |
|------|------|------------------------|
| **A. 明示** | postfix `.operator+(` → `cpp_member_this_pending` → 通常 `(` ハンドラ | [tccgen.c](tccgen.c) L8268〜（`gfunc_sret` + hidden ret 引数） |
| **B. 暗黙** | `expr_infix` → `cpp_try_member_binop()` → `cpp_finish_member_call()` | [tccgen.c](tccgen.c) L5691〜（A を切り出したコピー） |

Phase 1 では **int 返却**のみ検証。`cpp_finish_member_call()` は Phase 1 追加時に A からコピーしたが、**struct 返却 + Win x64 ABI** では未検証。

**仮説（優先度順）**:

1. **B 経路のみ壊れている** — A は動き B だけハング → `cpp_finish_member_call` と `(` ハンドラの diff が修正点
2. **両経路とも struct 返却 operator で壊れる** — sret + `this` 隠し引数 + ユーザ引数の順序/個数
3. **`c = a + b` の struct 代入** — operator 呼び出し後の vstack が struct 値になっておらず、代入側で異常

**P1 で必ず切り分ける**:

```
feat6a_vec2_explicit.cpp  — c = a.operator+(b); のみ
feat6a_vec2_binop.cpp     — c = a + b; のみ
feat6a_vec2_int.cpp       — struct 返却・by-value 引数（L2 なし）で B 経路だけ試す
```

### 3.2 `cpp_finish_member_call()` の要注意箇所

[tccgen.c](tccgen.c) L5691〜5795:

```c
/* s->type = 戻り値型（prototype sym の type フィールド） */
if ((s->type.t & VT_BTYPE) == VT_STRUCT) {
    ret_nregs = gfunc_sret(&s->type, variadic, &ret.type, ...);
    /* hidden ret pointer → nb_args++ */
}
...
vpushv(&user_args[i]);
gfunc_param_typed(s, sa);
...
memmove(..., cpp_member_this);  /* this 挿入 */
gfunc_call(nb_args);
/* struct をレジスタ/スタックから vtop に載せ直し */
```

postfix `(` ハンドラ（L8268〜8420）と **行単位 diff** し、差分がバグ源。

特に確認:

- `ret_nregs <= 0` 時の `vpop()` vs `vtop--`（A は `vtop--`、B は `vpop()` — L5715 vs L8297）
- sret hidden arg 追加後の `nb_args` / `memmove` オフセット
- struct 返却後の `vset(&s->type, VT_LOCAL | VT_LVAL, addr)` で vtop が struct lvalue になるか

### 3.3 `const T&` 引数（L2）

**既存の参照渡し**は Stage 2 で動作済み:

- 自由関数 `void g(int& x)` — `dev/test/a6/ref_param.cpp` ✅
- マングル `r`  suffix — `mangle_append_type()` L203

**未検証**: クラス型 `const Vec2&` を **メンバ operator** の仮引数にした場合。

失敗箇所の候補:

| 段階 | 内容 |
|------|------|
| 宣言 | `type_decl` / `parse_btype` で `const Vec2&` が field の func prototype に載るか |
| 呼び出し | `gfunc_param_typed()` → `gen_assign_cast()` で struct rvalue → `const T&` にアドレス渡し変換 |
| postfix `.` | 非 func field 分岐で `expect("struct or union")` が誤発火していないか |

`gen_assign_cast()` / `verify_assign_cast()` の `VT_REFERENCE` 経路を trace する（[tccgen.c](tccgen.c) L7237 `gfunc_param_typed`）。

### 3.4 再利用できる既存経路

| 経路 | 用途 |
|------|------|
| postfix `(` + `cpp_member_this_pending` | struct 返却 sret の**正解参照実装** |
| `gfunc_sret()` / `gfunc_call()` | x64 Win ABI |
| `mangle_append_type` の `'r'` | `const T&` マングル（追加変更不要の可能性大） |
| `cpp_prepare_member_func_call()` | L2 修正後もそのまま利用 |
| `cpp_find_field_for_call()` | FEAT-6B で overload/const 対応済み |

---

## 4. 実装方針

### 4.1 L1 修正 — `cpp_finish_member_call` を A 経路に揃える

**方針 A（推奨）**: postfix `(` 内の sret + `gfunc_call` + 戻り値 vstack 復元ブロックを **1 関数に切り出し**、A/B 両方から呼ぶ。

```c
static void cpp_emit_func_call(Sym *func_proto, int nb_user_args, int member_this);
```

**方針 B（最小 diff）**: `cpp_finish_member_call()` を L8268〜8420 と diff し、ずれている行だけ修正。

Phase 1 で int 返却が動いているため、**struct 返却ブロックのみ**触る。int 返却経路は regression テストで守る。

### 4.2 L2 修正 — `const T&` 仮引数

1. **宣言**: `feat6a_ref_arg.cpp`（int 返却 + `const Vec2&`）で parse 失敗位置を特定
2. **呼び出し**: `gen_assign_cast` で `(VT_STRUCT lvalue)` → `(const T&)` 変換を確認
   - 不足なら `gfunc_param_typed` 直前に `gaddrof()` 相当を C++ ガード付きで追加
3. **マングル**: `__tcc___cpp_op_plus_r...` が decl/def/call で一致するか確認のみ

### 4.3 テスト追加の順序

L1/L2 を **分離**してから統合テスト:

| 順 | ファイル | 検証 |
|----|---------|------|
| 1 | `feat6a_vec2_int.cpp` | struct 返却 + by-value 引数（L1 のみ） |
| 2 | `feat6a_ref_arg.cpp` | int 返却 + `const T&`（L2 のみ） |
| 3 | `feat6a_vec2.cpp` | §1.1 完全版（L1+L2） |

---

## 5. 実装ステップ（フェーズ分割）

各フェーズ末: x64 Release ビルド緑 + 既存 a9 feat6a/feat6b PASS。

| Phase | 内容 | 観測可能な進捗 |
|-------|------|----------------|
| **P0** | 切り分けテスト 3 本作成・現象記録 | explicit vs binop vs int-only |
| **P1** | L1: `cpp_finish_member_call` 修正 | `feat6a_vec2_binop.cpp` exit 0 |
| **P2** | L1: 明示呼び出し + struct 代入確認 | `feat6a_vec2_explicit.cpp` exit 0 |
| **P3** | L2: `const T&` 宣言・呼び出し | `feat6a_ref_arg.cpp` exit 0 |
| **P4** | 統合 + 回帰 | `feat6a_vec2.cpp` exit 0 |
| **P5** | Debug\|Win32 + `build.bat` + ドキュメント | 完了 |

### Step 0（P0）: 切り分け

```cpp
/* feat6a_vec2_explicit.cpp — 明示呼び出しのみ */
c = a.operator+(b);

/* feat6a_vec2_binop.cpp — 暗黙変換のみ */
c = a + b;

/* feat6a_vec2_int.cpp — by-value 引数 + struct 返却 */
Vec2 operator+(Vec2 o);
```

記録: どの経路がハングするか [問題と原因.md](問題と原因.md) に追記。

### Step 1（P1）: struct 返却（暗黙変換）

- `cpp_finish_member_call()` を postfix `(` ハンドラと diff
- 重点: `gfunc_sret` hidden arg、`memmove` + `this`、`gfunc_call` 後の struct vstack 復元
- 合格: `feat6a_vec2_binop.cpp` exit 0

### Step 2（P2）: struct 返却（明示 + 代入）

- `feat6a_vec2_explicit.cpp` exit 0
- `feat6a_vec2_int.cpp` exit 0（by-value 引数 regression）

### Step 3（P3）: `const T&` 引数

- parse エラーなら `type_decl` / `post_type` 側
- call エラーなら `gen_assign_cast` / `gfunc_param_typed` 側
- 合格: `feat6a_ref_arg.cpp`:

```cpp
class Vec2 {
public:
    int x, y;
    int operator+(const Vec2& o);
};
int Vec2::operator+(const Vec2& o) { return x + o.x + o.y; }
int main() {
    Vec2 a, b;
    a.x = 1; a.y = 2; b.x = 3; b.y = 4;
    return a + b - 10;
}
```

### Step 4（P4）: 統合

- `feat6a_vec2.cpp` — §1.1 完全版
- 既存 `feat6a_call/min/op_plus` + `feat6b_*` 回帰

### Step 5（P5）: 完了

- [実装済み.md](実装済み.md) §14 制限から L1/L2 を削除
- [問題と原因.md](問題と原因.md) に FEAT-6A-P2 節追加

---

## 6. テスト計画

| ファイル | 検証内容 | 方式 |
|---------|---------|------|
| `dev/test/a9/feat6a_vec2_int.cpp` | struct 返却 + by-value 引数 | exit 0 |
| `dev/test/a9/feat6a_vec2_binop.cpp` | `c = a + b` 暗黙変換 | exit 0 |
| `dev/test/a9/feat6a_vec2_explicit.cpp` | `c = a.operator+(b)` | exit 0 |
| `dev/test/a9/feat6a_ref_arg.cpp` | `const T&` 引数 + int 返却 | exit 0 |
| `dev/test/a9/feat6a_vec2.cpp` | §1.1 完全版 | exit 0 |

`run_all.bat` は `a9\*.cpp` glob のまま（切り分け用 3 本もゲートに含める）。

---

## 7. 想定される落とし穴

| 症状 | 原因 | 対策 |
|------|------|------|
| `a + b` のみハング、`.operator+` は OK | B 経路のみ壊れ | P1 `cpp_finish_member_call` |
| 両方ハング | sret + member `this` ABI | hidden arg 順序を x64 ABI で確認 |
| compile OK、return 値ゴミ | struct 返却後 vtop 復元不足 | postfix ハンドラ L8410〜 と diff |
| `const Vec2&` 宣言で parse error | `type_decl` / ref 修飾 | P3 trace |
| 呼び出しで type mismatch | rvalue → ref 変換不足 | `gen_assign_cast` |
| int 返却 operator 回帰 | struct 修正の副作用 | `feat6a_op_plus.cpp` 常時実行 |
| `.c` 回帰 | C++ 分岐漏れ | `tcc_state->cpp` ガード |

---

## 8. 完了判定基準

- [ ] `feat6a_vec2.cpp` exit 0
- [ ] `feat6a_ref_arg.cpp` exit 0
- [ ] `feat6a_vec2_binop.cpp` / `feat6a_vec2_explicit.cpp` exit 0
- [ ] 既存 `feat6a_*.cpp` + `feat6b_*.cpp` exit 0
- [ ] x64 Release + Debug Win32 ビルド成功
- [ ] smoke / repro_double6 リグレッションなし
- [ ] [実装済み.md](実装済み.md) §14 更新（Phase 2 完了）
- [ ] [問題と原因.md](問題と原因.md) に FEAT-6A-P2 記録

---

## 9. ロードマップ（FEAT-6B 以降）

| 順 | 機能 | 状態 | 内容 |
|----|------|------|------|
| 10 | FEAT-6B | **完了** | `const` メンバ関数 |
| 11 | FEAT-6A-P2 | **次回着手** | struct 返却 operator + `const T&` |
| 12 | FEAT-6A-ext | 未着手 | 非メンバ operator / `operator[]` |
| 13 | FEAT-6B-P2 | 未着手 | const メソッド内書き込み拒否（任意） |
| 14 | FEAT-5C | 未着手 | 仮想 PMF（任意） |

---

## 10. 一文まとめ

**FEAT-6A-P2** では暗黙 operator 呼び出し経路 `cpp_finish_member_call()` を明示呼び出し（postfix `(`）の sret 実装に揃え、加えて `const T&` 仮引数の宣言・実引数変換を通して `Vec2 operator+(const Vec2&)` を完成させる。

---

# （アーカイブ）FEAT-6B — const メンバ関数

**完了**（2026-06-06）: `FuncAttr.func_const`、`post_type()` trailing const、`cpp_find_field_for_call()`、マングル `_C`、`const T* this`。テスト `feat6b_this`, `feat6b_const_obj`, `feat6b_defn`, `feat6b_overload`, `feat6b_overload_rev` exit 0。制限: const メソッド内メンバ書き込み拒否は未接続。詳細は [実装済み.md](実装済み.md) §15。

---

# （アーカイブ）FEAT-6A — 演算子オーバーロード Phase 1

**完了**（2026-06-06）: 合成トークン `__cpp_op_plus` 等、postfix `.operator+` / `expr_infix` の `a + b` 変換。テスト `feat6a_call.cpp`, `feat6a_min.cpp`, `feat6a_op_plus.cpp` exit 0。制限: int 返却 + by-value 引数 → **FEAT-6A-P2**。詳細は [実装済み.md](実装済み.md) §14。

---

# （アーカイブ）FEAT-5B — メンバポインタ

**完了**（2026-06-06）: `VT_MPTR`、DMP、非仮想 PMF、`feat5b_*.cpp` 4 本 exit 0。詳細は [実装済み.md](実装済み.md) §13。

---

# （アーカイブ）FEAT-5A — 仮想関数

**完了**（2026-06-05）: 仮想関数 Phase 1〜4。`feat6a_*.cpp` 7 本。

---

# （アーカイブ）FEAT-4D / 4C / 4B

**FEAT-4D 完了**: Base 初期化子 `: Base(args)` codegen。  
**FEAT-4C 完了**: クラス外 ctor 定義 `Foo::Foo(int) : a(x) {}`。  
**FEAT-4B 完了**: 暗黙 ctor 呼び出し `Foo f(args);`。詳細は [実装済み.md](実装済み.md) §10.2。

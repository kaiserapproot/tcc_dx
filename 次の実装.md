# BUG-14: 同名メンバ関数のマングル衝突（2026-07-25 修正）

**完了日**: 2026-07-25（`bug14_same_method_name.cpp` / `bug14_virtual_base_object.cpp` 2 本 exit 0、build.bat ゲート緑=真の exit 0）  
**作業ブランチ**: `fix/cpp-bug14-inline-virtual-symbol`（ベース: `test/cpp-12e-exec-gate`）  
**参照**: [問題と原因.md](問題と原因.md) BUG-14。FEAT-5C（仮想 PMF）検証中に発覚

- 真因: `cpp_build_func_mangle` のリンク名にクラス名が無く、`Base::who` と
  `Derived::who`（や無関係 2 クラスの `f()`）が同一 asm_label に。加えて
  `external_sym` が同名同シグネチャのメンバを別クラスでも同一 Sym に merge。
  → 本体・vtable relocation が上書きされ、非仮想=クラッシュ / 仮想=誤ディスパッチ。
- 修正（Approach B）: Sym トークンは bare 維持（field 照合・静的・const・
  デフォルト引数を壊さない）。`external_sym` を `cpp_pending_member_class` で
  クラス違いは別 Sym にフォーク + `cpp_build_func_mangle` に `Sym *cls` を渡し
  asm_label を `__tcc_<Class>__<name>_<args>` にクラス修飾。lookup は既に
  parent_class 照合なので追加変更なし。
- Approach A（クラス修飾トークン）は `sym->v==メンバ名` 前提の箇所を軒並み
  壊したため破棄（12e 実行ゲートが回帰を捕捉）。

**次の候補**: FEAT-5C（仮想 PMF。本修正で基盤が正しくなったので再開）、16 継続

---

# 12e-P1: run_all.bat 実行テストゲート（2026-07-25 完了）

**完了日**: 2026-07-25（`run_all.bat` を compile+実行の 2 フェーズ化、build.bat が真の exit 0=健全を返すよう改善）  
**作業ブランチ**: `test/cpp-12e-exec-gate`（ベース: `fix/cpp-bug13-p2-free-unnamed-param`）  
**参照**: ロードマップ 12e、[問題と原因.md](問題と原因.md) BUG-12（compile-only が実行時バグを見逃した実例）

- 動機: 従来 `run_all.bat` は `-c` の **compile-only**。BUG-12（`feat6a_big_struct`
  が compile 通過・実行時に異常終了）を見逃していた。加えて build.bat は
  既知ベースライン（a8 `sm3`/`sm6`/`t_assign` の compile 失敗）で常に exit 1 を
  返し、実 regression と区別できなかった。
- 変更（`run_all.bat`、tccgen.c 非改変）:
  - **Phase 1（compile）**: 全ソースを `-c`。既知の未実装ギャップ（静的メンバ
    `Counter::x=v` 代入未対応の `sm3`/`sm6`/`t_assign`）を `KNOWNFAIL` リストで
    **非ゲート化**（情報表示のみ）。新規 compile 失敗のみ FAILED に計上。
  - **Phase 2（execute・新規）**: exit 0 が保証された集合（a9 全 78 + a7 メンバ
    4 + smoke hello）を**リンク＋実行し exit 0 を必須**。exe は `%TEMP%` 出力で
    作業ツリーを汚さない。実行時 regression を `[RUN FAIL]` で明示・計上。
- 効果: `run_all.bat` が健全時に **exit 0** を返し、build.bat（既存の
  `call run_all.bat` ゲート）が**真の green（exit 0=健全 / exit 1=実 regression）**
  になった。BUG-12 型の実行時 regression を機械的に捕捉。
- 検証: 健全時 exit 0、非ゼロ終了テスト混入で `[RUN FAIL]`＋exit 1 を確認
  （自己テストで実証・削除済み）。build.bat フルで exit 0 到達。
- スコープ: 12e の「基盤整備」のうち **実行テストゲート**を実装。残る
  `sym_malloc` 初期化ポリシー / `cpp_this_sym` 設計見直しは、駆動する具体バグが
  無い投機的 core 改変のため**据え置き**（必要が生じた時点で着手）。

**次の候補**: 16（FEAT-5C 仮想 PMF・任意）、12e 残（sym_malloc / cpp_this_sym・駆動バグ待ち）

---

# BUG-13-P2: 自由関数の無名引数を C++ で許容（2026-07-25 完了）

**完了日**: 2026-07-25（`bug13_p2_free_unnamed.cpp` 新規 + `feat6a_ext4_free.cpp` を無名ダミー標準形へ更新、exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `fix/cpp-bug13-p2-free-unnamed-param`（ベース: `feat/cpp-feat6a-ext4-postop`）  
**参照**: [問題と原因.md](問題と原因.md) BUG-13 追補

- BUG-13 はメンバ限定だったため、自由関数の無名引数（非メンバ後置
  `operator++(T&, int)` の定石）は定義パス 12456 の `expect("identifier")` で
  拒否されていた。C++ は関数定義でも無名引数を許容するので、C++ モード時のみ
  無名引数へ匿名 id（`anon_sym++`）を付与するよう変更（`.c` は K&R 規則維持）。
- これで非メンバ後置 operator も標準の無名ダミーで書けるようになり、
  FEAT-6A-ext4 の「非メンバは named 必須」制限を解消。

**次の候補**: 12e（基盤整備）、16（FEAT-5C 仮想 PMF）

---

# FEAT-6A-ext4: 後置 operator `a++` / `a--`（`operator++(int)`）（2026-07-25 完了）

**完了日**: 2026-07-25（`feat6a_ext4_post_inc/post_dec/prepost/free/full.cpp` 5 本 exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `feat/cpp-feat6a-ext4-postop`（ベース: `fix/cpp-bug13-unnamed-member-param`）  
**参照**: [実装済み.md](実装済み.md) §14、ロードマップ 21、前提 [BUG-13](#bug-13-無名引数を持つメンバ関数の呼び出しでtccがクラッシュ2026-07-25完了)

- 前置基盤（FEAT-6A-ext3）の素直な拡張。前置＝メンバ 0 引数／非メンバ 1 引数、
  後置＝メンバ 1 引数（ダミー `int`）／非メンバ 2 引数を **arity** で分離
  （マングルは引数型付きなので `operator++()` と `operator++(int)` は別シンボル）。
- 新規 `cpp_try_member_postop`（arity1 メンバへダミー `int 0` を 1 引数で
  `cpp_finish_member_call`）と `cpp_try_free_postop`（`operator++(T&,int)` を
  2 引数解決）。接続点は postfix ループ `inc(1,tok)` 直前に `VT_STRUCT`
  ガード付きフックのみ（`.c`／スカラは従来経路を 1 バイトも変えず）。
- 発見: 後置の定石 `operator++(int)` が**無名ダミー引数**を使うため、
  無名メンバ引数で tcc がクラッシュする既存バグ（[BUG-13](#bug-13-無名引数を持つメンバ関数の呼び出しでtccがクラッシュ2026-07-25完了)）を露出。
  BUG-13 を先行修正してから本 FEAT を完成させた。
- 制限（当初）: 非メンバ後置 operator のダミーは named 必須だったが、
  **[BUG-13-P2](#bug-13-p2-自由関数の無名引数をcで許容2026-07-25完了) で解消**（自由関数の無名引数を C++ で許容）。

**次の候補**: 12e（基盤整備）、16（FEAT-5C 仮想 PMF）

---

# BUG-13: 無名引数を持つメンバ関数の呼び出しで tcc がクラッシュ（2026-07-25 完了）

**完了日**: 2026-07-25（`bug13_unnamed_member.cpp` / `bug13_unnamed_operator.cpp` 2 本 exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `fix/cpp-bug13-unnamed-member-param`（ベース: `fix/cpp-bug12-sret-this`）  
**参照**: [問題と原因.md](問題と原因.md) BUG-13

- 真因: `post_type` は無名引数を `v==SYM_FIELD`（名前トークン無し）で積む。
  自由関数は定義時の宣言子検査（`expect("identifier")`, 12456）で弾くが、
  メンバ本体は `skip_or_save_block` で保存され `gen_function`→`gfunc_prolog`
  で後から emit されるため検査を通らない。`gfunc_prolog` の
  `sym_push(v & ~SYM_FIELD)` が無名だと `sym_push(0,...)` となり、名前記録
  分岐が `table_ident[0 - TOK_IDENT]`（負 index）を触って AV。
- 修正: `cpp_finish_member_inlines` の登録前に `cpp_name_unnamed_params` で
  無名引数へ匿名トークン id（`anon_sym++`, `SYM_FIRST_ANOM` 以上）を合成付与。
  `sym_push` は匿名分岐に入り table_ident を触らない。型は無変更で
  オーバーロード解決・マングルに影響なし。
- 制限: クラス外定義 `int C::m(int){}` は通常定義パス（12456）に乗るため
  無名引数は従来どおりエラー（クラッシュではない）。

**次の候補**: FEAT-6A-ext4（後置 operator。本バグ修正が前提）

---

# BUG-12: 8 バイト超 struct 返却メンバ呼び出しの this/sret 引数順不整合（2026-07-22 完了）

**完了日**: 2026-07-22（`feat6a_big_struct.cpp` 初 exit 0、`bug12_big_struct_op/args.cpp` 新規 2 本 exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `fix/cpp-bug12-sret-this`  
**参照**: [問題と原因.md](問題と原因.md) BUG-12、ロードマップ 12d

- 真因: Win64 ABI で struct 返却がレジスタに収まらない（sret）場合、
  隠しポインタが arg0/RCX を占有し `this` は arg1/RDX に押し出される
  （`gfunc_prolog` の型リスト走査前の暗黙挿入）。呼び出し側 2 箇所
  （`cpp_finish_member_call`、postfix `(` の `cpp_member_this_pending`
  ブロック）は無条件に `this` を arg0 へ挿入しており、sret を arg1 へ
  押し出して食い違っていた。
- 修正: sret が積まれている場合は `this` の挿入位置を position 1 に変更
  （sret はそのまま arg0 に留める）。8 バイト以下/2 冪サイズの struct
  （レジスタ返却）は分岐に入らず無変化。
- 演算子オーバーロード経路（`cpp_finish_member_call`）も同型のバグを
  含んでいたため同時に修正（`bug12_big_struct_op.cpp` で検証）。

**次の候補**: 12e（基盤整備）、16（FEAT-5C 仮想 PMF）、後置 `a++`（`operator++(int)`）

---

# FEAT-4E-P2: クラス外 dtor 定義 `P::~P() {}`（2026-07-19 完了）

**完了日**: 2026-07-19（`feat4e_p2_basic/explicit/global.cpp` 3 本 exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `feat/cpp-ooc-dtor`  
**参照**: [問題と原因.md](問題と原因.md) FEAT-4E-P2 節、[実装済み.md](実装済み.md) §7

- `parse_cpp_scope_qualifier` に `~` を追加し、`*v` を FEAT-4E の既存
  マングル名 `__cpp_dtor_<Cls>` に差し替えるだけで、通常の関数定義パスが
  インライン dtor と同一シンボルに合流。呼び出し側は無修正。
- `cpp_peek_out_of_class_ctor` を `Cls :: ~ Cls (` にも対応（+ 同関数の
  `mem != cls` 失敗パスの unget 二重復元という潜在バグを除去）。
- 前提: クラス内 `~P();` 宣言が必要（C++ 規格どおり）。

**次の候補**: 12d（`feat6a_big_struct` sret 引数順調査）、12e（基盤整備）、16（FEAT-5C 仮想 PMF）、後置 `a++`（`operator++(int)`）

---

# BUG-11: extern "C" 領域直後の `class` パースエラー（2026-07-19 完了）

**完了日**: 2026-07-19（`bug11_externc_class.cpp` exit 0、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `fix/cpp-externc-class`  
**参照**: [問題と原因.md](問題と原因.md) BUG-11

- 真因: extern "C" 終端（ブロック `}` / 単一宣言 `;`）の 1 トークン先読みが
  `lex_c` 有効のまま字句解析され、C++ キーワードが降格双子のまま残留。
- 修正: `cpp_repromote_stale_lookahead()` 新設（降格双子はハッシュチェーン
  非連結なので `tok_alloc` の名前引き直しで元キーワードに戻る）。
  ブロック開始側も `lex_c++` を `{` 消費前に移動して対称化。

**次の候補**: ロードマップ 20（クラス外 dtor 定義 `P::~P()`）、12d（big_struct 調査）、12e（基盤整備）

---

# FEAT-4G: グローバル自動 ctor/dtor（2026-07-18 完了）

**完了日**: 2026-07-18（`feat4g_basic/args/dtor/multi.cpp` 4 本 exit 0、
`CTOR→MAIN→DTOR` 出力順確認、build.bat ゲート緑=ベースライン同等）  
**作業ブランチ**: `feat/cpp-feat4g`  
**参照**: [実装済み.md](実装済み.md) §16、[問題と原因.md](問題と原因.md) BUG-10

- 方式: decl() 登録 → `cpp_finish_global_dyns` で `.init_array`/`.fini_array`
  thunk 発行 → PE リンク時に `_tcc_cpp_start` 起動コードを C ソース注入して
  entry 差し替え（crt1.o 再ビルド回避）。
- 初期実装はコンパイル時/実行時 AV を計 8 個抱えていた（dynarray 誤用、
  TOK_EOF 終端漏れ、二重解放、NUL 終端漏れ、リンク時 `s1->cpp`=0、
  エントリ名規約、`data_offset` 未同期による thunk 上書き、win64 shadow
  space 欠落 + シンボル番号の加算値誤用）。詳細は BUG-10。
- 発見事項（未修正・別タスク）: `extern "C" {}` ブロック後の `class` が
  パースエラー / クラス外 dtor 定義 `P::~P()` 未対応。

---

# BUG-9: ローカル参照宣言の値コピー（2026-06-12 完了）

**完了日**: 2026-06-12（`bug9_a/b/c.cpp` 3 本 exit 0、build.bat 緑）  
**作業ブランチ**: `fix/cpp-bug9-struct-ref`  
**参照**: [問題と原因.md](問題と原因.md) BUG-9 節

- 真因: 参照のローカル宣言初期化にバインド（アドレス格納）経路が無く、
  struct は memcpy・スカラは値格納になっていた。struct だけでなく
  **スカラ参照宣言も壊れていた**（動いていたのは参照引数・参照戻り値のみ）。
- 修正: `init_putv()` ローカル枝に参照バインド追加 +
  unary identifier push で参照変数を自動 deref（関数シンボルは除外）。

---

# 次の実装: FEAT-4F（暗黙デフォルト ctor `Foo f;`）

**FEAT-4F 完了日**: 2026-06-12（`ctor4f_*.cpp` 5 本 exit 0）  
**プラン作成日**: 2026-06-12  
**作業ブランチ**: `feat/cpp-feat4f`（ベース: `feat/cpp-ctor-overload`）  
**参照**: [実装済み.md](実装済み.md) §8、FEAT-4B（`Foo f(args)` 書き換え）

## 1. 概要

ユーザー定義のデフォルト ctor `P()` を持つクラスのローカル変数宣言
`P f;` で、ctor を自動的に呼ぶ。現在は確保のみで ctor が呼ばれない
（Stage 4 制限）。

## 2. 設計

FEAT-4B の書き換え方式をそのまま流用する。decl() の FEAT-4B フック
（識別子の次が `(` かを見る分岐）の else 側に分岐を追加:

- 条件: `tok == ';' || tok == ','`、storage が
  `VT_STATIC | VT_EXTERN | VT_TYPEDEF` を含まない、
  新ヘルパ `cpp_class_has_default_ctor()`（0 引数 ctor フィールドの存在）が真。
- 処理: `decl_initializer_alloc` で確保後、`unget` LIFO で
  `var . Ctor ( )` を合成して `expr_eq()` → `vpop()`。
  `,` なら continue で次の宣言子（`P a, b;` の b にも再適用）。

## 3. スコープ外（ドキュメント化）

- グローバル `P g;`（FEAT-4G `.init_array` 待ち）
- `static P s;`（init-once ガード未実装のため除外）
- 全引数デフォルト値の ctor `P(int a = 5)` での暗黙呼び出し
- 派生クラスに ctor が無い場合の基底 ctor 自動合成
- ctor を持つが 0 引数 ctor が無いクラスの `P f;` は従来通り無警告で確保のみ
  （C++ ではエラーだが既存テスト保護を優先）

## 4. テスト

- `ctor4f_basic.cpp` — `P a;` で ctor 実行
- `ctor4f_overload.cpp` — `P()` / `P(int)` 共存で `P a; P b(3);`
- `ctor4f_multi.cpp` — `P a, b;` 複数宣言子
- `ctor4f_dtor.cpp` — デフォルト ctor + dtor 連携（FEAT-4E 回帰）
- `ctor4f_skip.cpp` — `P *p; P &r = a; P c = a;` が暗黙呼び出しを**しない**こと

---

# （アーカイブ）FEAT-CTOR-OV（ctor オーバーロード）

**FEAT-CTOR-OV 完了日**: 2026-06-12（`ctor_ov_*.cpp` / `ctor_base_conv.cpp` 5 本 exit 0）  
**プラン作成日**: 2026-06-12  
**作業ブランチ**: `feat/cpp-ctor-overload`（ベース: `feat/cpp-sig-match`）  
**参照**: [実装済み.md](実装済み.md) §8、[問題と原因.md](問題と原因.md) 完全シグネチャ照合節

## 調査結果（2026-06-12）

「ctor オーバーロードはクラスメンバ登録段階でブロック」という既知の制限は
**陳腐化**していた。完全シグネチャ照合 + 引数変換遅延（`feat/cpp-sig-match`）
の導入により、以下は**既に動作**する:

- インライン ctor overload（`P(int)` / `P(double)` 本体クラス内）
- クラス外定義 ctor overload（`P::P(int)` / `P::P(double)`）
- mem-initializer 付きクラス外 ctor overload
- インライン / クラス外 混在
- デフォルト ctor `P()` + 引数付き `P(int)` の共存 + 明示 `a.P()` 呼び出し

`Foo f(args);` は FEAT-4B で `f.Foo(args)` に書き換えられ postfix `.`/`(` を
通るため、シグネチャ照合・変換遅延・再解決がそのまま効く。

## 残る欠陥（本 FEAT のスコープ）

`cpp_emit_base_ctor_call()`（派生 ctor の mem-initializer `: Base(args)`）のみ
独自経路で、2 つの欠陥がある:

1. **引数パース前に解決確定**: `cpp_find_ctor_field()`（最初の ctor フィールド）
   → `cpp_lookup_member_func()` で先に global を選び、引数の型を見ない。
   基底に overload ctor があると誤解決（probe: `: B(2.5)` が `B(int)` に行き EXIT=-102）。
2. **`gfunc_param_typed()` を一切呼ばない**: overload が無くても
   `: B(2.9)` の double が int param へ未変換のまま渡る（EXIT=858993457 のゴミ）。

## 実装ステップ

- P1: `cpp_emit_base_ctor_call` — 引数パース**後**に `cpp_resolve_func_call`
  （生の引数型でスコア、cur=初期 ctor global で parent_class/const フィルタ）
  → vtop[-na] の sym/type を差し替え。
- P2: 解決済みプロトタイプへ `vrotb(na)` × na 回で各引数を `gfunc_param_typed`
  変換（args は func の上に na 個、this 挿入前なので rotate 対象は args のみ）。
  デフォルト引数が残れば `cpp_apply_default_args`。
- P3: テスト `dev/test/a9/ctor_ov_*.cpp`（インライン / クラス外 / mem-init /
  基底 overload / 基底単独 double→int 変換）。
- P4: 回帰（a7/a8/a9 + run_all + build.bat）+ ドキュメント + 1 コミット。

## 落とし穴

- vstack 配置は `[func, arg1..argN]`。`vrotb(na)` は top na 個（= args のみ）を
  回すこと。this 挿入後に回すと this が混ざる。
- `cpp_resolve_func_call` の `cur` は `__cpp_ctor_<Base>` トークンの global。
  parent_class は base_class と一致している前提（external_sym 定義パスで設定済み）。

---

# （アーカイブ）FEAT-6A-ext3（単項 operator `!` `-` `~` `++` `--`）

**FEAT-6A-ext3 完了日**: 2026-06-11（`feat6a_ext3_*.cpp` 6 本実行 exit 0）  
**現在の次タスク**: §6 ロードマップ参照（FEAT-6B-P2 / FEAT-4F など。BUG-7・完全シグネチャ照合は 2026-06-12 完了）  
**作業ブランチ**: `feat/cpp-feat6a-ext3`（ベース: `feat/cpp-feat6a-ext2`）  
**参照**: [実装済み.md](実装済み.md) §14、[問題と原因.md](問題と原因.md) BUG-8

> **実装中の発見 2 点**（詳細 [問題と原因.md](問題と原因.md) FEAT-6A-ext3 節）:
> 1. `.cpp` の空 `()` が `FUNC_OLD`（K&R 引数任意）だったため arity overload が「互換」誤判定 → cpp ゲートで `FUNC_NEW`（= `(void)`）に変更
> 2. `cpp_lookup_member_func` がシグネチャ無視で先勝ち解決（**BUG-7 と同根**）→ arity フィルタ追加（param 型レベルの選別は BUG-7 で対応）

## 1. 概要とスコープ

メンバ／非メンバの**単項 operator** を実装する。対象: `!` `-`（単項マイナス） `~` 前置 `++` `--`。

| 目標 | 内容 |
|------|------|
| G1 | メンバ単項 operator（0 引数）宣言・定義・暗黙呼び出し `-a` `!a` `~a` `++a` `--a` |
| G2 | 非メンバ単項 operator（1 引数）フォールバック |
| G3 | **arity 解決**: 同名 `operator-` の単項（0 引数）／二項（1 引数）共存 |
| G4 | 非 struct オペランドの従来動作を完全維持（`.c` 回帰ゼロ） |

**非目標**: 後置 `a++`（`operator++(int)` ダミー引数）、単項 `*`（deref）、`operator()`、virtual operator。

## 2. 設計

- `cpp_operator_suffix` に `!`→"not"、`~`→"compl"、`TOK_INC`→"inc"、`TOK_DEC`→"dec" 追加（`-` は既存 "minus" を共用、arity で区別）
- **`cpp_find_operator_member` に `want_args` パラメータ追加**（呼び出し側: binop/subscript=1、unop=0）。arity 一致候補を優先、無ければ従来の any-arity にフォールバック（既存動作保護）
- **新規 `cpp_try_member_unop(op_tok)`**: vtop が struct → 0 引数メンバ呼び出し（`cpp_finish_member_call(s, NULL, 0)`）
- **新規 `cpp_try_free_unop(op_tok)`**: `cpp_resolve_free_func_call(v, 1)` で 1 引数解決（`cpp_try_free_binop` の 1 引数版）
- `unary()` の `'!'` / `'~'` / `'-'` / `TOK_INC` / `TOK_DEC`（前置）各 case に「メンバ → 非メンバ → 従来処理」フック

## 3. 落とし穴

| 症状 | 対策 |
|------|------|
| `-x`（int/float）回帰 | フックは `VT_STRUCT` ガードで即 return 0 |
| 二項 `a - b` が単項 field を拾う | `want_args=1` の arity 優先 |
| 明示 `.operator-()` の arity | `cpp_find_field_for_call` は未変更（混在クラスでは先勝ち。制限として記録） |
| 定数畳み込み経路 | struct は const 式に入らないため影響なし |

## 4. テスト（`dev/test/a9/`）

| ファイル | 検証 |
|---------|------|
| `feat6a_ext3_neg.cpp` | メンバ `operator-()` + `-a` |
| `feat6a_ext3_not.cpp` | メンバ `operator!()` + `!a`（if 文での利用含む） |
| `feat6a_ext3_arity.cpp` | 単項／二項 `operator-` 共存 + 両呼び出し |
| `feat6a_ext3_incdec.cpp` | 前置 `++a` / `--a`（`T&` 返却） |
| `feat6a_ext3_free.cpp` | 非メンバ `operator-(const T&)` / `operator!(const T&)` |
| `feat6a_ext3_full.cpp` | 統合 |

---

# （アーカイブ）FEAT-6A-ext2（代入系 operator `=` / `+=`）

**FEAT-6A-ext2 完了日**: 2026-06-11（`feat6a_ext2_*.cpp` 6 本実行 exit 0）  
**現在の次タスク**: §9 ロードマップ参照（BUG-7 候補 / FEAT-6A-ext3 単項 operator など）  
**作業ブランチ**: `feat/cpp-feat6a-ext2`（ベース: `fix/cpp-this-deref`）  
**参照**: [実装済み.md](実装済み.md) §14–15、[TCC_CPP.md](TCC_CPP.md) Stage 6、[問題と原因.md](問題と原因.md) BUG-8

> **着手時に BUG-8 を発見・修正**（2026-06-11）: `operator=` の正準形 `return *this;` で tcc 自体が AV。真因は `gen_function` の this 型構築（`pointed_type` が `{VT_STRUCT, ref=NULL}` を返す）。別ブランチ `fix/cpp-this-deref` で修正済み — [問題と原因.md](問題と原因.md) BUG-8 参照。  
> **FEAT-6A-ext 完了**（2026-06-11）: 非メンバ operator / `operator[]`、実行テスト exit 0 + BUG-6 修正。詳細はアーカイブ節参照。

---

## 1. 概要

Stage 6 の続きとして **代入演算子のオーバーロード**を実装する。対象は (1) メンバ `operator=`、(2) 複合代入 `operator+=`（メンバ／非メンバ）、(3) 同テーブルで安価に対応できる `-=` `*=` `/=`。

既存基盤（合成トークン `__cpp_op_*`、`cpp_try_member_binop`、`cpp_try_free_binop`、参照返却）をそのまま再利用し、新しい接続点は **`expr_eq()`** のみ（代入は右結合のため `expr_infix` を通らない）。

### 1.1 目的のコード（完了時）

```cpp
class Acc {
public:
    int v;
    Acc& operator=(const Acc& o) { v = o.v; return *this; }
    Acc& operator+=(const Acc& o) { v += o.v; return *this; }
};

int main() {
    Acc a, b, c;
    a.v = 1; b.v = 2; c.v = 3;
    a = b;          /* メンバ operator= */
    a += c;         /* メンバ operator+= */
    b = a = c;      /* T& 返却によるチェーン */
    return a.v + b.v - 6;   /* 3 + 3 - 6 = 0 */
}
```

### 1.2 現状の挙動（FEAT-6A-ext 完了後）

| 構文 | 結果 |
|------|------|
| `a = b`（struct、operator= なし） | ✅ memcpy コピー（`vstore`） |
| `a = b`（struct、メンバ `operator=` あり） | **operator= を呼ばない**（memcpy のまま） |
| `a += b`（struct） | **コンパイルエラー**（`gen_op` が struct を拒否） |
| `operator=` / `operator+=` の宣言 | **parse error**（`cpp_operator_suffix` 未登録 → `unsupported operator`） |
| `a + b` メンバ／非メンバ | ✅ FEAT-6A / 6A-ext |
| `obj[i]` | ✅ FEAT-6A-ext |

---

## 2. 目標と非目標

### 2.1 目標（FEAT-6A-ext2 スコープ）

| # | 内容 |
|---|------|
| G1 | メンバ `operator=` の宣言・定義・`a = b` 暗黙呼び出し（メンバのみ — C++ 標準） |
| G2 | `operator=` 未宣言クラスの `a = b` は **従来 memcpy を完全維持**（回帰ゼロ） |
| G3 | メンバ `operator+=` `-=` `*=` `/=` の宣言・定義・暗黙呼び出し |
| G4 | 非メンバ `operator+=` 等（メンバ解決失敗後のフォールバック） |
| G5 | `T&` 返却によるチェーン `a = b = c` |
| G6 | `feat6a_ext2_full.cpp` exit 0 |
| G7 | 既存 `feat6a_*` / `feat6b_*` / a7 / a8 回帰維持、`.c` 回帰ゼロ |

### 2.2 非目標（別 FEAT へ）

| 内容 | 理由 / 別タスク |
|------|----------------|
| コピー初期化 `Acc c = a;` での operator= 呼び出し | C++ 的にも初期化は copy ctor の領域（operator= は呼ばれないのが正しい）。現状の memcpy 初期化を維持 |
| 暗黙の copy-assignment operator 合成 | 未宣言時は従来 memcpy = 実質同等。合成不要 |
| `%=` `&=` `|=` `^=` `<<=` `>>=` | 需要が出たら `cpp_operator_suffix` にケース追加のみ |
| 単項 operator（`!` `-` `++` `--`） | FEAT-6A-ext3 |
| `operator()` | 将来 |
| const メソッド内メンバ書き込み拒否 | FEAT-6B-P2（任意） |
| BUG-7（const オーバーロード誤解決） | 別ブランチ（ロードマップ 12b） |

---

## 3. 現状コードの分析

### 3.1 トークンと既存テーブル

- 字句解析は `+=` を **単一トークン `TOK_A_ADD`（0xb0）** にする（[tcc.h](tcc.h) L1166〜）。`-=`/`*=`/`/=` は `TOK_A_SUB`/`TOK_A_MUL`/`TOK_A_DIV`。
- `TOK_ASSIGN(t)` マクロが複合代入の判定、`TOK_ASSIGN_OP(t)` が `+=` → `'+'` 変換（[tcc.h](tcc.h) L1177〜）。
- `cpp_operator_suffix()`（[tccgen.c](tccgen.c) L608）に case を足せば、宣言パース `cpp_parse_operator_token()`（L638）は **変更不要**で `operator=` / `operator+=` を受理する（`tok` がそのまま `'='` / `TOK_A_ADD` で来る）。

### 3.2 接続点は `expr_eq()`（L9241）

```c
static void expr_eq(void)
{
    expr_cond();
    if ((t = tok) == '=' || TOK_ASSIGN(t)) {
        test_lvalue();
        next();
        if (t == '=') {
            expr_eq();
        } else {
            vdup();
            expr_eq();
            gen_op(TOK_ASSIGN_OP(t));   /* struct はここで落ちる */
        }
        vstore();                        /* struct = struct は memcpy */
    }
}
```

- 代入は右結合のため `expr_infix`（L9030）には来ない。**operator 解決のフックは `expr_eq` に置く**。
- RHS 評価後の vstack は `[lhs(lvalue), rhs]` — `cpp_try_member_binop`（L6210）が期待するレイアウトと **同一**。そのまま再利用できる。
- `cpp_try_member_binop` は LHS が `VT_STRUCT` でない／メンバ未発見なら **vstack 無変更で 0 を返す** → 失敗時に従来 `vstore()` へ安全にフォールバックできる。

### 3.3 設計方針

| 経路 | 方針 |
|------|------|
| `t == '='` | RHS 評価後に `cpp_try_member_binop('=')`。成功 → `vstore()` をスキップ（vtop は operator= の返却値）。失敗 → 従来 `vstore()`（memcpy 維持）。**非メンバ `operator=` は試さない**（C++ 標準でメンバ限定） |
| `TOK_ASSIGN(t)` かつ LHS が `VT_STRUCT` | `vdup()`/`gen_op` を**迂回**して RHS 評価 → `cpp_try_member_binop(t)` → 失敗時 `cpp_try_free_binop(t)` → 両方失敗なら `tcc_error`（従来も struct `+=` はエラーなので回帰なし） |
| `TOK_ASSIGN(t)` かつ LHS が非 struct | **従来経路を 1 バイトも変えない**（`vdup` → `gen_op` → `vstore`） |

- マングル: `__tcc___cpp_op_assign_<args>` / `__tcc___cpp_op_plus_assign_<args>`（既存 `cpp_build_func_mangle` がそのまま動く）。
- `T&` 返却・`const T&` 引数は FEAT-6A-P2 / 6A-ext の基盤を再利用。チェーンは `expr_eq` の右結合再帰で自然に成立。

### 3.4 再利用できる既存経路

| 経路 | 用途 |
|------|------|
| `cpp_operator_suffix` / `cpp_operator_field_tok`（L608〜） | suffix 追加のみ |
| `cpp_parse_operator_token` / `cpp_parse_operator_decl_name`（L638〜） | 変更不要 |
| `cpp_try_member_binop`（L6210） | `=` / `+=` メンバ解決（変更不要の見込み） |
| `cpp_try_free_binop`（L6106） | 非メンバ `+=` 解決（変更不要の見込み） |
| `cpp_find_operator_member` / `cpp_prepare_member_func_call` / `cpp_finish_member_call` | メンバ呼び出し |
| 参照返却（`s->type.t & VT_REFERENCE` → `indir()`、L6101） | `T&` 返却 + チェーン |

---

## 4. 実装ステップ（フェーズ分割）

各フェーズ末: x64 Release ビルド緑 + 既存 a9 PASS。  
**規律**: 本 FEAT 全体で 1 feature branch = 1 コミット。壊れた状態でコミットしない。

| Phase | 内容 | 観測可能な進捗 |
|-------|------|----------------|
| **P0** | 事前 grep + 現状エラーの記録 | 現象記録 |
| **P1** | メンバ `operator=` | `feat6a_ext2_assign*.cpp` exit 0 |
| **P2** | メンバ複合代入 `+=` `-=` `*=` `/=` | `feat6a_ext2_pa_member.cpp` exit 0 |
| **P3** | 非メンバ複合代入 | `feat6a_ext2_pa_free.cpp` exit 0 |
| **P4** | 統合 + 回帰 + ドキュメント | `feat6a_ext2_full.cpp` exit 0 |

### Step 0（P0）: 事前調査

```batch
findstr /n "cpp_operator_suffix cpp_try_member_binop cpp_try_free_binop" tccgen.c
findstr /n "expr_eq" tccgen.c
findstr /n "TOK_A_ADD TOK_ASSIGN" tcc.h
dir /s /b dev\test\a9\*.cpp
```

- `operator=` 宣言を含む 5 行 `.cpp` で現状の `unsupported operator` を確認・記録。
- struct `a = b`（operator= なし）の memcpy 動作を再確認（回帰基準）。

### Step 1（P1）: メンバ `operator=`

| Step | 内容 |
|------|------|
| P1-1 | `cpp_operator_suffix` に `case '=': return "assign";` 追加 |
| P1-2 | クラス内宣言・クラス外定義 `Acc& Acc::operator=(const Acc&)` のマングル確認 |
| P1-3 | `expr_eq` — `t == '='` の RHS 評価後に `cpp_try_member_binop('=')`、成功時 `vstore` スキップ |
| P1-4 | `T&` 返却 + チェーン `a = b = c` 確認 |

合格: `feat6a_ext2_assign.cpp` + `feat6a_ext2_chain.cpp` exit 0。`feat6a_ext2_memcpy.cpp`（operator= なし）回帰。

### Step 2（P2）: メンバ複合代入

| Step | 内容 |
|------|------|
| P2-1 | `cpp_operator_suffix` に `TOK_A_ADD` → `"plus_assign"`、`TOK_A_SUB` → `"minus_assign"`、`TOK_A_MUL` → `"mul_assign"`、`TOK_A_DIV` → `"div_assign"` |
| P2-2 | `expr_eq` — `TOK_ASSIGN(t)` かつ LHS `VT_STRUCT` の分岐新設（`vdup`/`gen_op` 迂回 → RHS 評価 → `cpp_try_member_binop(t)`） |
| P2-3 | 失敗時 `tcc_error`（P3 で free フォールバック挿入予定の位置） |

合格: `feat6a_ext2_pa_member.cpp` exit 0。`.c` の `x += 1` / `int` 系回帰なし（smoke + CUnit）。

### Step 3（P3）: 非メンバ複合代入

- P2-3 の位置に `cpp_try_free_binop(t)` フォールバックを挿入（メンバ → 非メンバ → エラー）。
- 合格: `feat6a_ext2_pa_free.cpp` exit 0。

### Step 4（P4）: 統合・完了

- `feat6a_ext2_full.cpp` — §1.1 完全版 + `+=` 統合
- Release\|x64 + Debug\|Win32 + `build.bat`
- [実装済み.md](実装済み.md) §7 / §14 更新
- [問題と原因.md](問題と原因.md) に問題があれば追記

**コミットメッセージ例**: `feat(cpp): FEAT-6A-ext2 assignment operators`

---

## 5. テスト計画

配置: `dev/test/a9/`（`run_all.bat` の `a9\*.cpp` glob に自動包含。compile-only ゲート + 手動 link/実行で exit 0 確認）

| 優先 | ファイル | Phase | 検証内容 |
|------|---------|-------|---------|
| 1 | `feat6a_ext2_assign.cpp` | P1 | メンバ `operator=` + `a = b`（int メンバ書き換えで検証） |
| 2 | `feat6a_ext2_chain.cpp` | P1 | `T&` 返却 + `a = b = c` |
| 3 | `feat6a_ext2_memcpy.cpp` | P1 | **operator= なし**クラスの `a = b` が従来 memcpy（回帰） |
| 4 | `feat6a_ext2_pa_member.cpp` | P2 | メンバ `operator+=` / `operator-=` |
| 5 | `feat6a_ext2_pa_free.cpp` | P3 | 非メンバ `operator+=(A&, const A&)` |
| 6 | `feat6a_ext2_full.cpp` | P4 | §1.1 完全版 |

**回帰**: 既存 a9 全本（feat6a 8 + feat6a_ext 6 + feat6b 5）+ a8/a7 + smoke + repro_double6 + CUnit（`.c` の通常代入・複合代入）。

---

## 6. 主要タッチポイント

| 関数 / 箇所 | 行 | 変更内容 |
|-------------|-----|---------|
| `cpp_operator_suffix` | L608 | `'='` + `TOK_A_ADD`〜`TOK_A_DIV` の case 追加 |
| **`expr_eq`** | L9241 | `=` フック + struct 複合代入分岐（**本 FEAT の中核、最小差分**） |
| `cpp_try_member_binop` | L6210 | 変更なしの見込み（回帰保護） |
| `cpp_try_free_binop` | L6106 | 変更なしの見込み |
| `cpp_parse_operator_token` | L638 | 変更不要（suffix テーブル参照のみ） |

**触らない**: `expr_infix`、`vstore` 本体、`struct_decl()` 内 `gen_function()`、x86_64 `arg*8` ABI。

---

## 7. 想定される落とし穴

| 症状 | 原因 | 対策 |
|------|------|------|
| `.c` / 非 struct の `x += 1` 回帰 | `expr_eq` の分岐が広すぎ | struct 分岐は `tcc_state->cpp && (vtop->type.t & VT_BTYPE) == VT_STRUCT` で厳密ガード。判定は **`vdup()` より前**（RHS 評価前に LHS 型は確定済み） |
| operator= なし struct の `a = b` が壊れる | フック挿入位置ミス | `cpp_try_member_binop` は失敗時 vstack 無変更 → 失敗時は必ず従来 `vstore()` に到達させる。`feat6a_ext2_memcpy.cpp` で常時検証 |
| `a = b = c` で値が消える | operator= が `void` 返却 / `vstore` スキップ漏れ | 成功時は `cpp_finish_member_call` の返却値を vtop に残し `vstore` を呼ばない（1 値だけ残す） |
| `test_lvalue()` が operator= 経路で誤動作 | フック位置 | `test_lvalue()` は既存位置のまま（operator= でも `this` 取得に lvalue 必須） |
| 一時オブジェクト RHS（`a += b + c`）で落ちる | `const T&` バインド | FEAT-6A-P2 の参照バインド経路を通ることを確認（`feat6a_ext_free_plus` と同型） |
| 複合代入の二重評価 | `vdup` 残留 | struct 経路では `vdup()` を呼ばない（operator 呼び出しは LHS を 1 回だけ評価） |
| マングル不一致 | decl/call でトークン名不一致 | `get_tok_str(v)` を trace（`__cpp_op_assign` / `__cpp_op_plus_assign`） |
| `operator=` の free 関数宣言が通ってしまう | 宣言側ノーチェック | 許容（呼び出し側でメンバのみ解決）。余裕があれば宣言時 `tcc_error` |
| BUG-7（const overload 誤解決）と干渉 | `cpp_find_operator_member` 共用 | 本 FEAT では const overload の operator= を**テストに含めない**（BUG-7 修正後に追加） |

---

## 8. 完了判定基準

- [x] `operator=` / `operator+=` 宣言・クラス外定義 compile OK
- [x] `a = b`（operator= あり）が operator 呼び出し、なしが memcpy（両テスト exit 0）
- [x] `a = b = c` チェーン exit 0（`(a += b) += c` も exit 0）
- [x] メンバ／非メンバ `a += b` exit 0
- [x] `feat6a_ext2_full.cpp` exit 0
- [x] 既存 a9/a8/a7 + smoke + repro_double6 + CUnit 回帰なし（a9 実行 20 本 exit 0。run_all の a8 調査用 sm3/sm6/t_assign 失敗はベースラインと同一の既知問題）
- [x] x64 Release + Debug Win32 ビルド成功
- [x] [実装済み.md](実装済み.md) 更新
- [x] feature branch 上で 1 コミット（前提の BUG-8 は `fix/cpp-this-deref` で別コミット）

---

## 9. ロードマップ（FEAT-6A-ext2 以降）

| 順 | 機能 | 状態 | 内容 |
|----|------|------|------|
| 12 | FEAT-6A-ext | **完了** | 非メンバ operator / `operator[]`（実行 exit 0、2026-06-11） |
| 12a | BUG-6 | **完了（2026-06-11）** | `this` 仮引数 Sym の r/c 未初期化クラッシュ修正 |
| 12b | BUG-7 | **完了（2026-06-12）** | const オーバーロード誤解決。真因 2 件: parse_btype クラス名パスの const 脱落 + `cpp_resolve_func_call` の無差別再解決（`fix/cpp-bug7`） |
| 12g | 完全シグネチャ照合 | **完了（2026-06-12）** | 同 arity・param 型違い overload。`cpp_lookup_member_func` 2 パス化 + postfix `(` の引数変換遅延（`feat/cpp-sig-match`）。残: スコア順位付け・曖昧エラー |
| 12h | FEAT-CTOR-OV | **完了（2026-06-12）** | ctor オーバーロード。本体は 12g で成立済みと判明。`cpp_emit_base_ctor_call` のパース後解決 + 引数変換を修正（`feat/cpp-ctor-overload`） |
| 17 | FEAT-4F | **完了（2026-06-12）** | 暗黙デフォルト ctor `P a;`。decl の 4B フック拡張で `a.P()` に書き換え（`feat/cpp-feat4f`）。発見: struct ローカル参照 `P &r = a;` が実行時ハング（→ BUG-9） |
| 12i | BUG-9 | **完了（2026-06-12）** | ローカル参照宣言の値コピー（struct ハング / スカラ誤動作）。`init_putv` 参照バインド + identifier 自動 deref（`fix/cpp-bug9-struct-ref`） |
| 12c | テスト修正 | **完了（2026-06-12）** | `feat6a_ref_arg.cpp` の `+ y` 欠落を修正、exit 0（`fix/cpp-12c-ref-arg-test`） |
| 12d | BUG-12 | **完了（2026-07-22）** | `feat6a_big_struct` 8 バイト超 sret メンバ呼び出しの引数順不整合。`cpp_finish_member_call` + postfix `(` 汎用呼び出しの 2 箇所を修正（`fix/cpp-bug12-sret-this`） |
| 12e | 基盤整備 | **一部完了（2026-07-25）** | `run_all.bat` 実行テストゲート完了（compile+実行の 2 フェーズ化、build.bat 真の exit 0 化）（`test/cpp-12e-exec-gate`）。残: sym_malloc 初期化ポリシー、`cpp_this_sym` 設計見直し（駆動バグ待ちで据え置き） |
| 12f | BUG-8 | **完了（2026-06-11）** | `this` 明示デリファレンス（`this->x` / `*this`）AV 修正（`fix/cpp-this-deref`） |
| 13 | FEAT-6A-ext2 | **完了（2026-06-11）** | 代入 operator（`=` `+=` `-=` `*=` `/=`）。実行 exit 0 |
| 14 | FEAT-6A-ext3 | **完了（2026-06-11）** | 単項 operator（`!` `-` `~` 前置 `++` `--`）+ 空 `()` = `(void)` 化 + `cpp_lookup_member_func` arity フィルタ。後置 `++(int)` は 21 で対応 |
| 15 | FEAT-6B-P2 | **完了（2026-06-12）** | const メソッド内書き込み拒否。`this` の VT_CONSTANT を被指示型へ移動 + `verify_assign_cast` C++ エラー化（const T& 宛先は除外）（`feat/cpp-feat6b-p2`） |
| 16 | FEAT-5C | 未着手 | 仮想 PMF（任意） |
| 18 | FEAT-4G | **完了（2026-07-18）** | グローバル自動 ctor/dtor（`.init_array` thunk + PE 起動コード注入）。実装時の 8 バグは BUG-10 参照（`feat/cpp-feat4g`） |
| 19 | BUG-11 | **完了（2026-07-19）** | extern "C" 領域直後の `class` パースエラー。先読みトークンの降格残留を `cpp_repromote_stale_lookahead()` で再昇格（`fix/cpp-externc-class`） |
| 20 | FEAT-4E-P2 | **完了（2026-07-19）** | クラス外 dtor 定義 `P::~P() {}`。`parse_cpp_scope_qualifier` の `~` 対応 + OOC peek 拡張で既存 `__cpp_dtor_*` に合流（`feat/cpp-ooc-dtor`） |
| 12d | BUG-12 | **完了（2026-07-22）** | 8 バイト超 sret メンバ呼び出しの this/sret 引数順（`fix/cpp-bug12-sret-this`） |
| 21a | BUG-13 | **完了（2026-07-25）** | 無名引数メンバ関数呼び出しの AV（`sym_push` の table_ident 負 index）。`cpp_name_unnamed_params` で無名引数へ匿名 id 付与（`fix/cpp-bug13-unnamed-member-param`） |
| 21 | FEAT-6A-ext4 | **完了（2026-07-25）** | 後置 operator `a++` / `a--`（`operator++(int)`）。arity で前置と分離、postfix ループにフック。前提 BUG-13（`feat/cpp-feat6a-ext4-postop`） |
| 21b | BUG-13-P2 | **完了（2026-07-25）** | 自由関数の無名引数を C++ で許容（12456 検査を cpp のみ匿名 id 付与へ）。非メンバ後置 operator の named 必須制限を解消（`fix/cpp-bug13-p2-free-unnamed-param`） |

---

## 10. 一文まとめ

**FEAT-6A-ext2** では、`cpp_operator_suffix` に `=`/`+=` 系 suffix を追加し、代入の唯一の経路である `expr_eq()` に「struct 限定でメンバ → 非メンバ → 従来処理」のフックを最小差分で挿入して、既存の `cpp_try_member_binop` / `cpp_try_free_binop` / 参照返却基盤をそのまま代入演算子に転用する。

---

# （アーカイブ）FEAT-6A-ext — 非メンバ operator / `operator[]`

**完了**（2026-06-11、実行テスト exit 0 + BUG-6 修正）: `cpp_try_free_binop`（非メンバ二項）、`cpp_try_cpp_subscript`（メンバ／非メンバ `operator[]`、非メンバは **TCC 拡張**）、`expr_infix` / postfix `[` 接続。テスト `feat6a_ext_*.cpp` 6 本 exit 0。クラッシュの真因は BUG-6（`this` 仮引数 Sym の r/c 未初期化）— [問題と原因.md](問題と原因.md) 参照。詳細は [実装済み.md](実装済み.md) §14。

---

# （アーカイブ）FEAT-6A-P2 — struct 返却 operator + const T& 引数

**完了**（2026-06-06）: `cpp_finish_member_call` を postfix `(` の sret 実装に揃え、`const T&` 仮引数の宣言・実引数変換を接続。テスト `feat6a_vec2_int.cpp`, `feat6a_vec2_binop.cpp`, `feat6a_vec2_explicit.cpp`, `feat6a_ref_arg.cpp`, `feat6a_vec2.cpp` exit 0。詳細は [実装済み.md](実装済み.md) §14 Phase 2。

---

# （アーカイブ）FEAT-6B — const メンバ関数

**完了**（2026-06-06）: `FuncAttr.func_const`、`post_type()` trailing const、`cpp_find_field_for_call()`、マングル `_C`、`const T* this`。テスト `feat6b_this`, `feat6b_const_obj`, `feat6b_defn`, `feat6b_overload`, `feat6b_overload_rev` exit 0。制限: const メソッド内メンバ書き込み拒否は未接続。詳細は [実装済み.md](実装済み.md) §15。

---

# （アーカイブ）FEAT-6A — 演算子オーバーロード Phase 1

**完了**（2026-06-06）: 合成トークン `__cpp_op_plus` 等、postfix `.operator+` / `expr_infix` の `a + b` 変換。テスト `feat6a_call.cpp`, `feat6a_min.cpp`, `feat6a_op_plus.cpp` exit 0。制限: int 返却 + by-value 引数 → **FEAT-6A-P2**。詳細は [実装済み.md](実装済み.md) §14。

---

# （アーカイブ）FEAT-5B — メンバポインタ

**完了**（2026-06-06）: `VT_MPTR`、DMP、非仮想 PMF、`feat5b_*.cpp` 4 本 exit 0。詳細は [実装済み.md](実装済み.md) §13。

---

# （アーカイブ）FEAT-5A — 仮想関数

**完了**（2026-06-05）: 仮想関数 Phase 1〜4。`feat5a_*.cpp` 7 本。

---

# （アーカイブ）FEAT-4D / 4C / 4B

**FEAT-4D 完了**: Base 初期化子 `: Base(args)` codegen。  
**FEAT-4C 完了**: クラス外 ctor 定義 `Foo::Foo(int) : a(x) {}`。  
**FEAT-4B 完了**: 暗黙 ctor 呼び出し `Foo f(args);`。詳細は [実装済み.md](実装済み.md) §10.2。

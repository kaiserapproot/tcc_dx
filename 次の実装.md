# 次の実装: FEAT-6A-ext（非メンバ operator / operator[]）

**FEAT-6A-ext 完了日**: 2026-06-11（実行テスト exit 0 確認 + BUG-6 修正）  
**現在の次タスク**: **BUG-7 候補**（`feat6b_overload` const オーバーロード誤解決）または **FEAT-6A-ext2**（§10 参照）  
**作業ブランチ**: `fix/cpp-this-uninit-rc`（BUG-6）  
**参照**: [実装済み.md](実装済み.md) §14–15、[TCC_CPP.md](TCC_CPP.md) Stage 6、[問題と原因.md](問題と原因.md) BUG-6

> **FEAT-6A-P2 完了**（2026-06-06）: struct 返却 operator、`const T&` 引数。テスト `feat6a_vec2*.cpp` 5 本 + Phase 1 回帰 exit 0。  
> **FEAT-6B 完了**（2026-06-06）: trailing `const`、`cpp_find_field_for_call()`、マングル `_C`。  
> **FEAT-6A Phase 1 完了**（2026-06-06）: メンバ二項 operator、`a + b` 暗黙変換。

---

## 1. 概要

FEAT-6A Phase 1/2 および FEAT-6B 完了後、Stage 6 の残りは **非メンバ operator** と **`operator[]`** である。既存の合成トークン `__cpp_op_*` と `cpp_resolve_func_call` を再利用し、メンバ解決失敗後のフォールバックおよび postfix `[` 分岐を接続する。

### 1.1 目的のコード（完了時）

```cpp
class Vec2 {
public:
    int x, y;
    int& operator[](int i);
};

int& Vec2::operator[](int i) {
    return i ? y : x;
}

Vec2 operator+(const Vec2& a, const Vec2& b) {
    Vec2 r;
    r.x = a.x + b.x;
    r.y = a.y + b.y;
    return r;
}

int main() {
    Vec2 a, b, c;
    a.x = 1; a.y = 2;
    b.x = 3; b.y = 4;
    c = a + b;                  /* 非メンバ operator+ */
    return c[0] + c[1] - 10;    /* operator[] */
}
```

### 1.2 現状の挙動（FEAT-6B 完了後）

| 構文 | 結果 |
|------|------|
| メンバ `Vec2 operator+(const Vec2&)` + `a + b` | ✅ exit 0（`feat6a_vec2.cpp`） |
| 非メンバ `Vec2 operator+(const Vec2&, const Vec2&)` + `a + b` | **未対応**（通常 `gen_op`） |
| メンバ `int& operator[](int)` + `obj[i]` | **未対応**（生のポインタ演算） |
| 非メンバ `T operator[](const Vec&, int)` + `obj[i]` | **未対応** |
| 明示 `.operator+` / `.operator[]` | メンバ `+` のみ ✅（`[]` は未パース） |

---

## 2. 目標と非目標

### 2.1 目標（FEAT-6A-ext スコープ）

| # | 内容 |
|---|------|
| G1 | グローバル（非メンバ）二項 operator の宣言・定義・呼び出し |
| G2 | 暗黙変換 `a + b` でメンバが無い場合に非メンバ operator を解決 |
| G3 | メンバ `operator[]` の宣言・定義・`obj[i]` 暗黙呼び出し |
| G4 | 非メンバ `operator[]`（左辺を第 1 引数として解決）— **TCC 拡張**（ISO C++98 ではメンバのみ） |
| G5 | `feat6a_ext_full.cpp` exit 0 |
| G6 | 既存 `feat6a_*.cpp` / `feat6b_*.cpp` 回帰維持 |

### 2.2 非目標（別 FEAT へ）

| 内容 | 別タスク |
|------|---------|
| 代入系 operator（`=` `+=` 等） | FEAT-6A-ext2 |
| 単項 operator（`!` `-` `++`） | FEAT-6A-ext3 |
| `operator()` 関数呼び出し演算子 | 将来 |
| virtual operator | FEAT-5A 連携（任意） |
| namespace 内 operator | namespace 未実装のため除外 |
| ISO C++98 準拠の非メンバ `operator[]` | 標準外。**TCC 拡張**として G4 のみサポート（g++ 非互換） |
| const メソッド内メンバ書き込み拒否 | FEAT-6B-P2（任意） |

---

## 3. 現状コードの分析

### 3.1 既存の operator 基盤（再利用）

[tccgen.c](tccgen.c) L532〜559:

- `cpp_operator_suffix()` — `+` `-` `*` `/` → `"plus"` 等
- `cpp_operator_field_tok()` — 合成トークン `__cpp_op_plus` を生成
- マングル: `__tcc___cpp_op_plus_<args>`（`cpp_build_func_mangle`）
- 暗黙二項: `cpp_try_member_binop()` → 左オペランドの struct にメンバを探す

```c
static int cpp_try_member_binop(int op_tok)
{
    field = cpp_find_field_for_call(&vtop[-1].type,
                                    cpp_operator_field_tok(op_tok), &cumofs);
    if (!field || (field->type.t & VT_BTYPE) != VT_FUNC)
        return 0;
    ...
    cpp_finish_member_call(s, &rhs, 1);
    return 1;
}
```

### 3.2 未接続の 2 経路

| 経路 | 現状 | 必要な変更 |
|------|------|-----------|
| **A. 非メンバ二項** | `cpp_try_member_binop` 失敗 → 通常 `gen_op(t)` | メンバ失敗後に `cpp_try_free_binop()` でグローバル `__cpp_op_*` を `cpp_resolve_func_call` |
| **B. `operator[]`** | postfix `[` は生のポインタ演算（L8266〜） | struct 型なら `operator[]` メンバ／非メンバを優先 |

```c
else if (tok == '[') {
    next();
    gexpr();
    gen_op('+');
    indir();
    skip(']');
}
```

### 3.3 設計方針（マングル・シンボル）

非メンバ operator も **同じ合成トークン名** `__cpp_op_plus` を関数エントリ名に使う。

- 宣言: `Vec2 operator+(const Vec2&, const Vec2&);`  
  → `type_decl` で `v = __cpp_op_plus`（既存 `cpp_parse_operator_decl_name` を流用）
- マングル: `__tcc___cpp_op_plus_<T><T>`（`parent_class == NULL`）
- 呼び出し: `vtop` に `[left, right]` を積み、`sym_find(__cpp_op_plus)` + `cpp_resolve_func_call(v, 2)`

メンバ／非メンバの区別は **シンボルの `parent_class`** と **引数個数（+ hidden this）** で行う。

C++ 標準: **メンバ operator が非メンバより優先** → 必ずメンバを先に試す（現行順を維持）。

### 3.4 再利用できる既存経路

| 経路 | 用途 |
|------|------|
| `cpp_operator_field_tok` / `cpp_parse_operator_decl_name` | 宣言パース（`[]` 追加のみ） |
| `cpp_resolve_func_call` | 非メンバ 2 引数 overload 解決 |
| `cpp_finish_member_call` | メンバ `operator[]` 呼び出し |
| postfix `(` ハンドラ | 非メンバ struct 返却 sret の参照実装 |
| `cpp_find_field_for_call` | メンバ `operator[]` + const overload |
| `gfunc_sret()` / `gfunc_call()` | x64 Win ABI |

---

## 4. 実装方針

### 4.1 Phase 1 — 非メンバ二項 operator

**新規**: `cpp_try_free_binop(int op_tok)`

1. `vtop[-1]` / `vtop[0]` を実引数として vstack に保持
2. `v = cpp_operator_field_tok(op_tok)` で `sym_find(v)` + `cpp_resolve_func_call(v, 2)`
3. 通常関数呼び出し codegen（hidden `this` なし）
4. struct 返却は postfix `(` ハンドラの sret 経路を参照

**変更**: `expr_infix` — `cpp_try_member_binop` 失敗後 `cpp_try_free_binop` を試行

**宣言**: グローバル `operator+` が `external_sym` + マングルされることを確認（不足なら `decl()` を最小修正）。クラス外定義 `Vec2 operator+(...)` は修飾名なしパス（`Class::operator+` はメンバ専用のまま）。

### 4.2 Phase 2 — メンバ `operator[]`

1. `cpp_operator_suffix` に `'['` → `"index"` を追加（`__cpp_op_index`）
2. `cpp_parse_operator_token` — `operator[]` を `[` `]` でパース
3. postfix `.operator[]` / `->operator[]` — 既存 operator 分岐を `[]` 対応
4. postfix `[` — `vtop` が struct かつ `__cpp_op_index` メンバあり → `cpp_finish_member_call`
5. 戻り値が参照（`int&`）の場合、代入 `obj[i] = x` まで通す

**制約**: `[` ハンドラは配列・ポインタと競合する。**struct/class のみ** operator 解決を試し、それ以外は従来の `+`/`indir` へ。

### 4.3 Phase 3 — 非メンバ `operator[]`

**新規**: `cpp_try_free_subscript()`

- `obj[i]` でメンバ `operator[]` 無し → `operator[](obj, i)` としてグローバル解決
- 引数順: 左辺 struct、右辺 index（C++ 標準通り）

---

## 5. 実装ステップ（フェーズ分割）

各フェーズ末: x64 Release ビルド緑 + 既存 a9 feat6a/feat6b PASS。  
**規律**: 1 フェーズ = 1 コミット（本 FEAT 全体で 1 コミットでも可。壊れた状態でコミットしない）。

| Phase | 内容 | 観測可能な進捗 |
|-------|------|----------------|
| **P0** | 事前 grep + 切り分けテスト配置 | 現象記録 |
| **P1** | 非メンバ二項 operator | `feat6a_ext_free_*.cpp` exit 0 |
| **P2** | メンバ `operator[]` | `feat6a_ext_sub_member.cpp` exit 0 |
| **P3** | 非メンバ `operator[]` | `feat6a_ext_sub_free.cpp` exit 0 |
| **P4** | 統合 + 回帰 + ドキュメント | `feat6a_ext_full.cpp` exit 0 |

### Step 0（P0）: 事前調査

```batch
findstr /n "cpp_try_member_binop cpp_operator cpp_finish_member_call" tccgen.c
findstr /n "else if (tok == '\[')" tccgen.c
dir /s /b dev\test\a9\*.cpp
```

### Step 1（P1）: 非メンバ二項 operator

| Step | 内容 |
|------|------|
| P1-1 | `cpp_try_free_binop(int op_tok)` 新規 |
| P1-2 | `expr_infix` — メンバ失敗後 free binop |
| P1-3 | グローバル `operator+` 宣言・クラス外定義のマングル確認 |
| P1-4 | struct 返却非メンバ operator の sret 確認 |

合格: `feat6a_ext_free_int.cpp` + `feat6a_ext_free_plus.cpp` exit 0。`feat6a_min.cpp`（メンバ優先）回帰。

### Step 2（P2）: メンバ `operator[]`

| Step | 内容 |
|------|------|
| P2-1 | `cpp_operator_suffix` に `'['` → `"index"` |
| P2-2 | `cpp_parse_operator_token` — `operator[]` パース |
| P2-3 | postfix `.operator[]` / `->operator[]` |
| P2-4 | postfix `[` — struct 限定でメンバ `operator[]` |
| P2-5 | 参照返却 + 代入 `obj[i] = x` |

合格: `feat6a_ext_sub_member.cpp` exit 0。任意: `feat6a_ext_sub_const.cpp`（FEAT-6B 連携）。

### Step 3（P3）: 非メンバ `operator[]`

- `cpp_try_free_subscript()` — メンバ失敗後グローバル解決
- 合格: `feat6a_ext_sub_free.cpp` exit 0

### Step 4（P4）: 統合・完了

- `feat6a_ext_full.cpp` — §1.1 完全版
- Release\|x64 + Debug\|Win32 + `build.bat`
- [実装済み.md](実装済み.md) §14 制限削除
- [問題と原因.md](問題と原因.md) に FEAT-6A-ext 節追加

**コミットメッセージ例**: `feat(cpp): FEAT-6A-ext free operators and operator[]`

---

## 6. テスト計画

配置: `dev/test/a9/`（`run_all.bat` の `a9\*.cpp` glob に自動包含）

| 優先 | ファイル | Phase | 検証内容 |
|------|---------|-------|---------|
| 1 | `feat6a_ext_free_int.cpp` | P1 | 非メンバ `int operator+(A, A)` + `a + b` |
| 2 | `feat6a_ext_free_plus.cpp` | P1 | `Vec2 operator+` + struct 返却 + `const T&` |
| 3 | `feat6a_ext_sub_member.cpp` | P2 | `int& operator[](int)` + 読み書き |
| 4 | `feat6a_ext_sub_const.cpp` | P2 | `const int& operator[](int) const` + `cv[i]` 読み取り |
| 5 | `feat6a_ext_sub_free.cpp` | P3 | 非メンバ `operator[]`（**TCC 拡張**、g++ 非互換） |
| 6 | `feat6a_ext_full.cpp` | P4 | §1.1 完全版 |

**回帰**: 既存 a9 全本 + a8/a7 + smoke + repro_double6。

---

## 7. 主要タッチポイント

| 関数 / 箇所 | 役割 |
|-------------|------|
| `cpp_operator_suffix` / `cpp_parse_operator_token` | `[]` トークン追加 |
| `cpp_try_member_binop` | 変更なし（回帰保護） |
| **`cpp_try_free_binop`**（新規） | 非メンバ二項解決 |
| **`cpp_try_subscript`**（新規） | `[` 用メンバ／非メンバ統合 |
| `expr_infix` | free binop フォールバック |
| postfix `[`（L8266 付近） | struct 時 operator 分岐 |
| `type_decl` / `decl` | グローバル operator 宣言 |
| `cpp_resolve_func_call` | 2 引数 overload 解決 |
| `cpp_finish_member_call` | メンバ `operator[]` 呼び出し |

**触らない**: `struct_decl()` 内での `gen_function()` 呼び出し、x86_64 `arg*8` ABI。

---

## 8. 想定される落とし穴

| 症状 | 原因 | 対策 |
|------|------|------|
| メンバと非メンバの二重解決 | 順序ミス | メンバ → 非メンバ → `gen_op` |
| `int arr[3]; arr[i]` が壊れる | `[` 分岐が広すぎ | `VT_STRUCT` + `type.ref` のみ |
| 非メンバ struct 返却でハング | sret 経路 | postfix `(` 実装を通常関数呼び出しに適用 |
| `operator[]` parse error | `[]` が declarator と競合 | `cpp_parse_operator_token` で `]` 必須 |
| 非メンバ `operator[]` 解決失敗 | `next()`/`gexpr()` 後に `return 0` で C 配列パスへ落ちる | 事前 `cpp_has_free_func` + 失敗時 `tcc_error`（トークン消費後はフォールバック不可） |
| マングル不一致 | decl/call で token 名不一致 | `get_tok_str(v)` を trace |
| int 返却 operator 回帰 | free binop の副作用 | `feat6a_op_plus.cpp` 常時実行 |
| `.c` 回帰 | C++ 分岐漏れ | 全分岐 `tcc_state->cpp` ガード |

---

## 9. 完了判定基準

- [x] 非メンバ `a + b` compile OK（`feat6a_ext_free_int/plus`）
- [x] メンバ／非メンバ `obj[i]` compile OK（`feat6a_ext_sub_*.cpp`）
- [x] `feat6a_ext_full.cpp` compile OK
- [x] 既存 `feat6a_*.cpp` + `feat6b_*.cpp` compile OK（`run_all.bat`）
- [x] x64 Release + Debug Win32 ビルド成功
- [x] smoke / repro_double6 リグレッションなし
- [x] [実装済み.md](実装済み.md) §14 更新（FEAT-6A-ext 完了）
- [x] [問題と原因.md](問題と原因.md) に記録（BUG-6 として 2026-06-11 記載）
- [x] 実行テスト exit 0（`feat6a_ext_*` 全 6 本、2026-06-11。クラッシュの真因は BUG-6 `this` 未初期化 r/c — [問題と原因.md](問題と原因.md) 参照）

---

## 10. ロードマップ（FEAT-6A-ext 以降）

| 順 | 機能 | 状態 | 内容 |
|----|------|------|------|
| 12 | FEAT-6A-ext | **完了** | 非メンバ operator / `operator[]`（実行 exit 0、2026-06-11） |
| 12a | BUG-6 | **完了（2026-06-11）** | `this` 仮引数 Sym の r/c 未初期化によるクラッシュ修正（[問題と原因.md](問題と原因.md)） |
| 12b | BUG-7 候補 | **未着手** | `feat6b_overload`/`_rev` の const オーバーロード誤解決（EXIT=±100） |
| 12c | テスト修正 | **未着手** | `feat6a_ref_arg.cpp` の `+ y` 欠落（テスト自体のバグ） |
| 12d | 調査 | **未着手** | `feat6a_big_struct` 8 バイト超 sret メンバ呼び出しの引数順 |
| 13 | FEAT-6A-ext2 | 未着手 | 代入 operator（`=` `+=`） |
| 14 | FEAT-6B-P2 | 未着手 | const メソッド内書き込み拒否（任意） |
| 15 | FEAT-5C | 未着手 | 仮想 PMF（任意） |
| 16 | FEAT-4F | 未着手 | 暗黙デフォルト ctor `Foo f;` |
| 17 | FEAT-4G | 未着手 | グローバル自動 ctor/dtor |

---

## 11. 一文まとめ

**FEAT-6A-ext** では、既存の合成トークン `__cpp_op_*` と `cpp_resolve_func_call` を再利用し、(1) メンバ解決失敗後の非メンバ二項 operator、(2) struct 限定の `operator[]` 暗黙呼び出し、の 2 経路を `expr_infix` と postfix `[` に接続して Stage 6 の operator サブセットを完成させる。

---

# （アーカイブ）FEAT-6A-P2 — struct 返却 operator + const T& 引数

**完了**（2026-06-06）: `cpp_finish_member_call` を postfix `(` の sret 実装に揃え、`const T&` 仮引数の宣言・実引数変換を接続。テスト `feat6a_vec2_int.cpp`, `feat6a_vec2_binop.cpp`, `feat6a_vec2_explicit.cpp`, `feat6a_ref_arg.cpp`, `feat6a_vec2.cpp` exit 0。詳細は [実装済み.md](実装済み.md) §14 Phase 2。

---

# （アーカイブ）FEAT-6B — const メンバ関数

**完了**（2026-06-06）: `FuncAttr.func_const`、`post_type()` trailing const、`cpp_find_field_for_call()`、マングル `_C`、`const T* this`。テスト `feat6b_this`, `feat6b_const_obj`, `feat6b_defn`, `feat6b_overload`, `feat6b_overload_rev` exit 0。制限: const メソッド内メンバ書き込み拒否は未接続。詳細は [実装済み.md](実装済み.md) §15。

---

# （アーカイブ）FEAT-6A — 演算子オーバーロード Phase 1

**完了**（2026-06-06）: 合成トークン `__cpp_op_plus` 等、postfix `.operator+` / `expr_infix` の `a + b` 変換。テスト `feat6a_call.cpp`, `feat6a_min.cpp`, `feat6a_op_plus.cpp` exit 0。制限: int 返却 + by-value 引数 → **FEAT-6A-P2**。詳細は [実装済み.md](実装済み.md) §14。

---

# （アーカイブ）FEAT-5B — メンバポインタ

**完了**（2026-06-06）: `VT_MPTR`、DMP、非仮想 PMF、`feat5b_*.cpp` 4 本 exit 0。詳細は [実装済み.md](実装済み.md) §13。

---

# （アーカイブ）FEAT-5A — 仮想関数

**完了**（2026-06-05）: 仮想関数 Phase 1〜4。`feat5a_*.cpp` 7 本。

---

# （アーカイブ）FEAT-4D / 4C / 4B

**FEAT-4D 完了**: Base 初期化子 `: Base(args)` codegen。  
**FEAT-4C 完了**: クラス外 ctor 定義 `Foo::Foo(int) : a(x) {}`。  
**FEAT-4B 完了**: 暗黙 ctor 呼び出し `Foo f(args);`。詳細は [実装済み.md](実装済み.md) §10.2。

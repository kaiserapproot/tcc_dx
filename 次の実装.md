# 次の実装: FEAT-6A-P2（演算子オーバーロード Phase 2）

**FEAT-6B 完了日**: 2026-06-06  
**現在の次タスク**: **FEAT-6A-P2**（struct 返却 operator 修正 + `const T&` 引数）  
**参照**: [実装済み.md](実装済み.md)、[TCC_CPP.md](TCC_CPP.md) Stage 6、[問題と原因.md](問題と原因.md)

> **FEAT-6B 完了**（2026-06-06）: trailing `const`、`cpp_find_field_for_call()`、マングル `_C`。テスト `feat6b_*.cpp` 4 本 + `feat6b_overload_rev` exit 0。  
> **FEAT-6A 完了**（2026-06-06）: メンバ二項 operator Phase 1。  
> **FEAT-5B 完了**（2026-06-06）: DMP / 非仮想 PMF。

---

## 1. 概要

FEAT-6A Phase 1 で残った **2 つの既知制限**を解消する。

| # | 制限 | 現象 |
|---|------|------|
| L1 | struct 返却 operator | `Vec2 operator+(Vec2 o)` が compile/link 後 **ランタイムハング** |
| L2 | `const T&` 引数 | `.` / `->` 経由で `const Vec2&` 引数 operator が parse/type エラー |

### 1.1 目的のコード（完了時）

```cpp
class Vec2 {
public:
    int x, y;
    Vec2 operator+(const Vec2& o);
};

Vec2 Vec2::operator+(const Vec2& o) {
    Vec2 r;
    r.x = x + o.x;
    r.y = y + o.y;
    return r;
}

int main() {
    Vec2 a, b, c;
    a.x = 1; a.y = 2;
    b.x = 3; b.y = 4;
    c = a + b;
    return c.x + c.y - 10;   /* 0 */
}
```

---

## 2. 目標と非目標

### 2.1 目標

| # | 内容 |
|---|------|
| G1 | struct 返却メンバ operator の codegen（sret / 戻り値領域） |
| G2 | `const T&` をメンバ operator 引数・呼び出しで使用可能 |
| G3 | `feat6a_vec2.cpp`（§1.1 相当）exit 0 |
| G4 | 既存 `feat6a_*.cpp` / `feat6b_*.cpp` 回帰維持 |

### 2.2 非目標

| 内容 | 別タスク |
|------|---------|
| 代入系 operator（`=` `+=`） | FEAT-6A-ext |
| `operator[]` / 非メンバ operator | FEAT-6A-ext |
| virtual operator | FEAT-5A 連携（任意） |
| const メソッド内メンバ書き込み拒否 | FEAT-6B 残課題（G5 一部） |

---

## 3. 調査ポイント

### 3.1 struct 返却 operator ハング

- `cpp_finish_member_call()` / `gfunc_call()` の sret 経路
- メンバ関数の struct 返却は非 operator メソッドで動作するか（比較用 smoke）
- `cpp_try_member_binop()` が二項 operator 呼び出し後 vstack を正しく残すか

### 3.2 `const T&` 引数

- postfix `.` / `->` で `VT_REFERENCE | VT_CONSTANT` 引数の field アクセス
- `cpp_prepare_member_func_call()` に渡る引数型

---

## 4. 実装ステップ

| Phase | 内容 | テスト |
|-------|------|--------|
| **P1** | struct 返却の原因切り分け（非 operator メンバ vs operator） | 最小 repro |
| **P2** | operator 呼び出し codegen 修正 | `feat6a_vec2.cpp` exit 0 |
| **P3** | `const T&` 引数パース・呼び出し修正 | `feat6a_ref_arg.cpp` |
| **P4** | 回帰 + Debug\|Win32 | 全 a9 |

---

## 5. テスト計画

| ファイル | 内容 | 方式 |
|---------|------|------|
| `dev/test/a9/feat6a_vec2.cpp` | §1.1 完全版（struct 返却 + `const T&`） | exit 0 |
| `dev/test/a9/feat6a_ref_arg.cpp` | `const T&` 引数のみ（int 返却） | exit 0 |

---

## 6. 完了判定

- [ ] `feat6a_vec2.cpp` exit 0
- [ ] 既存 `feat6a_*.cpp` + `feat6b_*.cpp` exit 0
- [ ] x64 Release + Debug Win32 ビルド成功
- [ ] [実装済み.md](実装済み.md) 更新

---

## 7. ロードマップ

| 順 | 機能 | 状態 |
|----|------|------|
| 10 | FEAT-6B | **完了** |
| 11 | FEAT-6A-P2 | **次回着手** |
| 12 | FEAT-6A-ext | 未着手 |
| 13 | FEAT-5C | 未着手 |

---

## 8. 一文まとめ

**FEAT-6A-P2** では struct 返却 operator の sret/codegen バグと `const T&` 引数経路を修正し、§1.1 の `Vec2 operator+` を end-to-end で通す。

---

# （アーカイブ）FEAT-6B — const メンバ関数

**完了**（2026-06-06）: `FuncAttr.func_const`、`post_type()` trailing const、`cpp_find_field_for_call()`、マングル `_C`、`const T* this`。テスト `feat6b_this`, `feat6b_const_obj`, `feat6b_defn`, `feat6b_overload`, `feat6b_overload_rev` exit 0。制限: const メソッド内メンバ書き込み拒否は未接続。詳細は [実装済み.md](実装済み.md) §15。

---

# （アーカイブ）FEAT-6A — 演算子オーバーロード

**完了**（2026-06-06）: 合成トークン `__cpp_op_plus` 等、postfix `.operator+` / `expr_infix` の `a + b` 変換。テスト `feat6a_call.cpp`, `feat6a_min.cpp`, `feat6a_op_plus.cpp` exit 0。制限: int 返却のみ実行検証済み → FEAT-6A-P2 へ。詳細は [実装済み.md](実装済み.md) §14。

---

# （アーカイブ）FEAT-5B — メンバポインタ

**完了**（2026-06-06）: `VT_MPTR`、DMP、非仮想 PMF、`feat5b_*.cpp` 4 本 exit 0。詳細は [実装済み.md](実装済み.md) §13。

---

# （アーカイブ）FEAT-5A — 仮想関数

**完了**（2026-06-05）: 仮想関数 Phase 1〜4。`feat5a_*.cpp` 7 本。

---

# （アーカイブ）FEAT-4D / 4C / 4B

**FEAT-4D 完了**: Base 初期化子 `: Base(args)` codegen。  
**FEAT-4C 完了**: クラス外 ctor 定義 `Foo::Foo(int) : a(x) {}`。  
**FEAT-4B 完了**: 暗黙 ctor 呼び出し `Foo f(args);`。詳細は [実装済み.md](実装済み.md) §10.2。

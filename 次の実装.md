# 次の実装: FEAT-4B — 変数宣言での暗黙コンストラクタ呼び出し

**作成日**: 2026-05-30
**前提**: FEAT-4A（ctor 初期化子リスト codegen）完了済み
**参照**: [問題と原因.md](問題と原因.md) FEAT-4A セクション、[実装済み.md](実装済み.md)

---

## 1. 概要

クラス型の変数宣言時に、`Foo f(arg1, arg2);` の構文でコンストラクタを暗黙呼び出しできるようにする。現状は ctor を実装したが、明示的に `Foo f; f.Foo(3, 4);` の 2 段書きが必要で標準 C++ から大きく外れている。

### 1.1 目的のコード

```cpp
class Foo {
public:
    int a, b;
    Foo(int x, int y) : a(x), b(y) {}
};

int main() {
    Foo f(3, 4);              // ← この構文を動かす
    return f.a + f.b - 7;
}
```

### 1.2 現状の挙動

- `Foo f(3, 4);` は parse error: `decl()` が `(3, 4)` を関数宣言子の引数リストと誤解釈して "identifier required" を発火
- 標準書法が使えないため、ctor 検証は `Foo f; f.Foo(3, 4);` の非標準書法で行うしかない

---

## 2. 目標と非目標

### 2.1 目標（スコープ内）

| # | 構文 | 説明 |
|---|------|------|
| G1 | `Foo f(arg, ...);` | 引数付きの ctor 呼び出し（1 引数以上） |
| G2 | `Foo f;` | 既存動作維持（暗黙のデフォルト ctor は呼ばない、Stage 4 制限） |
| G3 | クラス内インライン定義 ctor | FEAT-4A で動作確認済みのものを再利用 |

### 2.2 非目標（スコープ外、別タスクへ）

| 構文 | 別タスク |
|------|---------|
| `Foo f = Foo(3, 4);` コピー初期化 | 将来 |
| `Foo f{3, 4};` ブレース初期化 | 将来 |
| `Foo *p = new Foo(3, 4);` `new` 式 | 将来 |
| 暗黙のデフォルト ctor 呼び出し（`Foo f;` で空の ctor を呼ぶ） | FEAT-4E と合わせて検討 |
| クラス外定義 ctor `Foo::Foo(int x)` の parse | FEAT-4C |
| デストラクタの自動呼び出し | FEAT-4E |

---

## 3. 現状コードの分析

### 3.1 parse 失敗の位置

`decl()` の関数定義/変数宣言判定パス（[tccgen.c:~9700](e:/work/work_github/tpp/tcc_dx/tccgen.c#L9700) 付近の `while (1) { /* iterate thru each declaration */ }` ループ）。

`Foo f(3, 4);` のパースフロー:
1. `parse_btype` で `Foo` を typedef → btype.t = VT_STRUCT、btype.ref = class_sym
2. `type_decl(&type, ...)` で `f(3, 4)` をパース
3. `type_decl` 内で `f` を識別子、`(3, 4)` を関数引数列と解釈
4. でも `3, 4` は識別子ではないので "identifier required" エラー

### 3.2 既存の ctor 呼び出し経路（FEAT-4A）

`f.Foo(3, 4)` パス:
1. unary で `f` を識別子として push
2. post-op `.` で field `Foo` を find_field
3. `cpp_lookup_member_func` で `__cpp_ctor_Foo` global sym を取得
4. `cpp_member_this_pending = 1` で `this = &f` を仮スタック
5. `(3, 4)` を引数列として push
6. `gfunc_call(3)` で `__cpp_ctor_Foo(&f, 3, 4)` を呼ぶ

→ **この経路をそのまま再利用すれば良い**。問題は parser 側で `Foo f(3, 4)` を「変数宣言 + ctor 呼び出し」と認識すること。

---

## 4. 実装方針

### 4.1 全体戦略

`decl()` の変数宣言処理で、対象型が C++ クラス で `tok == '('` の場合:
1. 普通の変数（VT_LOCAL の領域確保）を `decl_initializer_alloc` で作る
2. 直後に `&var.<ctor_field>` 経由で `__cpp_ctor_<Class>` を呼ぶ instruction を emit

### 4.2 検出条件

以下を**すべて**満たすと「暗黙 ctor 呼び出し」と判断:

| 条件 | 確認方法 |
|------|---------|
| C++ モード | `tcc_state->cpp` |
| 変数の型が class | `(type.t & VT_BTYPE) == VT_STRUCT && type.ref` に ctor field が存在 |
| 続トークンが `(` | `tok == '('` |
| 局所変数 / グローバル変数の宣言文脈 | `l == VT_LOCAL` または `l == VT_CONST`（後者は当面ローカルのみ対応で OK） |

### 4.3 implementation 構造案

```c
/* decl() の宣言処理ループ内、type_decl の直後 */
if (tcc_state->cpp
    && (type.t & VT_BTYPE) == VT_STRUCT
    && type.ref
    && tok == '(') {
    /* 1) 普通の変数領域を確保 */
    /* 2) 続く (args) を ctor 呼び出しに変換 */
    cpp_emit_ctor_call(v, &type, l);
    skip(';');
    continue;
}
```

### 4.4 `cpp_emit_ctor_call` の処理

```c
static void cpp_emit_ctor_call(int var_tok, CType *class_type, int storage)
{
    Sym *class_sym = class_type->ref;
    Sym *ctor_field;
    Sym *var_sym;
    int ctor_global_tok;
    Sym *ctor_global;

    /* 1) 変数を sym_push (VT_LOCAL | VT_LVAL, loc 確保) */
    /* type_size & align で loc -= size */
    /* var_sym = sym_push(var_tok, class_type, VT_LOCAL | VT_LVAL, loc) */

    /* 2) ctor field を find_field で取得（クラス名と同名の VT_FUNC field）*/
    ctor_field = find_field_by_name(class_sym, class_sym->v & ~SYM_STRUCT);
    if (!ctor_field) {
        /* ctor が定義されてない → 引数なしならゼロ初期化、引数ありなら error */
    }

    /* 3) ctor の global sym を __cpp_ctor_<Class> 名で取得 */
    ctor_global = cpp_lookup_member_func(ctor_field, class_type);

    /* 4) f.Foo(args) と同等の vstack 構築 */
    /* push ctor sym, push &var (this), push args */
    /* この部分はちょうど post-op `(` ハンドラの呼び出し時と同じパス */

    /* 5) gfunc_call() で呼ぶ */
}
```

### 4.5 代替案: parse 時点で「暗黙呼び出し」へ変換

`decl()` で `Foo f(3, 4);` を検出したら、内部的に以下のトークン列を `unget` で再生成して通常パスに流す:

```
Foo f;
f.Foo(3, 4);
```

メリット: 既存の `f.Foo(...)` パスをそのまま使えるので新規コード最小。
デメリット: トークン操作が複雑、`f` を 2 度書き戻すデリケートな処理が必要。

**推奨は §4.4 の vstack 直接構築方式**。FEAT-4A の経験で経路が把握できているため。

---

## 5. 実装ステップ

### Step 1: 検出ロジックの追加

[tccgen.c:~9700](e:/work/work_github/tpp/tcc_dx/tccgen.c#L9700) の `decl()` 変数宣言ループに、上記検出条件を追加。最初は「検出したらエラーメッセージで報告」して、parse の流れだけ確認する。

### Step 2: ctor field の検索

class_sym->next chain を走査し、`v == (class_sym->v & ~SYM_STRUCT) | SYM_FIELD` かつ `type.t & VT_BTYPE == VT_FUNC` の field を探すヘルパーを追加。

```c
static Sym *cpp_find_ctor_field(Sym *class_sym);
```

### Step 3: 変数領域の確保

`decl_initializer_alloc` の通常パスを流用。`has_init = 0` で初期化なしの変数として確保。

### Step 4: ctor 呼び出しの vstack 構築

ポイント:
- `vset(&ctor_global->type, VT_CONST | VT_SYM, 0)` で ctor 関数 sym を vtop へ
- `vset(&var->type, VT_LOCAL | VT_LVAL, var->c)` で var を vtop へ
- `gaddrof()` で `&var` に
- `cpp_member_this_pending = 1` を立てて引数 push 後の自動挿入を利用
- post-op `(` ハンドラの引数 parse 経路を `gexpr` で再現

→ **既存の post-op `(` ハンドラを関数化して呼び出す**のがクリーン。例: `cpp_parse_call_args(Sym *func, int reverse_args)`。

### Step 5: gfunc_call で呼び出し emit

通常の関数呼び出しと同じ。戻り値（void）は無視。

### Step 6: テスト作成

```cpp
// dev/test/a8/feat4b_basic.cpp
class Foo { public: int a; Foo(int x) : a(x) {} };
int main() {
    Foo f(5);
    return f.a - 5;
}
```

```cpp
// dev/test/a8/feat4b_two_args.cpp
class Pt { public: int x, y; Pt(int a, int b) : x(a), y(b) {} };
int main() {
    Pt p(3, 4);
    return p.x + p.y - 7;
}
```

```cpp
// dev/test/a8/feat4b_no_ctor.cpp
class Plain { public: int v; };
int main() {
    Plain p;        // ctor なし、既存動作維持
    p.v = 42;
    return p.v - 42;
}
```

### Step 7: リグレッション確認

Stage 3/4 既存テスト全 PASS、smoke / repro_double6 OK、x64 Release + Debug Win32 両ビルド成功。

---

## 6. 実装の難所と対策

### 6.1 `Foo f(other_var);` の曖昧性

C++ では `Foo f(other_var);` が「変数宣言 + ctor 呼び出し」と「関数宣言 `f` を返す `Foo`」の両方に解釈できる（most vexing parse）。

**対策**: TCC では「`(` の中身が型なら関数宣言、それ以外は ctor 呼び出し」というシンプル判定で十分（標準 C++ より緩い）。

```c
/* tok == '(' で次が type なら関数宣言、それ以外なら ctor 呼び出し */
```

### 6.2 グローバル変数での暗黙 ctor

`Foo g(5);` をグローバルスコープに書いた場合、ctor は `.init_array` で起動時に呼ぶ必要がある。

**対策**: Stage 4 内では**ローカル変数のみ**サポート。グローバル変数は次フェーズへ。

### 6.3 ctor が複数オーバーロードある場合

```cpp
class Foo { public: Foo(int); Foo(int, int); };
Foo f(3);    // どちらの ctor を呼ぶ？
```

**対策**: 既存の `cpp_resolve_func_call` をそのまま使えば引数数と型でディスパッチされる。FEAT-4A の経験で動作確認済み。

### 6.4 ctor が宣言のみ（クラス内インライン本体なし）

```cpp
class Foo { public: Foo(int); };  /* 本体なし */
Foo f(5);  /* ctor 本体が見つからずリンクエラー */
```

クラス外定義は FEAT-4C で対応するので、それまでは**クラス内インライン定義の ctor のみ**サポート。

---

## 7. 完了判定基準

- [ ] `dev/test/a8/feat4b_basic.cpp` exit 0
- [ ] `dev/test/a8/feat4b_two_args.cpp` exit 0
- [ ] `dev/test/a8/feat4b_no_ctor.cpp` exit 0（既存動作維持）
- [ ] 既存の Stage 3/4 全テスト PASS
- [ ] smoke / repro_double6 リグレッションなし
- [ ] x64 Release + Debug Win32 両ビルド成功
- [ ] CUnit `test_stage4_*` がコケない
- [ ] [問題と原因.md](問題と原因.md) に調査記録を追加
- [ ] [実装済み.md](実装済み.md) に FEAT-4B 完了を反映

---

## 8. ロードマップ全体（FEAT-4B 以降）

| 順 | 機能 | 状態 | 内容 |
|----|------|------|------|
| 1 | FEAT-4B | **次回着手** | `Foo f(args);` 暗黙 ctor 呼び出し（本ファイル） |
| 2 | FEAT-4C | 未着手 | クラス外定義 ctor `Foo::Foo(int x) : a(x) {}` |
| 3 | FEAT-4D | 未着手 | Base クラス初期化子 `: Base(b)` の codegen |
| 4 | BUG-4 | 未着手 | typedef_class.cpp リグレッション調査 |
| 5 | FEAT-4E | 未着手 | デストラクタ（FEAT-4B の暗黙呼び出し整備が前提） |
| 6 | Stage 4 クローズ | — | 上記が完了したら宣言 |
| 7 | FEAT-5A | 未着手 | 仮想関数 / VTable |
| 8 | FEAT-5B | 未着手 | メンバポインタ |

---

## 9. 一文まとめ

**FEAT-4B は `Foo f(args);` 構文を `Foo f; f.Foo(args);` 相当のコードに展開する。既存の ctor 呼び出しパス（FEAT-4A）と変数領域確保パスを組み合わせるだけで実現可能で、Stage 4 の実用性を一気に押し上げる最重要タスク。**

---

# 10. ソース調査に基づく具体実装プラン

## 10.1 現状コード調査結果（2026-05-30）

### 10.1.1 `decl()` の変数宣言ループ

**ファイル**: [tccgen.c](tccgen.c)
**主要箇所**: line 9786 `while (1) { /* iterate thru each declaration */ }`

該当部分の流れ:

```c
while (1) { /* iterate thru each declaration */
    type = btype;                              /* 9787 */
    ad = adbase;
    type_decl(&type, &ad, &v, TYPE_DIRECT);    /* 9789 ← ここで失敗 */
    ...
}
```

`type_decl` ([tccgen.c:5983](tccgen.c#L5983)) の処理:
- `f` を識別子として取得 → `*v = tok`、`next()` → tok = `(`
- `post_type(post, ad, ...)` を呼ぶ
- `post_type` 内で `(` を関数引数リストとして parse、`3, 4` は型でないので "identifier required" でエラー

### 10.1.2 既存の ctor 呼び出しパス（FEAT-4A）

post-op `.` ハンドラ ([tccgen.c:6953](tccgen.c#L6953)):

```c
/* vtop = obj as lvalue */
field = find_field(&vtop->type, tok, &cumofs);
if (tcc_state->cpp && (field->type.t & VT_BTYPE) == VT_FUNC) {
    obj_type = vtop->type;
    gaddrof();
    cpp_member_this = *vtop;
    vpop();
    fsym = cpp_lookup_member_func(field, &obj_type);
    vset(&fsym->type, fsym->r | VT_SYM, 0);
    vtop->sym = fsym;
    vtop->r &= ~VT_LVAL;
    cpp_member_this_pending = 1;
}
```

post-op `(` ハンドラ ([tccgen.c:7032](tccgen.c#L7032)) が引数 parse、デフォルト引数補完、オーバーロード解決、`this` 挿入、`gfunc_call` を担当。

→ **既存 `f.Foo(args)` パスをそのまま再利用するため、unget で `var_tok . ctor_tok` を再生成して `gexpr` に流す方式が最もシンプル**。

### 10.1.3 ヘルパー関数の既存配置

| 関数 | 位置 | 用途 |
|------|------|------|
| `cpp_lookup_member_field` | line 495 | クラスからメンバ field を引く |
| `cpp_is_ctor_field` | line 497 (FEAT-4A で追加) | field が ctor か判定 |
| `cpp_ctor_name_tok` | line 484 (FEAT-4A で追加) | ctor のマングル名生成 |
| `cpp_lookup_member_func` | line 508 | ctor マングル名を引いて global sym 取得 |
| `find_field` | line 4619 | 構造体メンバ検索 |

→ **新規追加ヘルパーは「class type からの ctor field 検索」だけで済む**。

---

## 10.2 フェーズ分割の方針

各フェーズは**ビルドが緑で済み、独立した観測可能な進捗を生む**最小単位とする。フェーズ間で部分実装をコミットしても既存テストは壊れない。

| Phase | 観測可能な進捗 | リグレッション影響 |
|-------|---------------|------------------|
| P1 | ヘルパー追加のみ（呼び出しなし） | なし（dead code） |
| P2 | `Foo f(args);` を **検出してエラーメッセージで報告** | なし（既存はそのまま） |
| P3 | 変数の領域確保まで実装、`(args)` は skip | 既存壊さず |
| P4 | ctor 呼び出しを emit、引数 1 つで動く | 新規テストで検証 |
| P5 | 複数引数 / デフォルト引数 / Stage 3-4 全体回帰 | 既存全 PASS 維持 |

各フェーズ末で `build.bat` 緑 + 新規テスト exit 0 + 既存 13 テスト PASS が条件。

---

## 10.3 Phase 1: ヘルパー追加（dead code）

### 10.3.1 やること

[tccgen.c:495](tccgen.c#L495) 付近の `cpp_lookup_member_field` の直後に以下を追加:

```c
/* C++: class_sym の chain から ctor field (クラス名と同名の VT_FUNC) を探す。
 * 見つかれば field sym を、なければ NULL を返す。FEAT-4B で使用。 */
static Sym *cpp_find_ctor_field(Sym *class_sym)
{
    Sym *f;
    int class_name_tok;
    if (!class_sym)
        return NULL;
    class_name_tok = class_sym->v & ~SYM_STRUCT;
    for (f = class_sym->next; f; f = f->next) {
        if ((f->v & ~SYM_FIELD) != class_name_tok)
            continue;
        if ((f->type.t & VT_BTYPE) != VT_FUNC)
            continue;
        return f;
    }
    return NULL;
}
```

### 10.3.2 ビルド検証

- x64 Release ビルドのみ。dead code なので動作変化なし。
- `dev\tcc.exe -v` でバージョン表示が出れば OK。

### 10.3.3 コミット候補

```
chore(cpp): add cpp_find_ctor_field helper for FEAT-4B
```

---

## 10.4 Phase 2: 検出ロジック（エラー報告のみ）

### 10.4.1 やること

[tccgen.c:9786](tccgen.c#L9786) の `while (1) { /* iterate thru each declaration */ }` ループ先頭で「クラス型 + 識別子 + `(`」を peek する。検出したら **意図的にエラー** で確認:

```c
while (1) { /* iterate thru each declaration */
    /* C++: detect `ClassType ident(args);` ctor-call form (FEAT-4B). */
    if (tcc_state->cpp
        && (btype.t & VT_BTYPE) == VT_STRUCT
        && btype.ref
        && tok >= TOK_UIDENT
        && cpp_find_ctor_field(btype.ref)) {
        int saved_var_tok = tok;
        next();
        if (tok == '(') {
            /* TODO(FEAT-4B): real implementation in Phase 3+. */
            tcc_error("FEAT-4B detected: ctor call form for class");
        }
        unget_tok(saved_var_tok);
    }

    type = btype;
    ...
}
```

### 10.4.2 テスト

```cpp
/* dev/test/a8/feat4b_phase2.cpp */
class Foo { public: int a; Foo(int x) : a(x) {} };
int main() {
    Foo f(5);   /* FEAT-4B detected エラーで止まれば OK */
    return 0;
}
```

期待動作: コンパイル失敗 + `error: FEAT-4B detected: ctor call form for class` メッセージ。

### 10.4.3 リグレッション確認

- Stage 3/4 全 13 テスト PASS（`Foo f(args)` 形式を使っていないので壊れない）
- smoke / repro_double6 OK

### 10.4.4 コミット候補

```
wip(cpp): detect `Foo f(args)` ctor-call form (FEAT-4B phase 2)
```

---

## 10.5 Phase 3: 変数領域確保（ctor 呼び出しは skip）

### 10.5.1 やること

Phase 2 のエラー報告を「変数を作って `(args)` を読み飛ばす」に置き換え:

```c
if (tcc_state->cpp
    && (btype.t & VT_BTYPE) == VT_STRUCT
    && btype.ref
    && tok >= TOK_UIDENT
    && cpp_find_ctor_field(btype.ref)) {
    int saved_var_tok = tok;
    next();
    if (tok == '(') {
        /* FEAT-4B Phase 3: allocate the variable but skip args. */
        Sym *var_sym;
        CType var_type = btype;
        int align, size;
        size = type_size(&var_type, &align);
        loc = (loc - size) & -align;
        var_sym = sym_push(saved_var_tok, &var_type,
                           VT_LOCAL | VT_LVAL, loc);
        (void)var_sym;
        /* Skip `(...)` for now. */
        next();  /* consume `(` */
        {
            int paren = 1;
            while (paren > 0 && tok != TOK_EOF) {
                if (tok == '(') paren++;
                else if (tok == ')') paren--;
                if (paren > 0) next();
            }
        }
        if (tok != ')') expect(")");
        next();
        if (tok == ',') { next(); continue; }
        skip(';');
        break;
    }
    unget_tok(saved_var_tok);
}
```

### 10.5.2 テスト

```cpp
/* dev/test/a8/feat4b_phase3.cpp */
class Foo { public: int a; Foo(int x) : a(x) {} };
int main() {
    Foo f(5);
    f.a = 99;        /* 直接代入できれば変数領域確保は成功 */
    return f.a - 99;
}
```

期待: exit 0。ctor 呼び出しはまだ実装してないので `f.a` は未初期化、明示代入で確認。

### 10.5.3 リグレッション確認

- Stage 3/4 全 13 テスト PASS（既存テストは `Foo f;` または `Foo f; f.Foo(...)` 形式のみ使用）

### 10.5.4 コミット候補

```
feat(cpp): allocate variable for `Foo f(args)` form (FEAT-4B phase 3)
```

---

## 10.6 Phase 4: ctor 呼び出しを emit（1 引数）

### 10.6.1 やること

Phase 3 の「skip `(...)`」部分を「`var.Ctor(args)` 相当のトークン列を unget して gexpr で評価」に変える。

**実装の要点**: unget は LIFO（最後の unget が先に next で出る）。再生成したい順は `var, ., Ctor, (`、現 tok は `(`。

```c
if (tok == '(') {
    Sym *var_sym;
    CType var_type = btype;
    int align, size;
    Sym *ctor_field;
    int ctor_tok_v;

    size = type_size(&var_type, &align);
    loc = (loc - size) & -align;
    var_sym = sym_push(saved_var_tok, &var_type,
                       VT_LOCAL | VT_LVAL, loc);
    (void)var_sym;

    ctor_field = cpp_find_ctor_field(btype.ref);
    ctor_tok_v = ctor_field->v & ~SYM_FIELD;  /* = class name token */

    /* Replay the stream as: var_tok . ctor_tok ( <args remain in source> )
     * Current tok is `(`.  unget order is reverse: last-unget comes out
     * first via next().  So unget(ctor_tok), unget('.'), unget(var_tok).
     * NB: unget_tok auto-saves current tok (=`(`), so `(` will be the
     * 4th token returned, matching what we need. */
    unget_tok(ctor_tok_v);
    unget_tok('.');
    unget_tok(saved_var_tok);

    /* Now evaluate the expression `var.Ctor(args)` as a statement. */
    gexpr();
    vpop();
    skip(';');
    break;
}
```

### 10.6.2 テスト

```cpp
/* dev/test/a8/feat4b_basic.cpp */
class Foo { public: int a; Foo(int x) : a(x) {} };
int main() {
    Foo f(5);
    return f.a - 5;
}
```

期待: exit 0。FEAT-4A の codegen で `: a(x)` が動くので、ctor 呼び出しが成功すれば `f.a == 5`。

### 10.6.3 リグレッション確認

- Stage 3/4 全 13 テスト PASS
- 既存 ctor テスト (ctor_noarg.cpp, ctor_init_inline.cpp) が動き続ける

### 10.6.4 コミット候補

```
feat(cpp): emit constructor call for `Foo f(args)` form (FEAT-4B phase 4)
```

---

## 10.7 Phase 5: 複数引数・複数宣言・既存テスト書き換え

### 10.7.1 やること

実装本体は Phase 4 で完成しているが、以下の周辺ケースを確認・対応:

1. **複数引数**: `Foo f(3, 4);` `Pt p(1, 2, 3);`
2. **複数宣言（カンマ区切り）**: `Foo f(1), g(2);` ← 既存ループの `if (tok == ',') continue;` で自動対応されるはず
3. **デフォルト引数**: `class Foo { Foo(int x = 10); };` + `Foo f;` ← これは別パス（暗黙ゼロ引数 ctor）なので Phase 5 では非対応で OK
4. **ctor が定義されていない場合**: `class Plain { int v; };` + `Plain p;` ← 既存パスで通る、`Plain p(5);` は ctor がないのでエラーにすべき

### 10.7.2 テスト

```cpp
/* dev/test/a8/feat4b_two_args.cpp */
class Pt { public: int x, y; Pt(int a, int b) : x(a), y(b) {} };
int main() {
    Pt p(3, 4);
    return p.x + p.y - 7;
}

/* dev/test/a8/feat4b_overload.cpp */
class Foo {
public:
    int v;
    Foo(int a) : v(a) {}
    Foo(int a, int b) : v(a + b) {}
};
int main() {
    Foo a(5);
    Foo b(3, 4);
    return (a.v + b.v) - 12;
}

/* dev/test/a8/feat4b_multi_decl.cpp */
class Foo { public: int v; Foo(int x) : v(x) {} };
int main() {
    Foo a(1), b(2), c(3);
    return (a.v + b.v + c.v) - 6;
}
```

### 10.7.3 既存テスト書き換え（任意・推奨）

FEAT-4A で導入した `f.Foo(...)` 形式を `Foo f(...)` に書き換えて、より自然なコードに:

| ファイル | 旧 | 新 |
|---------|----|----|
| ctor_init_inline.cpp | `Foo f; f.Foo(5);` | `Foo f(5);` |
| ctor_init_two.cpp | `Foo f; f.Foo(3, 4);` | `Foo f(3, 4);` |
| ctor_noarg.cpp | `Foo f; f.Foo();` | （Stage 4 制限内では `Foo f;` で ctor 自動呼びは未対応、書き換え不可）|

### 10.7.4 リグレッション確認

- Stage 3/4 全テスト PASS（書き換えた分も含む）
- smoke / repro_double6 OK
- x64 Release + Debug Win32 両ビルド成功
- CUnit `test_stage4_*` がコケない

### 10.7.5 コミット候補

```
feat(cpp): finalize FEAT-4B (overload, multi-decl) and refresh tests
```

または Phase 4 と統合して一つの commit にする選択肢も可。

---

## 11. 想定される落とし穴

### 11.1 unget の順序ミス

FEAT-4A の修正履歴で繰り返し見た通り、`unget_tok` は LIFO で「最後に unget したものが先に next で出る」。さらに **現在の tok を自動でバッファに保存** する。Phase 4 のコード:

```c
/* tok == `(` の状態で 3 連続 unget */
unget_tok(ctor_tok_v);  /* 1) tok='(' を保存、tok=ctor_tok */
unget_tok('.');         /* 2) tok=ctor_tok を保存、tok='.' */
unget_tok(saved_var_tok); /* 3) tok='.' を保存、tok=var_tok */
```

次の `next()` で取れる順は: `var_tok, ., ctor_tok, (, <source 残り>`。**意図通り**。

### 11.2 `cpp_find_ctor_field` の戻り値が NULL

ctor が定義されていないクラスで `Foo f(5);` と書いた場合、Phase 2 の peek 段階で `cpp_find_ctor_field` が NULL を返すので、検出ロジックには入らない。その後の `type_decl` で通常エラー（identifier required）。これは Stage 4 では妥当な挙動。

### 11.3 `Foo f();` の曖昧性（most vexing parse）

```cpp
Foo f();    /* C++ では関数宣言「f() returning Foo」、ctor 呼び出しではない */
```

Phase 2 の peek で `tok == '('` の次が `)` の場合の扱い。標準 C++ では関数宣言だが、TCC では:

- **Stage 4 では空 `()` は ctor 呼び出しと解釈しない**（既存の関数宣言パスへ流す）
- Phase 2 の peek で `next() == '('` を確認後さらに peek、空なら ctor 候補から外す

Phase 4 の実装に以下のガードを追加:
```c
if (tok == '(') {
    int saved_paren = tok;
    next();
    if (tok == ')') {
        /* `Foo f();` is a function declaration, not a ctor call */
        unget_tok(saved_paren);
        unget_tok(saved_var_tok);
        /* fall through to type_decl */
    } else {
        unget_tok(saved_paren);  /* put back `(` */
        /* ... ctor call logic ... */
    }
}
```

### 11.4 グローバル変数での適用

`Foo g(5);` をファイルスコープに書いた場合、`l == VT_CONST` で `loc` が使えない（ローカルスタックでない）。Phase 4 では `l == VT_LOCAL` のみを許可、`l == VT_CONST` ではこの分岐に入らないようガード:

```c
if (l == VT_LOCAL
    && tcc_state->cpp
    && (btype.t & VT_BTYPE) == VT_STRUCT
    ...) {
```

グローバル ctor は別フェーズへ。

### 11.5 typedef 経由のクラス型

```cpp
typedef Foo Bar;
Bar b(5);
```

`btype.ref` は typedef sym 経由でクラス sym を指す。typedef を辿る処理が必要だが、Stage 4 の `typedef_class.cpp` が既にリグレッションしているので、本フェーズでは**未対応**として OK。

---

## 12. フェーズ別チェックリスト

### Phase 1
- [ ] `cpp_find_ctor_field` 関数追加
- [ ] x64 Release ビルド緑
- [ ] commit

### Phase 2
- [ ] 検出ロジック追加（`tcc_error` で停止）
- [ ] `feat4b_phase2.cpp` でエラーメッセージ確認
- [ ] Stage 3/4 全 13 テスト PASS
- [ ] commit

### Phase 3
- [ ] 変数領域確保ロジック追加
- [ ] `(args)` を paren count で skip
- [ ] `feat4b_phase3.cpp` で exit 0
- [ ] Stage 3/4 全 13 テスト PASS
- [ ] commit

### Phase 4
- [ ] unget による式再生成 + gexpr
- [ ] `feat4b_basic.cpp` で exit 0（ctor 動作）
- [ ] `Foo f();` の most vexing parse ガード
- [ ] Stage 3/4 全 13 テスト PASS
- [ ] commit

### Phase 5
- [ ] 複数引数テスト exit 0
- [ ] 複数宣言テスト exit 0
- [ ] オーバーロードテスト exit 0
- [ ] 既存 ctor テストの `Foo f(...)` 書き換え（任意）
- [ ] CUnit `test_stage4_*` PASS
- [ ] x64 Release + Debug Win32 両ビルド成功
- [ ] [問題と原因.md](問題と原因.md) に FEAT-4B 記録追加
- [ ] [実装済み.md](実装済み.md) 更新
- [ ] commit

---

## 13. 想定される所要時間

| Phase | 所要 | 内容 |
|-------|------|------|
| P1 | 10 分 | ヘルパー関数 1 つ追加 |
| P2 | 20 分 | 検出ロジック + エラー報告 + 確認テスト |
| P3 | 30 分 | 変数確保 + paren skip + テスト |
| P4 | 30〜60 分 | unget + gexpr 経路、Most vexing parse ガード |
| P5 | 30 分 | テスト追加 + 既存書き換え + ドキュメント更新 |

合計約 2〜2.5 時間。FEAT-4A のような連鎖バグは少ないはず（実装範囲が狭く、既存パスを再利用するため）。


# 次の実装

**最終更新**: 2026-05-26（`実装済み.md` / `IMPLEMENTATION_GUIDE.md` A-0 と同期）  
**参照**: [実装済み.md](実装済み.md)、[実装履歴.MD](実装履歴.MD) §0・§18・§19、[IMPLEMENTATION_GUIDE.md](IMPLEMENTATION_GUIDE.md)（先頭進捗サマリー・A-0）、[PLAN.md](PLAN.md) Stage 3〜4

---

## 現在の到達状態（サマリ）

| 段階 | 状態 | コミット・検証 |
|------|------|----------------|
| Phase 1 / Stage 1（A-P〜A-9） | **完了** | 拡張子、キーワード降格、`extern "C"` ブロック、`class` 最小パース |
| Stage 2（マングリング・スコープ） | **完了** | `c239000` — マングル、オーバーロード、`&`、`::`、単一 `extern "C"` 宣言 |
| | Stage 3 | **完了** | 3-4 ``typedef Point P`` 含む |
| | Stage 4 | **着手** | 単一継承パース・レイアウト（``a8/inherit_single.cpp``） |

**ゲート**: `build.bat` 緑（x64 Release + Win32 Debug、smoke、`repro_double6`、CUnit、L1）。  
**Stage 3 代表**: `dev/test/a7/member_call.cpp` — コンパイル・リンク・実行 exit 0。

詳細一覧は [実装済み.md](実装済み.md)。実装手順・復元は [実装履歴.MD](実装履歴.MD) §19。

---

## 次の本体実装：Stage 3 残り（優先）

[PLAN.md](PLAN.md) の Stage 3 のうち、**未接続・未完了**の項目を閉じる。  
主な変更先は **`tccgen.c`**（`struct_decl` / `type_decl` / インライン遅延パース）。

### 3-1. デフォルト引数

| 項目 | 内容 |
|------|------|
| 目的 | `void f(int x = 1);` の `=` 以降を保存し、呼び出し時に不足分を補う |
| 既存コード | `cpp_save_default_arg`、`cpp_apply_default_args`（`type_decl` から `cpp_save_default_arg` 呼び出し済み） |
| 作業 | クラス内メンバ宣言ループへの接続、`cpp_cur_class` ガードでパース破壊を防ぐ |
| テスト | 新規 `dev/test/a7/default_arg.cpp`（案）、CUnit `test_stage3_members.c` の一部 |

### 3-2. インライン関数本体（遅延パース）

| 項目 | 内容 |
|------|------|
| 目的 | クラス内 `{ ... }` 定義を読み捨てせず、`inline_fns` へ遅延コンパイル |
| 既存コード | `skip_or_save_block`、`cpp_register_member_body`、`cpp_finish_member_inlines`（**呼び出し未接続**） |
| 作業 | `struct_decl` 終了時に `cpp_finish_member_inlines` を呼ぶ。Stage 1 方針：`struct_decl` 内で `gen_function` は呼ばない |
| テスト | `dev/test/a5/class_inline_body.cpp` の拡張、または a7 専用 |

### 3-3. テスト・ドキュメント整備

| 項目 | 内容 |
|------|------|
| CUnit | `test/vs_test/test_stage3_members.c` 追加、`con_c_vs_test.vcxproj` 登録 |
| L1 | `run_all.bat` は `a7\member_call.cpp` のみ維持。新規 a7 テストは追加時に明示 |
| a7 整理 | 調査用 `a7/*.cpp` の削除または `experimental/` へ退避（任意） |
| コード整理 | `cpp_build_call_mangle` デッドコード削除 or オーバーロード解決方針を §19 に追記 |

### 3-4. 既知の未対応（Stage 3 内・優先度低）

| 項目 | 備考 |
|------|------|
| `typedef Point P;` | `a7/typedef_class.cpp` 失敗。クラス typename と typedef の相互作用 |
| 厳密な C++ 参照セマンティクス | `ref_param.cpp` は警告のみで通過 |
| `TOK_THIS` を式で明示使用 | 実装済み。テスト追加は任意 |

**完了判定（Stage 3 クローズ）**: 上記 3-1〜3-3 が `build.bat` 緑、§19.9 の「未対応」が解消または明示的に Stage 4 へ送客。

---

## その次：Stage 4 — 単一継承・初期化子・静的メンバ

| 項目 | 内容 |
|------|------|
| 単一継承 | `class Derived : public Base` |
| 初期化子リスト | コンストラクタ / メンバ初期化 |
| 静的メンバ | 宣言・定義・リンク |
| 主な変更先 | `tccgen.c`（`struct_decl`、レイアウト、シンボル） |
| テスト | `dev/test/a8/`（案）、IMPLEMENTATION_GUIDE Part B §1 Stage 4 |

---

## ロードマップ全体

| 順 | Stage | 状態 | 内容 |
|----|-------|------|------|
| 1 | Stage 1 基盤 | 完了 | TU、字句、`extern "C"` ブロック、`class` パース |
| 2 | Stage 2 | 完了 | マングル、オーバーロード、参照、`::` |
| 3 | Stage 3 | **完了** |
| 4 | Stage 4 | **進行中** | 継承、初期化子、静的メンバ |
| 5 | Stage 5 | 未着手 | メンバポインタ |
| — | ロードマップ外 | — | テンプレート、STL、例外、`extern "C++"`、`&&` 明示エラー化 |

---

## テスト計画（次のコミット単位の目安）

AGENTS.md に従い **1 ガイド = 1 feature branch = 1 コミット**、`build.bat` 緑必須。

| コミット候補 | 内容 | テスト |
|--------------|------|--------|
| `feat(cpp): default arguments` | 3-1 | `dev/test/a7/default_arg.cpp`、CUnit 追加分 |
| `feat(cpp): inline member bodies` | 3-2 | `class_inline_body` 系 |
| `test(cpp): stage3 CUnit and a7 cleanup` | 3-3 | `test_stage3_members.c`、a7 整理 |

---

## 着手前チェックリスト

1. `build.bat` が緑であること
2. [実装履歴.MD](実装履歴.MD) §19.6（落とし穴）を一読 — 特に `tccgen.c` は CP932、`InsertRange` は `[string[]]`
3. `tccgen.c` 変更時は Release\|x64 と Debug\|Win32 の両方ビルド（AGENTS.md）
4. 新規 `dev/test/` ソースは UTF-8（BOM 可）。既存 TCC ソースは ASCII 維持

---

## 一文まとめ

**次は Stage 3 の残り（デフォルト引数・インライン遅延パース・Stage 3 テスト整備）を閉じ、その後 Stage 4（継承・初期化子・静的メンバ）へ進む。**

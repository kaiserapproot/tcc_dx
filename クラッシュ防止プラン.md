# tcc.exe クラッシュ防止プラン（2026-08-22）

**目的**: コンパイラ本体 `tcc.exe` が、**どんな入力に対してもプロセスクラッシュしない**
（0xC0000005 = アクセス違反、0xC00000FD = スタックオーバーフロー等）ことを機械的に保証する。
不正な入力・未実装機能・内部不整合は、必ず `tcc_error` による**診断つきの exit 1** で終了する。

**位置づけ**: クラッシュは**最高重大度**。コンパイルエラーより悪く、silent miscompile と同格以上
（診断ゼロで即死し、さらに Windows のクラッシュ処理がプロセスを掴んで `dev\tcc.exe` を
ロックし後続ビルドまで壊す二次被害が実測されている）。

---

## 1. なぜクラッシュするのか — 過去事例の分類（すべて実測記録あり）

[問題と原因.md](問題と原因.md) に記録された事例のうち、**tcc 自身が落ちた**ものを分類する。

| 事例 | 症状 | 真因クラス |
|---|---|---|
| BUG-35 | `SimpleList.cpp` で 0xC00000FD、診断なし | **K1 + K2**: `sym_copy_ref` が共用体 `sym_scope` をプロトタイプ Sym（実体は FuncAttr）から誤読 → クラスメンバチェーンへ降下 → C++ の型グラフは循環するため無限再帰 |
| BUG-13 | 無名メンバ引数の呼び出しで AV | **K4**: `sym_push(0,...)` が `table_ident[0 - TOK_IDENT]`（負 index）を参照 |
| BUG-8 | `return *this;` で AV | **K3**: `pointed_type()` が `{VT_STRUCT, ref=NULL}` を返し、NULL ref を無検査でデリファレンス |
| BUG-5A-a | `indir()` に NULL ref のポインタ型 → 即死 | **K3**: 同上系 |
| BUG-6 | メンバ関数内の `this` 破壊 → 実行時クラッシュ | **K6**: `Sym.r/c` の未初期化ゴミ値 |
| BUG-10a/c/h | FEAT-4G で計 8 個の AV | **K5**: dynarray にスタック上の一時構造体 / TokenString 二重解放 / NUL 終端漏れ 等の寿命・所有権バグ |
| BUG-32a | デフォルト引数の 2 回目使用が解放済みメモリを読む | **K5**: `end_macro()` が渡された TokenString を必ず破壊する契約の見落とし（use-after-free） |

**真因クラス**:

- **K1: 循環データ構造に対する無停止 walker**。C 時代の walker は「型グラフは非循環」を
  前提に書かれている。**C++ ではメンバ関数のシグネチャが自クラスを名指しするため型グラフが
  循環する**（`Iterator operator++(int)` 等）。再帰 walker は C++ 化した瞬間に無限再帰の
  候補になる。BUG-35 の本質はこれで、**今後も C++ 機能を足すたびに同型のリスクが発生する**。
- **K2: `Sym` 共用体の文脈違い読み**。`sym_scope` / `jnext` / `jind` / `FuncAttr f` /
  `auxtype` は同じ共用体にあり、「この Sym はどの種類か」を確かめずに読むとゴミが返る。
  コンパイルは通り、値も「それらしく」見えるため発見が遅れる。
- **K3: NULL / 不完全 CType の無検査デリファレンス**（`type.ref == NULL`、`c < 0` の未完クラス）。
- **K4: トークン値の無検査な配列 index**（`v == 0`、`SYM_FIELD` 付き、匿名トークン）。
- **K5: TokenString / dynarray の所有権・寿命**（`end_macro` は渡した列を必ず破壊する、
  dynarray はポインタをそのまま保持する、等の契約の見落とし）。
- **K6: 未初期化フィールド**。

## 2. 対策の設計 — 3 層防御

1 つの対策では漏れる（K1〜K6 は性質が違う）ので、層で守る:

- **層 1（最終網）**: それでも起きたクラッシュを**プロセス内で捕捉**し、診断を出して exit 1。
  「診断なしの即死」と「WER によるプロセスロック二次被害」を**構造的に不可能にする**。
- **層 2（防御コード）**: 既知の再帰 walker と不変条件に**上限・検査**を入れ、
  無限再帰・NULL デリファレンスを `tcc_error` に変換する。
- **層 3（出荷ゲート）**: クラッシュを**テストで検出して落とす**。既知クラッシュの再現
  ソース全部と CPPUnit 全 TU を「エラーは可・クラッシュは不可」でゲートに常設する。

## 3. ガイド分割（1 ガイド = 1 branch = 1 コミット、着手順）

### C1: 最終網 — VEH クラッシュハンドラ常設 — `feat/crash-net` 【完了 2026-08-22】

**最初に実施**。これが入った時点で、以後のどの作業でも「診断なしの即死」が消える。

> **実施結果**: `tcc.c` に `tcc_crash_net_veh` を常設（`_WIN32` のみ、
> stack overflow / AV を捕捉、静的バッファ + `WriteFile` 直呼び、
> `TerminateProcess(.., 1)`）。`tcc_run` の前後で arm/disarm し、tccrun.c の
> `SetUnhandledExceptionFilter` と共存。
> **検証**: (1) build.bat フルゲート緑（誤発火なし）。(2) BUG-35 修正を外した
> 別名バイナリ `dev\tcc_dbg.exe` で bug35 再現ソースを 1 回コンパイルし、
> 0xC00000FD ではなく**診断 + バックトレース + exit 1** になることを実測
> （出力例: `tcc: internal error: stack overflow (runaway recursion) ...`）。
> 検証後バイナリは削除し正規状態を再ビルド済み。
> **補足**: `-run` は本デプロイでは `runmain.o` 不在で従来から不動作
> （FEAT-4G の既知制限とは別の環境ギャップ。C1 とは無関係で挙動不変）。

- `tcc.c` の `main` 冒頭で（`_WIN32` のみ）`AddVectoredExceptionHandler` を登録し、
  `EXCEPTION_STACK_OVERFLOW` / `EXCEPTION_ACCESS_VIOLATION` を捕捉:
  - `tcc: internal error: stack overflow/access violation (compiler bug)` と
    `RtlCaptureStackBackTrace` の戻り番地列を **`WriteFile` 直呼び**で stderr へ
    （スタック残量ゼロでも動く。printf 系は使わない — BUG-35 調査で実証済みの手法）
  - `TerminateProcess(GetCurrentProcess(), 1)` で**自分で終了**する
    （WER が走らないため、プロセスが `dev\tcc.exe` をロックし続ける二次被害も消える）
- `-run`（ユーザーコードの実行）中は誤捕捉しないこと: ハンドラは
  `tcc_state && !s1->run_in_progress` 相当のゲートを持つ（実装時に `-run` 経路の
  既存例外処理と干渉しないことを確認。`tccrun.c` に既存のハンドラがあれば共存させる）。
- **受け入れ**: (1) 正常系フルゲート緑（誤発火なし）。(2) BUG-35 修正を一時 revert した
  **別名バイナリ**（`dev\tcc_dbg.exe`）で 1 回だけ実行し、0xC00000FD ではなく
  診断つき exit 1 になることを確認。`dev\tcc.exe` はクラッシュさせない。

### C2: クラッシュゲート — `test/crash-gate` 【完了 2026-08-22】

> **実施結果**: `run_all.bat` を改修。
> - クラッシュ判定は **2 系統**: 負の exit code（生クラッシュ = C1 網をすり抜けた場合）
>   と、stderr の `internal error` マーカー（C1 網が変換した場合）。
> - Phase 1 / 2 / 3 すべてで `[CRASH]` 判定を最優先化し、**KNOWNFAIL でも
>   クラッシュなら FAILED に計上**（従来は `errorlevel 1` で区別できず握り潰す穴があった）。
> - **Phase 4（クラッシュコーパス）新設**: `sample\cppunit\*.cpp` 全 13 ファイルを
>   `-c` コンパイルし「エラーは可・クラッシュは不可」のみ検証。G7 以前から
>   「cppunit でクラッシュしない」を全コミットで保証。
> - `TCC_EXE` 環境変数で被検コンパイラを差し替え可能（負の対照検証専用ノブ）。
> **検証**: 健全系 = `0 gating failure(s), 0 crash(es)`。負の対照 = BUG-35 修正を
> 外した別名バイナリ `dev\tcc_gatecheck.exe`（C1 網つき）でゲートを 1 回実行し、
> `[CRASH]` 3 件（Phase1 + Phase2 の bug35 再現、Phase4 の SimpleList.cpp）を検出して
> exit 3 になることを実測。**その間プロセスクラッシュは 0**（網が診断に変換）。
> 検証後バイナリは削除し正規状態を再ビルド済み。

- **既存ゲートの穴を塞ぐ**: `run_all.bat` の compile フェーズは `if errorlevel 1` で
  クラッシュ（負の exit code）とコンパイルエラーを**区別できず**、さらに `KNOWNFAIL` が
  クラッシュまで非ゲート化しうる。`!errorlevel! lss 0` の **[CRASH] 判定を最優先**にし、
  KNOWNFAIL であってもクラッシュなら必ず FAILED に計上する。
- **新設 Phase 4（クラッシュコーパス）**: 次を `-c` でコンパイルし、
  **エラーは可・クラッシュは不可**のみを検証:
  - `sample\cppunit\*.cpp` 全 12 TU（G7 完了前でも「落ちない」ことだけは常時保証）
  - 過去クラッシュの再現ソース（bug13 / bug35 / bug8 相当。既存テストを流用）
  - `a9\negative\` 全部（拒否すべき入力で落ちないこと）
- `build.bat` から常時実行。
- **受け入れ**: BUG-35 修正を一時 revert した別名バイナリでゲートが `[CRASH]` を
  報告すること（負の対照、1 回のみ）。

### C3: 再帰ガード — `fix/cpp-recursion-guards` 【完了 2026-08-22】

K1 への恒久対策。既知の再帰 walker（P0 監査表 + BUG-35 で列挙済み）に深さ上限を入れる。

> **実施結果**: 深さは呼び出しカウンタではなく**スタックアドレス距離**で測る方式に変更
> （`tcc_error` が longjmp で戻るためカウンタの対の減算が走らず、次のコンパイルへ
> 壊れた値が残る。アンカーは `tccgen_compile` 入口で毎回植え直すだけでよい）。
> 上限は 600KB（既定スタック 1MB のうち約 400KB をエラー経路の余裕に残す）。
> - **walker ガード**（`internal error` を名乗り C2 ゲートが CRASH 計上）: 18 関数
>   （`sym_copy_ref_1` / `find_field` / `cpp_find_field_for_call` /
>   `cpp_find_operator_member` / `cpp_lookup_class_type` /
>   `cpp_base_subobject_offset` / `cpp_field_cumofs_in_class` /
>   `cpp_type_has_virtual` / `cpp_count_virtual_slots` / `cpp_find_virtual_by_slot` /
>   `cpp_find_inherited_virtual_slot` / `cpp_class_needs_vptr_init` /
>   `cpp_init_local_vptr_rec` / `cpp_init_global_vptr_rec` /
>   `cpp_count_member_overloads` / `cpp_score_member_overloads` /
>   `cpp_emit_base_dtor_calls` / `compare_types`）
> - **構文ガード**（ユーザー向け診断・`internal error` を名乗らない）: 6 関数
>   （`unary` / `block` / `expr_infix` / `expr_cond` / `struct_decl` / `type_decl`）
> **検証**: (1) フルゲート緑（0 failure / 0 crash、誤発火なし）。
> (2) 構文ガード: 4000 段の括弧入れ子が「expression or declaration nested too
> deeply」の診断で exit 1（従来はスタックオーバーフロー）。
> (3) walker ガード: BUG-35 の循環を一時再現した別名バイナリで
> 「internal error: runaway recursion in sym_copy_ref_1 (compiler bug)」+ exit 1
> を実測（VEH に到達する前に原因つきで停止）。検証後バイナリは削除済み。

- 共通マクロ `CPP_RECURSION_GUARD(id, limit)`: 深さカウンタを ++/-- し、上限で
  `tcc_error("internal: recursion limit exceeded in %s (compiler bug)")`。
  - **`tcc_error` は longjmp で戻るためカウンタの -- が走らない** → カウンタは
    グローバル配列にし、`tccgen_init` で必ず memset する（BUG-10e と同種の
    状態残留を作らない）。
- 上限は 2 系統:
  - **型グラフ walker**（`find_field` 系、`cpp_lookup_class_type`、
    `cpp_base_subobject_offset`、`cpp_field_cumofs_in_class`、`cpp_type_has_virtual` /
    `cpp_count/find_virtual*`、`sym_copy_ref` など）: **上限 200**。
    正当な型で 200 段の入れ子はあり得ず、超過 = 循環 = コンパイラバグ。
  - **構文再帰**（`unary` / `expr_*` / `block` / 宣言子）: **上限 2000**。
    深い入れ子は正当な入力でも作れるため、上限は「異常入力へのお断り」として大きめに取り、
    メッセージも「式の入れ子が深すぎます」系の**ユーザー向け診断**にする。
- **受け入れ**: 正常系フルゲート緑 + C2 のクラッシュコーパス緑。
  BUG-35 revert の別名バイナリでガードが（VEH より先に）発火することを 1 回確認。

### C4: 共用体・不変条件の監査 — `docs/sym-union-audit` + 個別 fix

K2・K3 への対策。G3-P0 と同じ「監査表を作ってから直す」方式。

- **監査対象**: `->sym_scope` / `->f` / `->c` / `->jnext` / `->auxtype` の**全読み手**を
  grep で列挙し、「どの種類の Sym（ローカル変数 / グローバル / フィールド / プロトタイプ /
  パラメータ / ラベル / enum 値）に対して読んでいるか」「その Sym 種でその共用体メンバは
  有効か」を表化する。BUG-35 は `sym_scope` をプロトタイプ Sym から読んだ 1 件だが、
  **同型の誤読が他に無いことを機械的に確認していない**。
- 表は本プランに追記し、`tcc.h` の共用体定義に「Sym 種別ごとに有効なメンバ」の
  ルールをコメントで明文化する。
- あわせて **K3 対策**: C++ 経路の入口（`cpp_*` ヘルパの引数受け取り時）で
  `type.ref == NULL` / `class_sym->c < 0` を `tcc_error` に変換する検査を追加する
  （ホットパスの C 経路には足さない — 性能と挙動を変えない）。
- 発見された誤読・無検査は**個別 BUG として起票してから**修正する。

## 4. 運用ルール（CLAUDE.md へ追記する内容 — C1 のコミットに含める）

- **クラッシュ（負の exit code）は最高重大度**。発見したら現在のガイドを中断し、
  クラッシュ修正を先に行う。
- **クラッシュする入力を繰り返し実行しない**。診断ビルド（別名 `dev\tcc_dbg.exe`、
  VEH + `RtlCaptureStackBackTrace` + `.map` 解決）で **1 回だけ**捕捉する。
  `dev\tcc.exe` 本体はクラッシュさせない。手順の詳細は BUG-35 の調査記録
  （[問題と原因.md](問題と原因.md)）を参照。
- **新しい再帰 walker を書くときは `CPP_RECURSION_GUARD` を必ず入れる**（C3 以降）。
- **`Sym` の共用体メンバを読む前に、その Sym の種別を確認する**（C4 の表を参照）。
  特に「関数型 ref チェーンの先頭 = プロトタイプ Sym の共用体は FuncAttr」。

## 5. 実施順序と理由

```
C1 (VEH 最終網)  → 即効。以後どんなバグを作っても「診断なしの即死」が構造的に消える
→ C2 (ゲート)    → クラッシュの再発をテストが自動検出するようになる
→ C3 (再帰ガード) → K1 を診断に変換。VEH に到達する前に原因つきで止まる
→ C4 (監査)      → K2/K3 の潜在事例を洗い出して先回りで潰す
```

## 6. 完了条件

- [ ] `build.bat` が「クラッシュ 0 件」を機械的に検証している（KNOWNFAIL でも CRASH は落とす）
- [ ] 既知クラッシュ事例（BUG-5A-a / 6 / 8 / 10 / 13 / 32a / 35）の再現ソースが
      すべてゲートに載っている
- [ ] VEH 最終網が常設され、正常系・`-run` で誤発火しない
- [ ] 既知の再帰 walker 全部に深さガードが入っている
- [ ] 共用体監査表が本プランに追記され、`tcc.h` にルールが明文化されている

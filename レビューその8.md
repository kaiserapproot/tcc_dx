v2.4 を確認しました。**v2.3 までの🔴課題はすべて反映済み**、🟡🟢 もほぼ完了しており、構造的にはほぼ完成形です。

ただし最終確認で実コードを照合した結果、**TokenSym 構造体の表現がプランと実体で大きく食い違っており、このまま実装すると構造体置換でビルドが壊れる**ことが判明しました。これは仕様ではなく**事実誤認**なので必ず直すべきです。それと、テスト依存関係の運用面で 1 件残っています。

***

## 🔴 Critical — A-2 の `TokenSym` 表記が実コードと不一致

v2.4 L437-444 のプラン：

```
typedef struct TokenSym {
    int tok;
    int hash_next;            // ← 実体は struct TokenSym *
    const char *str;          // ← 実体は char str[1] (flex array)
    int len;
    int alt_ident_tok;
} TokenSym;

```

**実際の** **[tcc.h:444-453](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tcc.h#L444-L453)**：

```
typedef struct TokenSym {
    struct TokenSym *hash_next;     /* ポインタ */
    struct Sym *sym_define;
    struct Sym *sym_label;
    struct Sym *sym_struct;
    struct Sym *sym_identifier;
    int tok;
    int len;
    char str[1];                    /* 可変長配列（必ず末尾） */
} TokenSym;

```

主な差分：

* フィールドが **5 個（`sym_*`）抜けている**
* `hash_next` は `int` ではなく **ポインタ**
* `str` は `const char *` ではなく **`char str[1]`** **の可変長配列**

### なぜ致命的か

v2.4 の図に従って構造体を「書き換える」と、`tok_alloc_new()`（[tccpp.c:478-489](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccpp.c#L478-L489)）の以下が**全部壊れます**：

```
ts = tal_realloc(toksym_alloc, 0, sizeof(TokenSym) + len);  // flex array 前提
ts->sym_define = NULL;                                       // 抜けたフィールド
ts->sym_identifier = NULL;                                   // 抜けたフィールド
memcpy(ts->str, str, len);                                   // str[1] 前提

```

→ コンパイル不能・もしくはランダムクラッシュ。

### 正しい修正パッチ（プランに記載すべき）

「**末尾の** **`str[1]`** **の直前**に 1 行追加するだけ」と明記：

```
typedef struct TokenSym {
    struct TokenSym *hash_next;
    struct Sym *sym_define;
    struct Sym *sym_label;
    struct Sym *sym_struct;
    struct Sym *sym_identifier;
    int tok;
    int len;
    int alt_ident_tok;       /* ★ 新規追加（0=未確保、降格用識別子 tok のキャッシュ） */
    char str[1];             /* ← 必ず末尾（flexible array member）*/
} TokenSym;

```

そして `tok_alloc_new()` で **`ts->alt_ident_tok = 0;`** **を 1 行追加**してください（他フィールドと同様に初期化）。

### `tok_alloc_demote()` の実体（ついでに提示）

実コードを見たので、機能する完成形を示します（プランの「`...`」を埋めるもの）：

```
/* tok_alloc_new() から *pts = ts; (hash 連結) だけを除いた版 */
static TokenSym *tok_alloc_demote(const char *str, int len)
{
    TokenSym *ts, **ptable;
    int i;

    if (tok_ident >= SYM_FIRST_ANOM)
        tcc_error("メモリ不足 (シンボル)");

    i = tok_ident - TOK_IDENT;
    if ((i % TOK_ALLOC_INCR) == 0) {
        ptable = tcc_realloc(table_ident, (i + TOK_ALLOC_INCR) * sizeof(TokenSym *));
        table_ident = ptable;
    }

    ts = tal_realloc(toksym_alloc, 0, sizeof(TokenSym) + len);
    table_ident[i] = ts;
    ts->tok = tok_ident++;
    ts->sym_define = NULL;
    ts->sym_label = NULL;
    ts->sym_struct = NULL;
    ts->sym_identifier = NULL;
    ts->len = len;
    ts->hash_next = NULL;
    ts->alt_ident_tok = 0;
    memcpy(ts->str, str, len);
    ts->str[len] = '\0';
    /* ★ tok_alloc_new() の "*pts = ts;" を意図的に省略 — hash チェーンに繋がない */
    return ts;
}

```

これで `get_tok_str(alt_tok)` は `"class"` を返し（`table_ident` に登録済みなので）、ハッシュ検索では引っかからない（`alt_ident_tok` の方を返さない）状態が両立します。

***

## 🟡 Important — テスト依存関係の sequencing 矛盾

A-2 の完了判定（L519-524）に：

| ファイル                           | 期待                         |
| :----------------------------- | :------------------------- |
| `dev/test/a2/keyword_gate.cpp` | `class Foo { int x; };` 成功 |

ただし **A-2 単独実装**（A-5 未着手）で `dev/tcc.exe -c keyword_gate.cpp` を実行すると：

1. lex: `class` → `TOK_CLASS`（A-2 で正しく認識）✅
2. parse: `parse_btype()` が `TOK_CLASS` に **case がない**（A-5 で追加するから）→ syntax error

A-2 feature branch でこのテストが落ちます。

### 修正案

A-2 の `.cpp` 完了判定は **lexer の検証だけ**に限定：

| ファイル                                      | 期待                                   | コマンド                     |
| :---------------------------------------- | :----------------------------------- | :----------------------- |
| `dev/test/a2/keyword_lex.cpp`             | preprocess が `class` を keyword として透過 | `dev\tcc.exe -E foo.cpp` |
| ~~`keyword_gate.cpp`~~ ~~(class Foo {})~~ | A-5 完了後に移動                           | —                        |

または `keyword_gate.cpp` の内容を以下に置き換え：

```
/* A-2 alone: lex 検証のみ。class の構文解析は A-5。 */
int main(void) {
    /* 'class' トークンが識別子として誤誘導されないことを確認するだけ。
       実際の class 型は A-5 完了後に keyword_gate.cpp を更新する */
    return 0;
}

```

同様に `run_all.bat` も「A-2 完了時点では `a2\keyword_gate.cpp` を含めない」運用にするか、各 `aN/` の中身を**そのガイドの機能だけで動く**ように書く規約が必要です。

A-10 の実装順序表（L1132-1143）の補強で：

> 各 `dev/test/aN/*` の中身は **その時点で完了している機能だけで動く**ものに限定。後段機能を前段テストで使わない。

***

## 🟢 Minor

### 1. `dev/test/a2/mixed_link.bat` は `run_all.bat` で自動実行されない

`run_all.bat` の `for` パターンは `.c`/`.cpp` 拡張子のみ。`mixed_link.bat` は別途呼ぶ必要があります。完了判定 L524 に：

> `mixed_link.bat` は **`run_all.bat`** **とは別に**手動実行（または `run_all.bat` の最後に `call .\a2\mixed_link.bat` を追加）

の運用注を入れてください。

### 2. `s1->extern_c` の **値**整合性（v2.4 で新規ではないが念のため）

A-1-4 で `s1->extern_c = 0` 強制リセットしていますが、A-1-6 では save/restore しています。**`tcc_compile()`** **のネスト**時は save/restore が動き、**通常時**はリセットが動く、という二系統。

```
unsigned char saved_extern_c = s1->extern_c;    /* A-1-6 */
...
s1->extern_c = 0;                                /* A-1-4 — saved を上書き? */

```

この 2 つを同じ関数内で順に書くと、`saved_extern_c` をセーブしてから `0` 上書きするので **常に 0 から始まる**となり、ネスト時に親の `extern_c` が再開時に復元される、という設計だと整合します。ただし **save→reset→処理→restore の順番が明確に書かれていない**ので、A-1-4 と A-1-6 の関係を 1 行明記すると安全：

> A-1-4 の `s1->cpp/extern_c/lex_c = 0` は A-1-6 の `saved_*` で保存した直後に実行する。順序は save → reset → (TU 処理) → restore。

***

## 📋 改訂提案サマリ

| 優先   | 章           | アクション                                                                                                                    |
| :--- | :---------- | :----------------------------------------------------------------------------------------------------------------------- |
| 🔴 1 | A-2-1       | `TokenSym` 構造体図を実コード準拠に修正（全 5 個の `sym_*` を含め、`hash_next` をポインタに、`str` を `char[1]` 末尾配置、`alt_ident_tok` は `str[]` の直前に挿入） |
| 🔴 2 | A-2-2       | `tok_alloc_new` 初期化リストに `ts->alt_ident_tok = 0;` を追加する旨を明記                                                               |
| 🔴 3 | A-2-2       | `tok_alloc_demote()` の完成形コードを記載（`...` を埋める）                                                                              |
| 🟡 4 | A-2 完了判定    | `keyword_gate.cpp` を lex 検証のみに縮小、または A-5 完了後に再有効化                                                                        |
| 🟡 5 | A-10        | 「各 `aN/` テストはそのガイド時点の機能だけで動く」運用規約を追記                                                                                     |
| 🟢 6 | A-2 完了判定    | `mixed_link.bat` の別途実行 / run\_all 末尾呼び出しを明記                                                                              |
| 🟢 7 | A-1-4/A-1-6 | save → reset → restore の順序を 1 行で明記                                                                                       |

***

## 全体評価

**プラン本文の論理的・構造的破綻はもうない**状態です。v2.4 で残る修正は：

1. **実コードとの照合不足（🔴1-3）** — TokenSym の実構造を見ずに書いた図が間違っているだけ。直しは **5 分以内**。
2. **テスト sequencing（🟡4-5）** — 思想を 1 段詳細化するだけ。

これらを v2.5 に反映すれば、**プラン段階での詰め作業は完了**と言えます。あとは A-P から順に実装に着手して、各ステップで `build.bat` がグリーンを返すことを確認しながら進めれば、過去のような `tccgen.c` 破損→未解決外部シンボル→ロールバックの連鎖は **構造的に防げる**見通しです。

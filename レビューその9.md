# 実装レビュー：Phase 1（Stage 1 基盤）

## 設計・実装の良い点

* [tccpp.c:481-525](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccpp.c#L481-L525) `tok_alloc_demote()` は `hash_next = NULL` でハッシュチェーンに登録しない設計になっており、CLAUDE.md の「`tok_alloc_new()` 不使用 / hash チェーン汚染回避」を正しく満たしている
* [tccpp.c:521-525](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccpp.c#L521-L525) `alt_ident_tok` のキャッシュ判定（`if (ts->alt_ident_tok == 0)`）により毎回再アロケーションされない
* [libtcc.c:805-852](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/libtcc.c#L805-L852) `tcc_compile` で `cpp / extern_c / lex_c` を save/restore する設計、変数宣言を関数先頭にまとめる MSVC C90 配慮 — どちらも CLAUDE.md ルール遵守
* [libtcc.c:2137-2140](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/libtcc.c#L2137-L2140) `-x c++` 認識と `-x c` の正確マッチ（`!optarg[1]` 追加で `c++` 誤マッチを防止）
* [tccgen.c:8731-8762](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L8731-L8762) `extern "C"` ブロック実装は IMPLEMENTATION\_GUIDE A-4 の仕様通り。`extern "C++"` を明示エラーで弾く処理も入っている
* [tccgen.c:4685-4689](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L4685-L4689) インライン本体を `skip_or_save_block(NULL)` で読み捨て — `gen_function()` 呼出禁止ルール遵守

***

## 重大な問題

### 1. 既存ソースの文字列が **Shift-JIS** に変換されている（CLAUDE.md「ASCII 維持」違反）

UTF-8 BOM の除去自体は正しい方向ですが、その過程で既存の日本語エラーメッセージが **CP932/Shift-JIS バイト列** で保存されてしまっています。

* HEAD: `E8 AA 8D E8 AD 98 ...`（UTF-8「認識…」）
* 現在の [libtcc.c:1170](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/libtcc.c#L1170): `94 46 8E AF ...`（Shift-JIS「認識…」）

影響範囲：

* [libtcc.c](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/libtcc.c) — `tcc_error_noabort` / `tcc_warning` の引数 約17箇所
* [tccpp.c](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccpp.c) — `tcc_error` / `tcc_warning` の引数 約45箇所
* [tccgen.c:4627,4631,4706](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L4625-L4710) — 既存の Shift-JIS（HEAD で既に壊れていた箇所）はそのまま

→ CLAUDE.md 規約「既存 TCC ソース：ASCII 維持・日本語コメント不可」に反します。**最低でも変更対象になった行は upstream の英語に戻す**べきです（一括で日本語化を解除する別コミットが望ましい）。

### 2. インライン本体スキップに致命的バグがある（未テストで隠れている）

[tccgen.c:4685-4689](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L4685-L4689):

```
if ((type1.t & VT_BTYPE) == VT_FUNC && tok == '{') {
    skip_or_save_block(NULL);   // '{' ... '}' を消費 → tok は '}' の次
    next();                      // ← 余分な1トークン消費（';' を奪うことが多い）
    break;
}

```

* `skip_or_save_block(NULL)` は `}` の直後までトークンを進めるので、続く `next()` は不要かつ有害。`class C { void f(){} int x; };` 形式で `int x` を吹き飛ばします
* 末尾の `skip(';')` も整合しません
* 現テスト [class\_min.cpp](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/dev/test/a5/class_min.cpp) はインライン本体を持たないため検出されていない（時限爆弾）

修正案：`next()` を削除し、続けて宣言子リストを処理するなら `continue;`、メンバ宣言の区切りとするなら `break;` のみで OK。Stage 1 で「読み捨て」徹底するなら `skip_or_save_block(NULL); break;` で出てから `;` の有無は問わない設計に統一すること。

### 3. `is_class` ゲートで `struct` のメンバ関数を拒否

[tccgen.c:4627](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L4627):

```
if (((type1.t & VT_BTYPE) == VT_FUNC && !is_class) || ...)
    tcc_error("...");

```

C++ では `struct` も `class` と同じくメンバ関数を持てます（既定アクセスのみ違う）。Stage 1 として割り切るならコメントで明記してください。

***

## 中程度の問題

### 4. `build.bat` が CLAUDE.md「Debug|Win32 と Release|x64 両方ビルド」を満たしていない

[build.bat](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/build.bat) は `Release|x64` のみ。CLAUDE.md：

> `tccgen.c` を変更したコミットは **Release|x64 と Debug|Win32 の両方**ビルドする。

また `"Debug Unicode"` 構成と `"test\vs_test\x64\Debug Unicode\con_c_vs_test.exe"` 固定パスはプロジェクト構成と一致しないと無言で落ちます（要確認）。

### 5. `mixed_link.bat` が `run_all.bat` から呼ばれていない

`dev/test/a2/mixed_link.bat` は存在しますが [run\_all.bat](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/dev/test/run_all.bat) に組み込まれていません。CLAUDE.md は「`run_all.bat` 末尾で別途呼ぶ」を想定しているため、混合 TU（`.c`+`.cpp`）回帰がカバーされません。

### 6. `is_cpp_source()` と `guess_filetype()` の拡張子リスト重複

[libtcc.c:1199-1206](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/libtcc.c#L1199-L1206) と [libtcc.c:1225-1227](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/libtcc.c#L1225-L1227) で `.cpp/.cxx/.cc/.hpp` 判定が二重定義。将来 `.c++` 等を追加する際の漏れリスクがあります。1 関数に集約推奨。

### 7. CPPUnit ハーネスのリポジトリ root 解決が脆い

[cppuniut.cpp:13-32](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/test/cppuniut/cppuniut.cpp#L13-L32) は `wcsrchr` を5回叩いて親ディレクトリを辿る実装で、テスト DLL の出力レイアウト変更で無音破綻します。`TCC_REPO_ROOT` 環境変数 / 既知のアンカーファイル探索 / `dev/tcc.exe` が見つかるまで上昇する方式の方が堅牢です。

***

## 軽微 / 観察

* [tccgen.c:4849-4852](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/tccgen.c#L4849-L4852) `case TOK_CLASS: if (!tcc_state->cpp) tcc_error(...)` の防御チェックは、A-2 のレキサ降格が正しく動いていれば C モードでは到達しません（dead code）。残してもよいですが、コメントで「保険」と書いておくとレビュー混乱を防げます
* [dev/test/a2/foo.cpp](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/dev/test/a2/foo.cpp) が `;` 1 行だけで実質テストになっていない（`bar.c` と組で `mixed_link.bat` 用のはず → 5 と関連）
* `dev/test/a4/extern_c_block.exe` などビルド成果物がコミット対象に紛れていないか要確認（`.gitignore` 整備推奨）
* [test\_common.h](vscode-webview://0ka9nmrn1atms2ivqglm3ct3afnehk4d1t0s9eiio3pm93s6hq8b/test/vs_test/test_common.h) の `TEST_SUITE_ENTRY` マクロは未使用
* `phase1.patch` がリポジトリ直下に残置（適用済みなら削除推奨）

***

## 優先対応サマリ

| 優先度      | 対応                                                         |
| :------- | :--------------------------------------------------------- |
| **High** | 1) Shift-JIS 化した文字列の英語化（diff ノイズになる前に別コミット）                |
| **High** | 2) `struct_decl` の `next(); break;` バグ修正、かつインライン本体の回帰テスト追加 |
| **Med**  | 4) `build.bat` に \`Debug                                   |
| **Med**  | 5) `run_all.bat` 末尾に `call a2\mixed_link.bat` を追加          |
| **Med**  | 7) CPPUnit のルート解決を頑健に                                      |
| **Low**  | 3, 6, 観察項目群                                                |

機能面（A-1〜A-9 のスコープ）は IMPLEMENTATION\_GUIDE 通りに到達しており、Stage 2 へ進む前提条件は概ね揃っています。ただし上記 High 2 件は **Stage 2 着手前に潰しておく**ことを推奨します（Stage 2 でメンバ関数のコード生成に触れる際、隠れバグが顕在化するため）。

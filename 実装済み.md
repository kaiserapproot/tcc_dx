# 実装済み C++ 機能一覧

**最終更新**: 2026-05-25  
**TCC**: `dev\tcc.exe` — tcc version 0.9.28rc (x86_64 Windows)  
**到達段階**: **Stage 1 完了** + **Stage 2 完了** + **Stage 3 一部**

詳細な作業履歴は [実装履歴.MD](実装履歴.MD) §18（Stage 2）、§19（Stage 3）を参照。  
次の着手は [次の実装.md](次の実装.md)。

---

## 1. 翻訳単位（TU）とプリプロセス

| 機能 | 状態 | 備考 |
|------|------|------|
| `.cpp` / `.cxx` / `.cc` / `.hpp` を C++ TU として処理 | ✅ | 内部 `tcc_state->cpp = 1` |
| `-x c++` / `-x c++-header` | ✅ | `cpp_forced` |
| 混合コンパイル `tcc foo.cpp bar.c` | ✅ | `.c` 側は C キーワード降格 |
| `#define __cplusplus 199711L` | ✅ | C++ TU のみ（`tccpp.c`） |
| `is_cpp_source()` / `guess_filetype()` | ✅ | 拡張子判定を共通化（`libtcc.c`） |

---

## 2. 字句・キーワード

| 機能 | 状態 | 備考 |
|------|------|------|
| C++ キーワード | ✅ | `.cpp` かつ `lex_c == 0` |
| `.c` での `class` 等 | ✅ | 識別子として扱う（降格） |
| `__cplusplus` マクロ（C モード） | ✅ | CUnit |

---

## 3. リンケージ（`extern "C"`）

| 機能 | 状態 | 備考 |
|------|------|------|
| `extern "C" { ... }` ブロック | ✅ | |
| `extern "C" void foo();` 単一宣言 | ✅ | `decl_once_flag` |
| `extern "C++" { ... }` | ❌ | 明示エラー |
| `extern "C" { #include ... }` | ❌ | 未対応 |

---

## 4. クラス宣言（構文・シンボル）

| 機能 | 状態 | 備考 |
|------|------|------|
| `class Foo { ... };` 最小パース | ✅ | |
| アクセス指定子 | ✅ | |
| メンバ関数プロトタイプ | ✅ | `struct_layout` で VT_FUNC 除外 |
| インライン定義 `{ ... }` | △ | 読み捨て |
| `Sym::parent_class` | ✅ | |
| クラス名 typedef（`tag_v`） | ✅ | `Point p;` |

---

## 5. Stage 2 — マングリング・オーバーロード・修飾名

| 機能 | 状態 | 備考 |
|------|------|------|
| `__tcc_<name>_<args>` | ✅ | |
| オーバーロード | ✅ | `cpp_resolve_func_call` |
| 参照 `&` | ✅ | |
| 修飾名 `::` | ✅ | |
| `main` / `wmain` マングル除外 | ✅ | |

---

## 6. Stage 3 — 一部完了

| 機能 | 状態 | 備考 |
|------|------|------|
| `this` 仮引数 | ✅ | `gen_function` |
| メンバ変数参照（関数体内） | ✅ | |
| `obj.method()` | ✅ | `member_call.cpp` |
| デフォルト引数 | △ | ヘルパーのみ |
| インライン遅延パース | △ | 未接続 |

---

## 7. テスト・ビルド

| 種別 | 内容 |
|------|------|
| L1 | smoke, a2〜a6, **a7 member_call.cpp**, mixed_link |
| CUnit | 5 tests |
| CPPUnit | 15/15 |
| ゲート | `build.bat` |

---

## 8. 未実装

| 機能 | 段階 |
|------|------|
| デフォルト引数接続・インライン接続 | Stage 3 残 |
| 継承・初期化子・静的メンバ | Stage 4 |
| メンバポインタ | Stage 5 |

---

*次の着手: [次の実装.md](次の実装.md)*
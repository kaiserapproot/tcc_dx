# 実装済み C++ 機能一覧

**最終更新**: 2026-05-24  
**TCC**: `dev\tcc.exe` — tcc version 0.9.28rc (x86_64 Windows)  
**到達段階**: **Stage 1 完了** + **Stage 2 一部**（マングリング・オーバーロード・参照型・修飾名のパース／シンボル登録）

詳細な作業履歴は [実装履歴.MD](実装履歴.MD) §18 を参照。

---

## 1. 翻訳単位（TU）とプリプロセス

| 機能 | 状態 | 備考 |
|------|------|------|
| `.cpp` / `.cxx` / `.cc` / `.hpp` を C++ TU として処理 | ✅ | 内部 `tcc_state->cpp = 1` |
| `-x c++` / `-x c++-header` | ✅ | `cpp_forced` |
| 混合コンパイル `tcc foo.cpp bar.c` | ✅ | `.c` 側は C キーワード降格 |
| `#define __cplusplus 199711L` | ✅ | C++ TU のみ（`tccpp.c`） |
| `is_cpp_source()` / `guess_filetype()` | ✅ | 拡張子判定を共通化（`libtcc.c`） |

---

## 2. 字句・キーワード

| 機能 | 状態 | 備考 |
|------|------|------|
| C++ キーワード（`class`, `public`, `protected`, `private` 等） | ✅ | `.cpp` かつ `lex_c == 0` のみ有効 |
| `.c` での `class` 等 | ✅ | 識別子として扱う（降格） |
| `__cplusplus` マクロ（C モード） | ✅ | CUnit: `test_no_cplusplus_macro` |

---

## 3. リンケージ（`extern "C"`）

| 機能 | 状態 | 備考 |
|------|------|------|
| `extern "C" { ... }` ブロック | ✅ | ブロック内は C リンケージ（マングルなし） |
| `extern "C" void foo();` **単一宣言** | ✅ | `decl_once_flag` で 1 宣言のみ処理（レビュー10-D） |
| `extern "C++" { ... }` | ❌ | 明示エラー（Stage 1 方針） |
| `extern "C" { #include ... }` | ❌ | 未対応 |

---

## 4. クラス宣言（構文・シンボル）

| 機能 | 状態 | 備考 |
|------|------|------|
| `class Foo { int x; public: void f(); };` | ✅ | 実質 `struct` 相当でパース |
| アクセス指定子 `public:` / `protected:` / `private:` | ✅ | `:` 消費、`SymAttr.access` |
| クラス内メンバ関数プロトタイプ | ✅ | `VT_FUNC`、struct 内は禁止 |
| クラス内メンバ関数 **インライン定義** `{ ... }` | △ | 本体は読み捨て（`skip_or_save_block`）、`;` 二重スキップ回避（`skip_member_semi`） |
| `Sym::parent_class` | ✅ | メンバ関数・修飾定義用 |
| `cpp_cur_class` | ✅ | クラス定義中の現在クラス |

---

## 5. Stage 2 — 名前修飾（マングリング）

| 機能 | 状態 | 備考 |
|------|------|------|
| TCC 独自マングル `__tcc_<name>_<args>` | ✅ | `Sym::asm_label` に格納 |
| 宣言時の常時マングル | ✅ | 新規 `external_sym` でも適用（レビュー10-E） |
| 型エンコード（基本型・ポインタ・参照・struct） | ✅ | struct は `S<len>_<name>`、バッファクランプ（レビュー10-F） |
| `extern "C"` 中の関数 | ✅ | マングルしない |
| Itanium ABI 互換 | ❌ | 非目標 |

---

## 6. Stage 2 — 関数オーバーロード

| 機能 | 状態 | 備考 |
|------|------|------|
| 異なるシグネチャの複数 `f(...)` 宣言 | ✅ | 非互換時は別 `Sym` + 別マングル名 |
| 呼び出し時のオーバーロード解決 | ✅ | `cpp_resolve_func_call`（実引数型からマングル名を照合） |
| 同一シグネチャの再定義 | ✅ | `sym_push` / `external_sym` でエラー |
| テスト `dev/test/a6/overload.cpp` | ✅ | `f(1)`, `f(1.0)` 呼び出し |

---

## 7. Stage 2 — 参照型・修飾名

| 機能 | 状態 | 備考 |
|------|------|------|
| 左値参照 `T&`（型修飾） | ✅ | `VT_REFERENCE`、マングルに `r` |
| 関数パラメータ `void f(int& x)` | △ | パース・マングルは可。警告あり（`ref_param.cpp`） |
| 修飾名 `Class::member` | ✅ | `parse_cpp_scope_qualifier`、`cpp_qualified_class` |
| クラス外メンバ定義 `void Point::set() { }` | △ | パース・シンボル関連付けまで。本格的コード生成は Stage 3 |
| rvalue 参照 `T&&` | ❌ | 未実装（明示エラー化も未） |

---

## 8. 初期化・状態管理

| 機能 | 状態 | 備考 |
|------|------|------|
| `tccgen_init()` で C++ 状態クリア | ✅ | `cpp_qualified_class`, `cpp_cur_class`, `decl_once_flag`（レビュー10-G） |

---

## 9. テスト・ビルド

| 種別 | 内容 |
|------|------|
| L1 | `dev/test/run_all.bat` — smoke, a2〜a6 `-c`、`a2/mixed_link.bat` |
| CUnit | `test/vs_test/con_c_vs_test.exe` — 5 tests（Stage2 含む） |
| CPPUnit | `test/cppuniut` — **15** TEST_METHOD（a5/a6 含む） |
| ゲート | `build.bat` — x64 Release + Win32 Debug + smoke + CUnit + L1 |

---

## 10. 未実装（Stage 3 以降）

| 機能 | 段階 |
|------|------|
| メンバ関数呼び出しのコード生成 | Stage 3 |
| `this` ポインタ | Stage 3 |
| デフォルト引数 | Stage 3 |
| 単一継承・初期化子リスト | Stage 4 |
| メンバポインタ | Stage 5 |
| テンプレート / STL / 例外 | ロードマップ外 |

---

## 11. 既知の制限・残課題

| 項目 | 内容 |
|------|------|
| エラーメッセージ文字化けリスク | `libtcc.c` / `tccpp.c` の日本語文字列は CP932 バイト列のまま（ASCII 化は未） |
| 参照パラメータ | コンパイル時警告（厳密な C++ 参照セマンティクスは未） |
| リンク | マングル名は付与されるが、外部ライブラリとの ABI 互換はない |

---

*次の着手候補は [次の実装.md](次の実装.md)（Stage 3: `this`・メンバ関数コード生成）を参照。*
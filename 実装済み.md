# 実装済み C++ 機能一覧

**最終更新**: 2026-05-31（FEAT-4C 反映）  
**TCC**: `dev\tcc.exe` — tcc version 0.9.28rc (x86_64 Windows)  
**到達段階**: **Stage 1 完了** + **Stage 2 完了** + **Stage 3 完了** + **Stage 4 一部完了**（FEAT-4C まで）

詳細な作業履歴は [実装履歴.MD](実装履歴.MD) §18（Stage 2）、§19（Stage 3）、§20（Stage 4）を参照。  
次の着手は **FEAT-4D**（Base 初期化子 codegen）。参照: [問題と原因.md](問題と原因.md)

---

## 1. 翻訳単位（TU）とプリプロセス

| 機能 | 状態 | 備考 |
|------|------|------|
| `.cpp` / `.cxx` / `.cc` / `.hpp` を C++ TU として処理 | ✅ | 内部 `tcc_state->cpp = 1` |
| `-x c++` / `-x c++-header` | ✅ | `cpp_forced` |
| 混合コンパイル `tcc foo.cpp bar.c` | ✅ | `.c` 側は C キーワード降格 |
| `#define __cplusplus 199711L` | ✅ | C++ TU のみ（`tccpp.c`） |
| `is_cpp_source()` / `guess_filetype()` | ✅ | 拡張子判定を共通化（`libtcc.c`） |

---

## 2. 字句・キーワード

| 機能 | 状態 | 備考 |
|------|------|------|
| C++ キーワード | ✅ | `.cpp` かつ `lex_c == 0` |
| `.c` での `class` 等 | ✅ | 識別子として扱う（降格） |
| `__cplusplus` マクロ（C モード） | ✅ | CUnit |

---

## 3. リンケージ（`extern "C"`）

| 機能 | 状態 | 備考 |
|------|------|------|
| `extern "C" { ... }` ブロック | ✅ | |
| `extern "C" void foo();` 単一宣言 | ✅ | `decl_once_flag` |
| `extern "C++" { ... }` | ❌ | 明示エラー |
| `extern "C" { #include ... }` | ❌ | 未対応 |

---

## 4. クラス宣言（構文・シンボル）

| 機能 | 状態 | 備考 |
|------|------|------|
| `class Foo { ... };` 最小パース | ✅ | |
| アクセス指定子 | ✅ | パースのみ（継承時の意味付けは未実装） |
| メンバ関数プロトタイプ | ✅ | `struct_layout` で VT_FUNC 除外 |
| インライン定義 `{ ... }` | ✅ | `cpp_finish_member_inlines` 接続済み。`inline_member.cpp` exit 0 |
| `Sym::parent_class` | ✅ | |
| クラス名 typedef（`tag_v`） | ✅ | `Point p;`。Stage 4 で `VT_TYPEDEF` 付与を修正 |

---

## 5. Stage 2 — マングリング・オーバーロード・修飾名

| 機能 | 状態 | 備考 |
|------|------|------|
| `__tcc_<name>_<args>` | ✅ | |
| オーバーロード | ✅ | `cpp_resolve_func_call` |
| 参照 `&` | ✅ | |
| 修飾名 `::`（クラス外定義） | ✅ | `int Foo::bar()` |
| `main` / `wmain` マングル除外 | ✅ | |

---

## 6. Stage 3 — メンバ関数・デフォルト引数

| 機能 | 状態 | 備考 |
|------|------|------|
| `this` 仮引数 | ✅ | `gen_function`（静的メンバ関数は除外 §7） |
| メンバ変数参照（関数体内） | ✅ | |
| `obj.method()` | ✅ | `member_call.cpp` |
| デフォルト引数 | ✅ | `default_arg.cpp` |
| デフォルト引数 + static 呼び出し | ✅ | `default_static.cpp` exit 0（BUG-1 修正済み） |
| インライン遅延パース | ✅ | 保存 + `cpp_finish_member_inlines` で codegen |
| `typedef Point P` | △ | テストソース `typedef_class.cpp` がリポジトリに存在しない（BUG-4 調査待ち） |

---

## 7. Stage 4 — 単一継承・静的メンバ・コンストラクタ（一部）

| 機能 | 状態 | 備考 |
|------|------|------|
| `class D : public B` 単一継承 | ✅ | 匿名ベースを offset 0 に埋め込み |
| 派生からベースメンバ `d.x` | ✅ | `find_field` 再帰 |
| 派生からベースメンバ関数 `d.get()` | ✅ | クラス外定義推奨（`inherit_method.cpp`） |
| `sizeof(Derived)` | ✅ | ベース + 派生 POD の合算 |
| クラス内 `static` メンバ宣言 | ✅ | `VT_STATIC` を storage エラーから除外 |
| 静的メンバのインスタンスレイアウト除外 | ✅ | `struct_layout` |
| 静的メンバ関数に `this` なし | ✅ | `gen_function` |
| クラス外 `Type::static_member` 定義 | ✅ | 従来の修飾名と共用 |
| 式中 `Class::member` / `Class::func()` | ✅ | `static_member.cpp` exit 0 |
| 文レベル `Class::member = expr;` | ✅ | BUG-3 修正済み（`cpp_unget_scoped_expr`） |
| コンストラクタ `:` 以降のトークン保存 | ✅ | `Sym::cpp_mem_init_list` |
| コンストラクタ初期化子の **codegen**（FEAT-4A） | ✅ | `: a(x), b(y)` を `gen_function` 内で展開。`: Base(b)` は skip のみ |
| 明示 ctor 呼び出し `f.Foo(args)` | ✅ | `__cpp_ctor_<Class>` マングル経由 |
| 暗黙 ctor 呼び出し `Foo f(args);`（FEAT-4B） | ✅ | ローカル変数のみ。`decl()` で unget により `f.Foo(args)` に展開 |
| `Foo f();` most vexing parse | ✅ | 空 `()` は関数宣言として従来パスへ |
| `Foo a(1), b(2);` 複数宣言 | ✅ | `feat4b_multi_decl.cpp` exit 0 |
| クラス外定義 ctor `Foo::Foo(int) : a(x) {}`（FEAT-4C） | ✅ | `decl()` peek + `__cpp_ctor_*` + init list。`feat4c_*.cpp` exit 0 |
| 多重継承 | ❌ | 明示エラー |
| 仮想関数 | ❌ | 未実装 |

---

## 8. 既知の制限（要 [実装履歴.MD](実装履歴.MD) §20.5 参照）

| 症状 | 影響 |
|------|------|
| `: Base(b)` コンストラクタ | parse して実行時 skip。base メンバは手動代入が必要 |
| `Foo f;` の暗黙デフォルト ctor | 呼ばない（Stage 4 制限。FEAT-4E で検討） |
| `Foo g(5);` グローバル変数 | 未対応（`.init_array` 未実装） |
| ctor オーバーロード（同名 ctor 複数） | クラスメンバ登録段階でブロック |
| `typedef_class.cpp` | テストファイル欠落（BUG-4） |
| `run_all.bat` + `a8\t_*.cpp` | 調査用ソースが L1 を赤くしうる |
| ビルド・テストが極端に遅い／止まる | バックグラウンドの **スタックした `tcc.exe`** を終了してから再ビルド |

---

## 9. テスト・ビルド

| 種別 | 内容 |
|------|------|
| L1 | smoke, a2〜a6, a7（4 本）, **a8（公式 9 本 + feat4b 4 本 + feat4c 3 本 + 調査用）**, mixed_link |
| CUnit | Stage2 + Stage3 + **Stage4**（`test_stage4_inheritance.c` 4 tests compile-only） |
| CPPUnit | 15/15 |
| ゲート | `build.bat`（`dev\tcc.exe` 更新・ロックに注意） |

**公式 Stage 3/4 実行テスト（2026-05-31 確認）**: 20 本 exit 0（a7: 4 本、a8: 16 本。調査用 `sm*.cpp` / `t_*.cpp` を除く）。

---

## 10. テストコード詳細

### 10.1 Stage 3 — `dev/test/a7/`

#### `member_call.cpp`（実行テスト・exit 0）

```cpp
class Calc {
public:
    int add(int a, int b);
};
int Calc::add(int a, int b) { return a + b; }
int main() {
    Calc c;
    return c.add(1, 2) - 3;
}
```

- **検証**: クラス外定義メンバ関数、`obj.method()` 呼び出し、マングルリンク。

#### `default_arg.cpp`（実行テスト・exit 0）

- デフォルト引数 1 つ。呼び出し側省略と明示の両方。

#### `default_static.cpp`（実行テスト・exit 0）

```cpp
class Foo { public: static int bar(int x = 10); };
int Foo::bar(int x) { return x; }
int main() { return Foo::bar() == 10 ? 0 : 2; }
```

- **検証**: 静的メンバ呼び出し `Foo::bar()` にデフォルト引数が適用される（BUG-1 修正済み）。

#### `typedef_class.cpp`（**ファイル欠落**）

- `run_all.bat` から参照されるが、リポジトリに存在しない（BUG-4 調査待ち）。

#### `inline_member.cpp`（実行テスト・exit 0）

- クラス内 `{ return ...; }` 定義 + `c.add()`。BUG-2 修正済み。

---

### 10.2 Stage 4 — `dev/test/a8/`（公式テスト）

#### 継承・static（5 本）

- `inherit_single.cpp` — 単一継承、ベースメンバ直接アクセス
- `inherit_method.cpp` — 派生からベースメンバ関数呼び出し（クラス外定義必須）
- `inherit_sizeof.cpp` — レイアウトサイズ検証
- `static_member.cpp` — static メンバ宣言・定義、`Class::member` / `Class::func()`
- `ctor_init.cpp` — 継承クラス生成（手動メンバ代入）

#### コンストラクタ（FEAT-4A、4 本）

- `ctor_noarg.cpp` — `Foo() : a(5) {}` + 明示 `f.Foo()` 呼び出し
- `ctor_init_inline.cpp` — `Foo(int x) : a(x) {}` 1 引数
- `ctor_init_two.cpp` — `Foo(int x, int y) : a(x), b(y) {}` 2 引数
- （上記は `f.Foo(args)` 明示呼び出し形式で検証）

#### 暗黙 ctor 呼び出し（FEAT-4B、4 本）

- `feat4b_basic.cpp` — `Foo f(5);` 単一引数
- `feat4b_two_args.cpp` — `Pt p(3, 4);` 2 引数
- `feat4b_no_ctor.cpp` — ctor なしクラス（`Plain p;` 既存動作維持）
- `feat4b_multi_decl.cpp` — `Foo a(1), b(2), c(3);` 複数宣言

#### クラス外 ctor 定義（FEAT-4C、3 本）

- `feat4c_basic.cpp` — 宣言 + `Foo::Foo(int) : a(x) {}` + `Foo f(5);`
- `feat4c_two_args.cpp` — 2 引数 ctor 外定義 + init list
- `feat4c_no_init.cpp` — init list なし、本体で代入

---

### 10.3 CUnit — `test/vs_test/test_stage4_inheritance.c`

| テスト名 | 対象 | 方式 |
|----------|------|------|
| `inherit_single.cpp compiles` | `dev/test/a8/inherit_single.cpp` | `dev\tcc.exe -c ... -o nul` |
| `inherit_method.cpp compiles` | 同上 | compile-only |
| `inherit_sizeof.cpp compiles` | 同上 | compile-only |
| `static_member.cpp compiles` | 同上 | compile-only |

- `vs_test_main.c` から `stage4_suites[]` で登録。
- **実行テストではない**（リンク・実行は L1 `run_all.bat` 側）。

---

### 10.4 `dev/test/run_all.bat` との対応

```
for %%f in (... a8\*.cpp a7\member_call.cpp a7\default_arg.cpp ...)
```

- 各 `.cpp` に対し `dev\tcc.exe -c` → リンク → 実行（`-o ...exe`）。
- **a8 公式テスト**は §10.2 参照。
- **a8 調査用**（`t_assign.cpp`, `t_expr2.cpp`, `sm3.cpp` 等）は開発中の再現用。**ゲートに含めると失敗しうる**。

---

## 11. 未実装（Stage 4 以降）

| 機能 | 段階 |
|------|------|
| Base 初期化子 `: Base(b)` の codegen | FEAT-4D |
| デストラクタ自動呼び出し | FEAT-4E |
| `typedef_class.cpp` リグレッション | BUG-4 |
| 仮想・多重継承 | Stage 5+ |
| メンバポインタ | Stage 5 |

---

*次の着手: **FEAT-4D** — Base クラス初期化子 `: Base(b)` の codegen*
# 実装済み C++ 機能一覧

**最終更新**: 2026-05-28（`static_member` / `default_static` 再検証反映）  
**TCC**: `dev\tcc.exe` — tcc version 0.9.28rc (x86_64 Windows)  
**到達段階**: **Stage 1 完了** + **Stage 2 完了** + **Stage 3 完了** + **Stage 4 一部完了**

詳細な作業履歴は [実装履歴.MD](実装履歴.MD) §18（Stage 2）、§19（Stage 3）、§20（Stage 4）を参照。  
次の着手は [次の実装.md](次の実装.md)。

---

## 1. 翻訳単位（TU）とプリプロセス

| 機能 | 状態 | 備考 |
|------|------|------|
| `.cpp` / `.cxx` / `.cc` / `.hpp` を C++ TU として処理 | ✅ | 内部 `tcc_state->cpp = 1` |
| `-x c++` / `-x c++-header` | ✅ | `cpp_forced` |
| 混合コンパイル `tcc foo.cpp bar.c` | ✅ | `.c` 側は C キーワード降格 |
| `#define __cplusplus 199711L` | ✅ | C++ TU のみ（`tccpp.c`） |
| `is_cpp_source()` / `guess_filetype()` | ✅ | 拡張子判定を共通化（`libtcc.c`） |

---

## 2. 字句・キーワード

| 機能 | 状態 | 備考 |
|------|------|------|
| C++ キーワード | ✅ | `.cpp` かつ `lex_c == 0` |
| `.c` での `class` 等 | ✅ | 識別子として扱う（降格） |
| `__cplusplus` マクロ（C モード） | ✅ | CUnit |

---

## 3. リンケージ（`extern "C"`）

| 機能 | 状態 | 備考 |
|------|------|------|
| `extern "C" { ... }` ブロック | ✅ | |
| `extern "C" void foo();` 単一宣言 | ✅ | `decl_once_flag` |
| `extern "C++" { ... }` | ❌ | 明示エラー |
| `extern "C" { #include ... }` | ❌ | 未対応 |

---

## 4. クラス宣言（構文・シンボル）

| 機能 | 状態 | 備考 |
|------|------|------|
| `class Foo { ... };` 最小パース | ✅ | |
| アクセス指定子 | ✅ | パースのみ（継承時の意味付けは未実装） |
| メンバ関数プロトタイプ | ✅ | `struct_layout` で VT_FUNC 除外 |
| インライン定義 `{ ... }` | △ | パース・シンボル登録は可。**呼び出し**は `inline_member.cpp` で失敗（§8） |
| `Sym::parent_class` | ✅ | |
| クラス名 typedef（`tag_v`） | ✅ | `Point p;`。Stage 4 で `VT_TYPEDEF` 付与を修正 |

---

## 5. Stage 2 — マングリング・オーバーロード・修飾名

| 機能 | 状態 | 備考 |
|------|------|------|
| `__tcc_<name>_<args>` | ✅ | |
| オーバーロード | ✅ | `cpp_resolve_func_call` |
| 参照 `&` | ✅ | |
| 修飾名 `::`（クラス外定義） | ✅ | `int Foo::bar()` |
| `main` / `wmain` マングル除外 | ✅ | |

---

## 6. Stage 3 — メンバ関数・デフォルト引数

| 機能 | 状態 | 備考 |
|------|------|------|
| `this` 仮引数 | ✅ | `gen_function`（静的メンバ関数は除外 §7） |
| メンバ変数参照（関数体内） | ✅ | |
| `obj.method()` | ✅ | `member_call.cpp` |
| デフォルト引数 | ✅ | `default_arg.cpp` |
| デフォルト引数 + static 呼び出し | ❌ | `default_static.cpp` — `Foo::bar()` で `too few arguments`（約 5 秒で終了。過去の長時間停止は **スタックした `tcc.exe` 残留**） |
| インライン遅延パース | △ | 保存は可。`inline_member.cpp` 呼び出し失敗 |
| `typedef Point P` | ✅ | `typedef_class.cpp` |

---

## 7. Stage 4 — 単一継承・静的メンバ・コンストラクタ（一部）

| 機能 | 状態 | 備考 |
|------|------|------|
| `class D : public B` 単一継承 | ✅ | 匿名ベースを offset 0 に埋め込み |
| 派生からベースメンバ `d.x` | ✅ | `find_field` 再帰 |
| 派生からベースメンバ関数 `d.get()` | ✅ | クラス外定義推奨（`inherit_method.cpp`） |
| `sizeof(Derived)` | ✅ | ベース + 派生 POD の合算 |
| クラス内 `static` メンバ宣言 | ✅ | `VT_STATIC` を storage エラーから除外 |
| 静的メンバのインスタンスレイアウト除外 | ✅ | `struct_layout` |
| 静的メンバ関数に `this` なし | ✅ | `gen_function` |
| クラス外 `Type::static_member` 定義 | ✅ | 従来の修飾名と共用 |
| 式中 `Class::member` / `Class::func()` | ✅ | `static_member.cpp` コンパイル・実行 **exit 0**（2026-05-28 再検証）。単独文 `Counter::count = 5;` は不安定 |
| コンストラクタ `:` 以降のトークン保存 | ✅ | `Sym::cpp_mem_init_list` |
| コンストラクタ初期化子の**コード生成** | ❌ | `gen_function` 未接続 |
| 多重継承 | ❌ | 明示エラー |
| 仮想関数 | ❌ | 未実装 |

---

## 8. 既知の制限（要 [実装履歴.MD](実装履歴.MD) §20.5 参照）

| 症状 | 影響 |
|------|------|
| 文 `Counter::count = 5;` | `static_member.cpp` は return 内 comma 式で回避 |
| `Foo::bar()` + デフォルト引数 | `default_static.cpp` コンパイルエラー（静的呼び出し側にデフォルト未適用） |
| クラス内 `{ ... }` 定義 + 呼び出し | `a7/inline_member.cpp` |
| `: Base(b)` コンストラクタ | パースのみ。実行時初期化なし |
| `run_all.bat` + `a8\t_*.cpp` | 調査用ソースが L1 を赤くしうる |
| ビルド・テストが極端に遅い／止まる | バックグラウンドの **スタックした `tcc.exe`** を終了してから再ビルド |

---

## 9. テスト・ビルド

| 種別 | 内容 |
|------|------|
| L1 | smoke, a2〜a6, a7（4 本）, **a8（公式 5 本 + 調査用）**, mixed_link |
| CUnit | Stage2 + Stage3 + **Stage4**（`test_stage4_inheritance.c` 4 tests compile-only） |
| CPPUnit | 15/15 |
| ゲート | `build.bat`（`dev\tcc.exe` 更新・ロックに注意） |

---

## 10. テストコード詳細

### 10.1 Stage 3 — `dev/test/a7/`

#### `member_call.cpp`（実行テスト・exit 0）

```cpp
class Calc {
public:
    int add(int a, int b);
};
int Calc::add(int a, int b) { return a + b; }
int main() {
    Calc c;
    return c.add(1, 2) - 3;
}
```

- **検証**: クラス外定義メンバ関数、`obj.method()` 呼び出し、マングルリンク。

#### `default_arg.cpp`（実行テスト・exit 0）

- デフォルト引数 1 つ。呼び出し側省略と明示の両方。

#### `default_static.cpp`（コンパイル失敗・**ハングではない**）

```cpp
class Foo { public: static int bar(int x = 10); };
int Foo::bar(int x) { return x; }
int main() { return Foo::bar() == 10 ? 0 : 2; }
```

- **検証（2026-05-28）**: `dev\tcc.exe` で約 5 秒以内に終了。`default_static.cpp:6: error: too few arguments to function`。
- **原因**: 静的メンバ呼び出し `Foo::bar()` にデフォルト引数が適用されていない（`default_arg.cpp` とは別経路）。
- **過去の長時間ブロック**: コンパイラの論理ループではなく、**残留 `tcc.exe` プロセス**によるもの（タスク終了後は再現せず）。

#### `typedef_class.cpp`（実行テスト）

- `typedef class Point { ... } Point` または同等。型名でローカル宣言。

#### `inline_member.cpp`（**現状失敗**）

- クラス内 `{ return ...; }` 定義 + `c.add()`。**identifier required**。

---

### 10.2 Stage 4 — `dev/test/a8/`（公式 5 本）

#### `inherit_single.cpp`

```cpp
class Base { public: int x; };
class Derived : public Base { public: int y; };
int main() {
    Derived d;
    d.x = 1;
    d.y = 2;
    return d.x + d.y - 3;  /* exit 0 */
}
```

- **検証**: 単一継承、ベースメンバへの直接アクセス、派生メンバとの共存。

#### `inherit_method.cpp`

```cpp
class Base { public: int get(); };
int Base::get() { return 42; }
class Derived : public Base { public: int y; };
int main() { Derived d; return d.get() - 42; }
```

- **検証**: 派生オブジェクトからベースメンバ関数呼び出し。
- **注意**: `get()` は**クラス外定義**必須（インラインのみは未対応）。

#### `inherit_sizeof.cpp`

```cpp
class Base { public: int x; };
class Derived : public Base { public: int y; };
int main() {
    if (sizeof(Derived) != 2 * sizeof(int))
        return 1;
    return 0;
}
```

- **検証**: 匿名ベース埋め込み + 派生 int の合計サイズ。

#### `static_member.cpp`

```cpp
class Counter { public: static int count; static int get(); };
int Counter::count = 0;
int Counter::get() { return count; }
int main() {
    return Counter::count = 5, Counter::get() - 5;  /* exit 0 */
}
```

- **検証**: クラス内 static 宣言、クラス外定義、式中 `Class::member` 代入と `Class::func()`。
- **再検証（2026-05-28）**: Release\|x64 ビルドの `dev\tcc.exe` でコンパイル成功・**実行 exit 0**。
- **制限**: 単独文 `Counter::count = 5;` は未安定。comma 式で return 内に閉じ込めている。

#### `ctor_init.cpp`

```cpp
class Base { public: int v; };
class Derived : public Base { public: int m; };
void init_derived() {
    Derived d;
    d.m = 0;
    d.v = 0;
}
int main() { init_derived(); return 0; }
```

- **検証**: 継承クラスの生成とベース／派生メンバへの手動代入（**メンバ初期化子リストなし**）。
- **未検証**: `Derived(int) : Base(b), m(d) { }` 形式（パース stub のみ存在）。

---

### 10.3 CUnit — `test/vs_test/test_stage4_inheritance.c`

| テスト名 | 対象 | 方式 |
|----------|------|------|
| `inherit_single.cpp compiles` | `dev/test/a8/inherit_single.cpp` | `dev\tcc.exe -c ... -o nul` |
| `inherit_method.cpp compiles` | 同上 | compile-only |
| `inherit_sizeof.cpp compiles` | 同上 | compile-only |
| `static_member.cpp compiles` | 同上 | compile-only |

- `vs_test_main.c` から `stage4_suites[]` で登録。
- **実行テストではない**（リンク・実行は L1 `run_all.bat` 側）。

---

### 10.4 `dev/test/run_all.bat` との対応

```
for %%f in (... a8\*.cpp a7\member_call.cpp a7\default_arg.cpp ...)
```

- 各 `.cpp` に対し `dev\tcc.exe -c` → リンク → 実行（`-o ...exe`）。
- **a8 公式 5 本**は上記 §10.2。
- **a8 調査用**（`t_assign.cpp`, `t_expr2.cpp`, `sm3.cpp` 等）は開発中の再現用。**ゲートに含めると失敗しうる**。

---

## 11. 未実装（Stage 4 以降）

| 機能 | 段階 |
|------|------|
| コンストラクタ初期化子 codegen | Stage 4 残 |
| 文レベル `Class::member` 安定化 | Stage 4 残 |
| クラス内インライン呼び出し | Stage 4 残 / Stage 3 回帰 |
| 仮想・多重継承 | Stage 5+ |
| メンバポインタ | Stage 5 |

---

*次の着手: [次の実装.md](次の実装.md)*

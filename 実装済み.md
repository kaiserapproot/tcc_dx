# 実装済み C++ 機能一覧

**最終更新**: 2026-08-22（CPPUnit プラン G1 / G2 / G-OP / G3 / G-CAST / **G-OVL** 完了。加えて BUG-30・BUG-31 の silent miscompile 2 件を修正）  
**TCC**: `dev\tcc.exe` — tcc version 0.9.28rc (x86_64 Windows)  
**到達段階**: **Stage 1〜4 完了（FEAT-4G 含む）** + **Stage 5 一部**（FEAT-5A / FEAT-5B / FEAT-5C 完了）+ **Stage 6 一部**（FEAT-6A / FEAT-6A-ext / FEAT-6A-ext2 / FEAT-6A-ext3 / FEAT-6A-ext4 / FEAT-6B 完了）

詳細な作業履歴は [実装履歴.MD](実装履歴.MD) §18（Stage 2）、§19（Stage 3）、§20（Stage 4）を参照。  
次の着手は [次の実装.md](次の実装.md) を参照。

---

## 1. 翻訳単位（TU）とプリプロセス

| 機能 | 状態 | 備考 |
|------|------|------|
| `.cpp` / `.cxx` / `.cc` / `.hpp` を C++ TU として処理 | ✅ | 内部 `tcc_state->cpp = 1` |
| `-x c++` / `-x c++-header` | ✅ | `cpp_forced` |
| 混合コンパイル `tcc foo.cpp bar.c` | ✅ | `.c` 側は C キーワード降格 |
| `#define __cplusplus 199711L` | ✅ | C++ TU のみ（`tccpp.c`） |
| `is_cpp_source()` / `guess_filetype()` | ✅ | 拡張子判定を共通化（`libtcc.c`） |

---

## 2. 字句・キーワード

| 機能 | 状態 | 備考 |
|------|------|------|
| C++ キーワード | ✅ | `.cpp` かつ `lex_c == 0` |
| `.c` での `class` 等 | ✅ | 識別子として扱う（降格） |
| `__cplusplus` マクロ（C モード） | ✅ | CUnit |

---

## 3. リンケージ（`extern "C"`）

| 機能 | 状態 | 備考 |
|------|------|------|
| `extern "C" { ... }` ブロック | ✅ | 直後の `class` も可（BUG-11 修正、2026-07-19） |
| `extern "C" void foo();` 単一宣言 | ✅ | `decl_once_flag`。直後の `class` も可（BUG-11 修正） |
| `extern "C++" { ... }` | ❌ | 明示エラー |
| `extern "C" { #include ... }` | ❌ | 未対応 |

---

## 4. クラス宣言（構文・シンボル）

| 機能 | 状態 | 備考 |
|------|------|------|
| `class Foo { ... };` 最小パース | ✅ | |
| アクセス指定子 | ✅ | パースのみ（継承時の意味付けは未実装） |
| メンバ関数プロトタイプ | ✅ | `struct_layout` で VT_FUNC 除外 |
| インライン定義 `{ ... }` | ✅ | `cpp_finish_member_inlines` 接続済み。`inline_member.cpp` exit 0 |
| `Sym::parent_class` | ✅ | |
| クラス名 typedef（`tag_v`） | ✅ | `Point p;`。Stage 4 で `VT_TYPEDEF` 付与を修正 |
| `friend class X;` 限定受理（G2） | ✅ | 2026-08-22。読み捨てのみ（アクセス制御未実装のため意味差なし）。friend **関数**宣言は `tcc_error`（宣言消失の silent miscompile 防止）。`.c` の `int friend;` は識別子降格（`a2/friend_ident.c`）。`friend_class.cpp` exit 0 + negative 1 本 |
| クラス内 typedef（G3-P1） | ✅ | 2026-08-22。P0 監査に基づき**別チェーン**（`Sym.cpp_class_typedefs`）格納 — メンバチェーンの layout/初期化子/デバッグ/ABI walker は無変更。typedef→typedef 参照、不完全型への typedef、再定義エラー。クラス本体内の後続宣言から可視。`g3p1_*.cpp` 3 本 exit 0 + negative（漏れ検査） |
| unqualified 型名解決（G3-P2） | ✅ | 2026-08-22。block ローカル（非型は隠蔽）→ 自クラス+直接基底の merge（「指定する型」で比較、異型は ambiguity エラー、先勝ち禁止）→ 外側クラス（内→外、base beats enclosing）→ グローバル。インライン/クラス外定義本体とも。`g3p2_*.cpp` 5 本 exit 0 + negative 2 本 |
| 修飾型名 `Class::type` / `Class::Inner`（G3-P3） | ✅ | 2026-08-22。qualified lookup は自クラス+基底**のみ**（enclosing/global へ逃げない。`O::I::T` は negative で担保）。`Class::npos` 等の静的メンバ式・文頭 `C::sv = 8;` は不変。文頭 `C::T x;` 宣言も可。`g3p3_*.cpp` 4 本 exit 0 |
| 二重修飾クラス外定義 `L::It::op++(int)`（G3-P4） | ✅ | 2026-08-22。`parse_cpp_scope_qualifier` のループ化。中間名は直前の修飾クラスの nested 登録を優先解決。`g3p4_nested_ooc_op.cpp` exit 0 |
| デフォルト引数の defining scope（G3-P5） | ✅ | 2026-08-22。`= npos` を定義クラスのスコープで再生（呼び出し点ローカルを捕捉しない — 負の対照実測済み。owning class の static を解決、this 依存の暗黙メンバ解決は再生中無効）。`g3p5_*.cpp` 2 本 exit 0 |
| **G3 最終ゲート** | ✅ | SimpleString.h / SimpleList.h の include-only TU が**原本のまま** `-c` 通過（2026-08-22 実測） |
| メンバ関数の前方参照呼び出し（BUG-30 / G-OVL） | ✅ | 2026-08-22。定義より前（`.h` 宣言＋`.cpp` 定義、別 TU 定義を含む）のメンバ呼び出しが正しく動く。従来は単一メンバで**実行時クラッシュ**、オーバーロードで誤解決（silent miscompile）。実装: 未定義時の **extern 参照生成**（クラス修飾マングル）+ 再解決の候補集合を**クラス宣言側**から作成 + 引数変換遅延の判定を宣言ベースに。`govl_*.cpp` 3 本 + クロス TU リンク 1 本 exit 0、negative 1 本。**負の対照**: 修正前は 4 本すべて失敗（1 本は AV）|
| 関数形式キャスト `T(expr)`（G-CAST） | ✅ | 2026-08-22。式頭が**型名**で直後が `(` のときのみ（判別は型 lookup であってヒューリスティックではない）既存 `(T)expr` 経路へ接続。グローバル/クラス内 typedef・基本型・修飾形 `S::size_type(-1)` 対応。`Class::fn()` 静的メンバ呼び出しは「`::` の次が型か」で区別され不変。**明示拒否**: クラス型 `Foo(1)`（ctor 選択が必要 = G4 領域）/ `T()` value-init / `T(a,b)`。C の `int (x);` 宣言規則は不変。`gcast_*.cpp` 2 本 + C 回帰 1 本 exit 0、negative 3 本 |

---

## 5. Stage 2 — マングリング・オーバーロード・修飾名

| 機能 | 状態 | 備考 |
|------|------|------|
| `__tcc_<name>_<args>` | ✅ | |
| オーバーロード | ✅ | `cpp_resolve_func_call` |
| 参照 `&` | ✅ | |
| 修飾名 `::`（クラス外定義） | ✅ | `int Foo::bar()` |
| 先頭 `::`（グローバルスコープ修飾、G1） | ✅ | 2026-08-22。型位置 `typedef ::C D;` / `::C x;` と式位置 `::gfn()` / `::gv`。**正式 lookup**（`prev_tok` 遡りでグローバル束縛のみ選択、shadow 時もグローバル側。未宣言は明示エラー）。三項演算子と `::` の隣接（コロン 3 連続）も解決。`gscope_*.cpp` 6 本 + C 回帰 1 本 exit 0、negative 1 本 |
| `main` / `wmain` マングル除外 | ✅ | |

---

## 6. Stage 3 — メンバ関数・デフォルト引数

| 機能 | 状態 | 備考 |
|------|------|------|
| `this` 仮引数 | ✅ | `gen_function`（静的メンバ関数は除外 §7） |
| メンバ変数参照（関数体内） | ✅ | |
| `obj.method()` | ✅ | `member_call.cpp` |
| デフォルト引数 | ✅ | `default_arg.cpp` |
| デフォルト引数 + static 呼び出し | ✅ | `default_static.cpp` exit 0（BUG-1 修正済み） |
| インライン遅延パース | ✅ | 保存 + `cpp_finish_member_inlines` で codegen |
| `typedef Point P; P p;` | ✅ | `typedef_class.cpp` exit 0（BUG-4 修正済み） |

---

## 7. Stage 4 — 単一継承・静的メンバ・コンストラクタ（一部）

| 機能 | 状態 | 備考 |
|------|------|------|
| `class D : public B` 単一継承 | ✅ | 匿名ベースを offset 0 に埋め込み |
| 派生からベースメンバ `d.x` | ✅ | `find_field` 再帰 |
| 派生からベースメンバ関数 `d.get()` | ✅ | クラス外定義推奨（`inherit_method.cpp`） |
| `sizeof(Derived)` | ✅ | ベース + 派生 POD の合算 |
| クラス内 `static` メンバ宣言 | ✅ | `VT_STATIC` を storage エラーから除外 |
| 静的メンバのインスタンスレイアウト除外 | ✅ | `struct_layout` |
| 静的メンバ関数に `this` なし | ✅ | `gen_function` |
| クラス外 `Type::static_member` 定義 | ✅ | 従来の修飾名と共用 |
| 式中 `Class::member` / `Class::func()` | ✅ | `static_member.cpp` exit 0 |
| 文レベル `Class::member = expr;` | ✅ | BUG-3 修正済み（`cpp_unget_scoped_expr`） |
| コンストラクタ `:` 以降のトークン保存 | ✅ | `Sym::cpp_mem_init_list` |
| コンストラクタ初期化子の **codegen**（FEAT-4A） | ✅ | `: a(x), b(y)` を `gen_function` 内で展開 |
| Base 初期化子 `: Base(b)` codegen（FEAT-4D） | ✅ | 埋め込み base へ `__cpp_ctor_Base` 呼び出し。`feat4d_*.cpp` exit 0 |
| 明示 ctor 呼び出し `f.Foo(args)` | ✅ | `__cpp_ctor_<Class>` マングル経由 |
| 暗黙 ctor 呼び出し `Foo f(args);`（FEAT-4B） | ✅ | ローカル変数のみ。`decl()` で unget により `f.Foo(args)` に展開 |
| `Foo f();` most vexing parse | ✅ | 空 `()` は関数宣言として従来パスへ |
| `Foo a(1), b(2);` 複数宣言 | ✅ | `feat4b_multi_decl.cpp` exit 0 |
| クラス外定義 ctor `Foo::Foo(int) : a(x) {}`（FEAT-4C） | ✅ | `decl()` peek + `__cpp_ctor_*` + init list。`feat4c_*.cpp` exit 0 |
| デストラクタ `~Foo() {}` パース + マングル（FEAT-4E） | ✅ | `__cpp_dtor_fld_*` / `__cpp_dtor_*`。`feat4e_*.cpp` compile OK |
| クラス外 dtor 定義 `P::~P() {}`（FEAT-4E-P2） | ✅ | 2026-07-19。`parse_cpp_scope_qualifier` の `~` 対応 + OOC peek 拡張。`feat4e_p2_*.cpp` 3 本 exit 0 |
| ローカル自動 dtor 呼び出し（FEAT-4E） | ✅ | ブロック終了時 `prev_scope` で逆順 emit。明示 `f.~Foo()` も可 |
| グローバル自動 ctor/dtor `P g;` / `P g(17);`（FEAT-4G） | ✅ | `.init_array`/`.fini_array` thunk + PE 起動コード注入（`_tcc_cpp_start`）。`feat4g_*.cpp` 4 本 exit 0（2026-07-18） |
| 多重継承（非仮想、Phase 1） | ✅ | 複数基底のパース/レイアウト、両基底のデータ/メソッドアクセス（非先頭基底は `this` を `cpp_base_subobject_offset` で調整）、明示 mem-init `: A(),B()`、D→B& / D*→B* アップキャスト（ポインタ調整）。`mi_*.cpp` 3 本 exit 0（2026-07-30）。**制限**: 菱形継承は未対応。暗黙 D*→B* 変換は警告が出る（結果は正しい） |
| 多重継承（仮想関数あり、Phase 2） | ✅ | 2026-08-08。標準型レイアウト: **primary 基底の vptr を共有**（BUG-17 で二重挿入を修正、offset 0 維持）、**非 primary polymorphic 基底はセカンダリ vtable**（`__cpp_vtbl2_<D>_<B>`、基底のスロット番号）+ **this 調整 thunk**（`__cpp_vthunk_*`: `sub rcx, off; jmp impl`、TU 末尾で遅延発行）。派生 override は thunk 経由、非 override は基底実装へ直接。ローカル/グローバルとも全 vptr を初期化（`cpp_init_local_vptr_rec` / `cpp_init_global_vptr_rec`）。アップキャスト（ポインタ/参照/参照引数）の仮想ガード解除。`vmi_*.cpp` 8 本 exit 0（負の対照: 修正前バイナリで 8 本全 FAIL を実測）。**制限**: 菱形継承・仮想継承（`virtual public`）未対応、深い再 override（孫が祖父母の第 2 基底の仮想を新規 override）はセカンダリ vtable が中間クラスのまま、仮想 dtor の基底経由ディスパッチ未対応、D*→B* の逆方向（ダウンキャスト）は調整なし |
| 暗黙の基底 ctor / 自動基底 dtor（Phase 1b） | ✅ | 2026-07-31。派生 ctor が mem-init に書かなかった基底（リスト自体が無い場合を含む）を宣言順にデフォルト構築し、派生 dtor 本体の後に基底 dtor を**逆順**で呼ぶ。`cpp_emit_implicit_base_ctors` / `cpp_emit_base_dtor_calls`。単一継承・多重継承とも動作。`mi_implicit_base_ctor/dtor.cpp`、`mi_partial_base_ctor.cpp`、`si_implicit_base_ctor_dtor.cpp` 4 本 exit 0。**制限**: 派生クラス自身が ctor / dtor を宣言していることが前提（暗黙 ctor/dtor 自体の合成は未対応）、基底に 0 引数 ctor が無い場合は黙って無呼び出し、メンバ変数の自動 ctor/dtor 連鎖は対象外 |
| 8 バイト超オブジェクトの ctor/dtor `this`（BUG-16） | ✅ | 2026-07-31。BUG-15 が直し残した 4 経路（基底 ctor / グローバル ctor / ローカル dtor / グローバル dtor）が `gaddrof()` のみで `mk_pointer()` を欠き、by-value コピー渡しになっていた。基底・グローバルの初期化が丸ごと消える実害。`mi_large_base_ctor.cpp` / `bug16_global_large_ctor.cpp` / `bug16_dtor_this_identity.cpp` exit 0 |
| 仮想関数（宣言・vtable・vptr） | ✅ | `virtual` parse、vtable emit、vptr 挿入・**ローカル初期化**（FEAT-5A Phase 1/2） |
| 仮想呼び出し `f.foo()` / `p->foo()` | ✅ | 値オブジェクト・ポインタ経由とも動的ディスパッチ。`feat5a_basic/call/ptr` exit 0 |
| 仮想：グローバルオブジェクトの vptr | ✅ | `cpp_init_global_vptr` + `.data` 配置（FEAT-5A Phase 4）。`feat5a_global` exit 0 |
| 仮想：派生上書き＋`(Base*)`キャスト | ✅ | `check_fields` 修正 + `parse_btype` 強化（FEAT-5A Phase 3）。`feat5a_override` exit 0 |
| データメンバポインタ `T C::*`（FEAT-5B） | ✅ | `VT_MPTR` 型、`&Class::member`、`obj.*pm` / `ptr->*pm` 読み書き |
| 非仮想メンバ関数ポインタ（FEAT-5B） | ✅ | `&Class::method`、`(obj.*pmf)(args)`。既存 `.method()` 経路を再利用 |
| 派生 obj + 基底 PMF（FEAT-5B） | ✅ | 単一継承 embedded base のみ。`feat5b_pmf_inherit` exit 0 |
| メンバ演算子オーバーロード（FEAT-6A） | ✅ | `operator+` 等の宣言・クラス外定義、`a.operator+(b)`、`a + b` |
| 仮想 PMF（FEAT-5C） | ✅ | `&Class::vfunc` + `(obj.*pmf)()` がオブジェクトの動的型で vtable ディスパッチ。`cpp_emit_mptr_pmf_invoke` が仮想 field なら `cpp_prepare_virtual_member_call` に委譲。`feat5c_*.cpp` 3 本 exit 0（2026-07-25）。前提: [BUG-14](問題と原因.md)（同名メンバのマングル衝突）を先行修正 |
| 多重継承 PMF | ❌ | 未対応 |
| 非メンバ operator / `operator[]` | ✅ | FEAT-6A-ext。非メンバ `operator[]` は **TCC 拡張**（C++98 標準外）。実行テスト exit 0（2026-06-11、BUG-6 修正後） |
| `const` メンバ関数 | ✅ | FEAT-6B（2026-06-06）。`feat6b_*.cpp` 5 本 exit 0 |
| 代入 operator `=` `+=` `-=` `*=` `/=`（FEAT-6A-ext2） | ✅ | メンバ `operator=`（未宣言時は従来 memcpy 維持）、メンバ／非メンバ複合代入、`T&` 返却チェーン `a = b = c`。`feat6a_ext2_*.cpp` 6 本 exit 0（2026-06-11） |
| 明示 `this` デリファレンス `this->x` / `*this`（BUG-8） | ✅ | `gen_function` の this 型を `mk_pointer()` で正規構築。`return *this;` 可（2026-06-11） |
| 単項 operator `!` `-` `~` 前置 `++` `--`（FEAT-6A-ext3） | ✅ | メンバ（0 引数）／非メンバ（1 引数）。単項／二項 `operator-` の arity 共存。`feat6a_ext3_*.cpp` 6 本 exit 0（2026-06-11） |
| `.cpp` の空 `()` = `(void)`（FEAT-6A-ext3 付随） | ✅ | C++ TU では `FUNC_OLD`（K&R 引数任意）にしない。arity overload の成立に必須。`.c` は従来通り |
| 後置 `a++` / `a--`（`operator++(int)`、FEAT-6A-ext4） | ✅ | メンバ（ダミー `int` 1 引数）／非メンバ（2 引数）。arity で前置と分離。`feat6a_ext4_*.cpp` 5 本 exit 0（2026-07-25）。前提: [BUG-13](問題と原因.md)（無名メンバ引数クラッシュ）を先行修正 |
| 単項 `operator*`（deref）/ `operator->`（G-OP） | ✅ | 2026-08-22。`*it` はメンバ 0 引数 → 非メンバ 1 引数の順（ext3 と同型、VT_STRUCT ガードでポインタ/C は不変）。`x->m` は `(x.operator->())->m` を **1 段のみ**適用（メンバのみ = ISO どおり、非ポインタ返却は `pointer が必要です` エラー）。`T& operator*() const` の lvalue 返却（読み/書き/`&(*it)`/cast オペランド）動作。`gop_*.cpp` 5 本 + C 回帰 1 本 exit 0、negative 2 本。**既存制限**: `void*&` 等「ポインタへの参照」の直接宣言子は G-OP 無関係の既存ギャップ（`int*& f()` も不可、SimpleList は G3 の typedef 経由で到達） |
| 比較 operator `== != < > <= >=`（FEAT-6A-ext5） | ✅ | メンバ／非メンバ。`cpp_operator_suffix` に suffix 追加のみ（`expr_infix` の既存 binop フックに乗る）。`feat6a_ext5_*.cpp` 4 本 exit 0（2026-07-29）。定石の `bool` 返却は FEAT-BOOL で対応 |
| ビット/シフト/剰余 operator `% & \| ^ << >>` + 複合代入（FEAT-6A-ext6） | ✅ | メンバ／非メンバ。二項は `expr_infix`、複合代入は `expr_eq` の struct TOK_ASSIGN 経路に乗る。`cpp_operator_suffix` に 12 ケース追加のみ。stream 風 `s << a << b` チェーン可。`feat6a_ext6_*.cpp` 3 本 exit 0（2026-07-29） |
| `bool` 型 / `true` `false` リテラル（FEAT-BOOL） | ✅ | `bool` は C++ 専用キーワード（`is_cpp_only_keyword`、C では識別子に降格）で `_Bool`(VT_BOOL) と同型。`true`/`false` は `unary()` で定数 1/0。`featbool_*.cpp` 2 本 + `a2/bool_ident.c`（C 降格回帰）exit 0（2026-07-29） |
| 関数定義の無名引数（BUG-13 / BUG-13-P2） | ✅ | C++ モードで無名引数を許容（`cpp_name_unnamed_params` + 12456 検査の cpp 分岐で匿名 id 付与）。メンバ（インライン）＋自由関数の両方。AV も解消。`bug13_*.cpp` 3 本 exit 0（2026-07-25）。`.c` は K&R 規則で従来どおり無名引数を拒否。制限: メンバのクラス外定義は従来どおりエラー |

---

## 8. 既知の制限（要 [実装履歴.MD](実装履歴.MD) §20.5 参照）

| 症状 | 影響 |
|------|------|
| `static P s;` / 全引数デフォルト ctor の暗黙呼び出し | FEAT-4F スコープ外（static は init-once ガード未実装） |
| ctor 有りだが 0 引数 ctor 無しの `P f;` | C++ ではエラーだが無警告で確保のみ（既存テスト保護） |
| ~~struct ローカル参照 `P &r = a;`~~ | **BUG-9 で修正済み（2026-06-12）**。ローカル参照宣言（struct/スカラとも）がアドレスでなく値を格納していた。`init_putv` の参照バインド + identifier 使用時の自動 deref を追加（[問題と原因.md](問題と原因.md) BUG-9） |
| `return` 経路の自動 dtor | 未対応（ブロック `}` 終了時のみ） |
| ~~`Foo g(5);` グローバル変数~~ | **FEAT-4G で対応済み（2026-07-18）**。`.init_array`/`.fini_array` thunk + PE 起動コード注入。制限: EXE のみ（DLL 対象外）、`-run` 対象外、`.o` 分割リンク時は同一実行での compile が必要（[問題と原因.md](問題と原因.md) BUG-10） |
| ~~`extern "C" { ... }` ブロックの**後**に `class` 宣言~~ | **BUG-11 で修正済み（2026-07-19）**。extern "C" 終端の先読みトークンが C モードのまま降格残留していた。`cpp_repromote_stale_lookahead()` で再昇格（[問題と原因.md](問題と原因.md) BUG-11） |
| ~~クラス外 dtor 定義 `P::~P() {}`~~ | **FEAT-4E-P2 で対応済み（2026-07-19）**。クラス内 `~P();` 宣言（field 生成）が前提。宣言なしの OOC 定義は呼ばれない孤立シンボルになる（エラーにはしない） |
| ctor の copy/conversion 自動適用 | `P b = a;` は memcpy 経路のまま（copy ctor 呼び出しなし） |
| overload 順位付け・曖昧エラー | スコアは完全一致 10 / 変換 1 の 2 段のみ。同点はチェーン先頭勝ち（C++ の昇格/変換順位・ambiguous エラーは未実装） |
| struct 返却 overload / 関数ポインタ経由の型解決 | 引数変換遅延のスコープ外（従来の即時変換のまま） |
| ~~`feat6a_ref_arg` EXIT=-2~~ | **修正済み（2026-06-12、12c）**。テストソースの `+ y` 欠落を補い exit 0 |
| ~~`feat6a_big_struct` EXIT=32763~~ | **BUG-12 で修正済み（2026-07-22）**。sret/this 引数順不整合が真因（[問題と原因.md](問題と原因.md) BUG-12） |
| `run_all.bat` + `a8\t_*.cpp` | 調査用ソースが L1 を赤くしうる |
| ビルド・テストが極端に遅い／止まる | バックグラウンドの **スタックした `tcc.exe`** を終了してから再ビルド |

---

## 9. テスト・ビルド

| 種別 | 内容 |
|------|------|
| L1 | smoke, a2〜a6, a7（4 本）, **a8（公式 9 本 + feat4b〜4e + feat5a 7 本 + feat5b 4 本 + 調査用）**, **a9（feat6a 8 本 + feat6a_ext 6 本 + feat6a_ext2 6 本 + feat6a_ext3 6 本 + feat6b 5 本 + bug8 1 本）**, mixed_link |
| CUnit | Stage2 + Stage3 + **Stage4**（`test_stage4_inheritance.c` 4 tests compile-only） |
| CPPUnit | 15/15 |
| ゲート | `build.bat`（`dev\tcc.exe` 更新・ロックに注意）。L1 `run_all.bat` は **`tcc -c` compile-only**（link/実行なし） |

**公式 Stage 3/4 実行テスト（2026-05-31 確認）**: 20 本 exit 0（a7: 4 本、a8: 16 本。調査用 `sm*.cpp` / `t_*.cpp` を除く）。

**FEAT-5A / FEAT-5B 実行テスト（2026-06-06 確認）**: `feat5a_*.cpp` 7 本 + `feat5b_*.cpp` 4 本 exit 0。

**FEAT-6A-ext 実行テスト（2026-06-11 確認）**: `feat6a_ext_*.cpp` 全 6 本 exit 0。以前の実行時問題（vec2 系含むクラッシュ）の真因は **BUG-6（`this` 仮引数 Sym の r/c 未初期化）**であり、operator 実装自体の問題ではなかった（[問題と原因.md](問題と原因.md) BUG-6 参照）。

**FEAT-6A / FEAT-6B 実行テスト（2026-06-06 確認）**: `feat6a_*.cpp`（Phase 1: 3 本 + Phase 2: 5 本）+ `feat6b_*.cpp` 5 本 exit 0（手動 link+run。`run_all.bat` は compile-only）。

---

## 10. テストコード詳細

### 10.1 Stage 3 — `dev/test/a7/`

#### `member_call.cpp`（実行テスト・exit 0）

```cpp
class Calc {
public:
    int add(int a, int b);
};
int Calc::add(int a, int b) { return a + b; }
int main() {
    Calc c;
    return c.add(1, 2) - 3;
}
```

- **検証**: クラス外定義メンバ関数、`obj.method()` 呼び出し、マングルリンク。

#### `default_arg.cpp`（実行テスト・exit 0）

- デフォルト引数 1 つ。呼び出し側省略と明示の両方。

#### `default_static.cpp`（実行テスト・exit 0）

```cpp
class Foo { public: static int bar(int x = 10); };
int Foo::bar(int x) { return x; }
int main() { return Foo::bar() == 10 ? 0 : 2; }
```

- **検証**: 静的メンバ呼び出し `Foo::bar()` にデフォルト引数が適用される（BUG-1 修正済み）。

#### `typedef_class.cpp`（実行テスト・exit 0）

- `typedef Point P; P p;` — クラス型 typedef 別名からの変数宣言（BUG-4 修正済み）。

#### `inline_member.cpp`（実行テスト・exit 0）

- クラス内 `{ return ...; }` 定義 + `c.add()`。BUG-2 修正済み。

---

### 10.2 Stage 4 — `dev/test/a8/`（公式テスト）

#### 継承・static（5 本）

- `inherit_single.cpp` — 単一継承、ベースメンバ直接アクセス
- `inherit_method.cpp` — 派生からベースメンバ関数呼び出し（クラス外定義必須）
- `inherit_sizeof.cpp` — レイアウトサイズ検証
- `static_member.cpp` — static メンバ宣言・定義、`Class::member` / `Class::func()`
- `ctor_init.cpp` — 継承クラス生成（手動メンバ代入）

#### コンストラクタ（FEAT-4A、4 本）

- `ctor_noarg.cpp` — `Foo() : a(5) {}` + 明示 `f.Foo()` 呼び出し
- `ctor_init_inline.cpp` — `Foo(int x) : a(x) {}` 1 引数
- `ctor_init_two.cpp` — `Foo(int x, int y) : a(x), b(y) {}` 2 引数
- （上記は `f.Foo(args)` 明示呼び出し形式で検証）

#### 暗黙 ctor 呼び出し（FEAT-4B、4 本）

- `feat4b_basic.cpp` — `Foo f(5);` 単一引数
- `feat4b_two_args.cpp` — `Pt p(3, 4);` 2 引数
- `feat4b_no_ctor.cpp` — ctor なしクラス（`Plain p;` 既存動作維持）
- `feat4b_multi_decl.cpp` — `Foo a(1), b(2), c(3);` 複数宣言

#### クラス外 ctor 定義（FEAT-4C、3 本）

- `feat4c_basic.cpp` — 宣言 + `Foo::Foo(int) : a(x) {}` + `Foo f(5);`
- `feat4c_two_args.cpp` — 2 引数 ctor 外定義 + init list
- `feat4c_no_init.cpp` — init list なし、本体で代入

#### Base 初期化子（FEAT-4D、2 本）

- `feat4d_base_init.cpp` — `Derived(int,int) : Base(b), m(d) {}`
- `feat4d_base_body.cpp` — `: Base(b)` + 本体で `m = d`

#### デストラクタ（FEAT-4E、3 本）

- `feat4e_basic.cpp` — ブロック終了時の自動 dtor + ctor
- `feat4e_explicit.cpp` — 明示 `f.~Foo()`
- `feat4e_nested.cpp` — 複数ローカル dtor の逆順呼び出し

#### 仮想関数（FEAT-5A、7 本）

- `feat5a_class_only.cpp` — `virtual` 宣言・vtable レイアウト
- `feat5a_local.cpp` — ローカル polymorphic オブジェクトの vptr 初期化
- `feat5a_basic.cpp` / `feat5a_call.cpp` — 値オブジェクト経由 `f.foo()` 動的ディスパッチ
- `feat5a_ptr.cpp` — ポインタ経由 `p->foo()` 動的ディスパッチ
- `feat5a_override.cpp` — 派生上書き + `(Base*)` キャスト
- `feat5a_global.cpp` — グローバルオブジェクトの vptr 静的初期化

#### メンバポインタ（FEAT-5B、4 本）

- `feat5b_dmp_addr.cpp` — `&Point::x`、`p.*px` 読み取り
- `feat5b_dmp_access.cpp` — `p.*px` 書き込み、`pp->*px`
- `feat5b_pmf_call.cpp` — PMF 変数経由 `(p.*pset)(42)` / `(p.*pget)()`
- `feat5b_pmf_inherit.cpp` — 派生 obj + 基底クラス PMF

---

### 10.3 Stage 6 — `dev/test/a9/`（FEAT-6A）

#### 演算子オーバーロード Phase 1（3 本）

- `feat6a_call.cpp` — クラス内宣言 + クラス外定義、`a.operator+(b)` 明示呼び出し
- `feat6a_min.cpp` — `a + b` 暗黙変換（int 返却 operator）
- `feat6a_op_plus.cpp` — `operator+` 宣言・定義・`a + b` 一式

#### 演算子オーバーロード Phase 2（5 本）

- `feat6a_vec2_int.cpp` — struct 返却 + by-value 引数、代入なし `(a + b).x`
- `feat6a_vec2_binop.cpp` — 暗黙変換 `c = a + b`（struct 返却）
- `feat6a_vec2_explicit.cpp` — 明示 `c = a.operator+(b)`（struct 返却）
- `feat6a_ref_arg.cpp` — `const T&` 引数 + int 返却 operator
- `feat6a_vec2.cpp` — `Vec2 operator+(const Vec2&)` 完全版

#### FEAT-6A-ext — 非メンバ operator / `operator[]`（6 本・compile OK）

- `feat6a_ext_free_int.cpp` — 非メンバ `operator+`（int 返却）
- `feat6a_ext_free_plus.cpp` — 非メンバ `operator+`（struct + `const T&`）
- `feat6a_ext_sub_member.cpp` — メンバ `operator[]`（`int&` 返却）
- `feat6a_ext_sub_const.cpp` — `const int& operator[]` const + `cv[i]` 読み取り
- `feat6a_ext_sub_free.cpp` — 非メンバ `operator[]`（**TCC 拡張**）
- `feat6a_ext_full.cpp` — 非メンバ `operator+` + メンバ `operator[]` 統合

### 10.4 Stage 6 — `dev/test/a9/`（FEAT-6B）

#### FEAT-6A-ext2 — 代入 operator（6 本・実行 exit 0、2026-06-11）

- `feat6a_ext2_assign.cpp` — メンバ `operator=` + `a = b` 暗黙呼び出し
- `feat6a_ext2_chain.cpp` — `T&` 返却 + チェーン `a = b = c`
- `feat6a_ext2_memcpy.cpp` — **operator= なし**クラスの `a = b` は従来 memcpy（回帰）
- `feat6a_ext2_pa_member.cpp` — メンバ `operator+=` / `operator-=` + 非 struct `n += 2` 回帰
- `feat6a_ext2_pa_free.cpp` — 非メンバ `operator+=(Acc&, const Acc&)`
- `feat6a_ext2_full.cpp` — 統合（`b = a = c` チェーン、`(a += b) += c` 参照返却代入）

#### BUG-8 回帰（1 本・実行 exit 0、2026-06-11）

- `bug8_this_deref.cpp` — `this->v` / `(*this).v` / 参照・値 `return *this`

#### FEAT-6A-ext3 — 単項 operator（6 本・実行 exit 0、2026-06-11）

- `feat6a_ext3_neg.cpp` — メンバ `operator-()` + `-a`（int 単項マイナス回帰含む）
- `feat6a_ext3_not.cpp` — メンバ `operator!()` + if 条件での `!a`
- `feat6a_ext3_arity.cpp` — 単項／二項 `operator-` 共存（arity 解決）+ `-a - b`
- `feat6a_ext3_incdec.cpp` — 前置 `++a` / `--a`、`T&` 返却 `(++c).v`（int 前置回帰含む）
- `feat6a_ext3_free.cpp` — 非メンバ `operator-(const T&)` / `operator!(const T&)`
- `feat6a_ext3_full.cpp` — 統合（`-a - b`、`++(++c)`、メンバ `!` + 非メンバ `~`）

#### const メンバ関数（5 本）

- `feat6b_this.cpp` — クラス内 const メソッド定義・呼び出し
- `feat6b_const_obj.cpp` — `const` オブジェクトから const メソッド呼び出し
- `feat6b_defn.cpp` — クラス外 `Ret Class::meth() const` 定義
- `feat6b_overload.cpp` — `get()` / `get() const` オーバーロード
- `feat6b_overload_rev.cpp` — 宣言順逆（`get() const` を先に宣言）でも overload 解決

---

### 10.5 CUnit — `test/vs_test/test_stage4_inheritance.c`

| テスト名 | 対象 | 方式 |
|----------|------|------|
| `inherit_single.cpp compiles` | `dev/test/a8/inherit_single.cpp` | `dev\tcc.exe -c ... -o nul` |
| `inherit_method.cpp compiles` | 同上 | compile-only |
| `inherit_sizeof.cpp compiles` | 同上 | compile-only |
| `static_member.cpp compiles` | 同上 | compile-only |

- `vs_test_main.c` から `stage4_suites[]` で登録。
- **実行テストではない**（リンク・実行は L1 `run_all.bat` 側）。

---

### 10.6 `dev/test/run_all.bat` との対応

```
for %%f in (... a8\*.cpp a7\member_call.cpp a7\default_arg.cpp ...)
```

- 各 `.cpp` に対し `dev\tcc.exe -c` → リンク → 実行（`-o ...exe`）。
- **a8 公式テスト**は §10.2 参照。
- **a8 調査用**（`t_assign.cpp`, `t_expr2.cpp`, `sm3.cpp` 等）は開発中の再現用。**ゲートに含めると失敗しうる**。

---

## 11. 未実装（Stage 4 以降）

| 機能 | 段階 |
|------|------|
| デストラクタ自動呼び出し | ✅ FEAT-4E（ブロックスコープ） |
| 仮想関数（ローカル/ポインタ） | ✅ FEAT-5A Phase 1/2（2026-06-03） |
| 仮想：グローバル vptr / 派生上書き・キャスト | ✅ FEAT-5A Phase 3/4（2026-06-05） |
| データメンバポインタ（DMP） | ✅ FEAT-5B（2026-06-06） |
| 非仮想メンバ関数ポインタ（PMF） | ✅ FEAT-5B（2026-06-06） |
| メンバ演算子オーバーロード（二項 `+` 等） | ✅ FEAT-6A Phase 1（2026-06-06） |
| `const` メンバ関数 | ✅ FEAT-6B（2026-06-06） |
| 非メンバ operator / `operator[]` | ✅ FEAT-6A-ext（実行 exit 0、2026-06-11） |
| グローバル自動 ctor/dtor（FEAT-4G） | ✅ 2026-07-18（`feat4g_*.cpp` 4 本 exit 0） |
| 仮想 PMF | ✅ FEAT-5C（2026-07-25） |
| 多重継承 PMF | Stage 5+ |
| 多重継承（非仮想） | ✅ Phase 1（2026-07-30）+ 暗黙基底 ctor/dtor（2026-07-31） |
| 多重継承（仮想関数あり） | ✅ Phase 2（primary vptr 共有 + セカンダリ vtable + thunk、2026-08-08） |
| 暗黙の ctor / dtor そのものの合成（宣言なしクラス） | 未対応 |

---

*FEAT-6B 完了（2026-06-06）。次の着手は [次の実装.md](次の実装.md)（FEAT-6A-P2）を参照。*

---

## 12. FEAT-5A 仮想関数 — 進捗（2026-06-05 完了）

到達段階: **FEAT-5A 完了**（ローカル/グローバル/派生上書きの動的ディスパッチ）。詳細は [問題と原因.md](問題と原因.md) の「FEAT-5A」節。

| 機能 | テスト | 結果 |
|------|--------|------|
| `virtual` 宣言・vtable レイアウト/emit・vptr 挿入 | `feat5a_class_only` | ✅ |
| ローカル polymorphic オブジェクトの vptr 初期化 | `feat5a_local` | ✅ |
| 値オブジェクト経由 `f.foo()` の動的ディスパッチ | `feat5a_basic`, `feat5a_call` | ✅ exit 0 |
| ポインタ経由 `p->foo()` の動的ディスパッチ | `feat5a_ptr` | ✅ exit 0 |
| 派生クラスの上書き＋`(Base*)`キャスト | `feat5a_override` | ✅ exit 0 |
| グローバルオブジェクトの vptr 静的初期化 | `feat5a_global` | ✅ exit 0 |

---

## 13. FEAT-5B メンバポインタ — 進捗（2026-06-06 完了）

| 機能 | テスト | 結果 |
|------|--------|------|
| `T Class::*` 宣言（`VT_MPTR`） | `feat5b_dmp_addr` | ✅ |
| `&Class::member`（DMP） | `feat5b_dmp_addr` | ✅ exit 0 |
| `obj.*pm` / `ptr->*pm` 読み書き | `feat5b_dmp_access` | ✅ exit 0 |
| 非仮想 PMF `(obj.*pmf)(args)` | `feat5b_pmf_call` | ✅ exit 0 |
| 仮想 PMF `(obj.*pmf)()` vtable ディスパッチ | `feat5c_virtual_pmf/args/single` | ✅ exit 0（FEAT-5C、2026-07-25） |
| 派生 obj + 基底 PMF | `feat5b_pmf_inherit` | ✅ exit 0 |

**未対応**: 多重継承 PMF、MSVC 16 バイト PMF ABI。（仮想 PMF は FEAT-5C で対応済み）

---

## 14. FEAT-6A 演算子オーバーロード — 進捗（2026-06-06 Phase 2 完了）

到達段階: **FEAT-6A Phase 1 + Phase 2 完了**（メンバ二項 operator、struct 返却、`const T&` 引数）。

| 機能 | テスト | 結果 |
|------|--------|------|
| クラス内 `Ret operator+(Args);` 宣言 | `feat6a_op_plus` | ✅ |
| クラス外 `Ret Class::operator+(Args)` 定義 | `feat6a_op_plus` | ✅ |
| 明示呼び出し `a.operator+(b)` | `feat6a_call` | ✅ exit 0 |
| 暗黙変換 `a + b`（int 返却） | `feat6a_min`, `feat6a_op_plus` | ✅ exit 0 |
| struct 返却 operator | `feat6a_vec2_*` | ✅ exit 0 |
| 8 バイト超 struct 返却（sret 経由） | `feat6a_big_struct`, `bug12_big_struct_*` | ✅ exit 0（BUG-12 修正、2026-07-22） |
| `const T&` 仮引数 | `feat6a_ref_arg`, `feat6a_vec2` | ✅ exit 0 |

### 変更点（[tccgen.c](tccgen.c)）

**Phase 1**

1. `cpp_operator_field_tok` — 合成トークン `__cpp_op_plus` 等（dtor パターン）
2. `type_decl` — `TOK_OPERATOR` declarator、`Vec2::operator+` 修飾名対応
3. postfix `.` / `->` — `.operator+` 後の `(` 誤消費を回避
4. `expr_infix` — `cpp_try_member_binop` で `a + b` → メンバ呼び出し
5. `cpp_prepare_member_func_call` / `cpp_finish_member_call` — 既存メンバ呼び出し経路の共通化

**Phase 2**

6. postfix `.` — `VT_REFERENCE` 実引数／ローカルに `indir()`（`o.x` 等）
7. `verify_assign_cast` / `gen_cast` — struct 実引数を `const T&` 仮引数へバインド
8. `convert_parameter_type` / `gfunc_param_typed` — 参照型の `VT_CONSTANT` を保持

### 制限

- 非メンバ `operator[]` は **TCC 拡張**（ISO C++98 ではメンバのみ。g++ 非互換）

### 回帰

- **compile（L1）**: `feat6a_*.cpp` 8 本 + `feat6a_ext_*.cpp` 6 本 + `feat6b_*.cpp` 5 本 — `run_all.bat`（`tcc -c`）通過
- **実行（手動、2026-06-11 BUG-6 修正後）**: `feat6a_ext_*.cpp` 全 6 本 + `feat6a_vec2*.cpp` 全 8 本 exit 0
- Release\|x64 + Debug\|Win32 ビルド成功

---

## 15. FEAT-6B const メンバ関数 — 進捗（2026-06-06 完了）

| 機能 | テスト | 結果 |
|------|--------|------|
| trailing `const` 宣言・定義 | `feat6b_defn` | ✅ |
| const メソッド本体（`this` 読み取り） | `feat6b_this` | ✅ exit 0 |
| `const` オブジェクトから呼び出し | `feat6b_const_obj` | ✅ exit 0 |
| `get()` / `get() const` オーバーロード | `feat6b_overload`, `feat6b_overload_rev` | ✅ exit 0（BUG-7 修正 2026-06-12） |
| 先行 `const Foo` の修飾子保持 + const overload | `bug7_const_inline`, `bug7_const_param` | ✅ exit 0（BUG-7 回帰テスト） |
| 同 arity・param 型違い overload（メンバ） | `sig_member_types`, `sig_member_types_rev` | ✅ exit 0（完全シグネチャ照合 + 引数変換遅延、2026-06-12） |
| 同 arity・param 型違い overload（free 関数） | `sig_free_types` | ✅ exit 0（int / double / const char*） |
| ctor オーバーロード（インライン/クラス外/mem-init/デフォルト共存） | `ctor_ov_inline`, `ctor_ov_defn`, `ctor_ov_default` | ✅ exit 0（FEAT-CTOR-OV、2026-06-12。シグネチャ照合インフラで成立） |
| 基底 mem-initializer `: B(args)` の overload 解決 + 引数変換 | `ctor_ov_base`, `ctor_base_conv` | ✅ exit 0（`cpp_emit_base_ctor_call` をパース後解決 + `gfunc_param_typed` 接続に修正） |
| 暗黙デフォルト ctor `P a;`（FEAT-4F） | `ctor4f_basic`, `ctor4f_overload`, `ctor4f_multi`, `ctor4f_dtor`, `ctor4f_skip` | ✅ exit 0（2026-06-12。decl で `a.P()` に書き換え。ポインタ/参照/コピー初期化/static/extern は対象外） |
| ローカル参照宣言 `P &r = a;` / `int &r = x;`（BUG-9） | `bug9_a`, `bug9_b`, `bug9_c` | ✅ exit 0（2026-06-12。`init_putv` で参照バインド（アドレス格納）+ identifier 使用時の自動 deref。参照変数→参照引数 / 参照戻り値バインド / `&ref` も `bug9_c` で検証） |
| const メソッド内メンバ書き込み拒否（FEAT-6B-P2） | `feat6b_p2_ok`, `manual/feat6b_p2_err1`, `manual/feat6b_p2_err2` | ✅ 2026-06-12。正常系 exit 0、書き込み 2 形（暗黙 / `this->`）が compile error |

### 変更点

1. [tcc.h](tcc.h) — `FuncAttr.func_const`
2. [tccgen.c](tccgen.c) — `post_type()` trailing const、`cpp_find_field_for_call()`、`cpp_build_func_mangle()` `_C` サフィックス、`gen_function()` の `const T* this`、`is_compatible_func()` const 区別

### 制限

- ~~**const メソッド内のメンバ書き込み拒否**~~ — **FEAT-6B-P2 で接続済み（2026-06-12）**。
  `gen_function` の `this` 型で VT_CONSTANT を**被指示型側**に置き（従来はポインタ側で誰も見ていなかった）、
  `indir()` → `cpp_push_member_var` / postfix `.` の qualifier 伝播でメンバ lvalue が const 化。
  `verify_assign_cast` は C++ モードで `tcc_error`（C モードは従来どおり警告のみ）。
  **`const T&` 宛先は除外** — 引数バインドが param 型（トップレベル VT_CONSTANT 保持、
  `convert_parameter_type` 参照）を毎回 `gen_assign_cast` に通すため。
  テスト: `feat6b_p2_ok.cpp`（正常系 exit 0）、`manual/feat6b_p2_err1/err2.cpp`（暗黙・`this->` 書き込みが compile error）。
  残課題: const メンバフィールドの mem-init（初期化も代入扱いでエラーになる）。
- **const オブジェクト → non-const メソッド** — `cpp_find_field_for_call()` で compile error（`dev/test/a9/manual/feat6b_err.cpp` で手動確認、`run_all` 対象外）

---

## 16. FEAT-4G グローバル自動 ctor/dtor — 進捗（2026-07-18 完了）

| 機能 | テスト | 結果 |
|------|--------|------|
| デフォルト ctor `P g;` の main 前実行 | `feat4g_basic` | ✅ exit 0 |
| 引数付き ctor `P g(17);`（overload 解決・引数変換込み） | `feat4g_args` | ✅ exit 0 |
| dtor の main 後実行（atexit 経由） | `feat4g_dtor` | ✅ exit 0 + `CTOR→MAIN→DTOR` 出力順を手動確認 |
| 複数宣言 `P a(1), b(2);` 宣言順実行 | `feat4g_multi` | ✅ exit 0 |

### 仕組み

1. **登録**（`decl()` の FEAT-4B/4F フックのグローバル側）: グローバル struct 変数の
   ctor 引数トークンを保存し `cpp_register_global_dyn()` でヒープ登録。dtor 持ち
   クラスは fini エントリも自動追加。
2. **thunk 発行**（`cpp_finish_global_dyns()`、decl 完了直後）: エントリごとに
   `__cpp_gi_N` / `__cpp_gd_N`（`push rbp; mov rbp,rsp; sub rsp,0x20; call; leave; ret`）
   を text に発行し `.init_array` / `.fini_array` へポインタ登録。
3. **起動コード注入**（`tccpe.c` → `tcc_add_cpp_init_startup()`、tccelf.c）:
   `.init_array` 非空の EXE リンク時のみ、`__init_array_start/end` を歩く
   `_tcc_cpp_start` を C ソース注入でコンパイルし entry に差し替え
   （fini は `atexit` 登録で main 後に逆順実行）。crt1.o の再ビルド不要。

### 変更ファイル

- [tccgen.c](tccgen.c) — 登録・thunk 発行（`CppGlobalDynEntry` ほか）
- [tccelf.c](tccelf.c) — `tcc_add_cpp_init_startup()`、`have_section` ST_FUNC 化
- [tccpe.c](tccpe.c) — エントリ差し替えゲート（`cpp_global_ctors` フラグ）
- [tcc.h](tcc.h) — `cpp_init_startup_done` / `cpp_global_ctors`

### 制限

- EXE のみ（DLL は対象外、`-run` メモリ実行も対象外）
- `.cpp` → `.o` → 別実行でのリンクは不可（`cpp_global_ctors` フラグが同一
  TCCState 前提）
- `static P s;` は従来どおり対象外（init-once ガード未実装）
- 実装時に 8 個のバグを踏んだ — 詳細は [問題と原因.md](問題と原因.md) BUG-10
TCC（Tiny C Compiler）の最大の特徴である「1パス・高速」というアーキテクチャを維持しながら、C++の機能を導入するためのアプローチ（特に「遅延パース（Delayed Parsing）」によるクラス内前方参照の解決策）が非常に論理的かつ具体的に練られており、素晴らしい設計ドキュメントだと感じました。

ドキュメントのレビュー結果と、C++98として不足している仕様（テンプレート以外）についてまとめました。

***

### **1. ドキュメント内容へのレビューと修正提案**

**①** **`nullptr`** **は C++11 の機能である点** 第1ステージに `nullptr` キーワードの追加とありますが、`nullptr` は **C++11** で導入された仕様です。C++98（VC++ 6.0 互換）に準拠するのであれば、キーワードとしての `nullptr` は存在せず、`0` または `NULL` マクロを使用します。 実装する場合は「C++11拡張機能として実装する」と明記するか、C++98に厳密に寄せるなら対象外とするのが適切です。

**② 名前修飾（Name Mangling）の方式の矛盾** 第2ステージでマングルの例として `_ZN5Class3memEi` が挙げられていますが、これは **Itanium C++ ABI（GCC / Clang）** の形式です。 ドキュメントのタイトルに「VC++ 6.0 互換レベル」とあるため、もし VC++ でコンパイルされた外部ライブラリ（`.lib` など）と TCC 側で C++ レベルのリンクを行う想定であれば、**MSVC マングリング**（例: `?mem@Class@@QAEHXZ` など）を実装する必要があります。 ※TCC内部のソースコード間だけでリンクできれば良いのであれば、Itanium ABI の実装でも問題ありません。

**③ 32-bit (x86) の** **`__thiscall`** **呼び出し規約の記載漏れ** 第4章のABIの留意点において、Windows x64 や ARM のレジスタ渡しについては完璧に網羅されていますが、x86 (32-bit) の言及がありません。 VC++ の x86 におけるクラスのメンバ関数は、デフォルトで **`__thiscall`** 規約（引数はスタックに積み、`this` ポインタは `ECX` レジスタで渡す）となるため、DirectX の古い x86 バイナリなどを叩く可能性がある場合は `__thiscall` の対応が必須になります。

**④ デフォルト引数の記述箇所** 第3章のテストマトリクスに「Default Args（デフォルト引数）」の項目がありますが、第2章の「実装ステージ」のステップ内にデフォルト引数の実装に関する記述が漏れているようです（おそらく第2ステージか第3ステージに入ると思われます）。

***

### **2. C++98 として不足している仕様（テンプレート以外）**

DirectX などのオブジェクト指向APIを自然に扱うため、および C++98 として一般的な機能で、現在のプランに記載がない主要な仕様は以下の通りです。

#### **優先度：高（これがないと C++ として書きづらいもの）**

1. **参照渡し（References:** **`&`）**
   * 概要：ポインタを使わずに変数を参照渡しする機能（例: `void func(int& a)`）。C++のコードでは頻繁に登場します。
2. **`bool`,** **`true`,** **`false`** **キーワード**
   * 概要：C++98 の基本型ですが、第1ステージのキーワード追加リストに記載がありませんでした。
3. **動的メモリ割り当て (`new`** **/** **`delete`,** **`new[]`** **/** **`delete[]`)**
   * 概要：`malloc` / `free` に代わる演算子。クラスのコンストラクタ・デストラクタの自動呼び出しとセットで実装される必要があります。
4. **静的メンバ (`static`** **変数 /** **`static`** **メンバ関数)**
   * 概要：クラス単位で状態を持つ変数や、`this` ポインタを持たないクラス関数。
5. **名前空間 (`namespace`,** **`using namespace`)**
   * 概要：標準ライブラリ（`std::`）や DirectX（型名など）を利用する際の名前衝突を避けるためのスコープ解決。

#### **優先度：中（実装要件に応じて検討するもの）**

6. **多重継承 (Multiple Inheritance)**
   * 概要：現在の第4ステージには「単一継承のサポート」とありますが、DirectX の基盤である **COM（Component Object Model）** では、1つのクラスが複数のインターフェース（`IUnknown` など）を継承して実装するケースがあります。DirectX を「呼び出す」だけであれば単一継承（インターフェースのポインタとして扱う）で事足りますが、TCC側で COM オブジェクトを「実装」する場合は多重継承（またはそれに準ずる VTable の複数保持）が必要になる可能性があります。
7. **コンストラクタの初期化子リスト (Member Initializer List)**
   * 概要：`Class() : x(0), y(1) {}` のようなメンバ変数の初期化構文。
8. **インライン関数 (`inline`)**
   * 概要：C++ではクラス定義内に記述された関数は暗黙的に `inline` 扱いになります。遅延パース機構と組み合わせて、どこまでインライン展開を真面目にやるか（あるいは単なる通常関数としてコンパイルしてしまうか）の指針が必要です。
9. **フレンド宣言 (`friend`)**
   * 概要：特定の関数やクラスに対して `private` へのアクセスを許可する機能。

#### **優先度：低（非常に複雑なので意図的に省くべきもの）**

以下の機能は C++98 の仕様ですが、コンパイラの実装コストが跳ね上がるため、「実装しない制限事項（C C++ subset）」としてドキュメントに明記してしまうことをお勧めします。

10. **例外処理 (`try`,** **`catch`,** **`throw`)**
    * 実行時のスタック巻き戻し（Stack Unwinding）等の機構が必要になるため、大半の軽量コンパイラやゲーム開発の現場では意図的にオフにされます。
11. **実行時型情報 (RTTI:** **`dynamic_cast`,** **`typeid`)**
    * VTable に型情報を埋め込む必要があるため、実装難易度が高く、無くても DirectX の操作には影響しません。
12. **メンバポインタ (`.*`,** **`->*`)**
    * メンバ関数へのポインタなど。構文解析が非常に複雑になる要因の1つです。

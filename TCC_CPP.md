# TCC C++98 拡張実装プラン (VC++ 6.0 互換レベル)

## 1. 目的
Tiny C Compiler (TCC) に C++98 の基本機能を導入し、DirectX 等のオブジェクト指向 API をより自然に扱えるようにする。

## 2. 段階的実装ステージ (Stages)

### 第1ステージ：字句解析と基本型
- **実装内容**:
    - `class`, `public`, `protected`, `private`, `virtual`, `this`, `operator`, `nullptr` のキーワード登録 (`tcctok.h`, `tccpp.c`)。
    - `nullptr` を `(void*)0` として定数評価。
    - `class` を `struct` のエイリアスとして実装（デフォルトアクセス権のみ `private` に設定）。

### 第2ステージ：名前修飾 (Name Mangling) とスコープ
- **実装内容**:
    - 関数オーバーロードの許容。引数型に基づいたシグネチャ生成。
    - C++ 名前修飾アルゴリズムの実装（例: `_ZN5Class3memEi`）。
    - `Sym` 構造体に `parent_class` ポインタを追加し、クラススコープ内でのシンボル検索を実装。

### 第3ステージ：メンバ関数と `this` ポインタ
- **実装内容**:
    - クラス内関数定義のパース。
    - `gfunc_prolog` において、非静的メンバ関数の第1引数として `this` を暗黙的に割り当て。
    - `a.func()` 形式を `Class::func(&a)` への関数呼び出しに変換。

### 第4ステージ：継承とアクセス制御
- **実装内容**:
    - 単一継承のサポート。基底クラスの構造体を派生クラスの先頭に配置。
    - `public`/`protected`/`private` によるシンボル検索時のエラーチェック。
    - コンストラクタ/デストラクタの暗黙呼び出し（スタック変数の寿命管理）。

### 第5ステージ：仮想関数 (Virtual Functions)
- **実装内容**:
    - クラス構造体の先頭に `_vptr` (VTable ポインタ) を挿入。
    - `.rodata` セクションへの仮想関数テーブル (VTable) の生成。
    - `virtual` 関数呼び出し時、直接ジャンプではなく `_vptr` 経由の間接呼び出しコードを生成。

### 第6ステージ：演算子オーバーロードと const
- **実装内容**:
    - `operator+`, `operator[]` 等をマングル名への関数呼び出しに変換。
    - メンバ関数への `const` 属性付与と、`this` ポインタの `const` 化による整合性チェック。

---

## 3. テスト計画

拡張機能の正確性を検証するため、既存の C++ コンパイラとの比較および CUnit による自己テストの2軸で実施する。

### 3.1 外部コンパイラ（MSVC/Clang）による検証
1. **テストソースの作成**:
    - C++98 の機能（継承、仮想関数、演算子オーバーロード）を網羅した `cpp_feature_test.cpp` を作成。
2. **CppUnit による期待値の定義**:
    - MSVC または Clang を用いて、CppUnit フレームワーク上でテストを実行し、バイナリの挙動（メモリレイアウト、VTable のオフセット、実行結果）が仕様通りであることを確認する。
3. **拡張 TCC による検証**:
    - 同じ `cpp_feature_test.cpp` を拡張した TCC でコンパイル。
    - 生成された実行ファイルの出力結果が、MSVC/Clang 版と完全に一致することを自動スクリプトで検証する。

### 3.2 CUnit による内部ロジックテスト
拡張した TCC が、自身が生成する C++ コードを正しく処理できるかを検証する。

1. **libcunit.a の再ビルド**:
    - **目的**: 拡張した TCC が「C++ 的な拡張を施した CUnit ソース」をコンパイル・リンクできるかを確認。
    - **手順**: CUnit のソースを一部 `class` やメンバ関数を使用するように書き換え、拡張 TCC で `libcunit.a` をビルドし直す。
2. **機能単位テストの実装**:
    - `tccgen.c` に追加したマングリングロジックや VTable 生成ロジックをテストするための CUnit テストケースを作成。
    - 再ビルドした `libcunit.a` とリンクし、TCC 内部の状態遷移（Sym 属性の書き換え等）をテストする。

### 3.3 テストマトリクス
| テスト項目 | 検証内容 | 判定基準 |
| :--- | :--- | :--- |
| Name Mangling | `int f(int)` と `int f(double)` が別シンボルになるか | MSVC と同じマングルルールでの衝突回避 |
| `this` Pass | メンバ関数内でメンバ変数に正しくアクセスできるか | `this` レジスタ (RCX等) の値の正当性 |
| VTable Lookup | 基底クラスのポインタから派生クラスの関数が呼ばれるか | 実行時の出力文字列の一致 |
| Access Control | `private` メンバへの外部アクセスがエラーになるか | `tcc_error` の発生確認 |
| Default Args | 引数を省略した際に nullptr 等が正しく渡るか | スタック/レジスタへのデフォルト値ロード |

---

## 4. 実装上の技術的留意点

### 各プラットフォーム ABI の遵守 (thisポインタの割当)
C++のメンバ関数呼び出しにおいて、`this` ポインタを第1引数として渡すためのレジスタ割当を各アーキテクチャの `gfunc_call` および `gfunc_prolog` で制御する。

- **Windows x64 (x86_64-gen.c)**:
    - `this` ポインタは第1引数（`RCX`）として渡す。

- **ARM 32-bit (arm-gen.c)**:
    - **AAPCS** (Procedure Call Standard for the ARM Architecture) に基づき、`this` ポインタは第1引数（**`R0`**）として渡す。
    - 構造体の戻り値がある場合、`R0` が構造体ポインタ（sret）に使用されるケースがあるため、その際の `this` の位置（`R1` へのシフト等）の整合性を `arm-gen.c` 内で慎重に扱う必要がある。

- **ARM 64-bit / AArch64 (arm64-gen.c)**:
    - **AAPCS64** に基づき、`this` ポインタは第1引数（**`X0`**）として渡す。
    - **macOS (Apple Silicon)** でのビルドにおいても基本は AArch64 標準に従うが、スタックアライメント（16バイト境界）の維持が x64 以上に厳格であるため、メンバ関数呼び出し時のスタック操作に留意する。
    - 可変長引数（Variadic）を持つメンバ関数の場合、名前修飾されたシグネチャに応じたレジスタ/スタックの振り分けを `arm64-gen.c` の PE/ELF/Darwin 分岐ごとに最適化する。

### 1パス解析の制限解決
TCCの「1パス・高速」という設計を維持しつつ、C++の「クラス内メンバの宣言順序に依存しない参照」をサポートするために、**「遅延パース（Delayed Parsing）」**戦略を採用する。

#### メカニズムの概要
1. **プロトタイプの早期登録**: クラスパース中、関数名と引数リスト（シグネチャ）が現れた時点で、シンボルをスタックに登録する。
2. **本体トークンの保存**: 関数本体 `{ ... }` が現れた際、即座にコード生成（`gen_function`）を行わず、`tccgen.c` の `skip_or_save_block` を用いて、中身のトークン列を `TokenString` として `Sym` 構造体に保持する。
3. **クラス構造の確定**: クラスの閉じ括弧 `};` に到達するまでパースを継続し、全メンバ変数（オフセット）と全関数のプロトタイプを確定させる。
4. **遅延コンパイルの実行**: `};` 到達直後に、保存しておいた各メソッドのトークン列を順次読み込み、`begin_macro` 相当の仕組みで入力ストリームを切り替えて `gen_function` を実行する。

この方式により、TCCの基本構造を大きく変えることなく、クラス内の前方参照問題を解決する。

### メモリ管理
- クラス継承が深くなった場合の `Sym` 構造体の肥大化を避けるため、派生情報は `CType.ref` 内のリンクリストで管理し、アリーナアロケータ (`TinyAlloc`) のメモリ効率を維持する。

---

## 5. TCC 字句解析・トークナイザの構造
C++ キーワードの追加や遅延パースを実装する上で、TCC の字句解析（Lexer/Tokenizer）の仕組みを理解しておく必要がある。

### 主要な構成要素
| ファイル名 | 役割 |
| :--- | :--- |
| **`tccpp.c`** | **トークナイザ/プリプロセッサの実体**。文字入力、マクロ展開、トークン生成の全工程を管理する。 |
| **`tcctok.h`** | **トークン定義テーブル**。全てのキーワード（`int`, `if` 等）と演算子（`++`, `<<=` 等）が `DEF()` マクロで定義されている。 |
| **`tcc.h`** | `TokenString`, `CValue` (tokc), `TokenSym` 等の核心的なデータ構造を定義する。 |
| **`tccgen.c`** | パーサ。`next()` を呼び出して得られたトークン ID に基づき構文解析を行う。 |

### トークンの内部表現と管理
TCC では、パース中の現在のトークン状態はグローバル（または `TCCState`）で管理される。

- **トークン ID (`tok`)**:
    - トークンを識別する整数。`1`〜`255` は ASCII 文字そのもの。
    - `256` 以上は `tcctok.h` で定義されたキーワードや演算子。
    - `TOK_IDENT` 以上の値は、動的に登録された識別子（変数名、関数名等）を表す。
- **トークン定数値 (`tokc`)**:
    - `CValue` 型（union）。トークンが数値リテラル（`TOK_CINT`, `TOK_CCHAR` 等）や文字列リテラル（`TOK_STR`）の場合にその値を保持する。
- **識別子テーブル**:
    - 識別子はハッシュ化され、一意の ID が割り振られる。`struct TokenSym` によって文字列実体と対応付けられる。

### トークン生成の主要ロジック (`tccpp.c`)

1.  **`next_nomacro()`**:
    - **字句解析の核心**。空白、コメントをスキップし、次のトークンを切り出す。
    - 識別子の開始（`a-z`, `_`）を検知すると、`tok_alloc()` を経由して ID を取得。
    - 数値の開始（`0-9`）を検知すると、`parse_number()` を呼び出し `tokc` に値を格納。
    - 文字列（`"`）を検知すると、`parse_string()` でエスケープシーケンスを処理しながらバッファに格納。
2.  **`next()`**:
    - マクロ展開を考慮した高位のトークン取得関数。
    - 現在の入力ストリームがマクロ定義内（`TokenString`）であればそこから読み込み、そうでなければ `next_nomacro()` を呼ぶ。
    - トークンがマクロ名であれば、その引数をパースし、展開後のトークン列をスタックに積む。

### `TokenString` 構造（遅延パースの基盤）
C++ のクラスメンバ関数の本体を一時保存するために使用する。

- **データ構造**: `int` 型の動的配列。
- **内容**:
    - トークン ID がそのまま格納される。
    - `tokc` などの付随情報が必要なトークンの場合、特定のマーカー ID の後にデータが埋め込まれる。
- **`skip_or_save_block(TokenString **str)`**:
    - `{` と `}` の対応を数えながら、その間のトークンを `str` に書き込む。
    - C++ 実装では、クラス定義内のインライン関数定義が見つかった際にこの関数を呼び出し、`Sym` にトークン列をリンクする。

### 6. TCC パーサと再帰下降構文解析の構造
`tccgen.c` は再帰下降パーサ（Recursive Descent Parser）として実装されており、言語の文法規則に従って関数を再帰的に呼び出し、解析とコード生成を同時に行う。

#### 解析の階層構造と主要関数
パーサは大きな構造（宣言）から小さな単位（単項演算子）へとトップダウンで解析を進める。

1.  **宣言のエントリポイント: `decl()`**
    - プログラムのトップレベルやブロック内での変数・関数宣言を処理する。
    - `parse_btype()` で基本型を、`type_decl()` で派生型（ポインタ、配列等）を解析する。
    - 関数本体 `{ ... }` が見つかると `gen_function()` を呼び出す。

2.  **ステートメントの処理: `block()`**
    - `{ ... }` 内の各文（ステートメント）をループで処理する。
    - `if`, `while`, `for`, `switch`, `return` などのキーワードを判別し、それぞれの制御構造に応じたコード生成（`gjmp` 等）を行う。
    - 複合文（ネストした `{ }`）を見つけると自分自身（`block()`）を再帰呼び出しする。

3.  **式の解析階層（優先順位順）**:
    - `gexpr()`: カンマ演算子の解析。
    - `expr_eq()`: 代入演算子の解析。
    - `expr_cond()`: 三項演算子 (`? :`) の解析。
    - `expr_lor()`, `expr_land()`: 論理演算子の解析。
    - ...（ビット演算、比較、加減算、乗除算の順に階層化されている）...
    - **`unary()`**: 単項演算子（`*`, `&`, `!`, `++`, `--`）、リテラル、識別子、および**関数呼び出し**を処理する最深部の解析関数。

#### 仮想スタック (`vtop`) の役割
TCC はレジスタ割り当てを管理するために「仮想スタック（Value Stack）」を使用する。
- **`SValue` 構造体**: 各値（定数、レジスタ上の値、スタック上のアドレス等）の状態を保持する。
- **`vtop` ポインタ**: 現在解析中の式の「結果」を指すスタックの頂点。
- 解析例: `a + 1` のパース時
    1. `a` を解析し、そのアドレスを `vtop` にプッシュ。
    2. `+` を検知。
    3. `1` を解析し、定数値を `vtop` にプッシュ。
    4. `gen_op('+')` を呼び出し、`vtop[-1]` と `vtop[0]` を加算するコードを生成、結果をスタックに統合する。

#### C++ 実装におけるパーサの修正方針
- **`struct_decl()` の拡張**: `class` キーワードを処理し、デフォルトアクセス権の切り替えを実装。
- **`unary()` 内の関数呼び出し拡張**: 
    - `obj.method()` のドット演算子やアロー演算子をパースした際、それがメンバ関数であれば `vtop` にオブジェクトのアドレスを「隠し引数」として準備する。
    - `gfunc_call()` が呼ばれる前に、このアドレスを ABI 指定のレジスタ（x64 なら RCX）に配置する命令を発行する。
- **名前修飾の統合**: `decl()` で関数名を登録する直前に、引数型リストに基づいた名前修飾（Name Mangling）を実行し、一意のシンボル名を生成。

---

---
## 改訂履歴
| 版 | 日付 | 内容 |
| :--- | :--- | :--- |
| 1.0 | 2026-05-23 | 初版作成。実装 6 ステージと CppUnit/CUnit テスト計画の策定 |
| 1.1 | 2026-05-23 | 1パス問題の解決策（遅延パース）の具体的詳細を追記 |
```

<!--
[PROMPT_SUGGESTION]tccgen.cの中で、クラスのメンバ関数が呼ばれた際に自動的にRCXレジスタへthisポインタをセットするコード案を提示してください。[/PROMPT_SUGGESTION]
[PROMPT_SUGGESTION]C++の名前修飾（Name Mangling）を実現するために、Sym構造体にどの程度の変更を加えるのがTCCにとって最も効率的か教えてください。[/PROMPT_SUGGESTION]

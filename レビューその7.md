v2.3 を読みました。**前回指摘した🔴2 点（`extern "C++"`** **の** **`tcc_error`** **化、`run_all.bat`** **重複削除）と🟡8 点はすべて反映**されており、構造的な大きな破綻点はほぼ消えました。

しかし **A-2 の** **`demote_cpp_keyword_to_ident()`** **の実装コードに、致命的だが見落とされやすい欠陥**が 1 つ残っています。それと細かい運用上の問題が 2 件あります。

***

## 🔴 Critical — A-2 `tok_alloc()` 呼び出しが論理的に成立しない

v2.3 L445-457：

```
static int demote_cpp_keyword_to_ident(TokenSym *ts)
{
    if (ts->alt_ident_tok == 0) {
        const char *name = ts->str;
        int namelen = ts->len;
        ...
        ts->alt_ident_tok = tok_alloc(name, namelen)->tok;  // ★ 問題箇所
    }
    return ts->alt_ident_tok;
}

```

`tok_alloc()` は **ハッシュ表を引いて既存があれば返す**関数です。`tccpp_new()` で `"class"` → `TOK_CLASS` を既にハッシュ登録しているため：

```
tok_alloc("class", 5) → ハッシュにヒット → 既存の ts (TOK_CLASS) を返す
ts->alt_ident_tok = TOK_CLASS  ← 降格になっていない！
return TOK_CLASS

```

**降格効果がゼロ**。`.c` ファイルで `int class;` を書いても `tok = TOK_CLASS` のまま。

### 必要な実装：「ハッシュ非登録で `table_ident` だけに足す」専用ヘルパ

TCC の `tok_alloc()` は通常：

1. 新しい `TokenSym` を確保
2. `table_ident[tok - TOK_IDENT]` に格納（`get_tok_str` 用）
3. **ハッシュチェーンに連結**

降格用には **ステップ 3 を省く**必要があります（さもないと「class」のハッシュエントリが 2 つになり、次回検索でどちらが返るか不定）。

```
/* tccpp.c に追加：ハッシュ非登録の識別子アロケータ */
static TokenSym *tok_alloc_demote(const char *str, int len)
{
    /* tok_alloc_new の hash 登録部分を抜いた版。
       table_ident への登録のみ行い、tok_ident を消費する。
       詳細実装は tok_alloc_new を参考に、pts (PHashNext) 連結処理を省く。 */
    ...
}

static int demote_cpp_keyword_to_ident(TokenSym *ts)
{
    if (ts->alt_ident_tok == 0)
        ts->alt_ident_tok = tok_alloc_demote(ts->str, ts->len)->tok;
    return ts->alt_ident_tok;
}

```

### キャッシュが効く理由（補強）

字句解析は **同じ "class" 文字列 → 常に同じ** **`ts`**（ハッシュ表で一意管理）を返すため、`ts->alt_ident_tok` を **1 度書き込めば永続キャッシュ**として機能します。これは v2.3 の設計判断としては正しい。問題は「`alt_ident_tok` を生成する手段」だけです。

### プラン修正案（A-2 に追記）

| 項目   | 内容                                                                                           |
| :--- | :------------------------------------------------------------------------------------------- |
| 新ヘルパ | `tok_alloc_demote(const char*, int)` を `tccpp.c` に新設                                         |
| 実装   | 既存 `tok_alloc_new()` から **ハッシュチェーン連結（`pts = ...`** **部分）を除いた**コピー                            |
| 検証   | `dev/test/a2/repeated_ident.c` に `int class; class=2; class++;` を入れて 1 シンボルとして解決されることを smoke |

**v2.3 のままだと A-2 完了判定の** **`int class = 1;`** **が動きません**。実装段階で必ず気付きますが、気付いた時点で「`tok_alloc` を `tok_alloc_new` に変える」という誤った修正に走ると、今度はハッシュが汚染されて `.cpp` 側の `TOK_CLASS` 認識が壊れます（連鎖破綻）。**プランで明示しておくのが安全**。

***

## 🟡 Important — 運用上の落とし穴

### 1. `run_all.bat` の作業ディレクトリ問題（A-6 L805-820）

```
@echo off
setlocal
set TCC=..\tcc.exe

```

`build.bat` 側は `call dev\test\run_all.bat`（L1016）で呼ぶため、**カレントディレクトリはリポジトリルート**のまま。よって：

* `..\tcc.exe` → `e:\work\work_github\tpp\tcc.exe`（**存在しない**）
* `smoke\*.c` → `e:\work\work_github\tpp\tcc_dx\smoke\*.c`（**存在しない**）

→ **必ず失敗**します。

**修正**：`run_all.bat` 先頭で必ずディレクトリを移動：

```
@echo off
setlocal
pushd "%~dp0"
set TCC=..\tcc.exe
set FAILED=0
for %%f in (smoke\*.c smoke\*.cpp a2\*.c a2\*.cpp a3\*.c a3\*.cpp a4\*.cpp a5\*.cpp) do (
    echo === %%f ===
    "%TCC%" -c "%%f" -o "%%~dpnf.o"
    if errorlevel 1 set FAILED=1
)
popd
exit /b %FAILED%

```

ついでに：

* `a2\*` のままだと **`.o`** **ファイルや** **`.bat`** **まで巻き込んでループ**します（複数回実行時、生成された `class_min.o` を `tcc -c` しようとして失敗）。**拡張子で限定**してください。
* 出力先 `"%%~nf.o"` はカレント直下に集まる → 上記の通り **同名衝突**します。`"%%~dpnf.o"` でソースと同じ場所に。

### 2. A-2 完了判定 `mixed_link.bat` の TCC 呼び出し形

L494：

```
dev\tcc.exe -c foo.cpp -c bar.c

```

TCC では `-c` は「リンクしない」フラグで、ファイルとペアではありません。**`-c`** **を 2 回書くと 2 回目以降は無視される**ことが多く、実用上は：

```
dev\tcc.exe -c foo.cpp bar.c

```

または別 invokation：

```
dev\tcc.exe -c foo.cpp -o foo.o
dev\tcc.exe -c bar.c  -o bar.o

```

混合 TU 検証の **本質は「同一 TCCState で** **`.cpp`** **の後に** **`.c`** **が壊れないこと」**。前者の形（`-c foo.cpp bar.c`）が一番テスト意図に合います。

### 3. `A-1-4` の `s1->lex_c = 0;` リセットが抜けている

L319-322：

```
s1->cpp = 0;
s1->extern_c = 0;

```

`lex_c` のリセットが書かれていません。A-1-6 の save/restore には `lex_c` も含まれているので意図はあるはずですが、A-1-4 の初期化／終了処理にも追加すべき。一貫性のため：

```
s1->cpp = 0;
s1->extern_c = 0;
s1->lex_c = 0;

```

***

## 🟢 Minor

### 4. A-2 `demote_cpp_keyword_to_ident` のシグネチャ揺れ

v2.3 の現コード：

```
static int demote_cpp_keyword_to_ident(TokenSym *ts)

```

これは正しい。ただし上位サンプル L406 の **A-2-0 事前確認**で：

> 降格時の文字列取得：`ts->str` / `ts->len` が NULL/0 の場合は `tokc.str.data` / `tokc.str.size` にフォールバック

このフォールバックは **無意味**です。`ts != NULL` なら `ts->str` は常に有効なはず（`tok_alloc_new` が必ず文字列をセットする）。「NULL のとき tokc を見る」コードは死分岐で、却ってバグの温床。**A-2-0 のフォールバック行を削除**してシンプルに保ってください。

### 5. A-5-1 `case TOK_CLASS: struct_decl(...)` の前に `next()` が必要か明示なし

`case TOK_STRUCT:` 内で `struct_decl()` 呼び出し前に `next()` で `struct` キーワードを消費するパターンが既存実装にあるはず。同様に `TOK_CLASS` でも必要。プランの L671-673：

```
case TOK_STRUCT:
    struct_decl(&type1, VT_STRUCT, 0);
case TOK_CLASS:
    struct_decl(&type1, VT_STRUCT, 1);

```

このスニペット単独だと `next()` の有無が見えません。\*\*「既存 `case TOK_STRUCT:` の `next()` 直後の `struct_decl` 呼び出し行を置き換える/コピー」\*\*という運用注を入れてください。実装者が `case TOK_CLASS:` だけ見て書き写すと `class` キーワードを食べ忘れます。

### 6. A-4 完了判定の `class_min.cpp` 例にクラス外定義が残っている

L771：

```
void Point::set(int a) { x = a; }  /* クラス外定義は Stage 1 では optional / 未対応ならファイル分割 */

```

A-5 では `Point::set` の **`::`** **パース**（修飾名）について一切言及がありません。Stage 1 で `::` をどう扱うか未定義 → このコードはほぼ確実にパースエラー。

**修正**：完了判定コードから `void Point::set(int a) { x = a; }` の行を削除し、クラス内プロトタイプのみのテストに統一。コメントは脚注に：

> クラス外メンバ定義（`Point::set(...)` 形式）と修飾名（`::`）のパースは Stage 2 以降。

***

## 📋 改訂提案サマリ

| 優先   | 章        | アクション                                                                         |
| :--- | :------- | :---------------------------------------------------------------------------- |
| 🔴 1 | A-2      | `tok_alloc(name, namelen)` を **`tok_alloc_demote()`（ハッシュ非登録）** に置き換え。実装ヘルパも併記 |
| 🟡 2 | A-6      | `run_all.bat` に `pushd %~dp0` 追加。`for` パターンを拡張子限定に。`.o` 出力先を `%%~dpnf.o` に    |
| 🟡 3 | A-2      | 完了判定の `mixed_link.bat` を `dev\tcc.exe -c foo.cpp bar.c` に修正                   |
| 🟡 4 | A-1-4    | `lex_c = 0` リセットを追加                                                           |
| 🟢 5 | A-2-0    | `ts->str` NULL フォールバックの記述を削除                                                  |
| 🟢 6 | A-5-1    | `case TOK_CLASS:` で `next()` を含む運用注                                           |
| 🟢 7 | A-5 完了判定 | クラス外定義の行を削除                                                                   |

***

## 全体評価

v2.0 → v2.3 で **規律・依存関係・テスト戦略・Stage 1 スコープ確定** が完成し、プランとしての骨格は実装着手可能なレベルに到達しています。

唯一残る **🔴1（`tok_alloc_demote`** **の必要性）** は実装段階で気付きますが、気付いた時点で誤った方向に修正すると **`.cpp`** **側のキーワード認識が壊れる連鎖破綻**になります。これだけはプラン段階で明示しておくと、A-2 実装が安全に通せます。

🟡2（`run_all.bat`）は初回実行で必ず躓くので、書く前に直しておく価値があります。

🔴1 と 🟡2-4 を v2.4 に反映すれば、**Phase 1（A-P + A-1 ～ A-5）は計画通り実装すれば破綻しない**見通しが立ちます

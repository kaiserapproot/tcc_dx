v2.2 を読みました。**前回の🔴3 点（A-2 ハイブリッド・A-4 ブロック限定・lex\_c）はすべて反映**されており、構造的にはかなり成熟しました。

ただし **A-2 の核心関数** **`force_alloc_identifier()`** **の仕様が未解決のまま** であることと、**A-4** **`extern "C++"`** **ブロックの処理が文法上壊れている**ことが残っており、このまま実装に入ると A-2 / A-4 で再度ハマる可能性が高いです。優先度順に指摘します。

***

## 🔴 Critical（このまま実装すると詰む）

### 1. A-2 `force_alloc_identifier()` の仕様が成立していない

`demote_cpp_keyword_to_ident()` は「ハッシュをバイパスして識別子用 TokenSym を取る」設計ですが、これは **同一識別子の反復参照** で破綻します。

```
/* .c ファイルで A-2 降格が有効な場合 */
int class = 1;   /* 1回目: demote → 新規 TokenSym A (tok=4321) */
class = 2;       /* 2回目: demote → 新規 TokenSym B (tok=4322) */
                  /* → 別変数扱いになり sym_find が失敗 */

```

「ハッシュをバイパス」を文字どおり実装すると、`force_alloc_identifier()` 呼び出しのたびに **異なる tok ID** を返してしまい、変数として機能しません。

ところが「ハッシュを使う」と、既存の `tok_alloc()` は `"class"` → `TOK_CLASS` を返すので、降格になりません。

**実際に必要な設計**は次のどれかで、これを **プラン段階で確定**する必要があります：

| 案   | 概要                                                                                                                                                  | 難度                |
| :-- | :-------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------- |
| (a) | `TokenSym` を **キーワード/識別子の二重身分**にする。`ts` は 1 個。`tok` を返す時に `effective_cpp_lex()` で **TOK\_CLASS or TOK\_IDENT** を切り替える。`get_tok_str()` 等は ts 経由なので動く | 中（switch 文の検証が必要） |
| (b) | `tok_alloc()` の中で `effective_cpp_lex()` を見て **降格モードのときは「`tok_ident_table`** **側に別 entry を作る」**。同名でも常に同じ ID（→ hash を二系統持つ）                           | 大（hash 構造を改変）     |
| (c) | C++ 用キーワード文字列を **`_cppkw_class`** 等にプリフィックス化し、`.cpp` でだけ `#define class _cppkw_class` を予定義 — ハック的だがゼロ侵襲                                             | 小（ただし美しくない）       |

**v2.2 の記述**「`force_alloc_identifier()` は `tok_alloc()` の hash ヒットをバイパスし、必ず `tok_alloc_new()` で `TOK_IDENT` 範囲の新 ID を割り当てる」は **上記いずれにも該当せず、動かない設計**です。

**推奨**：案 (a) を以下のように明文化：

```
/* next_nomacro() — token_found: 直後 */
tok = ts->tok;
if (is_cpp_only_keyword(tok) && !effective_cpp_lex(tcc_state)) {
    /* 同じ ts を使うが、tok 値だけ識別子レンジに付け替える。
       ts->tok もこの TU 中だけ書き換える設計はしない（global state 汚染）。
       代わりに ts のフィールドに alt_ident_tok を持たせて、初回降格時に
       1 度だけ tok_alloc_new で取得しキャッシュする。 */
    if (ts->alt_ident_tok == 0)
        ts->alt_ident_tok = tok_alloc_new_internal(ts->str, ts->len)->tok;
    tok = ts->alt_ident_tok;
}

```

これで「同名は常に同じ alt\_ident\_tok」「キーワード時は本来の TOK\_CLASS」が両立します。`TokenSym` に **`int alt_ident_tok;`** **フィールドを追加**することを A-2 のサブステップに入れてください。

### 2. A-4 `extern "C++"` ブロックの実装が文法上壊れている

L561-566：

```
                if (len == 3 && !memcmp(s, "C++", 3)) {
                    next();    /* tok = '{' になる */
                    if (tcc_state->extern_c > 0)
                        tcc_state->extern_c--;
                    continue;  /* tok='{' のまま decl ループ先頭へ */
                }

```

* `extern "C++" {` のあと **`{`** **の中身を読まずに** **`continue`** している
* decl() のループ先頭で `tok == '{'` のまま再開 → `parse_btype()` が `{` を見て **エラーかブロック誤解釈**
* ブロック開閉のバランスも崩れる

そもそも `extern "C++" {` は **`extern "C" {`** **の入れ子の中で C++ リンケージに戻す** ためのもので、トップレベルで単独使用する正当性は薄いです。

**修正案（Stage 1 の確定スコープに合わせる）**：

```
                if (len == 3 && !memcmp(s, "C++", 3)) {
                    tcc_error("Stage 1: extern \"C++\" は未対応");
                }

```

`extern "C++"` 対応は Stage 2 に送り、表（L511-516）から削除する。これだけで A-4 のロジックが閉じます。

または「`extern "C"` ブロック内の `extern "C++" { ... }` 入れ子サポート」も実装するなら、`lex_c--/++` ペアを `{...}` で挟む正しいブロック処理が必要で、Stage 1 のスコープを超えます。

***

## 🟡 Important（実装中に高確率でハマる）

### 3. `build.bat` で `call dev\test\run_all.bat` が **2 回呼ばれている**

L943 と L952 に重複。**2 回目の呼び出しが既存 .obj を上書きして失敗** する可能性。1 回に統一してください。

### 4. UTF-8 BOM 規約が既存ファイルとの非整合を生む

A-11 #1 で「TCC 変更ソースは UTF-8 with BOM」とありますが、現在の `tcc.c`/`tccgen.c`/`tccpp.c` は upstream 由来で **ASCII（BOM なし）**。日本語コメントを 1 行加えると BOM が必要になり、**ONE\_SOURCE で** **`#include`** **した先の文字コードと衝突**するリスク。

**修正案**：規約を以下に細分化：

| 種類            | 規約                                           |
| :------------ | :------------------------------------------- |
| 新規追加ファイル      | UTF-8 BOM                                    |
| 既存 TCC ソース    | **ASCII 維持。日本語コメントを書かない**                    |
| 例外で BOM 化する場合 | A-1 の前に **単一コミット**で一括変換し、ONE\_SOURCE での挙動を確認 |

### 5. `extern_c` カウンタが **読まれない** ことの明示

A-4 では `tcc_state->extern_c++/--` しているが、Stage 1 中はこの値を参照する箇所が一切ない（mangling 未実装のため）。**残しておく根拠**（将来の name mangling 用）と、**Stage 1 では参照ゼロでよい**ことを明記しないと「使われていない＝消す」改変が将来入りかねません。

A-4 の脚注に 1 行追加：

> `extern_c` は本 Stage では increment/decrement のみ。Stage 2 の name mangling で参照する。Stage 1 で削除してはならない。

### 6. A-2 `next_nomacro()` 内の変数スコープ前提が未確認

`demote_cpp_keyword_to_ident((char *)p1, len)` の引数 `p1` と `len` が **`token_found:`** **ラベル直後の時点で in-scope か** が未検証。`tccpp.c` `next_nomacro()` の実装では `parse_ident_slow()` 経由などで `p1`/`len` が局所変数として確保されているはずだが、A-2 着手前に **`tccpp.c`** **を 5 分読んで確認**してから書くべき。プランに 1 行注記：

> 実装前に `tccpp.c` の `next_nomacro()` `token_found:` ラベル付近を読み、`p1`/`len` 相当の文字列ポインタ・長さ変数を特定すること。なければ `ts->str` / `ts->len` 経由で渡す。

### 7. A-5 `parse_btype()` の `goto basic_type2;` ラベル存在確認

A-5-5 で `case TOK_CLASS: ... goto basic_type2;` を提案していますが、`basic_type2` ラベルが現 `parse_btype()` に存在するか確認していない。実装前に `grep -n "basic_type2:" tccgen.c` で確認手順を追加してください。

### 8. A-7 `int class = 42;` を **MSVC が C モードでコンパイル** する前提

A-7-3 のテストは MSVC でビルドされます。MSVC `/TC`（C モード）で `class` は識別子として通る、ということを前提にしていますが、`con_c_vs_test.vcxproj` の `CompileAs` 設定（`CompileAsC` or `CompileAsCpp`）に依存します。

**追加確認手順** A-7-1 に：

```
con_c_vs_test.vcxproj の <CompileAs> が "CompileAsC" であること
（"Default" や "CompileAsCpp" だと class が予約語化して FAIL）

```

***

## 🟢 Minor

### 9. A-R5 サマリー表の重複

A-R（v2.1 反映）と A-R5（v2.2 反映）で「A-2 `tok=TOK_IDENT` 問題」が**両方の表に並んで**います。A-R は v2.1 時点で「ハイブリッド降格に確定」と書いていますが、実際の確定は v2.2。読み手の混乱要因。**A-R 表の該当行を「v2.1: 遅延登録に変更 → v2.2 で再見直し」のように時系列で書くか、A-R を削除**してください。

### 10. `dev/test/run_all.bat` の中身が未定義

P-5 で「最初は `@echo off & exit /b 0` で可。A-6 で拡張」とありますが、A-6 にも具体的な中身が書かれていません。最低限：

```
@echo off
for %%f in (dev\test\a2\*.cpp dev\test\a3\*.cpp dev\test\a4\*.cpp dev\test\a5\*.cpp) do (
    dev\tcc.exe -c %%f -o nul || exit /b 1
)
exit /b 0

```

のような **A-6 で書く具体例**を入れてください。

### 11. A-1-1 の `lex_c` 追加が **目立たない**

L247-248 にひっそり追加されていますが、上の構造体差分テキスト（L235-238）に **入っていない**。追加プロパティが 2 か所に分散して見えるので、コードブロックを 1 つに統合してください：

```
    /* C++ language options */
    unsigned char cpp;
    unsigned char cpp_forced;
    unsigned char extern_c;
    unsigned char lex_c;      /* extern "C" {} 内で C++ キーワードを識別子化 */

```

### 12. ABI セクション（Part B §3）に `gen_move_to_register` などの **架空 API** 記述

`gen_move_to_register(TREG_RCX, vtop)` は TCC に存在しません。これは「実装フックの**疑似コード**」だと注釈すべき。実装者が grep して「無い！」と困惑する。1 行注記：

> ※ `gen_move_to_register()` は疑似コード。実 API は `tccgen.c` の `gv()` / `save_reg_upstack()` 等を組み合わせて作る。

***

## 📋 改訂提案サマリ

| 優先    | 章         | 必要なアクション                                                                            |
| :---- | :-------- | :---------------------------------------------------------------------------------- |
| 🔴 1  | A-2       | `force_alloc_identifier` の設計を **TokenSym に** **`alt_ident_tok`** **を追加する案 (a)** に確定 |
| 🔴 2  | A-4       | `extern "C++"` を **Stage 1 未対応**に。L561-566 を `tcc_error` 化、表（L514）から削除              |
| 🟡 3  | A-8-1     | `call dev\test\run_all.bat` の **重複削除**（L943 か L952）                                 |
| 🟡 4  | A-11 #1   | UTF-8 BOM 規約を **新規/既存で分割**、既存ファイルは ASCII 維持                                         |
| 🟡 5  | A-4       | `extern_c` カウンタが Stage 1 で読まれない事実を明記（削除防止）                                          |
| 🟡 6  | A-2       | `next_nomacro()` 内変数スコープの **事前確認手順**を 1 行追加                                         |
| 🟡 7  | A-5       | `goto basic_type2;` ラベル存在の事前 grep 手順を追加                                             |
| 🟡 8  | A-7       | `<CompileAs>CompileAsC</CompileAs>` 確認を A-7-1 に追加                                   |
| 🟢 9  | A-R/A-R5  | 重複行の整理                                                                              |
| 🟢 10 | A-6       | `run_all.bat` の具体例追記                                                                |
| 🟢 11 | A-1-1     | `lex_c` をメインコードブロックに統合                                                              |
| 🟢 12 | Part B §3 | 架空 API に「疑似コード」注釈                                                                   |

***

## 全体評価

v2.1 → v2.2 で **規律・テスト戦略・依存関係はほぼ完成形** です。残るのは **A-2 と A-4 の細部設計** の 2 点のみで、これは「考え方」のレベルなので **コードを書き始めれば 30 分で詰められる規模**。ただし詰めずに実装に入ると、また `tccgen.c` 破損の同型問題が起きます。

特に **🔴1 の A-2** **`alt_ident_tok`** **案**は実装の中核なので、これだけは **コード着手前にプランへ正式に書き込む**ことを強く推奨します。これが固まれば、A-1〜A-9 を実装ガイドどおりに進めても破綻しない見通しがほぼ立ちます。
